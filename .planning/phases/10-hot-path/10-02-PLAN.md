---
phase: 10-hot-path
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/features/filters.rs
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - PERF-10
must_haves:
  truths:
    - "用户在 BENCHMARKS.md Phase 10 §D-G1 门控判定看到 `**结论：命中 D-G1.**` 时才执行本计划"
    - "用户运行 `cargo bench --bench bench_filters` 优化后，criterion 报告 `Performance has improved` 或 `No change`，绝不出现 `Performance has regressed`（throughput 无回归，D-O3 容差 ≤5%）"
    - "BENCHMARKS.md Phase 10 节扩充了 §优化前/后 Criterion 对比 子节，含同场景 throughput 数值表"
    - "全部 651+ cargo test 通过，clippy + fmt 净化（D-O3 质量闸）"
  artifacts:
    - path: "src/features/filters.rs"
      provides: "针对 10-01 samply 指认的热点函数的具体优化实现（如：合并 is_some() 预计算成 has_excludes 标志、减少 as_deref() 链调用、内联化等）"
      contains: "fn exclude_veto|fn include_and|fn should_keep"
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 10 §优化前/后 Criterion 对比 子节"
      contains: "### 优化前/后 Criterion 对比"
  key_links:
    - from: "10-01 BENCHMARKS.md §D-G1 门控判定 结论"
      to: "本计划任务执行（仅在命中时启动）"
      via: "grep -c '命中 D-G1' benches/BENCHMARKS.md"
      pattern: "\\*\\*结论：命中 D-G1\\."
    - from: "criterion 优化前 baseline"
      to: "criterion 优化后对比"
      via: "--save-baseline phase10_before / --baseline phase10_before"
      pattern: "phase10_before|phase10_after"
---

<objective>
Branch B-yes（条件执行）：若 10-01 §D-G1 门控判定 文本为 `**结论：命中 D-G1.**`（注意英文句号），则基于该判定指认的具体热点函数实施针对性优化，并提供 criterion 优化前/后吞吐对比数据写入 BENCHMARKS.md Phase 10 节。优化必须满足 D-O3 质量闸（clippy + test + 无回归）。

Purpose: D-O1 数据驱动 — 仅当 samply 客观证据满足 D-G1 三条件（>5% self time + src/ 业务逻辑 + 明确优化路径）时才实施优化，避免盲目 micro-optimization。
Output: src/features/filters.rs 针对热点函数的具体修改 + BENCHMARKS.md §优化前/后 Criterion 对比 子节。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hot-path/10-CONTEXT.md
@.planning/phases/10-hot-path/10-PATTERNS.md
@.planning/phases/10-hot-path/10-01-SUMMARY.md

<interfaces>
<!-- 热路径关键函数签名（用于优化目标定位） -->

来自 src/features/filters.rs：
- pub fn should_keep(&self, meta: &RecordMeta) -> bool  (line 395)
- fn exclude_veto(&self, meta: &RecordMeta) -> bool  (line 405)
- fn include_and(&self, meta: &RecordMeta) -> bool  (line 446)
- fn match_any_regex(patterns: Option<&[Regex]>, val: &str) -> bool  (line 285)
- pub fn has_any_filters(&self) -> bool  (MetaFilters，line 379)
- 字段：CompiledMetaFilters { usernames, client_ips, sess_ids, thrd_ids, statements, appnames, tags, exclude_usernames, exclude_client_ips, exclude_sess_ids, exclude_thrd_ids, exclude_statements, exclude_appnames, exclude_tags, trxids, ... }

潜在优化模式（PATTERNS.md §`src/features/filters.rs` 给出，仅参考；以 10-01 samply 实际指认为准）：
- 在 CompiledMetaFilters 增加预计算 has_excludes / has_includes 布尔标志字段，避免 7 次 is_some() 调用
- 简化 exclude_veto / include_and 中重复的 is_some() + match_any_regex 模板
- 减少 as_deref() 调用链
- 给热点小函数添加 #[inline] 属性

D-O3 质量闸（强制）：
- cargo clippy --all-targets -- -D warnings 必须通过
- cargo test 全量通过
- criterion 新 baseline 不低于优化前 throughput（容差 ≤ 5%）

criterion baseline 用法（来自 BENCHMARKS.md "How to compare"）：
- CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --save-baseline phase10_before
- CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --baseline phase10_before
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 前置确认 — 验证 10-01 已判定"命中 D-G1"并保存优化前 criterion baseline</name>
  <files>benches/BENCHMARKS.md（无修改，仅检查）, benches/baselines/（criterion 自动写入）</files>
  <read_first>
    - benches/BENCHMARKS.md（重点读 Phase 10 §D-G1 门控判定 子节，提取热点函数路径与优化路径描述）
    - .planning/phases/10-hot-path/10-01-SUMMARY.md（获取 10-01 计划的 Top N 数据快照与判定）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-G1 / D-O2 / D-O3 约束复核）
  </read_first>
  <action>
    第 1 步 - 检查判定门：执行 `grep -c "\\*\\*结论：命中 D-G1\\." benches/BENCHMARKS.md`（注意 grep 模式中的 `\.` 匹配英文句号 `.`，与 10-01 写入的字面文本一致）。如果返回 0（即 10-01 判定为"未命中 D-G1"或文本不存在），立即停止本计划并以 `[skip] 10-02 not applicable — 10-01 BENCHMARKS.md 判定未命中 D-G1，请执行 10-03 计划替代` 的明确日志退出（不修改任何源文件，不创建 SUMMARY 之外的产物）。

    第 2 步 - 从 BENCHMARKS.md §D-G1 门控判定 子节提取"热点函数"路径（形如 `src/<path>::<function>`）与"优化路径"一句话描述，将其作为后续 Task 2 的具体目标。在终端打印一次完整提取结果以便审计。

    第 3 步 - 保存优化前 criterion baseline：
    - `CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --save-baseline phase10_before`
    - 验证 `benches/baselines/` 下新增 `phase10_before` 目录（criterion 子目录结构）。

    本任务不修改 src/，仅做前置守门。
  </action>
  <verify>
    <automated>grep -c "\*\*结论：命中 D-G1\.\*\*" benches/BENCHMARKS.md &amp;&amp; find benches/baselines -name 'phase10_before' -type d | head -1 | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Behavior：`grep -c "\\*\\*结论：命中 D-G1\\.\\*\\*" benches/BENCHMARKS.md` 返回 1（注意：模式中的 `\.` 匹配英文句号 `.`，与 10-01 实际写入文本完全对齐；否则任务进入 skip 分支，整个 10-02 计划不再继续）
    - Source assertion：BENCHMARKS.md §D-G1 门控判定 子节包含具体 `src/<path>` 函数路径与优化路径描述
    - File exists：`benches/baselines/` 下存在 `phase10_before` 目录（`find benches/baselines -name "phase10_before" -type d | head -1 | grep -q .` exit 0）
    - 终端日志：打印"提取的热点函数：src/..."与"优化路径：..."两行
  </acceptance_criteria>
  <done>判定门检查通过，热点目标已锚定，phase10_before baseline 已保存</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 src/features/filters.rs 实施 10-01 指认的热点函数优化</name>
  <files>src/features/filters.rs</files>
  <read_first>
    - src/features/filters.rs（完整读取，特别是 lines 260-480 涵盖 CompiledMetaFilters / match_any_regex / should_keep / exclude_veto / include_and）
    - benches/BENCHMARKS.md §Phase 10 §D-G1 门控判定（明确知道要改的函数与优化方向）
    - .planning/phases/10-hot-path/10-PATTERNS.md（§`src/features/filters.rs` 子节给出参考优化模式，含 has_meta_filters 预计算先例 lines 43-44）
    - .planning/codebase/CONVENTIONS.md（命名/格式/属性使用规则：#[inline] 应用于热路径方法）
  </read_first>
  <action>
    基于 Task 1 提取的热点函数与优化路径（来自 BENCHMARKS.md），在 src/features/filters.rs 中实施针对性修改。修改必须严格满足以下契约：

    1. 修改范围最小化：仅触动 10-01 samply 指认的具体函数与其紧邻支持代码。不顺手重构无关函数。
    2. 行为等价：所有现有单元测试与集成测试必须不修改即通过；语义不得改变（exclude OR-veto 优先于 include AND 的逻辑顺序、空配置快路径、has_any_filters 语义全部保持）。
    3. 优化模式遵循项目约定：函数体 <40 行（CLAUDE.md 全局规则），描述性变量名，热路径方法标 `#[inline]`，公共方法标 `#[must_use]`（如适用，参照 CONVENTIONS.md "Attributes used frequently"）。
    4. 常见可选优化方向（以 10-01 实际指认为准）：
       - 在 CompiledMetaFilters 结构体增加 `has_excludes: bool` 与/或 `has_includes: bool` 预计算字段，在 `try_from_meta`（line 330 附近）构造时一次性计算，运行时 exclude_veto/include_and 入口先做 `if !self.has_excludes { return false; }` 短路。
       - 拆分或合并热点函数以减少分支预测失败。
       - 给小函数（match_any_regex 等）增加 `#[inline(always)]` 或确认 `#[inline]` 已存在。
    5. 修改完成后立即本地跑 `cargo clippy --all-targets -- -D warnings` 与 `cargo test` 确认基线通过。

    如果优化涉及新增结构字段，必须同时更新所有 `CompiledMetaFilters { ... }` 字面构造点（包括测试 fixture），确保编译通过；推荐采用 `Default + builder` 风格或集中构造函数避免重复。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --quiet 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion：`src/features/filters.rs` git diff 显示仅在 10-01 指认的热点函数及其支持代码内修改（diff hunks 不跨越 module，不触动 mod.rs / config.rs / run.rs 等无关模块）
    - Behavior：`cargo test` 全量通过，exit 0，且测试数 ≥ 651（CONTEXT.md/STATE.md 当前基线）
    - Behavior：`cargo clippy --all-targets -- -D warnings` 通过，exit 0
    - Behavior：`cargo fmt --check` 通过，exit 0
    - Source assertion：所有改动函数仍标 `#[inline]` 或 `#[inline(always)]`（如原本如此），未被意外移除
    - Source assertion：`grep -c "fn should_keep" src/features/filters.rs` 仍为 1（API 入口未删除）
  </acceptance_criteria>
  <done>热点函数优化已实施；clippy + test + fmt 全部通过；语义未变（测试零修改通过）</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 跑 criterion 优化后 baseline 并验证无回归，写入 BENCHMARKS.md §优化前/后对比 子节</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（Phase 10 节当前结构，确认插入位置在 §Filter Benchmark 与 §结论 之间）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-O2 / D-O3 数据要求）
    - .planning/phases/10-hot-path/10-PATTERNS.md（分支 B 模板：§优化前/后 Criterion 对比 表格格式）
  </read_first>
  <action>
    第 1 步 - 跑优化后 baseline 并与优化前对比：
    - `CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --save-baseline phase10_after`
    - 同时运行 `CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --baseline phase10_before`，记录 stdout 中每个场景的 `Performance has improved` / `No change in performance detected` / `Performance has regressed` 标注与百分比变化。

    第 2 步 - 回归守门：
    - 若任一场景出现 `Performance has regressed` 且回归百分比 > 5%（D-O3 容差），立即停止并以日志 `[blocked] regression detected in <scenario> X.X%，rollback 10-02 changes and document in BENCHMARKS.md` 退出。否则继续。
    - 7 个场景（no_pipeline / pipeline_passthrough / trxid_small / trxid_large / indicator_prescan / exclude_passthrough / exclude_active）全部不得回归 > 5%。

    第 3 步 - 在 BENCHMARKS.md Phase 10 节内 §Filter Benchmark 之后、§结论 之前插入新子节 `### 优化前/后 Criterion 对比`，内容包含：
    - 热点函数 + 优化简述（一句话引用 10-01 §D-G1 门控判定 的描述）。
    - 表格三列：Scenario / 优化前 median / 优化后 median / 变化（百分比，符号含正负），覆盖全部 7 个 bench 场景。
    - 折叠块 `<details><summary>cargo bench --baseline phase10_before 原始输出</summary>` 包裹完整 stdout。
    - 一句结论：`**优化达成：** 热点 X.X% → Y.Y%（减少 N.N pp），无场景回归 > 5%（D-O3 通过）。`

    第 4 步 - 更新 Phase 10 §结论 checkbox：将"若有热点：criterion 优化前/后 throughput 数据已记录"勾选为 `[x]`。
  </action>
  <verify>
    <automated>grep -c "### 优化前/后 Criterion 对比" benches/BENCHMARKS.md &amp;&amp; grep -c "phase10_before\|phase10_after" benches/BENCHMARKS.md &amp;&amp; cargo test --quiet 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion：`grep -c "### 优化前/后 Criterion 对比" benches/BENCHMARKS.md` 返回 1
    - Source assertion：BENCHMARKS.md 表格含全部 7 个场景行（`grep -cE "^\| .(no_pipeline|pipeline_passthrough|trxid_small|trxid_large|indicator_prescan|exclude_passthrough|exclude_active)" benches/BENCHMARKS.md` 返回 ≥ 7）
    - Source assertion：`grep -c "\*\*优化达成：\*\*" benches/BENCHMARKS.md` 返回 ≥ 1
    - File exists：`benches/baselines/` 下同时存在 `phase10_before` 与 `phase10_after` 目录
    - Behavior：`CRITERION_HOME=benches/baselines cargo bench --bench bench_filters -- --baseline phase10_before 2>&1 | grep -c "Performance has regressed"` 返回 0（任何场景未回归）— 若 grep 命中，须先回滚后才能进入完成状态
    - Behavior：`cargo test` 全量通过，exit 0
    - Behavior：`cargo clippy --all-targets -- -D warnings` 通过，exit 0
    - Source assertion：Phase 10 §结论 checkbox 中"criterion 优化前/后 throughput 数据已记录"被勾选 `[x]`
  </acceptance_criteria>
  <done>BENCHMARKS.md §优化前/后 Criterion 对比 子节已写入；零回归 > 5%；cargo test/clippy/fmt 全通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 优化补丁 → src/features/filters.rs | 修改热路径核心函数；行为正确性由现有 651+ 单元测试守护 |
| criterion baseline 对比 | benches/baselines/ 受 git 跟踪与否：当前作为本地参考；本计划不强制入库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | T (Tampering) | exclude_veto / include_and 逻辑顺序与短路语义 | mitigate | acceptance criteria 强制 cargo test 全量通过（≥651），任何语义偏移会被现有测试捕获；不修改测试以"绕过" |
| T-10-06 | D (DoS / 性能回归) | criterion 7 场景吞吐量 | mitigate | Task 3 强制 `Performance has regressed` 检查；> 5% 回归触发回滚日志，禁止继续 |
| T-10-07 | E (Elevation) | 新增结构字段未初始化 | mitigate | clippy + Rust 编译器强制所有 CompiledMetaFilters 构造点更新字段，编译期保证 |
| T-10-08 | I (Info Disclosure) | 优化补丁误暴露内部 invariant | accept | filters.rs 已是 pub crate 边界内代码，优化在同模块内，不扩大可见性 |
</threat_model>

<verification>
- Task 1 确认 10-01 判定为"命中 D-G1"（英文句号），否则整个计划 skip
- Task 2 优化补丁通过 cargo clippy + cargo test（≥651 测试）+ cargo fmt
- Task 3 criterion 全部 7 场景 `Performance has regressed` 0 命中（容差 5%）
- BENCHMARKS.md Phase 10 §优化前/后 Criterion 对比 子节存在并含 7 行场景对比表
- Phase 10 §结论 checkbox 含优化前/后数据已记录
</verification>

<success_criteria>
- 10-01 判定为"命中 D-G1"（英文句号 `.`）时本计划完整执行；判定为"未命中"时本计划自动 skip 并指向 10-03
- src/features/filters.rs 已实施 samply 指认的热点函数优化，无语义变化
- criterion 7 个场景无回归 > 5%，热点场景有实测改善（百分比记录在 BENCHMARKS.md）
- D-O3 质量闸全部通过：cargo clippy --all-targets -- -D warnings、cargo test、cargo fmt --check 全部 exit 0
- BENCHMARKS.md §优化前/后 Criterion 对比 子节完整记录前/后数值与变化百分比
</success_criteria>

<output>
After completion, create `.planning/phases/10-hot-path/10-02-SUMMARY.md` 包含：被优化的函数路径、优化策略、criterion 前/后吞吐量对比、各场景变化百分比、D-O3 质量闸通过记录。
</output>
</content>
</invoke>