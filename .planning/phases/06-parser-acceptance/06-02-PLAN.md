---
phase: 06-parser-acceptance
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: true
requirements:
  - PERF-09

must_haves:
  truths:
    - "cargo test 输出 0 failures，测试数量 ≥ 629"
    - "cargo clippy --all-targets -- -D warnings 输出 0 warnings"
    - "cargo fmt --check 无 diff（退出码 0）"
    - "v1.1 milestone 三项验收全部通过，可交付"
  artifacts:
    - path: ".planning/phases/06-parser-acceptance/06-02-SUMMARY.md"
      provides: "Phase 6 验收结果存档"
      contains: "PERF-09"
  key_links:
    - from: "cargo test"
      to: "src/ 所有测试"
      via: "Rust test harness"
      pattern: "test result: ok"
    - from: "cargo clippy"
      to: "所有源文件"
      via: "--all-targets -D warnings"
      pattern: "0 warnings"
---

<objective>
在 Wave 1 提交完成、git 工作区干净的基础上，执行 v1.1 milestone 全量验收（cargo test + clippy + fmt），确认所有 629+ 测试通过、零 clippy 警告、代码格式无 diff。

Purpose: 这是 v1.1「性能优化」milestone 的最终交付门控。三项全部通过即代表 milestone 可交付。
Output: 验收命令输出存档于 SUMMARY.md；ROADMAP.md 中 Phase 6 标记为完成。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-parser-acceptance/06-CONTEXT.md
@.planning/phases/06-parser-acceptance/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 执行三项验收命令并记录输出</name>
  <files></files>
  <read_first>
    - .planning/phases/06-parser-acceptance/06-01-SUMMARY.md（确认 Wave 1 已完成，git 工作区干净）
    - 运行 `git status --porcelain | grep -v "^?? .claude/"` 确认输出为空，再继续
  </read_first>
  <action>
按顺序执行以下三条命令，完整记录每条命令的输出（不截断）：

**命令 1 — 全量测试：**
```bash
cargo test 2>&1
```
期望：末行为 `test result: ok. N passed; 0 failed; ...`，其中 N ≥ 629。

**命令 2 — Clippy 零警告：**
```bash
cargo clippy --all-targets -- -D warnings 2>&1
```
期望：无 `warning:` 行，退出码 0。

**命令 3 — 代码格式检查：**
```bash
cargo fmt --check 2>&1
```
期望：无输出，退出码 0（有 diff 则退出码为 1，需修复后重试）。

如果任意命令失败：
- `cargo test` 失败 → 查看失败的测试名，定位代码问题后修复，重新运行
- `cargo clippy` 有 warning → 修复对应 lint 后重新运行，**不允许用 `#[allow(...)]` 忽略**
- `cargo fmt --check` 有 diff → 运行 `cargo fmt` 格式化后重新运行 `--check`

所有三项均通过后，将完整输出保存以便写入 SUMMARY.md。
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `cargo test` 末行匹配 `test result: ok\. [0-9]+ passed; 0 failed`，且通过数 ≥ 629
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无 `warning:` 行
    - `cargo fmt --check` 退出码为 0，无输出
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 ROADMAP.md，将 Phase 6 标记为完成</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md（读取完整内容，定位 Phase 6 条目的精确行号和格式）
  </read_first>
  <action>
在 ROADMAP.md 中定位 Phase 6 条目，完成以下两处修改：

**修改 1 — Progress 表格中 Phase 6 行：**

将：
```
| 6. 解析库集成 + 验收 | v1.1 | 0/? | Not started | — |
```
改为：
```
| 6. 解析库集成 + 验收 | v1.1 | 2/2 | Complete | 2026-05-10 |
```

**修改 2 — Phases 列表中 Phase 6 前缀：**

将：
```
- [ ] **Phase 6: 解析库集成 + 验收**
```
改为：
```
- [x] **Phase 6: 解析库集成 + 验收**
```

**修改 3 — Phase 6 Plans 占位符（若存在 "TBD"）：**

将 Phase 6 Details 段落中的 `**Plans**: TBD` 改为：
```
**Plans**: 2 plans
Plans:
- [x] 06-01-PLAN.md — 提交 Phase 4/5 遗留变更 + 记录 PERF-07 调研结论
- [x] 06-02-PLAN.md — 全量验收（cargo test + clippy + fmt）
```

仅修改上述三处，不改动其他内容。写回文件后用 grep 确认修改生效。
  </action>
  <verify>
    <automated>grep "Phase 6" /Users/guang/Projects/sqllog2db/.planning/ROADMAP.md | grep -v "^#"</automated>
  </verify>
  <done>
    - ROADMAP.md 中 Phase 6 进度表格行包含 "Complete" 和 "2026-05-10"
    - ROADMAP.md 中 Phase 6 列表项前缀为 `- [x]`
    - ROADMAP.md 中 Phase 6 Plans 数量为 "2 plans"（不含 "TBD"）
  </done>
</task>

<task type="auto">
  <name>Task 3: 提交验收结果（ROADMAP 更新）</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - 运行 `git diff .planning/ROADMAP.md` 确认只有 Phase 6 相关行被修改
  </read_first>
  <action>
暂存并提交 ROADMAP.md（以及执行过程中可能产生的其他规划文件更新）：

```bash
git add .planning/ROADMAP.md
# 如果 STATE.md 也有变更（执行框架更新），一并暂存：
git add .planning/STATE.md 2>/dev/null || true
git commit -m "$(cat <<'EOF'
docs(phase-6): complete v1.1 acceptance — all 629+ tests pass

- cargo test: 629+ passed, 0 failed (PERF-09 satisfied)
- cargo clippy --all-targets -- -D warnings: 0 warnings
- cargo fmt --check: no diff
- ROADMAP.md: Phase 6 marked complete (2/2 plans)
- v1.1 milestone delivery gate: PASSED
EOF
)"
```

提交后运行 `git log --oneline -3` 确认提交记录正确。
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
  </verify>
  <done>
    - `git log --oneline -1` 包含 "phase-6" 和 "acceptance"
    - `git log --oneline -1` 包含 "PERF-09"
    - `git status --porcelain | grep -v "^?? .claude/"` 输出为空
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码 → 生产代码 | cargo test 覆盖的功能边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | 测试套件完整性 | mitigate | 验收要求通过数 ≥ 629，防止测试被静默删除后「全部通过」 |
| T-06-04 | Repudiation | 验收记录 | mitigate | 完整命令输出写入 SUMMARY.md，保留可审计的验收证据 |
</threat_model>

<verification>
三项全量验收通过标志：
- `cargo test 2>&1 | grep "test result"` 输出匹配 `test result: ok\. [0-9]+ passed; 0 failed`
- `cargo test 2>&1 | grep "test result" | grep -oE "[0-9]+ passed" | grep -oE "^[0-9]+"` 值 ≥ 629
- `cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "^warning:"` 输出 0
- `cargo fmt --check; echo "exit:$?"` 输出包含 `exit:0`
- ROADMAP.md Phase 6 行包含 "Complete"
</verification>

<success_criteria>
- cargo test: ≥ 629 个测试通过，0 failures
- cargo clippy --all-targets -- -D warnings: 0 warnings，退出码 0
- cargo fmt --check: 无 diff，退出码 0
- ROADMAP.md Phase 6 标记为 Complete（2/2 plans，2026-05-10）
- git 工作区干净
- v1.1 milestone 三项验收门控全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/06-parser-acceptance/06-02-SUMMARY.md`，包含：
- 三项验收命令的完整输出原文
- 通过测试数量
- v1.1 milestone 交付声明
</output>
