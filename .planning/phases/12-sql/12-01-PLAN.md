---
phase: 12-sql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/sql_fingerprint.rs
autonomous: true
requirements:
  - TMPL-01

must_haves:
  truths:
    - "src/features/sql_fingerprint.rs 中新增 pub fn normalize_template(sql: &str) -> String"
    - "调用 normalize_template 对包含单行注释 `--` 的 SQL 去除注释段且保留结尾换行前的内容"
    - "调用 normalize_template 对包含多行注释 `/* ... */` 的 SQL 将注释块替换为单空格"
    - "对 `SELECT * FROM t WHERE id IN (1, 2, 3)` 和 `SELECT * FROM t WHERE id IN (10, 20, 30, 40)` 调用 normalize_template 返回相同的模板 key"
    - "对 `SELECT * FROM t WHERE name IN ('a', 'b')` 与 `IN ('xx', 'yy', 'zz')` 调用 normalize_template 返回相同模板 key（D-02）"
    - "select FROM t where id = 1 与 SELECT from T WHERE id = 2 经 normalize_template 后关键字均为大写形式"
    - "`WHERE col = '-- not a comment'` 经 normalize_template 后字符串字面量原文保留（D-15）"
    - "`SELECT  *  FROM  t` 经 normalize_template 后连续空白折叠为单空格"
    - "cargo test 中针对 normalize_template 的 7 项子测试全部通过"
    - "cargo clippy --all-targets -- -D warnings 通过且新增代码零 warning"
  artifacts:
    - path: "src/features/sql_fingerprint.rs"
      provides: "normalize_template() 函数 + 注释去除/IN 折叠/关键字大写/空白折叠四项变换 + 单元测试"
      contains: "pub fn normalize_template"
  key_links:
    - from: "src/features/sql_fingerprint.rs"
      to: "memchr crate"
      via: "memchr::memchr / memchr::memmem::find"
      pattern: "memchr::(memchr|memmem)"
    - from: "normalize_template()"
      to: "现有 fingerprint() 共享扫描结构"
      via: "NEEDS_SPECIAL_NORMALIZE 字节表（含 `'`、空白、`-`、`/`）+ prev_is_ident_byte 复用"
      pattern: "NEEDS_SPECIAL_NORMALIZE"
---

<objective>
在 src/features/sql_fingerprint.rs 中新增 pub fn normalize_template(sql: &str) -> String，完成 D-01 至 D-16 所有锁定决策定义的四项变换（注释去除、IN 列表折叠、关键字大小写统一、空白折叠），并保留字符串字面量原文。共享 fingerprint() 的字节级扫描结构（D-07/D-08），通过新增 NEEDS_SPECIAL_NORMALIZE 字节表（含 `-` 和 `/`）实现注释起始检测。

Purpose: 为 TMPL-01 提供稳定的模板 key 生成函数。Phase 13 的 TemplateAggregator::observe() 将以本函数返回值作为聚合 key（D-14）。
Output: src/features/sql_fingerprint.rs 新增 normalize_template + 私有辅助 + #[cfg(test)] 子模块包含 7 项行为子测试。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sql/12-CONTEXT.md
@.planning/phases/12-sql/12-RESEARCH.md
@.planning/phases/12-sql/12-PATTERNS.md
@.planning/phases/12-sql/12-VALIDATION.md
@src/features/sql_fingerprint.rs
</context>

<interfaces>
<!-- 现有 fingerprint() 暴露的内部约定，normalize_template 必须遵循 -->

From src/features/sql_fingerprint.rs:
- `const NEEDS_SPECIAL: [bool; 256]` — fingerprint() 专用查找表（保持不变）
- `pub fn fingerprint(sql: &str) -> String` — 现有指纹函数（保持行为不变）
- `fn prev_is_ident_byte(out: &[u8]) -> bool` — 标识符字节边界辅助（复用）

新增导出:
- `pub fn normalize_template(sql: &str) -> String`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 NEEDS_SPECIAL_NORMALIZE 字节表与 is_keyword 辅助（共享扫描基础）</name>
  <files>src/features/sql_fingerprint.rs</files>
  <read_first>
    - src/features/sql_fingerprint.rs（完整文件，重点 lines 1-18 NEEDS_SPECIAL 与 lines 106-109 prev_is_ident_byte）
    - .planning/phases/12-sql/12-CONTEXT.md（D-04/D-07/D-08）
    - .planning/phases/12-sql/12-PATTERNS.md（Pattern 2 关键字匹配、§NEEDS_SPECIAL 方案 A）
    - .planning/phases/12-sql/12-RESEARCH.md（Pitfall 4、Pitfall 5）
  </read_first>
  <behavior>
    - 新增 `const NEEDS_SPECIAL_NORMALIZE: [bool; 256]`：在原 NEEDS_SPECIAL 集合（`'`、空白、ASCII 数字）基础上额外将 `b'-'` 和 `b'/'` 标记为 true（采用 PATTERNS.md §NEEDS_SPECIAL 方案 A，不修改原 NEEDS_SPECIAL）。
    - 新增私有 `fn is_keyword(word: &[u8]) -> bool`：对输入 word 执行 ASCII case-insensitive 匹配，识别 SQL DML/DDL 关键字集合：SELECT, FROM, WHERE, AND, OR, JOIN, ON, AS, INSERT, UPDATE, DELETE, INTO, VALUES, SET, GROUP, ORDER, BY, HAVING, UNION, DISTINCT, LIMIT, CREATE, DROP, ALTER, IN, NOT, NULL, IS, BETWEEN, LIKE, EXISTS, CASE, WHEN, THEN, ELSE, END。实现采用 `word.to_ascii_uppercase()` 后 `matches!` 字节匹配（参照 RESEARCH.md Pattern 2，Assumption A2 允许 match 而非 phf）。
    - 新增私有 `fn is_ident_byte(b: u8) -> bool`：返回 `b.is_ascii_alphanumeric() || b == b'_' || b == b'.'`（与 prev_is_ident_byte 语义一致，但接受单字节用于 word 边界后向检测）。
  </behavior>
  <action>
在 src/features/sql_fingerprint.rs 现有 NEEDS_SPECIAL 常量之后追加 NEEDS_SPECIAL_NORMALIZE 常量，使用 const-fn 风格初始化（参照现有 NEEDS_SPECIAL 写法）。NEEDS_SPECIAL_NORMALIZE 必须包含 NEEDS_SPECIAL 全集再额外置 `b'-' as usize` 与 `b'/' as usize` 为 true。

在 prev_is_ident_byte 函数附近新增私有 fn is_keyword(word: &[u8]) -> bool 与 fn is_ident_byte(b: u8) -> bool。is_keyword 接受任意大小写形式输入并先做大写化（用 SmallVec 或栈数组避免堆分配；若 word.len() ≤ 16 用固定大小栈数组，超过则直接返回 false 因 SQL 关键字最长 `DISTINCT`/`BETWEEN` 为 7-8 字节）。

不修改任何现有公共/私有项的签名或行为。本任务仅添加新私有项，不写入主函数。
  </action>
  <verify>
    <automated>cargo build -p sqllog2db 2>&amp;1 | grep -E 'error|warning' | grep -v '^warning: unused' || true; cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c 'NEEDS_SPECIAL_NORMALIZE' src/features/sql_fingerprint.rs | grep -v '^#'` 至少为 2（声明 + 测试或后续任务引用）。
    - 源断言：`grep -E '^fn is_keyword' src/features/sql_fingerprint.rs` 输出 1 行。
    - 源断言：`grep -E '^fn is_ident_byte' src/features/sql_fingerprint.rs` 输出 1 行。
    - 源断言：NEEDS_SPECIAL_NORMALIZE 初始化代码块中存在 `b'-'` 与 `b'/'` 字面量。
    - 行为断言：现有 `cargo test fingerprint` 全部测试通过（不修改 fingerprint() 行为）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>NEEDS_SPECIAL_NORMALIZE、is_keyword、is_ident_byte 三项私有定义存在；现有 fingerprint 测试全绿；clippy 零 warning。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 normalize_template() 函数（注释去除 + 字面量保留 + 关键字大写 + 空白折叠 + IN 折叠）</name>
  <files>src/features/sql_fingerprint.rs</files>
  <read_first>
    - src/features/sql_fingerprint.rs（含 Task 1 新增的 NEEDS_SPECIAL_NORMALIZE / is_keyword / is_ident_byte）
    - .planning/phases/12-sql/12-CONTEXT.md（D-01 至 D-16 全部决策原文）
    - .planning/phases/12-sql/12-RESEARCH.md（Pattern 1-5、Pitfall 1-5、§Code Examples）
    - .planning/phases/12-sql/12-PATTERNS.md（§Pattern Assignments / sql_fingerprint.rs 节、§Shared Patterns）
  </read_first>
  <behavior>
    - 单遍字节扫描结构与 fingerprint() 完全一致：外层 while-loop + 内层 bulk-copy（用 NEEDS_SPECIAL_NORMALIZE）+ match 分发。
    - 字符串字面量 `b'\''` 分支：保留原文（不替换为 `?`），用 memchr::memchr 跳到闭合引号，处理 `''` 转义（与 fingerprint 字符串跳过逻辑一致但写回 out 时是原始字节 not `?`）。该分支必须先于注释检测分发（D-15）。
    - 单行注释 `b'-'` 且 i+1<len 且 bytes[i+1]==b'-'：用 memchr::memchr 找 `\n`；若找到 i += rel + 1，否则 i = len。out 不写入任何字节（D-16）。
    - 多行注释 `b'/'` 且 i+1<len 且 bytes[i+1]==b'*'：i += 2，用 memchr::memmem::find(&bytes[i..], b"*/") 跳过注释体；未闭合则 i = len。若 out 末尾不是空格则 push 单空格（D-16，避免 token 粘连）。
    - 字母字节 `b if b.is_ascii_alphabetic()`：向前消费完整 word（while is_ident_byte），调用 is_keyword(word)；若是关键字则 out.extend(word.to_ascii_uppercase()) （或循环 push 大写字节），否则原样写入 word。边界检查：前向用 prev_is_ident_byte(&out) == false；后向用 i == len 或 !is_ident_byte(bytes[i])（Pitfall 2、4）。
    - 空白字节 `b if b.is_ascii_whitespace()`：折叠为单空格（与 fingerprint 一致）。
    - 数字 `b if b.is_ascii_digit()`：在 normalize_template 中**不**替换为 `?`（与 fingerprint 的核心差异，研究文档 Pattern 1）；遇到数字直接 push 并继续 bulk-copy 模式（但因 NEEDS_SPECIAL_NORMALIZE 仍把数字标为特殊，需要在 match arm 中显式 push 后 advance）。
    - IN 列表折叠（D-01/D-02/D-03，采用 RESEARCH.md Pattern 5 策略 A 主循环内联）：在关键字写入逻辑中，当大写化结果等于 `b"IN"` 时，向前跳过空白，若下一个非空白字节是 `b'('`，则消费 `(` 至匹配 `)` 之间的全部内容并输出 `IN (?)`。括号嵌套处理：维护括号深度计数器，遇到嵌套 `(` 深度+1、`)` 深度-1，归零时停止。括号内出现 `SELECT`/`FROM` 关键字（子查询）时回退：保留原文不折叠（RESEARCH.md §Open Questions Q1 建议）。
    - 返回值：String::from_utf8(out).expect("normalize_template: invalid UTF-8") + trim_ascii，与 fingerprint 末尾完全相同的处理模式（PATTERNS.md §Shared Patterns / UTF-8 安全输出）。
    - 添加 `#[must_use]` 与 doc 注释（参照 fingerprint() 的 doc 风格）。
  </behavior>
  <action>
在 src/features/sql_fingerprint.rs 中 fingerprint() 函数之后新增 `pub fn normalize_template(sql: &str) -> String`。该函数复用 NEEDS_SPECIAL_NORMALIZE、prev_is_ident_byte、is_keyword、is_ident_byte。

实现要点（不内联代码体到本 action）：
- 单引号字面量保留原文（D-15）：写回 bytes[literal_start..i] 而非 `?`。
- 单行注释 `--` 跳至 `\n` 后位置（含 `\n`），out 不写入（D-16）。
- 多行注释 `/* */` 跳过整块，out 写入单空格（除非末尾已是空格）（D-16）。
- 关键字大写化：字母字节起始时 word_end = i + (while is_ident_byte 步进)；prev_is_ident_byte(out) 为 true 表示在标识符中部（如 outer_join 中的 outer），直接复制不大写化；word_end < len 且 is_ident_byte(bytes[word_end]) 同理（Pitfall 4 outer_key 反例）。
- IN 列表折叠（D-01/D-02/D-03）：当大写化后的 word == b"IN" 且通过前后边界检查时，先 push `IN` 到 out，然后向前 lookahead：跳过空白；若 bytes[k] == b'(`，则扫描到匹配 `)` 之间的全部字节，若该区间不含独立关键字 `SELECT` 或 `FROM`，则 push ` (?)` 到 out 并将 i 设为 `)` 之后；若含子查询关键字则放弃折叠，i 仅 advance 过 IN，主循环正常处理括号内容。
- 数字字面量在 normalize_template 不替换为 `?`（与 fingerprint 行为差异点）：数字分支仅 push 该字节并 i += 1（让 bulk-copy 再次接管）。
- 末尾返回值：String::from_utf8 后 trim_ascii，参照 fingerprint() lines 95-102 的写法。
- 该函数体允许超过 40 行的 CLAUDE.md 限制不适用此处？错误——CLAUDE.md 要求函数 <40 行。**因此必须拆分**：将注释处理、字面量保留、关键字大写、IN 折叠拆为私有辅助函数（如 `fn skip_line_comment(bytes: &[u8], i: usize) -> usize`、`fn skip_block_comment(bytes: &[u8], i: usize, out: &mut Vec<u8>) -> usize`、`fn copy_string_literal(bytes: &[u8], i: usize, out: &mut Vec<u8>) -> usize`、`fn write_word_uppercased(word: &[u8], out: &mut Vec<u8>)`、`fn try_fold_in_list(bytes: &[u8], cursor: usize, out: &mut Vec<u8>) -> Option<usize>`），主函数 normalize_template 仅作循环分发。每个辅助函数体必须 ≤ 40 行。

公开导出 `pub use` 暂不加（Plan 02 在 src/features/mod.rs 中统一新增）。本 plan 仅声明 `pub fn`。
  </action>
  <verify>
    <automated>cargo test --package sqllog2db --lib features::sql_fingerprint::tests::normalize_template -- --nocapture 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -E '^pub fn normalize_template' src/features/sql_fingerprint.rs` 输出 1 行。
    - 源断言：normalize_template 主函数体（pub fn 开头到对应 `}`）所在行数差 ≤ 40（CLAUDE.md 函数长度约束）。
    - 源断言：每个新增私有辅助函数体行数 ≤ 40。
    - 行为断言（在本任务 #[cfg(test)] mod tests 内新增）：
      * `normalize_template("-- comment\nSELECT 1")` 返回 `"SELECT 1"`（单行注释去除，D-16；数字 1 保留不替换）。
      * `normalize_template("/* multi */ SELECT 1")` 返回 `"SELECT 1"`（多行注释替换为空格，前导空格被 trim_ascii 移除）。
      * `normalize_template("SELECT * FROM t WHERE id IN (1, 2, 3)")` 与 `normalize_template("SELECT * FROM t WHERE id IN (10, 20, 30, 40)")` 返回相同 String（D-01/D-03）。
      * `normalize_template("SELECT * FROM t WHERE name IN ('a', 'b')")` 与 `normalize_template("SELECT * FROM t WHERE name IN ('xx', 'yy', 'zz')")` 返回相同 String（D-02）。
      * `normalize_template("select * from t where id = 1")` 中 select / from / where 三个关键字大写化结果出现在返回值中（D-04）。
      * `normalize_template("SELECT a FROM outer_join_t")` 返回值保留 `outer_join_t` 原样（不大写化 `OUTER`，Pitfall 4）。
      * `normalize_template("WHERE col = '-- not a comment'")` 返回值字面量 `'-- not a comment'` 原样保留（D-15）。
      * `normalize_template("SELECT  *  FROM  t")` 返回值连续空白折叠为单空格。
    - 命令：`cargo test features::sql_fingerprint` 全部通过（含现有 fingerprint 测试 + 新增 normalize_template 子测试至少 7 个）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>pub fn normalize_template 存在且行为符合所有 D-01 至 D-16 决策；现有 fingerprint 测试不回归；7 项 normalize_template 行为子测试全绿；clippy 零 warning。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 日志文件 → 解析器 | 外部 SQL 日志文本，可能含恶意构造的注释/字面量边界 |
| 解析器 → normalize_template | 已通过 dm-database-parser-sqllog 解析的 sql 字符串切片，UTF-8 已保证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | normalize_template 字节扫描 | mitigate | 字符串字面量优先分发（D-15），注释检测只能发生在字面量之外；测试 `'-- not a comment'` 反例覆盖 |
| T-12-02 | Denial of Service | 未闭合 `/* */` 注释 | mitigate | 未找到 `*/` 时 i = len，主循环立即结束，无死循环风险；测试需含未闭合注释样本 |
| T-12-03 | Information Disclosure | 输出 buffer UTF-8 校验 | mitigate | from_utf8 expect 论证：所有插入字节均为 ASCII（≤0x7F），不破坏多字节 UTF-8 序列；与现有 fingerprint() 一致 |
| T-12-SC | Tampering | 无 npm/pip/cargo install | accept | 本阶段不引入任何新依赖（RESEARCH.md 已确认），跳过包合法性审计 |
</threat_model>

<verification>
- `cargo test features::sql_fingerprint` 整体通过（含原 fingerprint 9 项 + normalize_template 至少 7 项新增）。
- `cargo clippy --all-targets -- -D warnings` 零 warning。
- `cargo fmt` 已执行，无格式差异。
- 现有 fingerprint() 测试套件零回归（NEEDS_SPECIAL 表与 fingerprint() 主函数未被修改）。
</verification>

<success_criteria>
- pub fn normalize_template 已声明并实现，覆盖 D-01 至 D-16 全部决策。
- 7 项行为子测试全部通过（注释去除 / IN 折叠 / 字符串字面量 IN 折叠 / 关键字大写 / 字面量内注释保护 / 空白折叠 / 关键字边界 outer_join_t）。
- 共享扫描结构通过 NEEDS_SPECIAL_NORMALIZE 字节表实现，未触及原 NEEDS_SPECIAL 与 fingerprint() 行为（D-07/D-08）。
- 所有新增函数体 ≤ 40 行（遵守 CLAUDE.md 全局约束）。
</success_criteria>

<output>
Create `.planning/phases/12-sql/12-01-SUMMARY.md` when done
</output>
