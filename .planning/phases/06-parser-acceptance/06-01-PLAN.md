---
phase: 06-parser-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - config.toml
  - benches/baselines/sqlite_export/1000/change/estimates.json
  - benches/baselines/sqlite_export/10000/change/estimates.json
  - benches/baselines/sqlite_export/50000/change/estimates.json
  - benches/baselines/sqlite_export/1000/base/
  - benches/baselines/sqlite_export/10000/base/
  - benches/baselines/sqlite_export/50000/base/
  - benches/baselines/sqlite_export_real/real_file/base/
  - benches/baselines/sqlite_single_row/
  - .planning/phases/04-csv/04-REVIEW.md
  - .planning/phases/04-csv/04-REVIEW-FIX.md
  - .planning/config.json
autonomous: true
requirements:
  - PERF-07

must_haves:
  truths:
    - "PERF-07 调研结论已明确记录：mmap 零拷贝和 par_iter() 在 0.9.1 已存在，1.0.0 改进自动生效，index()/RecordIndex 不集成"
    - "Cargo.toml 中 dm-database-parser-sqllog = \"1.0.0\" 已提交"
    - "所有 Phase 4/5 遗留的未提交变更已纳入一次原子提交，git status 干净"
  artifacts:
    - path: "Cargo.toml"
      provides: "解析库版本声明"
      contains: "dm-database-parser-sqllog = \"1.0.0\""
    - path: "benches/BENCHMARKS.md"
      provides: "PERF-07 调研结论注释"
      contains: "PERF-07"
  key_links:
    - from: "Cargo.toml"
      to: "dm-database-parser-sqllog 1.0.0"
      via: "crates.io 依赖声明"
      pattern: "dm-database-parser-sqllog.*1\\.0\\.0"
---

<objective>
提交所有 Phase 4/5 遗留的未提交变更（Cargo.toml 升级、benchmark 基线、review 记录、config.toml 简化），并在 BENCHMARKS.md 中记录 PERF-07 调研结论，正式关闭该需求。

Purpose: 保持仓库 git status 干净，使 Wave 2 验收在明确的代码状态上进行；PERF-07 结论明文存档，避免后续重复调研。
Output: 一次原子 git commit，包含所有遗留变更 + BENCHMARKS.md PERF-07 注释段。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-parser-acceptance/06-CONTEXT.md
@benches/BENCHMARKS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 BENCHMARKS.md 追加 PERF-07 调研结论段落</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（读取完整内容，确认末尾位置和现有格式风格）
    - .planning/phases/06-parser-acceptance/06-CONTEXT.md（读取 D-01 至 D-04 决策原文）
    - Cargo.toml（确认 dm-database-parser-sqllog 版本为 "1.0.0"，line 33）
  </read_first>
  <action>
在 BENCHMARKS.md 末尾（Phase 5 结论段之后）追加以下内容，与现有段落格式保持一致（三级 `##` 标题、表格、结论 checkbox 列表）：

```markdown
---

## Phase 6 — 解析库集成评估（PERF-07）

**Date:** 2026-05-10
**Goal:** 评估 dm-database-parser-sqllog 1.0.0 新 API，按需集成零拷贝或批量解析接口

### 调研结论

| API / 特性 | 版本引入 | 评估结论 |
|-----------|---------|---------|
| mmap 零拷贝读取 | 已有（0.9.1） | 当前 `LogParser::from_path()` 已使用，1.0.0 自动生效 |
| `par_iter()` 文件内并行 | 已有（0.9.1） | 预扫描路径（`scan_log_file_for_matches`）已调用，1.0.0 小文件单分区优化自动生效 |
| 更完整的编码检测（头+尾双采样） | 1.0.0 新增 | `LogParser::from_path()` 内部实现，无需代码变更，自动生效 |
| `MADV_SEQUENTIAL` 预读 hint | 1.0.0 新增 | mmap 层内部，无需代码变更，自动生效 |
| `index()` / `RecordIndex` 两阶段字节偏移索引 | 1.0.0 新增 | **不集成**：适用随机访问场景，当前为流式写入（顺序遍历），引入无收益 |

### 集成决策

- **Cargo.toml 版本**：0.9.1 → 1.0.0（已升级，`cargo check` 无 API 破坏性变更）
- **代码变更**：无（仅版本号，所有改进通过库内部升级自动获得）
- **index() 集成**：不集成，原因：两阶段字节偏移索引扫描适用大规模并行随机访问，与当前单线程顺序流式写入场景不符；如未来有大规模并行需求可重新评估

### 结论

- [x] PERF-07 评估完成，调研结论明文存档，需求关闭
- [x] 1.0.0 自动获得的改进（编码检测、MADV_SEQUENTIAL、小文件分区优化）无需代码变更
- [x] `index()` / `RecordIndex` 评估后决定不集成（原因：流式场景无收益）
- [x] `cargo check` 通过，无 API 破坏性变更
```
  </action>
  <verify>
    <automated>grep -c "PERF-07" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md</automated>
  </verify>
  <done>
    - benches/BENCHMARKS.md 末尾存在 "## Phase 6 — 解析库集成评估（PERF-07）" 标题
    - 文件中 "PERF-07" 字符串出现至少 3 次（标题 + 表格 + 结论 checkbox）
    - "index()" 和 "不集成" 同时出现在文件中
    - "dm-database-parser-sqllog" 出现在文件中
  </done>
</task>

<task type="auto">
  <name>Task 2: 提交所有 Phase 4/5 遗留变更 + BENCHMARKS.md 更新（原子 commit）</name>
  <files>
    Cargo.toml, Cargo.lock, config.toml,
    benches/BENCHMARKS.md,
    benches/baselines/sqlite_export/1000/change/estimates.json,
    benches/baselines/sqlite_export/10000/change/estimates.json,
    benches/baselines/sqlite_export/50000/change/estimates.json,
    benches/baselines/sqlite_export/1000/base/,
    benches/baselines/sqlite_export/10000/base/,
    benches/baselines/sqlite_export/50000/base/,
    benches/baselines/sqlite_export_real/real_file/base/,
    benches/baselines/sqlite_single_row/,
    .planning/phases/04-csv/04-REVIEW.md,
    .planning/phases/04-csv/04-REVIEW-FIX.md,
    .planning/config.json
  </files>
  <read_first>
    - 运行 `git status` 确认当前未提交文件列表与 D-06 清单匹配
  </read_first>
  <action>
按以下步骤提交所有遗留变更，使用一个原子 commit：

1. 暂存 D-06 清单中的所有文件（使用具体路径，不用 `git add -A`）：
   ```bash
   git add Cargo.toml Cargo.lock config.toml benches/BENCHMARKS.md
   git add "benches/baselines/sqlite_export/1000/change/estimates.json"
   git add "benches/baselines/sqlite_export/10000/change/estimates.json"
   git add "benches/baselines/sqlite_export/50000/change/estimates.json"
   git add "benches/baselines/sqlite_export/1000/base/"
   git add "benches/baselines/sqlite_export/10000/base/"
   git add "benches/baselines/sqlite_export/50000/base/"
   git add "benches/baselines/sqlite_export_real/real_file/base/"
   git add "benches/baselines/sqlite_single_row/"
   git add ".planning/phases/04-csv/04-REVIEW.md"
   git add ".planning/phases/04-csv/04-REVIEW-FIX.md"
   git add ".planning/config.json"
   ```

2. 创建提交（conventional commit 格式）：
   ```
   chore(phase-6): commit phase 4/5 artifacts and close PERF-07
   
   - Cargo.toml/Cargo.lock: upgrade dm-database-parser-sqllog 0.9.1 → 1.0.0
   - config.toml: phase 5 batch_size simplification
   - benches/baselines: add phase 5 sqlite baselines (single_row, sqlite_export
     base snapshots, sqlite_export_real base) and phase 5 change estimates
   - .planning/phases/04-csv: add 04-REVIEW-FIX.md, update 04-REVIEW.md
   - benches/BENCHMARKS.md: document PERF-07 evaluation and closure
   ```

3. 运行 `git status` 确认工作区干净（无未跟踪或已修改文件，除 `.claude/` 目录外）。
  </action>
  <verify>
    <automated>git status --porcelain | grep -v "^?? .claude/" | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - `git log --oneline -1` 显示 "chore(phase-6): commit phase 4/5 artifacts and close PERF-07"
    - `git status --porcelain | grep -v "^?? .claude/"` 输出为空（工作区干净）
    - `git show --name-only HEAD | grep "Cargo.toml"` 有输出（Cargo.toml 在提交中）
    - `git show --name-only HEAD | grep "BENCHMARKS.md"` 有输出
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → Cargo.toml | 第三方库版本升级，可能引入 API 破坏性变更 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Cargo.lock | accept | 版本锁定文件由 cargo 管理，升级前 `cargo check` 已验证无破坏性变更（D-04）|
| T-06-02 | Denial of Service | 第三方库升级 | mitigate | Wave 2 的 `cargo test` 全量回归作为最终验收门控，确保升级无功能退化 |
</threat_model>

<verification>
- `grep -c "PERF-07" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md` 输出 ≥ 3
- `grep "dm-database-parser-sqllog.*1\.0\.0" /Users/guang/Projects/sqllog2db/Cargo.toml` 有输出
- `git status --porcelain | grep -v "^?? .claude/"` 输出为空
- `git log --oneline -1` 包含 "phase-6" 和 "PERF-07"
</verification>

<success_criteria>
- BENCHMARKS.md 中 PERF-07 调研段落存在，包含 index() 不集成的明确原因
- Cargo.toml dm-database-parser-sqllog 版本为 "1.0.0"
- git 工作区干净（仅 .claude/ 未跟踪目录允许存在）
- 一次原子提交包含所有 D-06 清单文件
</success_criteria>

<output>
完成后创建 `.planning/phases/06-parser-acceptance/06-01-SUMMARY.md`
</output>
