---
phase: 05-sqlite
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - benches/BENCHMARKS.md
autonomous: false
requirements:
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "criterion 运行 sqlite_export 和 sqlite_single_row 两个 group，输出可量化对比数值"
    - "BENCHMARKS.md 含 Phase 5 section，记录实测 WAL+批量 vs 单行提交对比数值"
    - "sqlite_export/10000 median ≤ 7.424ms（Phase 3 hard limit，5% 容差）"
    - "prepare_cached 复用通过代码审查 + 注释确认（PERF-06）"
  artifacts:
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 5 SQLite 性能数据区块"
      contains: "Phase 5"
  key_links:
    - from: "criterion sqlite_export/10000"
      to: "Phase 3 baseline 7.070ms"
      via: "CRITERION_HOME=benches/baselines cargo bench --bench bench_sqlite -- --baseline v1.0"
      pattern: "sqlite_export/10000"
---

<objective>
运行 criterion benchmark 对比 Phase 5 实测数值与 v1.0 baseline，通过人工审查 prepare_cached 注释确认 PERF-06，并将所有数值更新到 BENCHMARKS.md。

Purpose: 量化验收三项优化的实际效果，并确保 sqlite_export/10000 不超过 7.424ms 的 hard limit。
Output: BENCHMARKS.md Phase 5 section（含 WAL+批量 vs 单行提交对比表 + Criterion 原文）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-sqlite/05-RESEARCH.md
@.planning/phases/05-sqlite/05-01-SUMMARY.md
@.planning/phases/05-sqlite/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 criterion benchmark + 代码审查 prepare_cached 注释</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - benches/BENCHMARKS.md (必读：找到 Phase 4 section 末尾，确认追加位置)
    - src/exporter/sqlite.rs (必读：确认 prepare_cached 调用处有复用说明注释)
  </read_first>
  <action>
    1. 运行全套测试确认无回归：
       ```bash
       cargo test
       ```

    2. 运行 sqlite_export + sqlite_single_row benchmark（含 baseline 对比）：
       ```bash
       cargo build --release 2>&1 | tail -3
       CRITERION_HOME=benches/baselines cargo bench --bench bench_sqlite -- \
           sqlite_export sqlite_single_row --baseline v1.0 --sample-size 20 2>&1 | tee /tmp/sqlite_bench_phase5.txt
       ```
       记录 sqlite_export/1000、sqlite_export/10000、sqlite_export/50000、sqlite_single_row/1000、sqlite_single_row/10000 的 median 和 vs v1.0 变化百分比。

    3. 检查 sqlite_export/10000 的 median 是否 ≤ 7.424ms。若超出，输出错误信息并停止。

    4. 在 `export_one_preparsed()` / `export()` / `export_one_normalized()` 中 `prepare_cached` 调用处，确认存在以下注释（或等效文字说明）。若缺失则添加：
       ```rust
       // prepare_cached() 通过 StatementCache（LRU，容量 16）复用已编译的 statement。
       // 每次调用的开销为 RefCell::borrow_mut() + HashMap lookup (O(1))，
       // 而不是 sqlite3_prepare_v3()（O(parse)）。PERF-06 满足。
       ```
       注释仅需在 `export_one_preparsed()` 中添加一次（热路径入口处）即可。

    完成后运行 `cargo clippy --all-targets -- -D warnings` 和 `cargo fmt`。
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -5 && echo "---" && cat /tmp/sqlite_bench_phase5.txt | grep -E "sqlite_export/10000|sqlite_single_row" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` 退出码为 0，failure 为 0
    - `/tmp/sqlite_bench_phase5.txt` 文件存在，内容包含 `sqlite_export/10000` 和 `sqlite_single_row`
    - sqlite_export/10000 的 median 数值（从 criterion 输出读取）≤ 7.424 ms
    - `grep -n "prepare_cached\|PERF-06\|StatementCache" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 至少包含 "PERF-06" 或 "StatementCache" 字样的注释行
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无警告
  </acceptance_criteria>
  <done>全套测试通过，benchmark 输出已采集，sqlite_export/10000 ≤ 7.424ms，prepare_cached 注释已添加</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Criterion benchmark 已运行完毕，输出记录在 /tmp/sqlite_bench_phase5.txt。
    三项 SQLite 优化已实施：
    - WAL 模式已启用（test_sqlite_wal_mode_enabled 通过）
    - page_size=65536 在 WAL 下生效（test_sqlite_wal_page_size 通过）
    - 批量事务路径已覆盖（test_sqlite_batch_commit 通过）
    - prepare_cached 复用已通过代码注释确认（PERF-06）
  </what-built>
  <how-to-verify>
    1. 查看 benchmark 输出：
       ```bash
       cat /tmp/sqlite_bench_phase5.txt | grep -E "sqlite_export|sqlite_single_row"
       ```
       确认有 sqlite_export/10000 和 sqlite_single_row 的数据行。

    2. 确认 sqlite_export/10000 的 median ≤ 7.424ms（Phase 3 hard limit）。

    3. 确认 sqlite_single_row 的数值明显慢于 sqlite_export（WAL + 批量提交 vs 单行提交的对比）。
       注：WAL + synchronous=NORMAL 下差异可能小于 OFF 模式，但仍应有可量化差距。

    4. 可选：查看 benchmark 对比报告是否包含 "Performance has improved" 或 "No change in performance"（取决于实际测量）。

    5. 确认全套测试通过：
       ```bash
       cargo test 2>&1 | tail -3
       ```
  </how-to-verify>
  <resume-signal>输入 "approved" 继续（或描述问题）</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 更新 BENCHMARKS.md — 写入 Phase 5 实测数值</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md (必读：找到 Phase 4 section 末尾，确认追加位置)
  </read_first>
  <action>
    读取 BENCHMARKS.md 全文，在 Phase 4 section 末尾之后追加 Phase 5 section，用 Task 1 采集的实际数值填入表格（替换 TBD 占位符）：

    ```markdown
    ---

    ## Phase 5 — SQLite 性能优化（WAL + 批量事务）

    **Date:** {今天日期}
    **Goal:** WAL 模式 + 批量事务，sqlite_export/10000 ≤ 7.424ms hard limit
    **Test environment:** Apple Silicon (Darwin 25.4.0), release build (opt-level=3, LTO=fat, strip=symbols, panic=abort), Rust stable, Criterion {样本数} samples.

    ### 各 Wave 数值

    | Group | v1.0 baseline | Phase 5 实测（WAL + batch_size=10000） | vs v1.0 |
    |-------|--------------|----------------------------------------|---------|
    | sqlite_export/1000    | 0.851 ms | {实测值} | {变化%} |
    | sqlite_export/10000   | 7.070 ms | {实测值} | {变化%} |
    | sqlite_export/50000   | 35.603 ms | {实测值} | {变化%} |
    | sqlite_single_row/1000 | — | {实测值} | — |
    | sqlite_single_row/10000 | — | {实测值} | — |

    > 注：sqlite_single_row 为 PERF-04 可量化对照组（batch_size=1，每条 INSERT 独立 BEGIN/COMMIT）。
    > WAL + synchronous=NORMAL 模式下，批量事务 vs 单行提交差异体现在事务边界 fsync 开销。

    ### Criterion 输出原文

    <details>
    <summary>cargo bench --bench bench_sqlite --baseline v1.0 (Phase 5)</summary>

    ```
    {将 /tmp/sqlite_bench_phase5.txt 的关键内容粘贴至此}
    ```

    </details>

    ### 优化实施总结

    | 优化项 | 实施内容 | 验证方式 |
    |--------|---------|---------|
    | PERF-04 批量事务 | batch_size=10_000，每 N 条 COMMIT+BEGIN | criterion sqlite_single_row 对照 |
    | PERF-05 WAL 模式 | journal_mode=WAL + page_size=65536（顺序修正） | test_sqlite_wal_mode_enabled / test_sqlite_wal_page_size |
    | PERF-06 prepared statement | prepare_cached() LRU 复用（已是现状，代码注释确认） | 代码审查 + 注释 |

    ### 结论

    - [x] PERF-04 批量事务 benchmark 可量化（sqlite_single_row 对照组已建立）
    - [x] PERF-05 WAL 模式已启用（集成测试断言通过）
    - [x] PERF-06 prepared statement 复用确认（代码注释 + 代码审查）
    - [{通过则 x，超限则空}] sqlite_export/10000 ≤ 7.424ms hard limit（实测：{实测值}）
    ```

    用 Task 1 采集的实际数值替换所有 `{实测值}` 和 `{变化%}` 占位符。
  </action>
  <verify>
    <automated>grep -c "Phase 5" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 5" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md` 输出 ≥ 1（section 存在）
    - `grep -n "sqlite_export/10000" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md` 包含 Phase 5 section 中的实测行（含具体毫秒数，非 TBD）
    - `grep -n "sqlite_single_row" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md` 包含 sqlite_single_row/10000 的实测值行
    - `grep -n "PERF-04\|PERF-05\|PERF-06" /Users/guang/Projects/sqllog2db/benches/BENCHMARKS.md` 在 Phase 5 section 中均有 `[x]` 标记行
  </acceptance_criteria>
  <done>BENCHMARKS.md Phase 5 section 已写入，所有 TBD 替换为实测数值，三项需求验收标记完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /tmp/sqlite_bench_phase5.txt → BENCHMARKS.md | benchmark 输出为本机产生，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | benchmark 测量噪声导致误判 hard limit | mitigate | hard limit = v1.0 median × 1.05（7.424ms）已含 5% 容差；若超限由 checkpoint 人工确认是否为硬件波动 |
| T-05-10 | Information Disclosure | /tmp/sqlite_bench_phase5.txt | accept | 本机临时文件，无 PII，仅性能数据 |
</threat_model>

<verification>
Plan 03 完成后整体验证：

```bash
# 全套测试最终确认
cargo test

# benchmark 采集确认（含 baseline 对比）
CRITERION_HOME=benches/baselines cargo bench --bench bench_sqlite -- sqlite_export --baseline v1.0 --sample-size 10

# BENCHMARKS.md 有 Phase 5 内容
grep "Phase 5" benches/BENCHMARKS.md
```
</verification>

<success_criteria>
- cargo test 全套测试通过，failure 为 0
- sqlite_export/10000 median ≤ 7.424ms（5% 容差 hard limit）
- criterion 输出 sqlite_single_row group 数据（PERF-04 可量化对照）
- BENCHMARKS.md Phase 5 section 含实测数值（无 TBD 占位符）
- prepare_cached 复用已通过代码注释确认（PERF-06）
- PERF-04、PERF-05、PERF-06 在 BENCHMARKS.md 均标记 [x]
</success_criteria>

<output>
完成后创建 `.planning/phases/05-sqlite/05-03-SUMMARY.md`
</output>
