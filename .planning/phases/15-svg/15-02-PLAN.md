---
phase: 15-svg
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/template_aggregator.rs
  - src/features/mod.rs
autonomous: true
requirements: [CHART-02, CHART-03]
must_haves:
  truths:
    - "TemplateAggregator 暴露 pub struct ChartEntry<'a> { key, count, histogram } 供 charts 模块借用"
    - "iter_chart_entries() 返回的迭代器按 count 降序排列（count 相同时按 key 升序，与 finalize() 排序一致）"
    - "iter_chart_entries() 接收 &self（不消耗），后续仍可调用 finalize()"
    - "ChartEntry 通过 features::ChartEntry 在外部可见（pub use 导出）"
  artifacts:
    - path: "src/features/template_aggregator.rs"
      provides: "pub struct ChartEntry<'a> + impl TemplateAggregator { pub fn iter_chart_entries(&self) -> impl Iterator<Item = ChartEntry<'_>> } + 3 单元测试"
      contains: "pub struct ChartEntry"
    - path: "src/features/mod.rs"
      provides: "pub use template_aggregator::ChartEntry"
      contains: "pub use template_aggregator::ChartEntry"
  key_links:
    - from: "src/features/template_aggregator.rs::iter_chart_entries"
      to: "TemplateEntry.histogram（私有字段）"
      via: "同 impl 块内访问私有字段（Rust 可见性规则）"
      pattern: "iter_chart_entries"
---

<objective>
为 Phase 15 暴露聚合器的图表数据访问接口：在 `src/features/template_aggregator.rs` 新增 `pub struct ChartEntry<'a>` 和 `pub fn iter_chart_entries(&self) -> impl Iterator<Item = ChartEntry<'_>>` 方法，按 count 降序排列；在 `src/features/mod.rs` 通过 `pub use` 导出 `ChartEntry`。

Purpose: D-01 要求图表生成在 `finalize()` 之前发生且直接访问内部 histogram。histogram 字段当前私有，必须由 TemplateAggregator 模块本身暴露 chart-oriented 借用接口。Plan 03/04 将通过 ChartEntry 读取 key/count/histogram 三元组。
Output: 公共借用视图 + 排序保证 + 单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-svg/15-CONTEXT.md
@.planning/phases/15-svg/15-RESEARCH.md
@.planning/phases/15-svg/15-PATTERNS.md

<interfaces>
<!-- 现有相关定义（来自 src/features/template_aggregator.rs） -->

From src/features/template_aggregator.rs:
- struct TemplateEntry { histogram: Histogram<u64>, first_seen: String, last_seen: String }（private struct + private fields）
- impl TemplateAggregator { pub fn new() -> Self }
- impl TemplateAggregator { pub fn observe(&mut self, key: &str, exectime_us: u64, ts: &str) }
- impl TemplateAggregator { pub fn merge(&mut self, other: TemplateAggregator) }
- impl TemplateAggregator { pub fn finalize(self) -> Vec<TemplateStats> }（consumes self；排序逻辑 L124-129：`stats.sort_unstable_by(|a,b| b.count.cmp(&a.count).then_with(|| a.template_key.cmp(&b.template_key)))`）
- pub struct TemplateStats { ... pub count: u64, ... }
- entries 字段类型：`ahash::AHashMap<String, TemplateEntry>`

From src/features/mod.rs:
- pub use template_aggregator::TemplateAggregator;
- pub use template_aggregator::TemplateStats;
（追加 pub use template_aggregator::ChartEntry;）

From hdrhistogram 7.5.4:
- Histogram<u64>::len(&self) -> u64
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 template_aggregator.rs 新增 ChartEntry + iter_chart_entries() + 3 单元测试</name>
  <files>src/features/template_aggregator.rs</files>
  <read_first>
    - src/features/template_aggregator.rs（完整读取一次：理解 TemplateEntry 私有结构、finalize() 排序逻辑 L124-129、entries 字段类型 ahash::AHashMap<String, TemplateEntry>、现有 6 个单元测试模式 L137-254）
    - .planning/phases/15-svg/15-CONTEXT.md（D-01 要求图表生成在 finalize 之前，访问内部 histogram）
    - .planning/phases/15-svg/15-RESEARCH.md（§"Pattern 4: TemplateAggregator 公开迭代接口" — ChartEntry 字段顺序为 key/count/histogram）
    - .planning/phases/15-svg/15-PATTERNS.md（§"src/features/template_aggregator.rs" 节，特别是排序模式）
  </read_first>
  <behavior>
    - Test test_iter_chart_entries_empty: 新建 aggregator 不 observe，iter_chart_entries().collect::<Vec<_>>() 为空
    - Test test_iter_chart_entries_single_key: observe 同一 key 3 次后，迭代器返回 1 项，count=3，key 与 observe 传入一致，histogram 引用可调用 .len() 返回 3
    - Test test_iter_chart_entries_sort_order: observe key_a 2 次、key_b 5 次、key_c 1 次 → 迭代结果 [key_b(5), key_a(2), key_c(1)]，与 finalize 排序一致
  </behavior>
  <action>
    在 src/features/template_aggregator.rs 进行 2 处新增：

    1. 在文件顶层（紧跟现有 `pub struct TemplateStats` 定义之后、`pub struct TemplateAggregator` 之前），新增：

       `pub struct ChartEntry<'a>`，字段顺序与可见性：
       - `pub key: &'a str`
       - `pub count: u64`
       - `pub histogram: &'a hdrhistogram::Histogram<u64>`

       派生 `Debug`（用于 #[derive(Debug)] 一致性，无需 Clone/Copy 因为含引用）。添加 doc 注释 "图表生成专用的只读视图：在 finalize 前调用，不消耗 self"。

    2. 在 `impl TemplateAggregator` 块内（位置：紧跟 finalize 方法之后，即 L131 之前），新增方法：

       `pub fn iter_chart_entries(&self) -> impl Iterator<Item = ChartEntry<'_>>`

       实现逻辑：
       - 用 `self.entries.iter()` 遍历 `(String, TemplateEntry)`
       - map 为 `ChartEntry { key: k.as_str(), count: entry.histogram.len(), histogram: &entry.histogram }`
       - collect 到 `Vec<ChartEntry<'_>>`
       - 排序：`sort_unstable_by(|a, b| b.count.cmp(&a.count).then_with(|| a.key.cmp(b.key)))`（与 finalize L124-129 完全一致的二级排序）
       - 返回 `vec.into_iter()`

       不要使用 `#[must_use]` 注解（与 finalize 不同：finalize 消耗 self 才标记，iter 返回迭代器无副作用，按现有 Rust 惯例可省略）。

       函数体须 ≤ 40 行（实际约 15 行，无需拆分）。

    3. 在文件底部的 `#[cfg(test)] mod tests` 模块内追加 3 个测试（见 behavior 节）。测试代码风格遵循现有 6 个测试的模式（L137-254）：直接构造 TemplateAggregator::new()，observe 若干次后断言。

    禁止：不要修改 TemplateEntry 的可见性（保持私有）；不要修改 finalize() 的实现；不要为 ChartEntry 添加 Serialize（与 TemplateStats 不同，ChartEntry 是借用视图，不导出）；不要返回 Box<dyn Iterator>（用 `impl Iterator` 即可）；count 必须用 `entry.histogram.len()`（hdrhistogram::Histogram::len 返回总记录数，与 finalize 中 `h.len()` 一致）。
  </action>
  <verify>
    <automated>cargo test --lib features::template_aggregator::tests::test_iter_chart_entries</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub struct ChartEntry<'a>" src/features/template_aggregator.rs` 返回 1 行
    - `grep -n "pub fn iter_chart_entries" src/features/template_aggregator.rs` 返回 1 行
    - `grep -n "pub key: &'a str" src/features/template_aggregator.rs` 返回 1 行
    - `grep -n "pub count: u64" src/features/template_aggregator.rs` 返回 1 行（ChartEntry 内）
    - `grep -n "pub histogram: &'a hdrhistogram::Histogram<u64>" src/features/template_aggregator.rs` 返回 1 行
    - `grep -c "struct TemplateEntry" src/features/template_aggregator.rs` 仍 = 1（私有结构未改变可见性）
    - `cargo test --lib features::template_aggregator::tests::test_iter_chart_entries_empty` 通过
    - `cargo test --lib features::template_aggregator::tests::test_iter_chart_entries_single_key` 通过
    - `cargo test --lib features::template_aggregator::tests::test_iter_chart_entries_sort_order` 通过
    - `cargo test --lib features::template_aggregator::tests` 全部测试通过（无回归 6 个原有测试）
    - `cargo clippy --all-targets -- -D warnings` 无新增警告
  </acceptance_criteria>
  <done>ChartEntry 与 iter_chart_entries 编译通过；3 个新测试通过；TemplateEntry 仍私有；排序与 finalize 一致；clippy 干净。</done>
</task>

<task type="auto">
  <name>Task 2: 在 src/features/mod.rs 通过 pub use 导出 ChartEntry</name>
  <files>src/features/mod.rs</files>
  <read_first>
    - src/features/mod.rs（前 15 行：现有 pub use 模式 L1-13）
    - src/features/template_aggregator.rs（确认 Task 1 已新增 pub struct ChartEntry）
  </read_first>
  <action>
    在 src/features/mod.rs 顶部的 pub use 块中，紧跟现有 `pub use template_aggregator::TemplateStats;`（L13）之后追加一行：

    `pub use template_aggregator::ChartEntry;`

    保持现有缩进与风格。这使外部模块（charts/）可以通过 `crate::features::ChartEntry` 访问。

    禁止：不要使用 `pub use template_aggregator::*`（项目惯例为显式导出）；不要新增其他 re-export。
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | grep -E "(error|warning)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub use template_aggregator::ChartEntry" src/features/mod.rs` 返回 1 行
    - `cargo build --lib` 编译成功
    - `cargo clippy --all-targets -- -D warnings` 通过
  </acceptance_criteria>
  <done>ChartEntry 通过 features::ChartEntry 在 crate 外可见；项目编译通过；clippy 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TemplateAggregator 内部 → Chart 模块 | 私有 histogram 通过 ChartEntry 借用暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | ChartEntry 暴露内部 histogram 引用 | accept | 借用为 &Histogram（只读），无修改能力；模板 key 与 count 本就由 finalize 公开 |
| T-15-05 | Tampering | iter_chart_entries 排序稳定性 | mitigate | 二级排序 (count desc, key asc) 与 finalize 完全一致，保证两个接口顺序可对比验证 |
</threat_model>

<verification>
- `cargo test --lib features::template_aggregator` 全部通过（6 原有 + 3 新增 = 9 个）
- `cargo build --lib` 编译成功
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
</verification>

<success_criteria>
- ChartEntry struct 公开可见且字段顺序为 key/count/histogram
- iter_chart_entries() 借用 &self（不消耗）且排序与 finalize 一致
- features::ChartEntry 在 crate 内任意位置可访问
- 3 个新测试通过，无回归
</success_criteria>

<output>
Create `.planning/phases/15-svg/15-02-SUMMARY.md` when done
</output>
