---
phase: 08-exclude-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/filters.rs
autonomous: true
requirements:
  - FILTER-03

must_haves:
  truths:
    - "配置 exclude_usernames 后，匹配该字段的记录从输出中消失"
    - "七个 exclude_* 字段（username/client_ip/sess_id/thrd_id/statement/appname/tag）均可独立配置"
    - "OR veto 语义：任意一个 exclude 字段命中即丢弃该记录（return false）"
    - "未配置任何 exclude_* 字段时，has_filters() 仍返回 false，快路径不受影响"
    - "非法 exclude 正则在 validate 阶段通过 ConfigError::InvalidValue 报错"
    - "cargo test 全量通过，含新增的 exclude 单元测试"
  artifacts:
    - path: "src/features/filters.rs"
      provides: "MetaFilters 含 7 个 exclude_* 字段；CompiledMetaFilters 含对应编译字段；should_keep OR-veto 重构；has_any_filters；validate_regexes 扩展；新单元测试"
      contains: "exclude_usernames"
  key_links:
    - from: "MetaFilters::has_filters()"
      to: "pipeline.is_empty() 快路径"
      via: "FiltersFeature::has_filters() → self.meta.has_filters()"
      pattern: "exclude_usernames.*is_some_and"
    - from: "CompiledMetaFilters::should_keep()"
      to: "exclude OR-veto"
      via: "match_any_regex(self.exclude_usernames.as_deref(), meta.user) → return false"
      pattern: "exclude_usernames.*return false"
    - from: "FiltersFeature::validate_regexes()"
      to: "exclude 字段正则校验"
      via: "validate_pattern_list(\"features.filters.exclude_usernames\", ...)"
      pattern: "validate_pattern_list.*exclude_"
---

<objective>
在 `src/features/filters.rs` 中实现 FILTER-03 排除过滤器的核心逻辑：

1. `MetaFilters` struct 新增 7 个 `exclude_*: Option<Vec<String>>` 字段（D-01/D-02）
2. `MetaFilters::has_filters()` 扩展，任一 exclude 字段非空即返回 true（D-06）
3. `CompiledMetaFilters` struct 新增 7 个 `exclude_*: Option<Vec<Regex>>` 字段（D-03）
4. `CompiledMetaFilters::from_meta()` 扩展，编译 exclude 字段（D-03）
5. `CompiledMetaFilters` 新增 `has_any_filters()` 方法（include 或 exclude 任一 → true）（D-05）
6. `CompiledMetaFilters::should_keep()` 重构：exclude OR-veto 先于 include AND 检查（D-04）
7. `FiltersFeature::validate_regexes()` 追加 7 个 exclude 字段的正则校验调用（D-08）
8. 新增单元测试覆盖所有场景

Purpose: 让用户可通过 exclude_* 配置精确排除不需要的记录，OR veto 语义（任一命中即丢弃），与现有 include AND 过滤形成完整语义组合。

Output: 修改后的 `src/features/filters.rs`，包含完整实现和新单元测试。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-exclude-filter/08-CONTEXT.md
@.planning/phases/08-exclude-filter/08-PATTERNS.md

<interfaces>
<!-- 执行器需要了解的现有接口，从 src/features/filters.rs 提取 -->

关键结构体（当前状态，需扩展）：

```rust
// MetaFilters（L62-74）— 需追加 7 个 exclude 字段
pub struct MetaFilters {
    pub start_ts: Option<String>,
    pub end_ts: Option<String>,
    pub sess_ids: Option<Vec<String>>,
    pub thrd_ids: Option<Vec<String>>,
    pub usernames: Option<Vec<String>>,
    #[serde(default, deserialize_with = "vec_to_hashset")]
    pub trxids: Option<TrxidSet>,
    pub statements: Option<Vec<String>>,
    pub appnames: Option<Vec<String>>,
    pub client_ips: Option<Vec<String>>,
    pub tags: Option<Vec<String>>,
    // 新增（D-01/D-02）：
    // pub exclude_usernames: Option<Vec<String>>,
    // pub exclude_client_ips: Option<Vec<String>>,
    // pub exclude_sess_ids: Option<Vec<String>>,
    // pub exclude_thrd_ids: Option<Vec<String>>,
    // pub exclude_statements: Option<Vec<String>>,
    // pub exclude_appnames: Option<Vec<String>>,
    // pub exclude_tags: Option<Vec<String>>,
}

// MetaFilters::has_filters()（L202-212）— 需追加 7 个 exclude 分支
pub fn has_filters(&self) -> bool {
    self.trxids.as_ref().is_some_and(|v| !v.is_empty())
        || self.client_ips.as_ref().is_some_and(|v| !v.is_empty())
        || ...
}

// CompiledMetaFilters（L296-305）— 需追加 7 个 exclude 字段
pub struct CompiledMetaFilters {
    pub usernames: Option<Vec<Regex>>,
    pub client_ips: Option<Vec<Regex>>,
    pub sess_ids: Option<Vec<Regex>>,
    pub thrd_ids: Option<Vec<Regex>>,
    pub statements: Option<Vec<Regex>>,
    pub appnames: Option<Vec<Regex>>,
    pub tags: Option<Vec<Regex>>,
    pub trxids: Option<TrxidSet>,
}

// CompiledMetaFilters::from_meta()（L315-326）— 需追加 7 个 exclude 编译
// CompiledMetaFilters::has_filters()（L330-339）— 仅检查 include 字段（保留不变）
// CompiledMetaFilters::should_keep()（L344-379）— 需在 include 检查前插入 exclude OR-veto

// 可复用函数（无需修改）：
fn compile_patterns(patterns: Option<&[String]>) -> Result<Option<Vec<Regex>>, String>
fn match_any_regex(patterns: Option<&[Regex]>, val: &str) -> bool  // None/Some([]) → true（通过）
fn validate_pattern_list(field: &str, patterns: Option<&[String]>) -> crate::error::Result<()>
```

RecordMeta 字段映射（L11-21）：
```rust
pub struct RecordMeta<'a> {
    pub trxid: &'a str,
    pub ip: &'a str,      // ← exclude_client_ips 对比此字段
    pub sess: &'a str,    // ← exclude_sess_ids 对比此字段
    pub thrd: &'a str,    // ← exclude_thrd_ids 对比此字段
    pub user: &'a str,    // ← exclude_usernames 对比此字段
    pub stmt: &'a str,    // ← exclude_statements 对比此字段
    pub app: &'a str,     // ← exclude_appnames 对比此字段
    pub tag: Option<&'a str>,  // ← exclude_tags 对比此字段（需特殊处理）
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 MetaFilters 和 CompiledMetaFilters 数据结构</name>
  <files>src/features/filters.rs</files>

  <read_first>
    - src/features/filters.rs（完整文件——执行前必须读取当前状态，理解现有字段顺序和 serde 注解）
    - .planning/phases/08-exclude-filter/08-PATTERNS.md（所有扩展点的精确代码片段）
  </read_first>

  <behavior>
    - MetaFilters::has_filters() 追加 7 个 exclude 分支后，exclude_usernames = Some(vec!["guest"]) 时返回 true
    - MetaFilters::has_filters() 所有 exclude_* 均为 None 时返回 false（不影响空配置快路径）
    - CompiledMetaFilters::has_any_filters() 只有 include 时等价于 has_filters()
    - CompiledMetaFilters::has_any_filters() 只有 exclude 时返回 true（纯 exclude 配置激活 meta 路径）
    - CompiledMetaFilters::from_meta() 对 exclude_usernames = Some(vec!["^guest"]) 编译出 Some(vec![Regex])
    - CompiledMetaFilters::from_meta() 对 exclude_usernames = None 产生 exclude_usernames = None
  </behavior>

  <action>
**Step 1: MetaFilters struct 追加 7 个 exclude 字段（D-01/D-02）**

在 `MetaFilters` struct 的 `tags: Option<Vec<String>>` 字段之后，追加以下 7 个字段（无需 serde 注解，默认 deserialize 即可，key 名与字段名一致，serde flatten 在 FiltersFeature 层处理）：

```rust
pub exclude_usernames: Option<Vec<String>>,
pub exclude_client_ips: Option<Vec<String>>,
pub exclude_sess_ids: Option<Vec<String>>,
pub exclude_thrd_ids: Option<Vec<String>>,
pub exclude_statements: Option<Vec<String>>,
pub exclude_appnames: Option<Vec<String>>,
pub exclude_tags: Option<Vec<String>>,
```

**Step 2: MetaFilters::has_filters() 扩展（D-06）**

在 `MetaFilters::has_filters()` 方法末尾（`tags` 分支之后）追加 7 个 OR 分支：

```rust
|| self.exclude_usernames.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_client_ips.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_sess_ids.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_thrd_ids.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_statements.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_appnames.as_ref().is_some_and(|v| !v.is_empty())
|| self.exclude_tags.as_ref().is_some_and(|v| !v.is_empty())
```

**Step 3: CompiledMetaFilters struct 追加 7 个 exclude 字段（D-03）**

在 `CompiledMetaFilters` struct 的 `trxids: Option<TrxidSet>` 字段之后，追加：

```rust
pub exclude_usernames: Option<Vec<Regex>>,
pub exclude_client_ips: Option<Vec<Regex>>,
pub exclude_sess_ids: Option<Vec<Regex>>,
pub exclude_thrd_ids: Option<Vec<Regex>>,
pub exclude_statements: Option<Vec<Regex>>,
pub exclude_appnames: Option<Vec<Regex>>,
pub exclude_tags: Option<Vec<Regex>>,
```

**Step 4: CompiledMetaFilters::from_meta() 扩展（D-03）**

在 `from_meta()` 的 `Self { ... }` 块中，`trxids: meta.trxids.clone()` 之后追加 7 个字段编译：

```rust
exclude_usernames: compile_patterns(meta.exclude_usernames.as_deref()).expect("regex validated"),
exclude_client_ips: compile_patterns(meta.exclude_client_ips.as_deref()).expect("regex validated"),
exclude_sess_ids: compile_patterns(meta.exclude_sess_ids.as_deref()).expect("regex validated"),
exclude_thrd_ids: compile_patterns(meta.exclude_thrd_ids.as_deref()).expect("regex validated"),
exclude_statements: compile_patterns(meta.exclude_statements.as_deref()).expect("regex validated"),
exclude_appnames: compile_patterns(meta.exclude_appnames.as_deref()).expect("regex validated"),
exclude_tags: compile_patterns(meta.exclude_tags.as_deref()).expect("regex validated"),
```

**Step 5: CompiledMetaFilters 新增 has_any_filters() 方法（D-05）**

在 `has_filters()` 方法之后紧跟新增方法：

```rust
/// 是否有任何过滤条件（include 或 exclude 任一非空）。
/// 供 `FilterProcessor::new()` 预计算 `has_meta_filters`，
/// 确保纯 exclude 配置也激活 meta 检查路径。
#[must_use]
pub fn has_any_filters(&self) -> bool {
    self.has_filters()
        || self.exclude_usernames.is_some()
        || self.exclude_client_ips.is_some()
        || self.exclude_sess_ids.is_some()
        || self.exclude_thrd_ids.is_some()
        || self.exclude_statements.is_some()
        || self.exclude_appnames.is_some()
        || self.exclude_tags.is_some()
}
```

注意：`has_any_filters()` 函数行数 = 10 行，符合 ≤40 行规范。
  </action>

  <verify>
    <automated>cargo test -p sqllog2db --lib features::filters::tests 2>&1 | tail -5</automated>
  </verify>

  <done>
    - MetaFilters 含 exclude_usernames/exclude_client_ips/exclude_sess_ids/exclude_thrd_ids/exclude_statements/exclude_appnames/exclude_tags 字段
    - MetaFilters::has_filters() 在任一 exclude 字段非空时返回 true
    - CompiledMetaFilters 含对应 7 个 exclude_* 字段（Option<Vec<Regex>>）
    - CompiledMetaFilters::from_meta() 正确编译 7 个 exclude 字段
    - CompiledMetaFilters::has_any_filters() 存在且正确
    - cargo test 通过（现有测试不回归）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重构 should_keep OR-veto 逻辑 + 扩展 validate_regexes + 新增单元测试</name>
  <files>src/features/filters.rs</files>

  <read_first>
    - src/features/filters.rs（Task 1 完成后的当前状态——必须读取，不要基于假设修改）
    - .planning/phases/08-exclude-filter/08-PATTERNS.md（should_keep 重构的精确代码模板，含 exclude_tags 特殊处理）
  </read_first>

  <behavior>
    新增测试用例（全部期望结果在 action 中明确给出）：

    exclude 基本场景：
    - 配置 exclude_usernames = ["^guest"] 时，user="guest_01" 的记录 → should_keep 返回 false（被排除）
    - 配置 exclude_usernames = ["^guest"] 时，user="admin_dba" 的记录 → should_keep 返回 true（保留）
    - 配置 exclude_client_ips = ["^10\\.0"] 时，ip="10.0.0.1" → false；ip="192.168.1.1" → true

    OR veto 语义：
    - 配置 exclude_usernames=["guest"] 且 exclude_client_ips=["^10"] 时，任一命中即返回 false
    - user="admin", ip="10.0.0.1" → false（ip 命中 exclude）
    - user="guest_u", ip="192.168.1.1" → false（user 命中 exclude）
    - user="admin", ip="192.168.1.1" → true（两者均未命中）

    exclude 与 include 同时配置（AND 组合）：
    - include_usernames=["^admin"], exclude_client_ips=["^10"]
    - user="admin", ip="10.0.0.1" → false（exclude ip 命中，veto 优先）
    - user="admin", ip="192.168.1.1" → true（exclude 未命中，include 满足）
    - user="sys_user", ip="192.168.1.1" → false（exclude 未命中，但 include 不满足）

    exclude_tags 特殊处理（tag 为 Option<&str>）：
    - 配置 exclude_tags = ["^SEL"] 时，tag=Some("SELECT") → false（命中 exclude）
    - 配置 exclude_tags = ["^SEL"] 时，tag=None → true（无 tag 值时不触发 exclude，保留）
    - 配置 exclude_tags = ["^SEL"] 时，tag=Some("INSERT") → true（未命中 exclude，保留）

    空配置快路径：
    - MetaFilters 所有 exclude_* 均为 None → has_filters() 返回 false
    - CompiledMetaFilters 所有 exclude_* 均为 None → has_any_filters() 等价于 has_filters()
  </behavior>

  <action>
**Step 1: 重构 CompiledMetaFilters::should_keep()（D-04）**

在 `should_keep()` 函数开头（`#[inline]` `#[must_use]` 之后，现有第一行 `if !match_any_regex(self.usernames...` 之前），插入 exclude OR-veto 检查块。

exclude OR-veto 检查块（对前 6 个字段，tag 单独处理）：

```rust
// === 1. Exclude OR-veto（任一命中 → 丢弃，短路最快）===
// match_any_regex(None, _) 返回 true（未配置 = 通过），故：
// exclude 命中时 match_any_regex 返回 true → return false（丢弃）
if self.exclude_usernames.is_some()
    && match_any_regex(self.exclude_usernames.as_deref(), meta.user)
{
    return false;
}
if self.exclude_client_ips.is_some()
    && match_any_regex(self.exclude_client_ips.as_deref(), meta.ip)
{
    return false;
}
if self.exclude_sess_ids.is_some()
    && match_any_regex(self.exclude_sess_ids.as_deref(), meta.sess)
{
    return false;
}
if self.exclude_thrd_ids.is_some()
    && match_any_regex(self.exclude_thrd_ids.as_deref(), meta.thrd)
{
    return false;
}
if self.exclude_statements.is_some()
    && match_any_regex(self.exclude_statements.as_deref(), meta.stmt)
{
    return false;
}
if self.exclude_appnames.is_some()
    && match_any_regex(self.exclude_appnames.as_deref(), meta.app)
{
    return false;
}
// exclude_tags：tag 为 Option<&str>，无 tag 值时不触发 exclude（保留该记录）
if let (Some(excl_tags), Some(t)) = (&self.exclude_tags, meta.tag) {
    if excl_tags.iter().any(|re| re.is_match(t)) {
        return false;
    }
}

// === 2. Include AND 检查（现有逻辑，不变）===
```

注意：`should_keep()` 函数修改后总行数可能超过 40 行。按 CLAUDE.md 规范，若超过 40 行须拆分。检查方法：将 exclude 块提取为私有方法 `fn exclude_veto(&self, meta: &RecordMeta) -> bool`，返回 true 表示应丢弃。如果 `should_keep` 主体超过 40 行，必须执行此拆分。

**Step 2: 扩展 FiltersFeature::validate_regexes()（D-08）**

在 `validate_regexes()` 函数中，现有 7 个 include 字段校验（最后一行是 `tags` 校验）之后、`record_sql` 校验之前，追加 7 个 exclude 字段校验：

```rust
validate_pattern_list(
    "features.filters.exclude_usernames",
    self.meta.exclude_usernames.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_client_ips",
    self.meta.exclude_client_ips.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_sess_ids",
    self.meta.exclude_sess_ids.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_thrd_ids",
    self.meta.exclude_thrd_ids.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_statements",
    self.meta.exclude_statements.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_appnames",
    self.meta.exclude_appnames.as_deref(),
)?;
validate_pattern_list(
    "features.filters.exclude_tags",
    self.meta.exclude_tags.as_deref(),
)?;
```

注意：`validate_regexes()` 如果追加后超过 40 行，须拆分。可将 7 个 exclude 校验提取为 `fn validate_exclude_regexes(&self) -> crate::error::Result<()>`，在 `validate_regexes()` 中调用 `self.validate_exclude_regexes()?;`。

**Step 3: 新增单元测试**

在 `#[cfg(test)]` 模块的 `// ── CompiledMetaFilters ────────` 测试组之后，添加新测试组 `// ── exclude filters ────────────────────────────────────`，包含以下测试函数：

测试辅助函数（新增，用于构造带 exclude 字段的 MetaFilters）：
```rust
fn make_compiled_with_exclude(
    exclude_usernames: Option<Vec<String>>,
    exclude_client_ips: Option<Vec<String>>,
) -> CompiledMetaFilters {
    let meta = MetaFilters {
        exclude_usernames,
        exclude_client_ips,
        ..MetaFilters::default()
    };
    CompiledMetaFilters::from_meta(&meta)
}
```

测试函数列表（名称与期望行为对应，按 behavior 块中的用例编写）：
- `test_exclude_username_drops_matching_record` — exclude_usernames=["^guest"], user="guest_01" → false
- `test_exclude_username_retains_nonmatching_record` — exclude_usernames=["^guest"], user="admin_dba" → true
- `test_exclude_ip_drops_matching_record` — exclude_client_ips=["^10\\.0"], ip="10.0.0.1" → false
- `test_exclude_or_veto_any_hit_drops` — exclude_usernames=["guest"] + exclude_client_ips=["^10"]: user="admin", ip="10.0.0.1" → false
- `test_exclude_or_veto_no_hit_passes` — 同配置: user="admin", ip="192.168.1.1" → true
- `test_exclude_with_include_veto_wins` — include_usernames=["^admin"], exclude_client_ips=["^10"]: user="admin", ip="10.0.0.1" → false
- `test_exclude_with_include_both_pass` — 同配置: user="admin", ip="192.168.1.1" → true
- `test_exclude_with_include_include_fails` — 同配置: user="sys_user", ip="192.168.1.1" → false
- `test_exclude_tags_drops_matching` — exclude_tags=["^SEL"], tag=Some("SELECT") → false
- `test_exclude_tags_retains_no_tag` — exclude_tags=["^SEL"], tag=None → true（无 tag 不触发 exclude）
- `test_exclude_tags_retains_nonmatching` — exclude_tags=["^SEL"], tag=Some("INSERT") → true
- `test_has_any_filters_exclude_only` — CompiledMetaFilters with only exclude_usernames set → has_any_filters() = true, has_filters() = false
- `test_meta_has_filters_with_exclude` — MetaFilters with exclude_usernames=Some(vec!["x"]) → has_filters() = true
- `test_meta_has_filters_all_none` — MetaFilters::default() → has_filters() = false
- `test_exclude_invalid_regex_validate_fails` — FiltersFeature with exclude_usernames=Some(vec!["[invalid"]) → validate_regexes() 返回 Err
  </action>

  <verify>
    <automated>cargo test -p sqllog2db --lib features::filters::tests 2>&1 | grep -E "(test result|FAILED|error)" | head -10</automated>
  </verify>

  <done>
    - should_keep() 在 exclude 命中时返回 false（OR-veto 短路）
    - should_keep() 在无 exclude 命中时执行现有 include AND 逻辑（行为不变）
    - exclude_tags 处理 tag=None 时不触发 exclude（保留记录）
    - validate_regexes() 对非法 exclude 正则返回 ConfigError::InvalidValue
    - 全部 15 个新增测试通过
    - cargo clippy -- -D warnings 通过（无新告警）
    - 函数均不超过 40 行（超过则已拆分）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config → MetaFilters | 用户提供的 exclude_* 字符串正则，在 validate 阶段校验格式 |
| MetaFilters → CompiledMetaFilters | 运行时编译，validate 后 expect() 调用，校验已前置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | `validate_pattern_list` / `Regex::new()` | mitigate | `validate_regexes()` 在 startup 阶段拦截非法正则，阻止运行时 panic；`from_meta()` 的 `expect()` 只在已验证后触发 |
| T-08-02 | Tampering | `should_keep()` exclude 逻辑顺序 | mitigate | exclude OR-veto 在 include AND 之前执行，确保 exclude 不被 include 绕过（已在 D-04 设计中明确） |
| T-08-03 | Information Disclosure | 错误信息含 regex 模式字符串 | accept | `ConfigError::InvalidValue` 含 pattern 内容，但该信息仅在 validate 阶段输出给配置文件作者，非终端用户数据泄露 |
| T-08-04 | Denial of Service | 正则回溯（ReDoS） | accept | `regex` crate 使用 NFA 引擎，线性时间保证，无指数回溯风险 |
</threat_model>

<verification>
```bash
# 编译检查
cargo build 2>&1 | grep -E "^error" | head -20

# 全量测试（包含新增 exclude 测试）
cargo test -p sqllog2db 2>&1 | tail -10

# Clippy 无告警
cargo clippy --all-targets -- -D warnings 2>&1 | grep -v "^$" | head -20

# 格式检查
cargo fmt --check 2>&1

# 验证新增字段存在
grep -c "exclude_usernames" src/features/filters.rs

# 验证 validate_regexes 包含 exclude 校验
grep -c "exclude_" src/features/filters.rs
```
</verification>

<success_criteria>
1. `grep -c "exclude_usernames" src/features/filters.rs` 输出 ≥ 5（struct 字段 + from_meta + has_any_filters + should_keep + validate_regexes + test）
2. `cargo test -p sqllog2db --lib features::filters::tests` 输出 `test result: ok`
3. `cargo clippy --all-targets -- -D warnings` 输出无 error/warning 行
4. `cargo fmt --check` 输出无差异
5. 全部函数 ≤ 40 行（超出时已拆分为私有辅助函数）
</success_criteria>

<output>
After completion, create `.planning/phases/08-exclude-filter/08-01-SUMMARY.md`
</output>
