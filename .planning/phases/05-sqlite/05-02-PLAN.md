---
phase: 05-sqlite
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/exporter/sqlite.rs
autonomous: true
requirements:
  - PERF-04
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "SQLite 写入后数据库 journal_mode 为 WAL（pragma_update_and_check 验证）"
    - "page_size 在 WAL 下实际生效为 65536（测试断言 PRAGMA page_size 返回 65536）"
    - "批量事务路径在 batch_size=2 写 5 条时触发 2 次中间 COMMIT（测试断言行数 == 5）"
    - "export() / export_one_normalized() / export_one_preparsed() 三条路径均调用 batch_commit_if_needed"
    - "initialize() 函数体不超过 40 行（CLAUDE.md 要求）"
    - "finalize() 在 COMMIT 后执行 wal_checkpoint(TRUNCATE)"
    - "649+ 测试全部通过（无回归）"
  artifacts:
    - path: "src/exporter/sqlite.rs"
      provides: "initialize_pragmas() 辅助函数 + batch_commit_if_needed() 辅助函数"
      contains: "initialize_pragmas"
    - path: "src/exporter/sqlite.rs"
      provides: "test_sqlite_wal_mode_enabled / test_sqlite_wal_page_size / test_sqlite_batch_commit 集成测试"
      contains: "test_sqlite_wal_mode_enabled"
  key_links:
    - from: "initialize() → initialize_pragmas()"
      to: "WAL 模式启用 + page_size=65536 先于 WAL"
      via: "initialize_pragmas(&conn).map_err(|e| Self::db_err(...))"
      pattern: "initialize_pragmas"
    - from: "export_one_preparsed() / export() / export_one_normalized()"
      to: "batch_commit_if_needed()"
      via: "self.batch_commit_if_needed()? 在 record_success() 之后调用"
      pattern: "batch_commit_if_needed"
    - from: "finalize() → COMMIT → wal_checkpoint(TRUNCATE)"
      to: "WAL 文件截断，避免遗留 .db-wal"
      via: "conn.execute_batch(\"PRAGMA wal_checkpoint(TRUNCATE);\")"
      pattern: "wal_checkpoint"
---

<objective>
在 sqlite.rs 中实现三项 SQLite 性能优化：修正 PRAGMA 顺序 + 启用 WAL 模式（PERF-05）、批量事务逻辑（PERF-04）、文档化 prepare_cached 复用并添加 WAL checkpoint（PERF-06），同时新增 3 个集成测试覆盖这些新行为。

Purpose: 这是本 Phase 的核心实现计划。依赖 Plan 01 提供的 SqliteExporter config.batch_size 字段。
Output: sqlite.rs 中 initialize_pragmas()、batch_commit_if_needed() 两个辅助函数；SqliteExporter 结构体新增 row_count/batch_size 字段；3 个新集成测试（RED→GREEN）；finalize() 含 wal_checkpoint(TRUNCATE)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-sqlite/05-RESEARCH.md
@.planning/phases/05-sqlite/05-PATTERNS.md
@.planning/phases/05-sqlite/05-01-SUMMARY.md

<interfaces>
<!-- SqliteExporter 结构体当前字段（sqlite.rs lines 9-20） -->
```rust
pub struct SqliteExporter {
    database_url: String,
    table_name: String,
    insert_sql: String,
    overwrite: bool,
    append: bool,
    conn: Option<Connection>,
    stats: ExportStats,
    pub(super) normalize: bool,
    pub(super) field_mask: crate::features::FieldMask,
    pub(super) ordered_indices: Vec<usize>,
}
// Plan 02 新增（紧跟 stats 之后）：
//   row_count: usize,    // 当前批次已写行数
//   batch_size: usize,   // 每批提交行数，来自 config，默认 10_000
```

<!-- new() 签名（line 34，保持不变，避免破坏 13 个现有测试调用点） -->
```rust
pub fn new(database_url: String, table_name: String, overwrite: bool, append: bool) -> Self
// new() 中新增字段初始化：row_count: 0, batch_size: 10_000
```

<!-- from_config() 当前实现（lines 100-107） -->
```rust
pub fn from_config(config: &crate::config::SqliteExporter) -> Self {
    Self::new(
        config.database_url.clone(),
        config.table_name.clone(),
        config.overwrite,
        config.append,
    )
}
// Plan 02 更新为：let mut exporter = Self::new(...); exporter.batch_size = config.batch_size; exporter
```

<!-- initialize() 当前 PRAGMA 问题代码（lines 217-227） -->
```rust
conn.execute_batch(
    "PRAGMA journal_mode = OFF;     // ← 改为 WAL（通过 pragma_update_and_check）
     PRAGMA synchronous = OFF;      // ← 改为 NORMAL
     PRAGMA cache_size = 1000000;
     PRAGMA locking_mode = EXCLUSIVE;
     PRAGMA temp_store = MEMORY;
     PRAGMA mmap_size = 30000000000;
     PRAGMA page_size = 65536;      // ← Bug! 必须第一个！
     PRAGMA threads = 4;",
)
```

<!-- export_one_preparsed() 结尾（lines 322-327） -->
```rust
        .map_err(|e| Self::db_err(format!("insert failed: {e}")))?;
        self.stats.record_success();
        Ok(())
// Plan 02 在 record_success() 之后插入：self.batch_commit_if_needed()?;
```

<!-- finalize() 当前实现（lines 330-339） -->
```rust
fn finalize(&mut self) -> Result<()> {
    if let Some(conn) = &self.conn {
        conn.execute_batch("COMMIT;")
            .map_err(|e| Self::db_err(format!("commit failed: {e}")))?;
    }
    // ...info! log...
    Ok(())
}
// Plan 02 在 COMMIT 之后追加：
// conn.execute_batch("PRAGMA wal_checkpoint(TRUNCATE);")
//     .map_err(|e| Self::db_err(format!("wal checkpoint failed: {e}")))?;
```

<!-- db_err 辅助函数（line 109） -->
```rust
fn db_err(reason: impl Into<String>) -> Error {
    Error::Export(ExportError::DatabaseFailed { reason: reason.into() })
}
// 所有 rusqlite 错误通过此函数包装，initialize_pragmas 的错误在调用处 .map_err(|e| Self::db_err(...))
```

<!-- ExportStats 字段（用于 batch_commit_if_needed 中记录 flush） -->
<!-- 注意：ExportStats 可能无 flush_operations / last_flush_size 字段，此时省略这两行即可 -->
<!-- 仅递增 row_count 并执行 COMMIT; BEGIN 即可满足需求 -->
```rust
// 安全做法：batch_commit_if_needed 中仅操作已知存在的字段
fn batch_commit_if_needed(&mut self) -> Result<()> {
    self.row_count += 1;
    if self.row_count % self.batch_size == 0 {
        let conn = self.conn.as_ref().unwrap();
        conn.execute_batch("COMMIT; BEGIN")
            .map_err(|e| Self::db_err(format!("batch commit failed: {e}")))?;
    }
    Ok(())
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sqlite.rs — 结构体字段 + new/from_config + initialize_pragmas()</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs (必读：完整文件，理解结构体布局、initialize() 当前实现、现有测试模式)
    - src/exporter/mod.rs (必读：确认 ExportStats 结构体有哪些字段，避免引用不存在字段)
  </read_first>
  <behavior>
    - SqliteExporter 新增字段 `row_count: usize`（在 `stats` 之后）和 `batch_size: usize`（在 `row_count` 之后）
    - `new()` 中初始化 `row_count: 0, batch_size: 10_000`（保持函数签名不变）
    - `from_config()` 更新：先调用 `Self::new(...)` 再设 `exporter.batch_size = config.batch_size`
    - 提取 `fn initialize_pragmas(conn: &Connection) -> std::result::Result<(), rusqlite::Error>` 为模块级私有函数（不是 impl 方法），实现 PRAGMA 正确顺序
    - `initialize()` 中替换原 `conn.execute_batch(...)` 调用块为 `initialize_pragmas(&conn).map_err(|e| Self::db_err(...))?`
    - `initialize()` 中在 `self.conn = Some(conn)` 之后加 `self.row_count = 0;`（支持重复调用）
    - 确保 `initialize()` 函数体 ≤ 40 行
  </behavior>
  <action>
    读取 sqlite.rs 和 src/exporter/mod.rs 全文后执行：

    1. 在 `SqliteExporter` struct 中 `stats: ExportStats,` 之后插入：
       ```rust
       row_count: usize,
       batch_size: usize,
       ```

    2. 在 `new()` 的 `stats: ExportStats::new(),` 之后插入：
       ```rust
       row_count: 0,
       batch_size: 10_000,
       ```

    3. 修改 `from_config()` 为：
       ```rust
       pub fn from_config(config: &crate::config::SqliteExporter) -> Self {
           let mut exporter = Self::new(
               config.database_url.clone(),
               config.table_name.clone(),
               config.overwrite,
               config.append,
           );
           exporter.batch_size = config.batch_size;
           exporter
       }
       ```

    4. 在 `impl SqliteExporter` 块之前（`use` 语句之后）插入模块级私有函数：
       ```rust
       fn initialize_pragmas(conn: &Connection) -> std::result::Result<(), rusqlite::Error> {
           // page_size 必须在 journal_mode=WAL 之前设置（WAL 启用后 page_size 被锁定）
           conn.execute_batch("PRAGMA page_size = 65536;")?;

           // 原子设置并验证 WAL 模式已启用（pragma_update_and_check 是标准 API）
           let journal_mode: String = conn.pragma_update_and_check(
               None,
               "journal_mode",
               "WAL",
               |row| row.get(0),
           )?;
           if journal_mode != "wal" {
               return Err(rusqlite::Error::SqliteFailure(
                   rusqlite::ffi::Error::new(rusqlite::ffi::SQLITE_ERROR),
                   Some(format!("journal_mode=WAL not supported, got: {journal_mode}")),
               ));
           }

           // 其余 PRAGMA（顺序不敏感）
           conn.execute_batch(
               "PRAGMA synchronous = NORMAL;
                PRAGMA cache_size = 1000000;
                PRAGMA locking_mode = EXCLUSIVE;
                PRAGMA temp_store = MEMORY;
                PRAGMA mmap_size = 30000000000;
                PRAGMA threads = 4;",
           )?;
           Ok(())
       }
       ```

    5. 在 `impl Exporter for SqliteExporter` 的 `initialize()` 方法中，将原来的 `conn.execute_batch("PRAGMA journal_mode = OFF; ...")` 整块替换为：
       ```rust
       initialize_pragmas(&conn)
           .map_err(|e| Self::db_err(format!("set PRAGMAs failed: {e}")))?;
       ```
       并在 `self.conn = Some(conn);` 之后加：
       ```rust
       self.row_count = 0;
       ```

    完成后运行 `cargo test --lib -- exporter::sqlite` 确认现有 13 个测试通过（此时批量提交测试尚未添加，预期通过）。再运行 `cargo clippy --all-targets -- -D warnings` 和 `cargo fmt`。
  </action>
  <verify>
    <automated>cargo test --lib -- exporter::sqlite 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "initialize_pragmas" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 至少 2 行（函数定义 + 调用处）
    - `grep -n "journal_mode.*WAL\|WAL.*journal_mode" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 有匹配（不再是 OFF）
    - `grep -n "page_size = 65536" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 出现在 `journal_mode` 相关行的上方（文件行号更小）
    - `grep -n "row_count\|batch_size" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 含结构体字段声明和 new() 初始化
    - `cargo test --lib -- exporter::sqlite` 退出码为 0，所有现有测试通过
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无警告
  </acceptance_criteria>
  <done>initialize_pragmas() 提取完成，WAL 模式替换 OFF，page_size 顺序修正，row_count/batch_size 字段存在，现有 13 个测试全部通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sqlite.rs — batch_commit_if_needed() + finalize() WAL checkpoint + 3 个集成测试</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs (必读：Task 1 完成后的当前状态，确认字段和结构体布局)
  </read_first>
  <behavior>
    - 新增 `fn batch_commit_if_needed(&mut self) -> Result<()>` 方法（impl SqliteExporter 中）：row_count += 1，若整除 batch_size 则执行 `COMMIT; BEGIN`
    - `export()`、`export_one_normalized()`、`export_one_preparsed()` 三个方法各自在 `self.stats.record_success();` 之后插入 `self.batch_commit_if_needed()?;`
    - `finalize()` 在 `COMMIT` 之后追加 `PRAGMA wal_checkpoint(TRUNCATE);`
    - 新增 3 个集成测试：test_sqlite_wal_mode_enabled（断言 journal_mode=wal）、test_sqlite_wal_page_size（断言 page_size=65536）、test_sqlite_batch_commit（batch_size=2 写 5 条，断言 COUNT=5）
    - 批量提交测试中，直接赋值 `exporter.batch_size = 2;`（字段访问，不改 new() 签名）
    - 所有新测试沿用 `{}` 块确保 exporter drop 后再打开验证连接
  </behavior>
  <action>
    读取 Task 1 完成后的 sqlite.rs 全文，执行以下修改：

    1. 在 `impl SqliteExporter` 块中（`db_err` 函数之后）插入新方法：
       ```rust
       fn batch_commit_if_needed(&mut self) -> Result<()> {
           self.row_count += 1;
           if self.row_count % self.batch_size == 0 {
               let conn = self.conn.as_ref().unwrap();
               conn.execute_batch("COMMIT; BEGIN")
                   .map_err(|e| Self::db_err(format!("batch commit failed: {e}")))?;
           }
           Ok(())
       }
       ```

    2. 在 `export()` 方法中，`self.stats.record_success();` 之后插入：
       ```rust
       self.batch_commit_if_needed()?;
       ```

    3. 在 `export_one_normalized()` 方法中，`self.stats.record_success();` 之后插入：
       ```rust
       self.batch_commit_if_needed()?;
       ```

    4. 在 `export_one_preparsed()` 方法中，`self.stats.record_success();` 之后插入：
       ```rust
       self.batch_commit_if_needed()?;
       ```

    5. 修改 `finalize()` 方法，在 COMMIT 执行成功后追加 WAL checkpoint：
       ```rust
       fn finalize(&mut self) -> Result<()> {
           if let Some(conn) = &self.conn {
               conn.execute_batch("COMMIT;")
                   .map_err(|e| Self::db_err(format!("commit failed: {e}")))?;
               // WAL checkpoint：将 WAL 合并到主文件，避免遗留 .db-wal 文件
               conn.execute_batch("PRAGMA wal_checkpoint(TRUNCATE);")
                   .map_err(|e| Self::db_err(format!("wal checkpoint failed: {e}")))?;
           }
           info!(
               "SQLite export finished: {} (success: {}, failed: {})",
               self.database_url, self.stats.exported, self.stats.failed
           );
           Ok(())
       }
       ```

    6. 在 `#[cfg(test)] mod tests` 的现有测试之后追加 3 个新测试：

       ```rust
       #[test]
       fn test_sqlite_wal_mode_enabled() {
           let dir = tempfile::TempDir::new().unwrap();
           let dbfile = dir.path().join("wal_mode.db");

           {
               let mut exporter = SqliteExporter::new(
                   dbfile.to_string_lossy().into(),
                   "tbl".into(),
                   true,
                   false,
               );
               exporter.initialize().unwrap();
               exporter.finalize().unwrap();
           } // EXCLUSIVE lock released

           let conn = rusqlite::Connection::open(&dbfile).unwrap();
           let mode: String = conn
               .pragma_update_and_check(None, "journal_mode", "wal", |row| row.get(0))
               .unwrap();
           assert_eq!(mode, "wal", "PRAGMA journal_mode=WAL 必须返回 'wal'");
       }

       #[test]
       fn test_sqlite_wal_page_size() {
           let dir = tempfile::TempDir::new().unwrap();
           let dbfile = dir.path().join("page_size.db");

           {
               let mut exporter = SqliteExporter::new(
                   dbfile.to_string_lossy().into(),
                   "tbl".into(),
                   true,
                   false,
               );
               exporter.initialize().unwrap();
               exporter.finalize().unwrap();
           }

           let conn = rusqlite::Connection::open(&dbfile).unwrap();
           let page_size: i64 = conn
               .query_row("PRAGMA page_size", [], |r| r.get(0))
               .unwrap();
           assert_eq!(page_size, 65536, "WAL 模式下 page_size 必须为 65536，当前为 {page_size}");
       }

       #[test]
       fn test_sqlite_batch_commit() {
           let dir = tempfile::TempDir::new().unwrap();
           let logfile = dir.path().join("batch.log");
           let dbfile = dir.path().join("batch.db");
           write_test_log(&logfile, 5);

           let parser = LogParser::from_path(logfile.to_str().unwrap()).unwrap();
           let records: Vec<_> = parser.iter().filter_map(std::result::Result::ok).collect();

           {
               let mut exporter = SqliteExporter::new(
                   dbfile.to_string_lossy().into(),
                   "tbl".into(),
                   true,
                   false,
               );
               // batch_size=2：每 2 条触发一次中间 COMMIT（5 条 → 2 次中间 COMMIT，最终 finalize COMMIT 第5条）
               exporter.batch_size = 2;
               exporter.initialize().unwrap();
               for r in &records {
                   exporter.export_one_normalized(r, None).unwrap();
               }
               exporter.finalize().unwrap();
           }

           let conn = rusqlite::Connection::open(&dbfile).unwrap();
           let count: i64 = conn
               .query_row("SELECT COUNT(*) FROM tbl", [], |r| r.get(0))
               .unwrap();
           assert_eq!(count, 5, "5 条记录经过批量提交后必须全部持久化，实际: {count}");
       }
       ```

    完成后依次运行：
    - `cargo test --lib -- exporter::sqlite` （全部测试，含新增 3 个）
    - `cargo test`（649+ 全套测试无回归）
    - `cargo clippy --all-targets -- -D warnings`
    - `cargo fmt`
  </action>
  <verify>
    <automated>cargo test --lib -- exporter::sqlite 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch_commit_if_needed" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 至少 4 行（函数定义 + 3 处调用）
    - `grep -n "wal_checkpoint" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 至少 1 行（finalize 中）
    - `grep -n "test_sqlite_wal_mode_enabled\|test_sqlite_wal_page_size\|test_sqlite_batch_commit" /Users/guang/Projects/sqllog2db/src/exporter/sqlite.rs` 正好 3 行（测试函数定义）
    - `cargo test --lib -- exporter::sqlite` 退出码为 0，测试数量比 Task 1 完成时多 3 个（共 16 个以上）
    - `cargo test` 退出码为 0，输出含 `test result: ok`，failure 为 0
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无警告
  </acceptance_criteria>
  <done>batch_commit_if_needed() 在三条 export 路径中调用，finalize() 含 WAL checkpoint，3 个新集成测试全部通过，全套测试无回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite 文件系统 → Connection | WAL 模式下 .db-wal / .db-shm 文件在进程外可读 |
| initialize_pragmas → SQLite C 库 | PRAGMA 设置通过 FFI 传入 C 层，错误需明确处理 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | PRAGMA page_size 顺序 | mitigate | initialize_pragmas() 强制 page_size 为第一个 PRAGMA；WAL 启用后 page_size 被 SQLite 锁定，顺序错误会被 test_sqlite_wal_page_size 断言捕获 |
| T-05-05 | Tampering | journal_mode=WAL 未验证 | mitigate | 使用 `pragma_update_and_check` 并断言返回值 == "wal"，不匹配则返回 Err，initialize() 失败快速退出 |
| T-05-06 | Information Disclosure | WAL 文件残留（.db-wal/.db-shm） | mitigate | finalize() 中 COMMIT 后执行 `PRAGMA wal_checkpoint(TRUNCATE)` 确保 WAL 合并截断；测试使用 TempDir 自动清理 |
| T-05-07 | Denial of Service | batch_size=0 导致 % 0 panic | mitigate | `batch_commit_if_needed` 中 `row_count % batch_size`：若 batch_size=0 触发 Rust 整数除零 panic。PERF-05 通过 config validate() 阻止（Plan 01 Task 1 中 usize 解析已排除负数；validate() 需检查 batch_size > 0）。在 `SqliteExporter::validate()` 中加断言：`if self.batch_size == 0 { return Err(...) }` |
| T-05-08 | Elevation of Privilege | locking_mode=EXCLUSIVE 锁住文件 | accept | 设计选择，文档已知；测试中 {} 块确保 drop，生产环境仅单进程访问 |
</threat_model>

<verification>
Plan 02 完成后整体验证（按顺序执行）：

```bash
# 1. SQLite 单元测试（含新增 3 个）
cargo test --lib -- exporter::sqlite

# 2. 全套测试无回归
cargo test

# 3. 静态分析
cargo clippy --all-targets -- -D warnings
```

三个命令均退出码为 0。
</verification>

<success_criteria>
- initialize_pragmas() 函数存在，page_size 在 WAL 之前，journal_mode=WAL 通过 pragma_update_and_check 验证
- batch_commit_if_needed() 在三条 export 路径中均被调用
- finalize() 在 COMMIT 后执行 wal_checkpoint(TRUNCATE)
- test_sqlite_wal_mode_enabled 断言 journal_mode=wal 通过
- test_sqlite_wal_page_size 断言 page_size=65536 通过
- test_sqlite_batch_commit 断言 5 条记录全部持久化通过
- cargo test 全套 649+ 测试无回归
- cargo clippy --all-targets -- -D warnings 零警告
- initialize() 函数体 ≤ 40 行（CLAUDE.md 要求）
</success_criteria>

<output>
完成后创建 `.planning/phases/05-sqlite/05-02-SUMMARY.md`
</output>
