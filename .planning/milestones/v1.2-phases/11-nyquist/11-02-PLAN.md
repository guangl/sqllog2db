---
phase: 11-nyquist
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .planning/phases/06-parser-acceptance/06-VALIDATION.md
autonomous: true
requirements: [DEBT-03]
tags: [nyquist, validation, documentation, retroactive-creation]

must_haves:
  truths:
    - "06-VALIDATION.md 文件被新创建（Phase 6 目录下原本不存在该文件）"
    - "06-VALIDATION.md 对齐 Phase 3-5 标准结构：frontmatter + 测试基础设施 + 采样频率 + Per-Task Verification Map + Wave 0 + Manual-Only + Validation Sign-Off + Approval（D-04）"
    - "06-VALIDATION.md frontmatter 包含 nyquist_compliant: true（D-04：直接以已完成状态创建）"
    - "06-VALIDATION.md frontmatter 包含 status: complete"
    - "06-VALIDATION.md frontmatter 包含 wave_0_complete: true"
    - "06-VALIDATION.md Per-Task Verification Map 至少覆盖 Phase 6 的 5 个任务（6-01-01、6-01-02、6-02-01、6-02-02、6-02-03），所有 Status 列均为 ✅ green（D-04）"
    - "06-VALIDATION.md 中 Plan 01（Wave 1）的两个任务覆盖：依赖升级 dm-database-parser-sqllog 1.0.0 + PERF-07 调研记录（D-04）"
    - "06-VALIDATION.md 中 Plan 02（Wave 2）的三个任务覆盖：cargo test + cargo clippy + cargo fmt（D-04）"
    - "06-VALIDATION.md Validation Sign-Off 6 条均勾选 [x]"
    - "06-VALIDATION.md Approval 行明确标注追签性质（'signed retroactively'，执行于 2026-05-10，补签于 Phase 11）"

  artifacts:
    - path: ".planning/phases/06-parser-acceptance/06-VALIDATION.md"
      provides: "Phase 6 Nyquist 补签文件（从零创建）"
      contains: "nyquist_compliant: true"

  key_links:
    - from: ".planning/phases/06-parser-acceptance/06-VALIDATION.md"
      to: ".planning/phases/06-parser-acceptance/06-VERIFICATION.md"
      via: "证据引用 — 3/3 truths verified"
      pattern: "Approval: signed"
    - from: ".planning/phases/06-parser-acceptance/06-VALIDATION.md"
      to: ".planning/phases/06-parser-acceptance/06-01-SUMMARY.md"
      via: "Plan 01 任务引用 — PERF-07 调研 + 依赖升级"
      pattern: "PERF-07"
    - from: ".planning/phases/06-parser-acceptance/06-VALIDATION.md"
      to: ".planning/phases/06-parser-acceptance/06-02-SUMMARY.md"
      via: "Plan 02 任务引用 — cargo test/clippy/fmt 验收"
      pattern: "PERF-09"
---

<objective>
从零创建 Phase 6 的 VALIDATION.md，对齐 Phase 3-5 的标准文件结构（D-04），并直接以已完成状态写入（所有任务 ✅ green、Sign-Off 全部勾选、Approval 标注追签）。

Purpose: Phase 6（解析库集成 + 验收）目录下当前无 VALIDATION.md 文件，是 Nyquist 审计链上唯一的"缺失文件型"缺口（其余 Phase 3/4/5 为"未回溯型"缺口，由 Plan 01 处理）。本 plan 完成 DEBT-03 Success Criterion #4。

依据：Phase 6 已于 2026-05-10 执行完毕（见 06-VERIFICATION.md，3/3 truths verified，651 passed/0 failed/0 clippy warnings/fmt clean）。VALIDATION.md 是"执行前的验证策略"文件，但此处属于追溯补签——故依据 D-04 直接以已完成状态写入，无需经历 pending 阶段。

Output: 一个新文件 `.planning/phases/06-parser-acceptance/06-VALIDATION.md`，约 70-90 行，结构对齐 03/04/05-VALIDATION.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-nyquist/11-CONTEXT.md

依赖参考（Plan 01 完成后产出的标准格式样本）：
@.planning/phases/03-profiling-benchmarking/03-VALIDATION.md
@.planning/phases/04-csv/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 从零创建 06-VALIDATION.md（对齐 Phase 3-5 结构 + 直接已完成状态）</name>
  <files>.planning/phases/06-parser-acceptance/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-profiling-benchmarking/03-VALIDATION.md（Plan 01 完成后的标准结构样本——frontmatter + 7 个章节布局）
    - .planning/phases/04-csv/04-VALIDATION.md（Plan 01 完成后的多任务结构样本——Per-Task Verification Map 多行示例）
    - .planning/phases/06-parser-acceptance/06-VERIFICATION.md（权威证据来源 D-02：3/3 truths verified，PERF-07 + PERF-09 SATISFIED，cargo test 651 passed，clippy 0 warnings，fmt exit:0）
    - .planning/phases/06-parser-acceptance/06-01-SUMMARY.md（Plan 01 任务详情：Task 1 BENCHMARKS.md PERF-07 段落 + Task 2 依赖升级 1.0.0 原子提交 4654846，requirements-completed 含 PERF-07）
    - .planning/phases/06-parser-acceptance/06-02-SUMMARY.md（Plan 02 任务详情：cargo test 651 passed/clippy 0 warnings/fmt 0 diff，requirements-completed 含 PERF-09）
    - .planning/phases/11-nyquist/11-CONTEXT.md（D-04 决策定义 + §specifics 中 Approval 行模板）
  </read_first>
  <action>
依据 D-04（对齐 Phase 3-5 结构 + 直接以已完成状态创建），写入文件 `.planning/phases/06-parser-acceptance/06-VALIDATION.md`，内容如下（使用 Write 工具一次性创建，逐字段对照下方规格）：

**frontmatter（YAML）：**
- `phase: 6`
- `slug: parser-acceptance`
- `status: complete`
- `nyquist_compliant: true`
- `wave_0_complete: true`
- `created: 2026-05-15`

**正文章节顺序与内容：**

1. **标题与说明行**：`# Phase 6 — Validation Strategy` 后跟引言 `> Per-phase validation contract for feedback sampling during execution.`

2. **Test Infrastructure 表格**（5 列：Property / Value）：
   - Framework: `cargo test (Rust 内置) + cargo clippy + cargo fmt`
   - Config file: `Cargo.toml`
   - Quick run command: `cargo test`
   - Full suite command: `cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --check`
   - Estimated runtime: `~30 seconds (test) + ~5 seconds (clippy/fmt)`

3. **Sampling Rate 列表**：
   - After every task commit: `cargo test`
   - After every plan wave: `cargo test && cargo clippy --all-targets -- -D warnings`
   - Before `/gsd-verify-work`: 三项验收命令全部通过
   - Max feedback latency: `~30 seconds`

4. **Per-Task Verification Map 表格**（10 列：Task ID / Plan / Wave / Requirement / Threat Ref / Secure Behavior / Test Type / Automated Command / File Exists / Status）。共 5 行任务：
   - `6-01-01` / `01` / `1` / `PERF-07` / `—` / `N/A` / `documentation` / `grep -c "PERF-07" benches/BENCHMARKS.md` / `✅` / `✅ green`
   - `6-01-02` / `01` / `1` / `PERF-07` / `—` / `N/A` / `build` / `grep "dm-database-parser-sqllog.*1\.0\.0" Cargo.toml` / `✅` / `✅ green`
   - `6-02-01` / `02` / `2` / `PERF-09` / `—` / `N/A` / `unit+integration` / `cargo test` / `✅` / `✅ green`
   - `6-02-02` / `02` / `2` / `PERF-09` / `—` / `N/A` / `lint` / `cargo clippy --all-targets -- -D warnings` / `✅` / `✅ green`
   - `6-02-03` / `02` / `2` / `PERF-09` / `—` / `N/A` / `format` / `cargo fmt --check` / `✅` / `✅ green`
   - 表下加图例：`*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`

5. **Wave 0 Requirements 列表**：
   - `- [x] Cargo.toml 中 dm-database-parser-sqllog 升级至 1.0.0（PERF-07 调研结论确认 0.9.1 → 1.0.0 改进自动生效，见 06-01-SUMMARY.md commit 4654846）`
   - 注释行：`*现有 criterion + cargo test/clippy/fmt 基础设施覆盖 Phase 6 所有验收要求，无新增基础设施需求。*`

6. **Manual-Only Verifications 表格**（4 列：Behavior / Requirement / Why Manual / Test Instructions）：
   - 单行：`PERF-07 调研结论的合理性` / `PERF-07` / `调研结论涉及 API 设计取舍（index()/RecordIndex 是否集成），需人工判断流式场景适用性` / `阅读 benches/BENCHMARKS.md "Phase 6 — 解析库集成评估（PERF-07）" 段落，确认 mmap/par_iter()/编码检测/MADV_SEQUENTIAL 自动生效 + index() 不集成理由清晰`
   - 注：依据 06-VERIFICATION.md，PERF-07 调研结论已被用户认可（human_verification 块标记 result: pass）

7. **Validation Sign-Off 列表**（6 条全部勾选 `[x]`）：
   - `- [x] All tasks have <automated> verify or Wave 0 dependencies`
   - `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`
   - `- [x] Wave 0 covers all MISSING references`
   - `- [x] No watch-mode flags`
   - `- [x] Feedback latency < 30s`
   - `- [x] nyquist_compliant: true set in frontmatter`

8. **Approval 行**（D-04 + §specifics 模板）：
   - `**Approval:** signed retroactively 2026-05-15（执行于 2026-05-10，VALIDATION.md 补签于 Phase 11，DEBT-03）。验收依据：06-VERIFICATION.md 3/3 truths verified（cargo test 651 passed / clippy 0 warnings / fmt 0 diff）；Plan 01 PERF-07 调研结论已记录于 benches/BENCHMARKS.md；Plan 02 v1.1 milestone 三项验收门控全部通过。`

文件总长度预计 70-90 行。**禁止**：自创不在样本中的章节、复制 Phase 3/4/5 的具体任务 ID（如 3-01-01、4-02-02 等）、引入未在 06-VERIFICATION.md 中出现的验证证据。

完成后文件应能通过下述 acceptance criteria 中的所有 grep 断言。
  </action>
  <verify>
    <automated>test -f .planning/phases/06-parser-acceptance/06-VALIDATION.md && grep -E "^(status|nyquist_compliant|wave_0_complete):" .planning/phases/06-parser-acceptance/06-VALIDATION.md | grep -c "true\|complete" | grep -q "^3$" && grep -c "✅ green" .planning/phases/06-parser-acceptance/06-VALIDATION.md | awk '$1 >= 5' | grep -q . && grep -c "6-01-01\|6-01-02\|6-02-01\|6-02-02\|6-02-03" .planning/phases/06-parser-acceptance/06-VALIDATION.md | awk '$1 >= 5' | grep -q . && grep -c "PERF-07" .planning/phases/06-parser-acceptance/06-VALIDATION.md | awk '$1 >= 2' | grep -q . && grep -c "PERF-09" .planning/phases/06-parser-acceptance/06-VALIDATION.md | awk '$1 >= 3' | grep -q . && grep -c "signed retroactively 2026-05-15" .planning/phases/06-parser-acceptance/06-VALIDATION.md | grep -q "^1$" && grep -c "\[ \]" .planning/phases/06-parser-acceptance/06-VALIDATION.md | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - 文件 `.planning/phases/06-parser-acceptance/06-VALIDATION.md` 存在
    - 06-VALIDATION.md frontmatter 包含字符串 `nyquist_compliant: true`
    - 06-VALIDATION.md frontmatter 包含字符串 `status: complete`
    - 06-VALIDATION.md frontmatter 包含字符串 `wave_0_complete: true`
    - 06-VALIDATION.md frontmatter 包含字符串 `phase: 6`
    - 06-VALIDATION.md frontmatter 包含字符串 `slug: parser-acceptance`
    - 06-VALIDATION.md 至少出现 5 次字符串 `✅ green`（覆盖 5 个任务的 Status 列）
    - 06-VALIDATION.md 包含任务 ID 字符串 `6-01-01`、`6-01-02`、`6-02-01`、`6-02-02`、`6-02-03`（5 个 Phase 6 任务）
    - 06-VALIDATION.md 至少出现 2 次字符串 `PERF-07`（Per-Task Map + Wave 0 / Manual-Only）
    - 06-VALIDATION.md 至少出现 3 次字符串 `PERF-09`（Per-Task Map 中三个 Plan 02 任务的 Requirement 列）
    - 06-VALIDATION.md 包含字符串 `signed retroactively 2026-05-15`
    - 06-VALIDATION.md 不包含任何未勾选 checkbox `- [ ]`（Wave 0 单条 + Sign-Off 6 条全部勾选）
    - 06-VALIDATION.md 包含字符串 `Approval: signed retroactively`
  </acceptance_criteria>
  <done>06-VALIDATION.md 从零创建完成，对齐 Phase 3-5 结构；DEBT-03 Success Criterion #4 满足；Nyquist 审计链对 Phase 3/4/5/6 全部闭合。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯文档创建任务，单一新增 Markdown 文件，无代码、无 IPC、无网络调用、无依赖安装 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | (none) | 无 | accept | 本 plan 仅新建 `.planning/phases/06-parser-acceptance/06-VALIDATION.md` 一个本地 Markdown 文件；无代码、无 SQL、无网络、无新依赖、无 npm/pip/cargo install；威胁面为空。新文件可通过 `git diff` 完整审查。 |
</threat_model>

<verification>
创建完成后：

```bash
# 文件存在性
ls -la .planning/phases/06-parser-acceptance/06-VALIDATION.md

# frontmatter 校验
grep -E "^(phase|slug|status|nyquist_compliant|wave_0_complete|created):" .planning/phases/06-parser-acceptance/06-VALIDATION.md

# 5 个任务 Status 全部 green
grep -c "✅ green" .planning/phases/06-parser-acceptance/06-VALIDATION.md

# Approval 行存在
grep "Approval:" .planning/phases/06-parser-acceptance/06-VALIDATION.md
```

预期：文件存在；frontmatter 6 行完整；至少 5 次 `✅ green`；`Approval: signed retroactively 2026-05-15` 出现 1 次。

无 cargo 命令需运行（纯文档创建）。
</verification>

<success_criteria>
- 06-VALIDATION.md 文件已创建（之前 Phase 6 目录无此文件）
- 文件结构对齐 Phase 3-5 标准（7 个章节顺序一致）
- frontmatter `nyquist_compliant: true`、`status: complete`、`wave_0_complete: true`
- Per-Task Verification Map 5 行任务全部 `✅ green`
- Validation Sign-Off 6 条全部勾选
- Approval 行明确标注追签性质（`signed retroactively 2026-05-15`）
- DEBT-03 Success Criterion #4 完成 → Phase 11 phase goal（Nyquist 审计链对 Phase 3/4/5/6 无缺口）达成
</success_criteria>

<output>
Create `.planning/phases/11-nyquist/11-02-SUMMARY.md` when done，记录：新文件路径与行数、对齐 Phase 3-5 结构的章节清单、5 个任务的 Requirement 映射证据（引用 06-VERIFICATION.md + 06-01/06-02-SUMMARY.md）、Phase 11 DEBT-03 4/4 Success Criteria 全部达成的确认信号。
</output>
