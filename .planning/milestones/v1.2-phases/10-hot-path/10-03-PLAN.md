---
phase: 10-hot-path
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - PERF-10
must_haves:
  truths:
    - "用户在 BENCHMARKS.md Phase 10 §D-G1 门控判定看到 `**结论：未命中 D-G1.**` 时才执行本计划"
    - "BENCHMARKS.md Phase 10 节增补 §当前瓶颈分析 子节，对每个 Top N 函数解释为何不算可消除热点（D-G1/D-G2 逐条对照）"
    - "Phase 10 §结论 checkbox 全部勾选 `[x]`，含'已记录已达当前瓶颈结论'"
    - "全部 651+ cargo test 通过（验证本计划纯文档修改未误触 src/）"
  artifacts:
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 10 §当前瓶颈分析 子节，含每个 Top N 函数的 D-G1/D-G2 对照说明"
      contains: "### 当前瓶颈分析"
  key_links:
    - from: "10-01 BENCHMARKS.md §D-G1 门控判定 结论"
      to: "本计划任务执行（仅在未命中时启动）"
      via: "grep -c '未命中 D-G1' benches/BENCHMARKS.md"
      pattern: "\\*\\*结论：未命中 D-G1\\."
    - from: "Phase 10 §当前瓶颈分析"
      to: "PERF-10 验收通过（D-G3 规定无需独立 VERIFICATION.md）"
      via: "BENCHMARKS.md 内嵌签署"
      pattern: "已达当前瓶颈"
---

<objective>
Branch B-no（条件执行）：若 10-01 §D-G1 门控判定 文本为 `**结论：未命中 D-G1.**`（注意英文句号），则在 BENCHMARKS.md Phase 10 节增补 §当前瓶颈分析 子节，对 10-01 Top N 函数逐一以 D-G1/D-G2 标准说明为何不构成可消除热点，给出"已达当前瓶颈"的明确结论与签署。本计划不修改任何 src/ 代码。

Purpose: D-O1 数据驱动 — 当 samply 客观证据未满足 D-G1 三条件时，正式记录结论而非盲目优化，避免 v1.1 已警示的"micro-optimization without evidence"反模式。D-G3 明确规定此情况无需独立 VERIFICATION.md，仅以 BENCHMARKS.md 段落收尾。
Output: BENCHMARKS.md Phase 10 节增补 §当前瓶颈分析 子节 + §结论 checkbox 全勾选；不触动 src/。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hot-path/10-CONTEXT.md
@.planning/phases/10-hot-path/10-PATTERNS.md
@.planning/phases/10-hot-path/10-01-SUMMARY.md

<interfaces>
<!-- 本计划纯文档修改，无 API/类型接口 -->

D-G1 判定标准（来自 10-CONTEXT.md，逐条对照所需）：
1. 单一函数占全局 self time > 5%
2. 该函数属于 src/ 下 sqllog2db 自身业务逻辑（非第三方库内部）
3. 存在明确减少分配/clone/循环的优化路径

D-G2：第三方库内部开销（regex / alloc / memchr / dm_database_parser_sqllog 等）即使 >5% 也不算可消除。

D-G3：未命中时仅在 BENCHMARKS.md Phase 10 节用段落文字记录"已达当前瓶颈"结论 + Top N 函数列表 + criterion 对比数据，不需创建 VERIFICATION.md。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 前置确认 — 验证 10-01 已判定"未命中 D-G1"</name>
  <files>benches/BENCHMARKS.md（无修改，仅检查）</files>
  <read_first>
    - benches/BENCHMARKS.md（重点读 Phase 10 §D-G1 门控判定 子节，确认文本为"未命中 D-G1."，英文句号）
    - .planning/phases/10-hot-path/10-01-SUMMARY.md（确认 10-01 结论快照）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-G1/D-G2/D-G3 三条标准）
  </read_first>
  <action>
    第 1 步 - 检查判定门：执行 `grep -c "\\*\\*结论：未命中 D-G1\\." benches/BENCHMARKS.md`（注意 grep 模式中的 `\.` 匹配英文句号 `.`，与 10-01 写入的字面文本一致）。如果返回 0（即 10-01 判定为"命中 D-G1"或文本不存在），立即停止本计划并以日志 `[skip] 10-03 not applicable — 10-01 BENCHMARKS.md 判定命中 D-G1，请执行 10-02 计划替代` 退出（不修改任何文件）。

    第 2 步 - 从 BENCHMARKS.md §samply Profiling 结论 子节提取 Top N 函数列表（每行格式：`N. <function> — X.X%`），保留每行的 src/ 或第三方归属标注。Task 2 将基于此列表逐项做 D-G1/D-G2 对照。

    本任务不修改任何文件，仅守门 + 数据提取。
  </action>
  <verify>
    <automated>grep -c "\*\*结论：未命中 D-G1\.\*\*" benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - Behavior：`grep -c "\\*\\*结论：未命中 D-G1\\.\\*\\*" benches/BENCHMARKS.md` 返回 1（注意：模式中的 `\.` 匹配英文句号 `.`，与 10-01 实际写入文本完全对齐；否则任务进入 skip 分支）
    - Source assertion：BENCHMARKS.md §samply Profiling 结论 子节含至少 5 行格式为 `N. <function> — X.X%` 的 Top N 列表
    - 终端日志：打印提取的 Top N 函数清单与每项归属（src/ 或第三方）
  </acceptance_criteria>
  <done>判定门检查通过为"未命中 D-G1"；Top N 列表已提取到内存</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 BENCHMARKS.md Phase 10 节插入 §当前瓶颈分析 子节并签署结论</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（Phase 10 节当前结构；§Filter Benchmark 与 §结论 之间为插入点）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-G1/D-G2/D-G3 标准 + Phase 10 验收条件 4 "全部 651 测试通过，基准无回归"）
    - .planning/phases/10-hot-path/10-PATTERNS.md（分支 A 模板：§samply Profiling 结论 + "已达当前瓶颈" 段落写法）
  </read_first>
  <action>
    在 BENCHMARKS.md Phase 10 节内 §Filter Benchmark 之后、§结论 之前插入新子节 `### 当前瓶颈分析（D-G1 未命中说明）`，内容包含：

    1. 引言段（1-2 句）：说明"按 D-G1 三条标准（>5% self time + src/ 业务逻辑 + 明确优化路径）逐项核对，所有 Top N 函数均不构成可消除热点"。

    2. 逐项分析表格 — 四列：函数 / self time / 不构成热点的原因（D-G1 哪条不满足 / 是否触发 D-G2）/ 备注。覆盖 Task 1 提取的 Top N 全部行。原因分类示例：
       - `第三方库内部（D-G2 排除）` — 来自 regex、alloc、memchr、dm_database_parser_sqllog 等的函数
       - `self time < 5%（D-G1 第 1 条不满足）` — 即使是 src/ 函数但占比未达门控
       - `无明确优化路径` — src/ 函数且 >5%，但无法定位减少分配/clone/循环的具体方案（需要简短解释为何）

    3. 结论签署段（明确文字，便于审计 grep。注意：结尾句号一律使用英文 `.`，不要使用中文 `。`，以便下游 grep 模式 `\.` 精确命中）：
       ```
       **结论：已达当前瓶颈.** 当前性能受限于：
       - 第三方解析库（dm_database_parser_sqllog）内部 self time（D-G2 不可消除）
       - 系统级内存分配与 mmap I/O（alloc / memchr 等）
       - 流式 single-thread 架构的固有读-解析-写回串行依赖

       依据 D-G3，本结论以本节段落形式签署，不另开 VERIFICATION.md。
       PERF-10 验收通过：bench scenarios 已补全（D-B1），samply 已采集（D-P1/P2/P3），门控判定明确（D-G1 未命中），全量测试无回归（≤5% 容差）。
       ```

    4. 更新 Phase 10 §结论 checkbox：将"若无热点：已记录'已达当前瓶颈'结论"勾选为 `[x]`，且整个 §结论 列表的全部 6 项均为 `[x]`。

    第 2 步 - 跑一次 `cargo bench --bench bench_filters` 与 `cargo test`，确认从 10-01 至今基线无回归（容差 ≤5%）。如果 criterion 报告任何场景 `Performance has regressed` > 5%，需在 §当前瓶颈分析 末尾追加一行 `**注意：** <场景> 出现 X.X% 回归，原因 <说明>，触发回滚检查` 并视情况退回到 10-01 复查（这种情况理论上不应发生，因本计划不改 src/）。
  </action>
  <verify>
    <automated>grep -c "### 当前瓶颈分析" benches/BENCHMARKS.md &amp;&amp; grep -c "\*\*结论：已达当前瓶颈\.\*\*" benches/BENCHMARKS.md &amp;&amp; cargo test --quiet 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion：`grep -c "### 当前瓶颈分析（D-G1 未命中说明）" benches/BENCHMARKS.md` 返回 1
    - Source assertion：`grep -c "\\*\\*结论：已达当前瓶颈\\.\\*\\*" benches/BENCHMARKS.md` 返回 1（英文句号）
    - Source assertion：`grep -c "PERF-10 验收通过" benches/BENCHMARKS.md` 返回 ≥ 1
    - Source assertion：§当前瓶颈分析 子节包含至少 5 行表格行（覆盖 Task 1 提取的 Top N，`grep -cE "^\| \`" benches/BENCHMARKS.md` 在 Phase 10 节内部计数 ≥ 5）
    - Source assertion：Phase 10 §结论 子节中所有 checkbox 均为 `[x]`。使用 awk 限定到 Phase 10 节范围内做精确检查（避免跨节误匹配其他 phase 的 ### 结论）：
      ```
      awk '/^## Phase 10 — / { in_p10=1; next } /^## Phase / && in_p10 { exit } in_p10 && /^### 结论/ { in_concl=1; next } in_p10 && /^### / && in_concl { exit } in_concl { print }' benches/BENCHMARKS.md | grep -c '^- \[ \]'
      ```
      上述命令必须返回 0（即 Phase 10 §结论 子节内无任何未勾选的 `- [ ]` 行）；同时
      ```
      awk '/^## Phase 10 — / { in_p10=1; next } /^## Phase / && in_p10 { exit } in_p10 && /^### 结论/ { in_concl=1; next } in_p10 && /^### / && in_concl { exit } in_concl { print }' benches/BENCHMARKS.md | grep -c '^- \[x\]'
      ```
      必须返回 ≥ 6（覆盖全部 6 项 checkbox）。
    - Behavior：`cargo test` 全量通过，exit 0，测试数 ≥ 651（无回归）
    - Behavior：`cargo bench --bench bench_filters 2>&1 | grep -c "Performance has regressed"` 返回 0（criterion 自动对比上次 baseline 无回归）
    - Source assertion：src/ 下无文件被修改（`git diff --stat src/ | grep -c "^"` 返回 0；本计划只动 BENCHMARKS.md）
  </acceptance_criteria>
  <done>BENCHMARKS.md §当前瓶颈分析 子节完成；"已达当前瓶颈."（英文句号）明文签署；§结论 checkbox 全勾选；测试与基准无回归</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BENCHMARKS.md 签署文本 | 人工撰写的瓶颈分析；下游审计依赖文本准确性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | T (Tampering) | "已达当前瓶颈"签署被错误使用（实际命中 D-G1 但被签为未命中） | mitigate | Task 1 强制 grep 校验 10-01 判定文本（英文句号），文本不一致即 skip；判定来源唯一锚定于 BENCHMARKS.md，避免人为绕过 |
| T-10-10 | I (Info Disclosure) | 瓶颈分析可能泄露内部架构假设 | accept | 描述都是公开技术决策（流式 single-thread / 第三方库 / mmap），无敏感信息 |
| T-10-11 | R (Repudiation) | 后续无法追溯为何未优化 | mitigate | §当前瓶颈分析 表格逐项对照 D-G1/D-G2，附 self time 数值与归属，形成完整审计链 |
| T-10-12 | D (DoS) | 文档计划本身无运行时风险 | accept | 仅 markdown 编辑，无代码路径 |
</threat_model>

<verification>
- Task 1 确认 10-01 判定为"未命中 D-G1."（英文句号），否则整个计划 skip
- BENCHMARKS.md Phase 10 §当前瓶颈分析 子节存在且含 Top N 逐项 D-G1/D-G2 对照表
- BENCHMARKS.md 含明文签署 `**结论：已达当前瓶颈.**`（英文句号）与 `PERF-10 验收通过`
- Phase 10 §结论 全部 checkbox 勾选 `[x]`（用 awk 限定到 Phase 10 范围内校验）
- src/ 下零文件被修改（git diff --stat src/ 无输出）
- cargo test 全量通过；cargo bench 无回归 > 5%
</verification>

<success_criteria>
- 10-01 判定为"未命中 D-G1."（英文句号）时本计划完整执行；判定为"命中"时本计划自动 skip 并指向 10-02
- BENCHMARKS.md Phase 10 节增补 §当前瓶颈分析 子节，含 D-G1/D-G2 逐项对照与"已达当前瓶颈."签署
- 全量 cargo test 与 cargo bench 无回归（容差 ≤5%）
- D-G3 验收方式遵守：仅 BENCHMARKS.md 段落签署，不创建独立 VERIFICATION.md
- PERF-10 需求通过验收（Phase 10 ROADMAP Success Criteria 1/3/4 全部满足）
</success_criteria>

<output>
After completion, create `.planning/phases/10-hot-path/10-03-SUMMARY.md` 包含：未命中 D-G1 的逐项原因列表、"已达当前瓶颈."结论摘要、PERF-10 验收签署。
</output>
</content>
</invoke>