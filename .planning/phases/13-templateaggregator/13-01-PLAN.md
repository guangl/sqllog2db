---
phase: 13-templateaggregator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml                            # 新增 hdrhistogram = "7.5.4" 依赖
  - src/features/template_aggregator.rs   # 新建：TemplateEntry + TemplateAggregator + TemplateStats + #[cfg(test)] mod tests
  - src/features/mod.rs                   # 新增 pub mod template_aggregator; pub use template_aggregator::TemplateAggregator;（接入 Phase 13）
  - src/features/sql_fingerprint.rs       # 清理 ScanMode::Normalize 与 normalize_template 上的 #[allow(dead_code)]（Wave 1 模块新增 use 即解封 dead_code，Wave 2 才接入热路径）
autonomous: true
requirements:
  - TMPL-02

tags:
  - rust
  - hdrhistogram
  - aggregator

must_haves:
  truths:
    - "Cargo.toml [dependencies] 段含 `hdrhistogram = \"7.5.4\"` 一行（D-03 / RESEARCH Standard Stack）"
    - "src/features/template_aggregator.rs 新建文件，包含私有 struct TemplateEntry { histogram: Histogram<u64>, first_seen: String, last_seen: String }"
    - "src/features/template_aggregator.rs 含 `#[derive(Debug, Clone, serde::Serialize)] pub struct TemplateStats { template_key, count, avg_us, min_us, max_us, p50_us, p95_us, p99_us, first_seen, last_seen }` 共 10 个 pub 字段（D-02 / D-04）"
    - "src/features/template_aggregator.rs 含 `#[derive(Debug, Default)] pub struct TemplateAggregator { entries: ahash::AHashMap<String, TemplateEntry> }`（D-09 / D-10：不实现 LogProcessor，不存 Vec<u64>）"
    - "TemplateAggregator 暴露三个 pub 方法：`new() -> Self`、`observe(&mut self, key: &str, exectime_us: u64, ts: &str)`、`merge(&mut self, other: TemplateAggregator)`、`finalize(self) -> Vec<TemplateStats>`"
    - "TemplateEntry::new(first_seen: String) 内部用 `Histogram::<u64>::new_with_bounds(1, 60_000_000, 2).expect(...)` 构造直方图（D-03）"
    - "observe() 实现以 `let _ = entry.histogram.record(exectime_us);` 静默忽略超界，避免 unused Result 触发 -D warnings"
    - "observe() 使用 `entries.entry(key.to_string()).or_insert_with(|| TemplateEntry::new(ts.to_string()))` 懒建条目"
    - "observe() 更新 first_seen / last_seen 时按字符串字典序比较（`ts < entry.first_seen.as_str()` / `ts > entry.last_seen.as_str()`，D-04 / D-05）"
    - "merge() 内对每个外部 entry 调用 `let _ = entry.histogram.add(&other_entry.histogram);` 合并直方图（hdrhistogram add() 量程一致时无精度损失）"
    - "merge() 内 first_seen / last_seen 同样按字典序取极值（`if other_entry.first_seen < entry.first_seen { entry.first_seen = other_entry.first_seen; }` 与对应 last_seen 取 max）"
    - "finalize() 返回 Vec<TemplateStats> 按 `b.count.cmp(&a.count)` 降序 `sort_unstable_by` 排序（最频繁优先，RESEARCH Pattern 4）"
    - "finalize() 中 avg_us 计算为 `h.mean() as u64`，min_us/max_us 用 `h.min()` / `h.max()`，百分位用 `h.value_at_quantile(0.50/0.95/0.99)`"
    - "src/features/mod.rs 新增 `pub mod template_aggregator;` 与 `pub use template_aggregator::TemplateAggregator;` 两行（紧接 `pub use sql_fingerprint::normalize_template;` 块后）"
    - "src/features/mod.rs 删除 `// Phase 13 will re-enable this when TemplateAggregator::observe() is wired in.` 注释行 + `#[allow(unused_imports)]` 行（normalize_template 在本模块内被 Wave 2 接入；Wave 1 提前删除避免 unused_attributes 警告）"
    - "src/features/sql_fingerprint.rs 删除 ScanMode::Normalize 上 `#[allow(dead_code)]`（lines 25-27 的注释与属性）以及 normalize_template 函数上 `#[allow(dead_code)]`（line 43）：本计划新增 template_aggregator.rs 不直接调用 normalize_template，但 pub use 已解封 dead_code，属性保留会触发 unused_attributes 警告"
    - "template_aggregator.rs 含 `#[cfg(test)] mod tests` 至少包含以下 6 项单元测试函数：test_observe_single、test_finalize_percentiles、test_merge_equivalent、test_merge_timestamps、test_observe_first_last_seen、test_finalize_sorts_by_count_desc"
    - "cargo test template_aggregator 全部新增测试通过"
    - "cargo build --release 成功；cargo tree | grep hdrhistogram 输出 hdrhistogram v7.5.4"
    - "cargo clippy --all-targets -- -D warnings 通过零警告"
  artifacts:
    - path: "src/features/template_aggregator.rs"
      provides: "TemplateAggregator + TemplateStats + TemplateEntry + 单元测试"
      contains: "pub struct TemplateAggregator"
      min_lines: 120
    - path: "src/features/mod.rs"
      provides: "pub use template_aggregator::TemplateAggregator 导出 + 清理 Phase 12 占位注释/allow(unused_imports)"
      contains: "pub use template_aggregator::TemplateAggregator"
    - path: "Cargo.toml"
      provides: "hdrhistogram 7.5.4 依赖声明"
      contains: "hdrhistogram = \"7.5.4\""
  key_links:
    - from: "src/features/template_aggregator.rs"
      to: "hdrhistogram crate"
      via: "use hdrhistogram::Histogram"
      pattern: "use hdrhistogram::Histogram"
    - from: "src/features/template_aggregator.rs"
      to: "ahash::AHashMap"
      via: "use ahash::AHashMap"
      pattern: "use ahash::AHashMap"
    - from: "src/features/template_aggregator.rs"
      to: "serde::Serialize"
      via: "TemplateStats #[derive(Serialize)]"
      pattern: "#\\[derive\\(.*Serialize.*\\)\\]"
    - from: "src/features/mod.rs"
      to: "src/features/template_aggregator.rs"
      via: "pub mod template_aggregator + pub use TemplateAggregator"
      pattern: "pub use template_aggregator::TemplateAggregator"
---

<objective>
新建 `src/features/template_aggregator.rs` 模块，定义私有 `TemplateEntry`、公共 `TemplateStats`、公共 `TemplateAggregator`，并实现 `new()` / `observe()` / `merge()` / `finalize()` 四个公共方法；同时在 `Cargo.toml` 添加 `hdrhistogram = "7.5.4"` 依赖、在 `src/features/mod.rs` 暴露 `TemplateAggregator`、清理 Phase 12 留下的占位 `#[allow(dead_code)]` / `#[allow(unused_imports)]` / 相关注释。本计划只产出**纯累积器**，热循环接入由 Plan 02 完成。

Purpose: 锁定决策 D-01~D-12 全部落在一个独立模块内，TemplateAggregator 与 hdrhistogram 量程/单位/字典序合并语义在此固化；Plan 02 只需要透传 `Option<&mut TemplateAggregator>` 即可，不再纠结结构定义。
Output: 新文件 `src/features/template_aggregator.rs`（~150 行含测试）+ Cargo.toml 一行依赖 + `src/features/mod.rs` 两行 pub mod/pub use + sql_fingerprint.rs 删除 2 处 `#[allow(dead_code)]`。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-templateaggregator/13-CONTEXT.md
@.planning/phases/13-templateaggregator/13-RESEARCH.md
@.planning/phases/13-templateaggregator/13-PATTERNS.md
@.planning/phases/13-templateaggregator/13-VALIDATION.md
@src/features/mod.rs
@src/features/sql_fingerprint.rs
@Cargo.toml
</context>

<interfaces>
<!-- Plan 02 将引用的新公共类型，本计划必须保持签名锁定 -->

From src/features/template_aggregator.rs (NEW):
```rust
pub struct TemplateAggregator { /* private entries: AHashMap<String, TemplateEntry> */ }

impl TemplateAggregator {
    pub fn new() -> Self;
    pub fn observe(&mut self, key: &str, exectime_us: u64, ts: &str);
    pub fn merge(&mut self, other: TemplateAggregator);
    pub fn finalize(self) -> Vec<TemplateStats>;
}

#[derive(Debug, Clone, serde::Serialize)]
pub struct TemplateStats {
    pub template_key: String,
    pub count: u64,
    pub avg_us: u64,
    pub min_us: u64,
    pub max_us: u64,
    pub p50_us: u64,
    pub p95_us: u64,
    pub p99_us: u64,
    pub first_seen: String,
    pub last_seen: String,
}
```

From src/features/mod.rs (existing exports — Plan 02 will use these):
- `pub use template_aggregator::TemplateAggregator;`
- `pub use sql_fingerprint::normalize_template;`（Phase 12 已存在，Plan 02 在 cli/run.rs 调用）

From hdrhistogram 7.5.4 (external):
- `Histogram::<u64>::new_with_bounds(low: u64, high: u64, sigfig: u8) -> Result<Histogram<u64>, CreationError>` — 量程 [low, high]
- `Histogram::record(&mut self, value: u64) -> Result<(), RecordError>` — 超界返回 Err
- `Histogram::add<B: Borrow<Histogram<u64>>>(&mut self, source: B) -> Result<(), AdditionError>` — 合并直方图（量程一致时无损）
- `Histogram::mean(&self) -> f64`、`min() -> u64`、`max() -> u64`、`len() -> u64`、`value_at_quantile(q: f64) -> u64`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 hdrhistogram 依赖并创建 template_aggregator.rs 骨架与测试占位</name>
  <files>Cargo.toml, src/features/template_aggregator.rs, src/features/mod.rs</files>
  <read_first>
    - Cargo.toml（完整文件，重点 lines 32-69 现有 [dependencies] 段）
    - src/features/mod.rs（完整文件，重点 lines 1-11 pub mod/pub use 块）
    - .planning/phases/13-templateaggregator/13-RESEARCH.md（重点 Pattern 1 TemplateEntry 与 Code Examples 完整骨架）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（重点 template_aggregator.rs 章节）
  </read_first>
  <behavior>
    - Cargo.toml 解析后 `cargo tree | grep hdrhistogram` 输出 `hdrhistogram v7.5.4`
    - `cargo build --release` 成功（即使 template_aggregator 仍为骨架）
    - `cargo check` 在新文件含 `pub struct TemplateAggregator;` 占位时通过
    - mod.rs 新增 `pub mod template_aggregator;` 后该模块在 features 命名空间内可见
  </behavior>
  <action>
    Cargo.toml：在 `ahash = "0.8"` 行之后插入 `hdrhistogram = "7.5.4"` 一行，与 ahash 同区块（features 相关依赖聚集）。
    新建 src/features/template_aggregator.rs：
    - 文件顶部加 `use ahash::AHashMap;`、`use hdrhistogram::Histogram;`、`use serde::Serialize;`
    - 定义私有 `struct TemplateEntry { histogram: Histogram<u64>, first_seen: String, last_seen: String }`
    - 定义 `impl TemplateEntry { fn new(first_seen: String) -> Self { let histogram = Histogram::<u64>::new_with_bounds(1, 60_000_000, 2).expect("D-03: valid bounds 1µs–60s sigfig=2"); Self { histogram, first_seen: first_seen.clone(), last_seen: first_seen } } }`
    - 定义 `#[derive(Debug, Clone, Serialize)] pub struct TemplateStats { ... }` 含 10 个 pub 字段：template_key: String、count: u64、avg_us: u64、min_us: u64、max_us: u64、p50_us: u64、p95_us: u64、p99_us: u64、first_seen: String、last_seen: String
    - 定义 `#[derive(Debug, Default)] pub struct TemplateAggregator { entries: AHashMap<String, TemplateEntry> }`
    - 占位 `impl TemplateAggregator { pub fn new() -> Self { Self::default() } /* observe/merge/finalize 留 todo!() 由 Task 2 实现 */ }`
    src/features/mod.rs：在 `pub use sql_fingerprint::normalize_template;` 之后（Phase 12 已存在）插入两行：`pub mod template_aggregator;` 和 `pub use template_aggregator::TemplateAggregator;`；同时**删除** lines 9-10 的占位注释 `// Phase 13 will re-enable this when TemplateAggregator::observe() is wired in.` 和 `#[allow(unused_imports)]` 属性（仅保留 `pub use sql_fingerprint::normalize_template;` 一行）。
    禁止：在本任务实现 observe/merge/finalize 逻辑——保留 `todo!()` 让 cargo check 通过即可。禁止删除 sql_fingerprint.rs 的 allow(dead_code)（留给 Task 3）。
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -5 && cargo tree 2>&1 | grep -E '^hdrhistogram v' | head -2</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml 第 32-70 行区间存在精确字符串 `hdrhistogram = "7.5.4"`
    - 文件 src/features/template_aggregator.rs 存在且包含 `pub struct TemplateAggregator`
    - 文件 src/features/template_aggregator.rs 存在 `pub struct TemplateStats` 且字段顺序匹配 must_haves.truths 中字段顺序
    - 文件 src/features/mod.rs 包含 `pub mod template_aggregator;` 一行
    - 文件 src/features/mod.rs 包含 `pub use template_aggregator::TemplateAggregator;` 一行
    - 文件 src/features/mod.rs 不再包含字符串 `Phase 13 will re-enable this`
    - 文件 src/features/mod.rs 不再包含字符串 `#[allow(unused_imports)]`
    - cargo build --release 退出码 0
    - cargo tree 输出含 `hdrhistogram v7.5.4`
  </acceptance_criteria>
  <done>新增依赖落地、骨架与公共导出落地、cargo build 通过。observe/merge/finalize 仍是 todo!()。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 observe/merge/finalize 三个方法并补齐单元测试</name>
  <files>src/features/template_aggregator.rs</files>
  <read_first>
    - src/features/template_aggregator.rs（Task 1 产物，含骨架与 todo!()）
    - .planning/phases/13-templateaggregator/13-RESEARCH.md（Pattern 2 / Pattern 3 / Pattern 4 / Common Pitfalls 1/3）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（observe / merge / finalize 三段实现规范）
    - src/features/sql_fingerprint.rs（lines 329-439 #[cfg(test)] mod tests 测试组织模式，遵循同款风格）
  </read_first>
  <behavior>
    - test_observe_single：调用 `observe("SELECT 1", 500, "2025-01-15 10:00:00.000")` 一次后 finalize 返回 Vec 长度 1，元素 count=1、min_us=500、max_us=500、avg_us=500、first_seen=last_seen="2025-01-15 10:00:00.000"
    - test_finalize_percentiles：observe 同一 key 100 次，exectime_us 从 1 递增到 100，断言 p50_us 在 [49, 52]、p95_us 在 [93, 97]、p99_us 在 [97, 101]（hdrhistogram sigfig=2 允许 <2% 误差）
    - test_merge_equivalent：构造两个 aggregator 分别观测同一 key 50 次，merge 后 finalize 与单 aggregator 观测 100 次的 count 相同（count=100）
    - test_merge_timestamps：a.observe(key, 1, "2025-01-15 10:00:00.000")、b.observe(key, 1, "2025-01-15 12:00:00.000"); a.merge(b) 后 finalize 元素 first_seen="2025-01-15 10:00:00.000"、last_seen="2025-01-15 12:00:00.000"
    - test_observe_first_last_seen：同 key 三次 observe 用乱序时间戳（中间 → 较早 → 较晚），finalize 后 first_seen=最早字符串，last_seen=最晚字符串
    - test_finalize_sorts_by_count_desc：observe 两个 key A=10 次、B=3 次，finalize Vec[0].template_key=="A"、Vec[1].template_key=="B"
  </behavior>
  <action>
    在 src/features/template_aggregator.rs 中实现以下三个方法体（替换 Task 1 留下的 todo!()）：
    1. observe：`let entry = self.entries.entry(key.to_string()).or_insert_with(|| TemplateEntry::new(ts.to_string())); let _ = entry.histogram.record(exectime_us); if ts < entry.first_seen.as_str() { entry.first_seen = ts.to_string(); } if ts > entry.last_seen.as_str() { entry.last_seen = ts.to_string(); }`
    2. merge：迭代 `for (key, other_entry) in other.entries`，对每个 key 取或建 entry，`let _ = entry.histogram.add(&other_entry.histogram);`，first_seen/last_seen 按字典序取极值
    3. finalize：take self.entries，map 每个 (key, entry) 为 TemplateStats（count = h.len()、avg_us = h.mean() as u64、min_us/max_us 直接用 h.min()/h.max()、p50/p95/p99 用 h.value_at_quantile(0.50/0.95/0.99)），收集后 `sort_unstable_by(|a, b| b.count.cmp(&a.count))` 按 count 降序
    所有 `record()` / `add()` 调用必须以 `let _ = ...` 包裹，避免 unused Result 触发 -D warnings（RESEARCH Common Pitfalls 1）。
    单元测试：在文件底部新增 `#[cfg(test)] mod tests { use super::*; ... }`，至少包含 must_haves 列出的 6 项测试函数；测试中可以直接构造 TemplateAggregator::new()，无需 mock hdrhistogram。
    禁止：实现 `LogProcessor` trait（D-09）。禁止把 `entries` 改为 pub（D-09 强调侧路径而非 trait 注入）。禁止使用 Vec<u64> 全量存储样本（D-10）。
  </action>
  <verify>
    <automated>cargo test template_aggregator 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo test template_aggregator 2>&1` 退出码 0
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_observe_single .* ok'` 有输出
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_finalize_percentiles .* ok'` 有输出
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_merge_equivalent .* ok'` 有输出
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_merge_timestamps .* ok'` 有输出
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_observe_first_last_seen .* ok'` 有输出
    - 命令 `cargo test template_aggregator 2>&1 | grep -E 'test test_finalize_sorts_by_count_desc .* ok'` 有输出
    - 命令 `grep -c 'fn observe' src/features/template_aggregator.rs` 等于 1
    - 命令 `grep -c 'fn merge' src/features/template_aggregator.rs` 等于 1
    - 命令 `grep -c 'fn finalize' src/features/template_aggregator.rs` 等于 1
    - 命令 `grep -c 'todo!()' src/features/template_aggregator.rs` 等于 0
    - 命令 `grep -c 'impl LogProcessor' src/features/template_aggregator.rs` 等于 0（D-09 不实现 trait）
    - 命令 `grep -c 'Vec<u64>' src/features/template_aggregator.rs` 等于 0（D-10 禁止全量样本）
    - 每个方法的源码长度（impl 块内 `pub fn observe`、`pub fn merge`、`pub fn finalize` 各自函数体）单独均不超过 40 行（CLAUDE.md 约束）
  </acceptance_criteria>
  <done>三个公共方法完整实现，6 项单元测试全部通过；mod tests 风格与 sql_fingerprint.rs 一致。</done>
</task>

<task type="auto">
  <name>Task 3: 清理 sql_fingerprint.rs 的 #[allow(dead_code)] 与 -D warnings 闭环</name>
  <files>src/features/sql_fingerprint.rs</files>
  <read_first>
    - src/features/sql_fingerprint.rs（lines 22-46，重点 ScanMode::Normalize 与 normalize_template 上的 allow 属性）
    - src/features/mod.rs（Task 1 已写入：`pub use sql_fingerprint::normalize_template;` 暴露 normalize_template，dead_code 已自动解封）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（"`#[allow(dead_code)]` 清理"小节）
  </read_first>
  <action>
    src/features/sql_fingerprint.rs 删除以下 3 处属性 / 注释：
    1. line 25 `// Phase 13 will re-enable this variant when TemplateAggregator::observe() is wired in.`
    2. line 26 `#[allow(dead_code)]`（作用于 `Normalize` variant）
    3. line 43 `#[allow(dead_code)]`（作用于 `normalize_template` 函数；注意保留 line 41 的 `Phase 13 will call this via TemplateAggregator::observe()` 文档注释——它是 doc-comment 而非 allow）
    完成后 `Normalize` variant 的定义形如 `Fingerprint,\n    Normalize,`，`normalize_template` 函数签名前只保留 `#[must_use]` 与 doc-comment。
    禁止：删除 doc-comment、`#[must_use]`、或函数体内任何逻辑。禁止改动 fingerprint() 路径相关代码。
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings 2>&1 | tail -10 && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo clippy --all-targets -- -D warnings` 退出码 0
    - 命令 `cargo test` 退出码 0（含 Phase 12 既有测试 + Task 2 新增测试全部通过）
    - 命令 `grep -c '#\[allow(dead_code)\]' src/features/sql_fingerprint.rs` 等于 0
    - 命令 `grep -c 'Phase 13 will re-enable this variant' src/features/sql_fingerprint.rs` 等于 0
    - 命令 `grep -c '^pub fn normalize_template' src/features/sql_fingerprint.rs` 等于 1（公共签名仍存在）
    - 命令 `grep -c 'Normalize,' src/features/sql_fingerprint.rs` ≥ 1（ScanMode::Normalize variant 仍存在）
  </acceptance_criteria>
  <done>Phase 12 残留的 dead_code 占位全部清理，全项目 -D warnings 编译通过，全部测试绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户日志行 → TemplateAggregator::observe() | 外部数据（pm.exectime f32 + record.ts 字符串）跨进入内存累积器；无网络或文件 I/O |
| crates.io → Cargo build | 新增 hdrhistogram 依赖落地，需确认包合法性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Denial of Service | TemplateAggregator::observe() | mitigate | hdrhistogram::Histogram::record() 在 exectime_us 超界（>60_000_000）时返回 Err，用 `let _ = ...` 静默忽略，避免单条异常 SQL 导致整文件处理中断（RESEARCH Common Pitfalls 1） |
| T-13-02 | Tampering | hdrhistogram bounds | mitigate | `new_with_bounds(1, 60_000_000, 2)` 由 TemplateEntry::new() 唯一构造，量程不可由外部调用方修改；merge() 的 add() 在量程一致时无精度损失（hdrhistogram 文档保证） |
| T-13-03 | Information Disclosure | TemplateAggregator::entries | accept | entries 为私有字段，无 pub 访问器；first_seen/last_seen 仅记录 ts 字符串而非完整 SQL，不泄露查询参数。Phase 14 输出阶段再评估 |
| T-13-04 | Resource Exhaustion (memory) | AHashMap<String, TemplateEntry> | mitigate | 每模板内存固定 ~24 KB（histogram）+ 两 String（first/last_seen ≤ 64B）；模板数量上限受 normalize_template 输出 key 的多样性限制；D-10 明确禁止 Vec<u64> 全量样本以保证恒定单模板内存 |
| T-13-05 | Integer Overflow | (pm.exectime * 1000.0) as u64 | accept (in Plan 02) | 本 Plan 不接入热循环；Plan 02 在调用点 guard `exectime_us > 0`。`f32 * 1000.0` 超 u64::MAX 不可能（exectime 上限约 1e9 ms）。仅在 Plan 02 task 2 处理 |
| T-13-SC | Tampering | hdrhistogram crates.io 安装 | mitigate | RESEARCH.md Package Legitimacy Audit 标记为 [ASSUMED] 高置信度（Jon Gjengset 维护，crates.io 85M+ 下载）；slopcheck 不可用环境下作为弱信号；新增 Task verify 步骤含 `cargo tree | grep hdrhistogram` 输出形态校验，确认安装版本号一致 |
</threat_model>

<verification>
- 全部 acceptance_criteria 中的 grep / cargo 命令逐条通过
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo test` 全部测试通过（含 Phase 12 既有 + Task 2 新增 6 项）
- `cargo build --release` 通过且二进制可启动（`./target/release/sqllog2db --help` 退出码 0）
</verification>

<success_criteria>
1. Cargo.toml 含 `hdrhistogram = "7.5.4"`，cargo tree 输出 hdrhistogram v7.5.4
2. src/features/template_aggregator.rs 存在并包含三个公共方法 + 6 项单元测试（全部通过）
3. src/features/mod.rs 暴露 TemplateAggregator 类型；Phase 12 残留的 unused_imports allow 已删除
4. src/features/sql_fingerprint.rs 残留的 2 处 #[allow(dead_code)] 已删除
5. cargo clippy --all-targets -- -D warnings 零警告
6. 不实现 LogProcessor trait（D-09），不使用 Vec<u64> 存样本（D-10）
</success_criteria>

<output>
Create `.planning/phases/13-templateaggregator/13-01-SUMMARY.md` when done.
Key fields:
- affects: ["src/features/template_aggregator.rs (NEW)", "src/features/mod.rs", "src/features/sql_fingerprint.rs", "Cargo.toml"]
- provides: ["TemplateAggregator", "TemplateStats", "hdrhistogram 7.5.4 依赖"]
- patterns: ["侧路径 Option<&mut T> 接入预备", "hdrhistogram 量程统一构造", "AHashMap entry().or_insert_with() 懒建", "let _ = record/add() 静默忽略超界"]
- decisions: ["TemplateAggregator 不实现 LogProcessor (D-09)", "Histogram bounds 1..60_000_000 sigfig 2 (D-03)", "first_seen/last_seen 字典序 (D-04/D-05)"]
- tests_added: 6
</output>
</content>
</invoke>