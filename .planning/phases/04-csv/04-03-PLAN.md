---
phase: 04-csv
plan: 03
type: execute
wave: 2
depends_on: ["04-csv-02"]
files_modified:
  - src/config.rs
  - src/exporter/csv.rs
  - src/cli/run.rs
autonomous: true
requirements: [PERF-02, PERF-08]
must_haves:
  truths:
    - "CSV 配置支持 include_performance_metrics 布尔字段，默认 true（不改变现有默认行为）"
    - "include_performance_metrics=false 时，热循环跳过 record.parse_performance_metrics() 调用"
    - "include_performance_metrics=false 时，CSV 输出省略 exectime/rowcount/exec_id 三列（数据行 + header）"
    - "include_performance_metrics=true（默认）时，CSV 输出与 Phase 3 行为字节级一致"
    - "支持 --set exporter.csv.include_performance_metrics=false 命令行覆盖"
    - "配置项变更通过新增单元测试验证；现有 629+ 测试无回归"
  artifacts:
    - path: "src/config.rs"
      provides: "CsvExporter 配置 struct 新增字段 + apply_one 解析支持"
      contains: "include_performance_metrics: bool"
    - path: "src/exporter/csv.rs"
      provides: "CsvExporter 运行时支持 include_performance_metrics 标志，header/数据行均跳过性能指标列"
      contains: "include_performance_metrics"
    - path: "src/cli/run.rs"
      provides: "热循环根据 include_performance_metrics 选择 parse_performance_metrics 或合成空 PerformanceMetrics"
      contains: "include_performance_metrics"
  key_links:
    - from: "src/cli/run.rs::process_log_file"
      to: "src/config.rs::CsvExporter::include_performance_metrics"
      via: "通过 ExporterManager / ExporterKind::Csv 暴露配置标志"
      pattern: "include_performance_metrics"
    - from: "Config TOML [exporter.csv]"
      to: "Config::apply_one('exporter.csv.include_performance_metrics', ...)"
      via: "--set 命令行覆盖"
      pattern: "exporter\\.csv\\.include_performance_metrics"
---

<objective>
Wave 2：实施 D-05/D-06 兜底方案——`include_performance_metrics` 配置项。

Purpose: 当 Wave 1 格式化层优化提升不足 10% 时，提供"用户层"控制项，关闭后跳过 `parse_performance_metrics()`（flamegraph top-3 热路径之一，含 `find_indicators_split` memrchr 扫描），从调用层降低开销以达到 ≥10% 提升目标。
Output: 新配置项 + cli 接入 + csv exporter header/数据行行为变更，全部通过单元测试与现有套件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv/04-CONTEXT.md
@.planning/phases/04-csv/04-RESEARCH.md
@.planning/phases/04-csv/04-PATTERNS.md
@.planning/phases/04-csv/04-02-SUMMARY.md

@src/config.rs
@src/exporter/csv.rs
@src/cli/run.rs
@src/exporter/mod.rs

<interfaces>
当前 CsvExporter config struct (src/config.rs:299–316)：

```rust
#[derive(Debug, Deserialize, Clone)]
pub struct CsvExporter {
    pub file: String,
    #[serde(default = "default_true")]
    pub overwrite: bool,
    #[serde(default)]
    pub append: bool,
}

impl Default for CsvExporter {
    fn default() -> Self {
        Self {
            file: "outputs/sqllog.csv".to_string(),
            overwrite: true,
            append: false,
        }
    }
}
```

`default_true` helper（src/config.rs，已存在）：

```rust
fn default_true() -> bool { true }
```

apply_one 现有 csv.* keys（src/config.rs:130–144）：

```rust
"exporter.csv.file" => { ... }
"exporter.csv.overwrite" => { ... = parse_bool(value)?; }
"exporter.csv.append" => { ... = parse_bool(value)?; }
```

exporter::csv::CsvExporter struct（src/exporter/csv.rs:23–34），已有 pub(crate) 字段先例：

```rust
pub struct CsvExporter {
    path: PathBuf,
    overwrite: bool,
    append: bool,
    writer: Option<BufWriter<File>>,
    stats: ExportStats,
    itoa_buf: itoa::Buffer,
    line_buf: Vec<u8>,
    pub(crate) normalize: bool,
    pub(crate) field_mask: crate::features::FieldMask,
    pub(crate) ordered_indices: Vec<usize>,
}
```

`from_config` 现有逻辑（src/exporter/csv.rs:64–73）：

```rust
pub fn from_config(config: &config::CsvExporter) -> Self {
    let mut e = Self::new(&config.file);
    if config.append { e.append = true; } else { e.overwrite = config.overwrite; }
    e
}
```

cli/run.rs 热循环 parse_performance_metrics 调用点（src/cli/run.rs:174–176）：

```rust
if passes {
    // DML 或通过过滤的 PARAMS：需要完整 pm 用于导出。
    let pm = record.parse_performance_metrics();
```

ExporterManager（src/exporter/mod.rs，需查看 pub fields/methods 以决定 process_log_file 如何取得 include_performance_metrics）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/config.rs 新增 include_performance_metrics 字段 + apply_one 支持 + 单元测试</name>
  <files>src/config.rs</files>
  <read_first>
    - src/config.rs（必读完整文件，重点：行 299–329 `CsvExporter` struct + Default + validate；行 96–174 `apply_one` 函数；行末 `#[cfg(test)]` 测试模块）
    - .planning/phases/04-csv/04-CONTEXT.md（D-05/D-06：默认 true，关闭时跳过性能指标）
    - .planning/phases/04-csv/04-PATTERNS.md（章节 "3. src/config.rs — 兜底配置项"）
  </read_first>
  <action>
**Step 1：修改 `CsvExporter` struct（src/config.rs 行 299–306），追加 include_performance_metrics 字段。**

修改前：
```rust
#[derive(Debug, Deserialize, Clone)]
pub struct CsvExporter {
    pub file: String,
    #[serde(default = "default_true")]
    pub overwrite: bool,
    #[serde(default)]
    pub append: bool,
}
```

修改后：
```rust
#[derive(Debug, Deserialize, Clone)]
pub struct CsvExporter {
    pub file: String,
    #[serde(default = "default_true")]
    pub overwrite: bool,
    #[serde(default)]
    pub append: bool,
    /// 关闭时跳过 parse_performance_metrics()，CSV 省略 exectime/rowcount/exec_id 三列。
    /// 默认 true，保持现有行为不变（D-06）。
    #[serde(default = "default_true")]
    pub include_performance_metrics: bool,
}
```

**Step 2：修改 `Default for CsvExporter`（src/config.rs 行 308–316）：**

修改后：
```rust
impl Default for CsvExporter {
    fn default() -> Self {
        Self {
            file: "outputs/sqllog.csv".to_string(),
            overwrite: true,
            append: false,
            include_performance_metrics: true,
        }
    }
}
```

**Step 3：在 `apply_one`（src/config.rs 行 139–144 `exporter.csv.append` 之后）追加新 key 解析：**

```rust
"exporter.csv.append" => {
    self.exporter
        .csv
        .get_or_insert_with(Default::default)
        .append = parse_bool(value)?;
}
"exporter.csv.include_performance_metrics" => {
    self.exporter
        .csv
        .get_or_insert_with(Default::default)
        .include_performance_metrics = parse_bool(value)?;
}
```

**Step 4：在 src/config.rs 末尾的 `#[cfg(test)] mod tests` 中（如不存在则在文件末尾追加完整模块），追加 4 个测试：**

```rust
    #[test]
    fn test_csv_exporter_default_include_performance_metrics_true() {
        use super::CsvExporter;
        let cfg = CsvExporter::default();
        assert!(cfg.include_performance_metrics, "默认必须为 true（D-06）");
    }

    #[test]
    fn test_apply_one_csv_include_performance_metrics_false() {
        use super::Config;
        let mut cfg = Config::default();
        cfg.apply_one("exporter.csv.include_performance_metrics", "false")
            .expect("apply_one should succeed for valid bool");
        assert!(
            !cfg.exporter.csv.as_ref().unwrap().include_performance_metrics,
            "--set 覆盖后应为 false"
        );
    }

    #[test]
    fn test_apply_one_csv_include_performance_metrics_invalid() {
        use super::Config;
        let mut cfg = Config::default();
        let r = cfg.apply_one("exporter.csv.include_performance_metrics", "maybe");
        assert!(r.is_err(), "非法布尔值必须返回错误");
    }

    #[test]
    fn test_csv_toml_default_include_performance_metrics() {
        // TOML 未指定 include_performance_metrics 时，serde default 必须生效（true）
        let toml = r#"
[sqllog]
directory = "sqllogs"
[exporter.csv]
file = "/tmp/x.csv"
overwrite = true
append = false
"#;
        let cfg: super::Config = toml::from_str(toml).unwrap();
        assert!(
            cfg.exporter.csv.as_ref().unwrap().include_performance_metrics,
            "未指定时 serde 默认必须为 true"
        );
    }
```

如果 `src/config.rs` 没有 `#[cfg(test)] mod tests`，在文件末尾追加完整模块包含上述 4 个测试。先 grep 确认：`grep -n "#\[cfg(test)\]" src/config.rs`。

**约束：**
- 字段名必须为 `include_performance_metrics`（蛇形命名，与 TOML key 一致）
- 默认值必须为 `true`（D-06）
- `parse_bool` helper 已存在（src/config.rs 行 104–114），直接复用，**不要重新定义**
- apply_one 内禁止改动其他 match arm
- `cargo test config` 必须输出 4 个新测试通过
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt -- --check` 无 diff
  </action>
  <verify>
    <automated>cargo test config 2>&amp;1 | tail -20 ; grep -c "pub include_performance_metrics: bool" src/config.rs ; grep -c "include_performance_metrics: true," src/config.rs ; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub include_performance_metrics: bool" src/config.rs` 输出 `1`
    - `grep -c "include_performance_metrics: true," src/config.rs` 输出 `1`
    - `grep -c "exporter.csv.include_performance_metrics" src/config.rs` 输出至少 `1`
    - `grep -c "fn test_csv_exporter_default_include_performance_metrics_true" src/config.rs` 输出 `1`
    - `grep -c "fn test_apply_one_csv_include_performance_metrics_false" src/config.rs` 输出 `1`
    - `grep -c "fn test_apply_one_csv_include_performance_metrics_invalid" src/config.rs` 输出 `1`
    - `grep -c "fn test_csv_toml_default_include_performance_metrics" src/config.rs` 输出 `1`
    - `cargo test config` 退出码 `0`，输出包含 4 个新测试名且全部 passed
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>config struct 与 apply_one 支持 include_performance_metrics，默认 true，4 个新测试通过</done>
</task>

<task type="auto">
  <name>Task 2: CsvExporter 运行时支持 include_performance_metrics（header/数据行联动）</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（必读完整文件：行 23–34 struct；行 64–73 from_config；行 78–258 write_record_preparsed；行 291–308 build_header）
    - src/features/mod.rs 或 src/features/fields.rs 中 `FIELD_NAMES` 常量定义（grep 路径：`grep -rn "FIELD_NAMES" src/features/`），确认 idx 11=exectime, 12=rowcount, 13=exec_id, 14=normalized_sql
    - .planning/phases/04-csv/04-CONTEXT.md（D-05/D-06）
  </read_first>
  <action>
**Step 1：CsvExporter struct（src/exporter/csv.rs 行 23–34）新增字段：**

在 `pub(crate) ordered_indices: Vec<usize>,` 后追加：
```rust
    /// 是否在输出中包含性能指标列（exectime/rowcount/exec_id）。
    /// 关闭时 header 和数据行都跳过这三列；调用方（cli/run.rs）也应跳过 parse_performance_metrics()。
    pub(crate) include_performance_metrics: bool,
```

**Step 2：`CsvExporter::new`（行 47–62）默认值设为 true。**

在 `ordered_indices: (0..crate::features::FIELD_NAMES.len()).collect(),` 行后追加：
```rust
            include_performance_metrics: true,
```

**Step 3：`from_config`（行 64–73）按 config 值设置：**

修改后：
```rust
pub fn from_config(config: &config::CsvExporter) -> Self {
    let mut e = Self::new(&config.file);
    if config.append {
        e.append = true;
    } else {
        e.overwrite = config.overwrite;
    }
    e.include_performance_metrics = config.include_performance_metrics;
    e
}
```

**Step 4：`build_header`（行 291–308）跳过性能指标列。**

在现有 `if idx == 14 && !self.normalize { continue; }` 之后追加：
```rust
        // idx 11/12/13 (exectime/rowcount/exec_id) 在 include_performance_metrics=false 时跳过（D-05/D-06）
        if matches!(idx, 11 | 12 | 13) && !self.include_performance_metrics {
            continue;
        }
```

**Step 5：`write_record_preparsed` 新增参数 + 数据行跳过性能指标列。**

5a. 函数签名（行 78–90）末尾追加 `include_performance_metrics: bool,` 参数：
```rust
pub(crate) fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    writer: &mut BufWriter<File>,
    path: &Path,
    normalize: bool,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    ordered_indices: &[usize],
    include_performance_metrics: bool,
) -> Result<()> {
```

5b. 全量路径（field_mask == ALL，行 127–136）将三列段落条件化：

修改前：
```rust
            line_buf.push(b',');
            if pm.exec_id != 0 || pm.exectime > 0.0 {
                line_buf.extend_from_slice(itoa_buf.format(f32_ms_to_i64(pm.exectime)).as_bytes());
                line_buf.push(b',');
                line_buf.extend_from_slice(itoa_buf.format(i64::from(pm.rowcount)).as_bytes());
                line_buf.push(b',');
                line_buf.extend_from_slice(itoa_buf.format(pm.exec_id).as_bytes());
            } else {
                line_buf.extend_from_slice(b",,");
            }
```

修改后（整段包入 if）：
```rust
            if include_performance_metrics {
                line_buf.push(b',');
                if pm.exec_id != 0 || pm.exectime > 0.0 {
                    line_buf.extend_from_slice(itoa_buf.format(f32_ms_to_i64(pm.exectime)).as_bytes());
                    line_buf.push(b',');
                    line_buf.extend_from_slice(itoa_buf.format(i64::from(pm.rowcount)).as_bytes());
                    line_buf.push(b',');
                    line_buf.extend_from_slice(itoa_buf.format(pm.exec_id).as_bytes());
                } else {
                    line_buf.extend_from_slice(b",,");
                }
            }
```

5c. 投影路径 idx 11/12/13 的 match arm 顶部加守卫。每个 arm 在原有 `w_sep!();` 之前插入 `if !include_performance_metrics { continue; }`。

idx 11 修改后：
```rust
                    11 => {
                        if !include_performance_metrics {
                            continue;
                        }
                        w_sep!();
                        if has_metrics {
                            line_buf.extend_from_slice(
                                itoa_buf.format(f32_ms_to_i64(pm.exectime)).as_bytes(),
                            );
                        }
                    }
```

对 idx 12 和 idx 13 做同样处理（在 `w_sep!();` 之前插入相同的两行守卫）。

5d. 调用点更新：

`write_record`（行 262–288）调用 `write_record_preparsed` 时在 `ordered_indices,` 后追加 `true,`：
```rust
    Self::write_record_preparsed(
        itoa_buf,
        line_buf,
        sqllog,
        &meta,
        &pm,
        writer,
        path,
        normalize,
        normalized_sql,
        field_mask,
        ordered_indices,
        true, // write_record 是兼容路径，默认包含性能指标
    )
```

`export_one_preparsed`（行 419–431）调用 `write_record_preparsed` 时追加 `self.include_performance_metrics,`：
```rust
        Self::write_record_preparsed(
            &mut self.itoa_buf,
            &mut self.line_buf,
            sqllog,
            meta,
            pm,
            writer,
            &self.path,
            self.normalize,
            normalized,
            self.field_mask,
            &self.ordered_indices,
            self.include_performance_metrics,
        )?;
```

`export` / `export_one_normalized` 调用的是 `write_record`（已在 5d 第一段加 `true`），不需要重复修改。

**Step 6：在 csv.rs 测试模块（行 461 起）追加 3 个新单元测试。**

在最后一个测试 `test_csv_field_order_normalized_sql_skipped_when_normalize_false` 后追加：

```rust
    #[test]
    fn test_csv_header_skips_pm_when_disabled() {
        let dir = tempfile::TempDir::new().unwrap();
        let path = dir.path().join("out.csv");
        let mut exporter = CsvExporter::new(&path);
        exporter.include_performance_metrics = false;
        exporter.initialize().unwrap();
        exporter.finalize().unwrap();
        let content = std::fs::read_to_string(&path).unwrap();
        let header = content.lines().next().unwrap();
        assert!(!header.contains("exectime"), "header: {header}");
        assert!(!header.contains("rowcount"), "header: {header}");
        assert!(!header.contains("exec_id"), "header: {header}");
        assert!(header.contains("sql"), "sql column should remain");
    }

    #[test]
    fn test_csv_data_row_skips_pm_when_disabled() {
        let dir = tempfile::TempDir::new().unwrap();
        let logfile = dir.path().join("test.log");
        let outfile = dir.path().join("out.csv");
        write_test_log(&logfile, 3);

        let parser = LogParser::from_path(logfile.to_str().unwrap()).unwrap();
        let records: Vec<_> = parser.iter().filter_map(std::result::Result::ok).collect();

        let mut exporter = CsvExporter::new(&outfile);
        exporter.include_performance_metrics = false;
        exporter.initialize().unwrap();
        for r in &records {
            exporter.export(r).unwrap();
        }
        exporter.finalize().unwrap();

        let content = std::fs::read_to_string(&outfile).unwrap();
        let header = content.lines().next().unwrap();
        let header_cols = header.split(',').count();
        // 关闭性能指标后 header 列数 == 全量列数 - 3
        // 全量含 normalized_sql：15；关闭性能指标后剩 12 列
        assert_eq!(header_cols, 12, "header: {header}");
        // 数据行列数也应为 12（注意 SQL 列含双引号但不含逗号）
        for line in content.lines().skip(1) {
            let cols = line.split(',').count();
            assert_eq!(cols, 12, "data row: {line}");
        }
    }

    #[test]
    fn test_csv_default_include_pm_true_keeps_existing_behavior() {
        // 默认（include_performance_metrics=true）输出与历史行为一致
        let dir = tempfile::TempDir::new().unwrap();
        let logfile = dir.path().join("test.log");
        let outfile = dir.path().join("out.csv");
        write_test_log(&logfile, 2);

        let parser = LogParser::from_path(logfile.to_str().unwrap()).unwrap();
        let records: Vec<_> = parser.iter().filter_map(std::result::Result::ok).collect();

        let mut exporter = CsvExporter::new(&outfile);
        // 不显式设置 include_performance_metrics，应为默认 true
        exporter.initialize().unwrap();
        for r in &records {
            exporter.export(r).unwrap();
        }
        exporter.finalize().unwrap();

        let content = std::fs::read_to_string(&outfile).unwrap();
        let header = content.lines().next().unwrap();
        assert!(header.contains("exectime"));
        assert!(header.contains("rowcount"));
        assert!(header.contains("exec_id"));
    }
```

**约束：**
- `include_performance_metrics` 字段在 `CsvExporter` struct（运行时类型）中是 `pub(crate)`
- `write_record_preparsed` 新增参数 `include_performance_metrics: bool` 必须放在签名最末位
- 默认值（new()）必须为 true
- `from_config` 必须把 config 值赋值过来
- 全量路径：当 include=false 时整段（push(',') + 三列）一并跳过；不要遗留多余逗号
- 投影路径：idx 11/12/13 在 include=false 时通过 `continue` 跳过整个 match arm（w_sep! 也不能执行）
- 不要修改 `#[inline]` 属性
- 修改后必须运行 `cargo test --lib -- exporter::csv`（≥17 测试）+ `cargo test`（629+ 全套）+ clippy + fmt 全部通过
  </action>
  <verify>
    <automated>cargo test --lib -- exporter::csv 2>&amp;1 | tail -10 ; grep -c "pub(crate) include_performance_metrics: bool" src/exporter/csv.rs ; grep -c "include_performance_metrics: true," src/exporter/csv.rs ; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) include_performance_metrics: bool" src/exporter/csv.rs` 输出 `1`
    - `grep -c "include_performance_metrics: true," src/exporter/csv.rs` 输出 至少 `1`（new() 中）
    - `grep -c "e.include_performance_metrics = config.include_performance_metrics;" src/exporter/csv.rs` 输出 `1`
    - `grep -c "matches!(idx, 11 | 12 | 13)" src/exporter/csv.rs` 输出 `1`（build_header 守卫）
    - `grep -c "if !include_performance_metrics" src/exporter/csv.rs` 输出 至少 `4`（全量路径 1 + 投影路径 idx 11/12/13 共 3 处）
    - `grep -c "include_performance_metrics: bool," src/exporter/csv.rs` 输出 `1`（write_record_preparsed 签名）
    - `grep -c "fn test_csv_header_skips_pm_when_disabled" src/exporter/csv.rs` 输出 `1`
    - `grep -c "fn test_csv_data_row_skips_pm_when_disabled" src/exporter/csv.rs` 输出 `1`
    - `grep -c "fn test_csv_default_include_pm_true_keeps_existing_behavior" src/exporter/csv.rs` 输出 `1`
    - `cargo test --lib -- exporter::csv` 输出 `test result: ok` 且 passed ≥ 17（12 原测 + 2 boundary + 3 新测）
    - `cargo test` 退出码 `0`，全套 ≥ 629 个测试通过
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>CsvExporter 运行时支持 include_performance_metrics，header 和数据行均跳过性能指标列，3 个新测试通过</done>
</task>

<task type="auto">
  <name>Task 3: cli/run.rs 热循环 lazy parse_pm + ExporterManager 暴露配置标志</name>
  <files>src/cli/run.rs, src/exporter/mod.rs</files>
  <read_first>
    - src/exporter/mod.rs（必读完整文件，重点：ExporterManager / ExporterKind 当前结构、from_config 路径，确认如何把 csv 配置标志传递到 process_log_file）
    - src/cli/run.rs（必读行 1–270，重点：handle_run / process_log_file 签名、热循环 parse_performance_metrics 调用点行 174–176）
    - src/exporter/csv.rs（确认 Task 2 已新增 pub(crate) include_performance_metrics 字段）
    - .planning/phases/04-csv/04-CONTEXT.md（D-05/D-06）
  </read_first>
  <action>
**Step 1：暴露 ExporterManager 上的 csv include_performance_metrics 标志。**

先 grep `src/exporter/mod.rs` 确认 ExporterManager 当前结构，预期在 ExporterManager 或 ExporterKind 上新增一个 `pub fn csv_include_performance_metrics(&self) -> bool` 方法（默认 true）。

具体修改：
- 在 `src/exporter/mod.rs` 的 `ExporterManager` impl 块或 `ExporterKind` enum 上新增方法：

```rust
impl ExporterKind {
    /// 当前 active exporter 是否应包含性能指标列（仅 CSV 路径有意义）。
    /// 用于 cli/run.rs 热循环判断是否调用 record.parse_performance_metrics()。
    pub fn csv_include_performance_metrics(&self) -> bool {
        match self {
            Self::Csv(c) => c.include_performance_metrics,
            // SQLite/DryRun 永远需要完整 pm（schema 固定）
            _ => true,
        }
    }
}
```

如果 ExporterManager 通过 `kind: ExporterKind` 字段持有 active exporter，则在 ExporterManager 上转发：

```rust
impl ExporterManager {
    pub fn csv_include_performance_metrics(&self) -> bool {
        self.kind.csv_include_performance_metrics()
    }
}
```

请先 grep 确认 ExporterManager 字段名（`grep -n "pub struct ExporterManager" src/exporter/mod.rs` 或读完整文件），决定加在哪里。

**Step 2：cli/run.rs 热循环 lazy parse_pm。**

定位 `process_log_file` 函数（行 ~110 起），在循环开始前从 exporter_manager 取出 `include_pm` 标志：

```rust
let include_pm = exporter_manager.csv_include_performance_metrics();
```

将该标志传入热循环（如 process_log_file 是单独函数则需在签名中新增 `include_pm: bool` 参数；如 process_log_file 已能访问 exporter_manager 引用则直接使用）。

在行 174–176 的热循环点，把：
```rust
if passes {
    // DML 或通过过滤的 PARAMS：需要完整 pm 用于导出。
    let pm = record.parse_performance_metrics();
```

改为：
```rust
if passes {
    // DML 或通过过滤的 PARAMS：CSV 关闭性能指标时使用空 pm 以避开 find_indicators_split（D-05/D-06）
    let pm = if include_pm {
        record.parse_performance_metrics()
    } else {
        // 合成空 pm：sql 字段必须保留（来自 record.body()）以保证 CSV 输出 SQL 列正确
        // PerformanceMetrics 字段：sql, exectime, rowcount, exec_id（具体字段以上游 crate 为准）
        // 直接构造仅 sql 来自 body() 的 PerformanceMetrics；其余数值字段为 0/默认
        PerformanceMetrics {
            sql: record.body(),
            exectime: 0.0,
            rowcount: 0,
            exec_id: 0,
        }
    };
```

注意：`PerformanceMetrics` 来自上游 crate `dm_database_parser_sqllog`。**先 grep 确认其字段构造**：
```bash
grep -n "pub struct PerformanceMetrics" ~/.cargo/registry/src/*/dm-database-parser-sqllog-*/src/*.rs
```

如果上游 PerformanceMetrics 字段不可全部 pub 直接构造（比如 sql 是 `Cow<'a, str>`），改为 fall-back：保留 `record.parse_performance_metrics()` 调用，但增加 fast-path 跳过——只要 `include_pm == false` 时**写入 csv 时不读取这些数值字段**就够了（Task 2 已实现 header/数据行跳过）。即把行 176 改为：

```rust
let pm = if include_pm {
    record.parse_performance_metrics()
} else {
    // 关闭性能指标时仍需 pm.sql；直接调用 parse_performance_metrics() 是一次必要开销
    // 但 csv 写入路径在 Task 2 中已跳过 exectime/rowcount/exec_id 字段，
    // 故 find_indicators_split 仍执行；如需完全跳过须确认上游 API 是否支持 sql-only 模式
    record.parse_performance_metrics()
};
```

**优选实施方式：根据上游 PerformanceMetrics 字段可见性二选一。**

- **方式 A（推荐）：上游 PerformanceMetrics 字段全部 pub** → 直接构造空 pm，跳过 `parse_performance_metrics()`，节省 find_indicators_split 调用
- **方式 B（兜底）：上游字段非全 pub** → 保留调用，仅在 csv 写入跳过（Task 2 已完成）；本 Task 仅暴露 `include_pm` 标志便于将来扩展

**任务执行时先 cargo doc / grep 确认上游字段，再选择 A 或 B。结果记录到 SUMMARY 中。**

**Step 3：新增 cli 集成测试或单元测试。**

在 `src/cli/run.rs` 同文件 `#[cfg(test)] mod tests`（如不存在则新建），追加：

```rust
    #[test]
    fn test_include_performance_metrics_false_csv_excludes_pm_columns() {
        // 集成测试：构造一份 toml，配置 include_performance_metrics=false，
        // 运行 handle_run 后断言 CSV header 不含性能指标列
        use crate::config::Config;
        use std::sync::atomic::AtomicBool;
        use std::sync::Arc;

        let dir = tempfile::TempDir::new().unwrap();
        let log_path = dir.path().join("t.log");
        std::fs::write(
            &log_path,
            "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:A ip:10.0.0.1) [SEL] SELECT 1. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n",
        ).unwrap();
        let csv_path = dir.path().join("out.csv");

        let toml = format!(
            r#"
[sqllog]
directory = "{logdir}"
[error]
file = "{dir}/errors.log"
[logging]
file = "{dir}/app.log"
level = "warn"
retention_days = 1
[exporter.csv]
file = "{csv}"
overwrite = true
append = false
include_performance_metrics = false
"#,
            logdir = dir.path().to_string_lossy().replace('\\', "/"),
            dir = dir.path().to_string_lossy().replace('\\', "/"),
            csv = csv_path.to_string_lossy().replace('\\', "/"),
        );
        let cfg: Config = toml::from_str(&toml).unwrap();

        super::handle_run(
            &cfg,
            None,
            false,
            true,
            &Arc::new(AtomicBool::new(false)),
            80,
            false,
            None,
            1,
        ).unwrap();

        let content = std::fs::read_to_string(&csv_path).unwrap();
        let header = content.lines().next().unwrap();
        assert!(!header.contains("exectime"), "header should skip exectime: {header}");
        assert!(!header.contains("rowcount"), "header should skip rowcount: {header}");
        assert!(!header.contains("exec_id"), "header should skip exec_id: {header}");
    }
```

如果 `handle_run` 不在 `pub` 范围内，调整为合适的 visibility 调用方式（如使用 `crate::cli::run::handle_run`）。

**约束：**
- `csv_include_performance_metrics()` 方法必须 `pub`（在 ExporterKind/ExporterManager 上）；非 csv 路径返回 true（不影响 sqlite/dryrun）
- 热循环逻辑变更必须保留 PARAMS 记录的 normalize 路径（行 226–238 的 else 分支不要改动）
- 不要修改 `parse_meta()` 调用（它不在本任务范围）
- 修改后必须运行：cargo test（629+ 全套通过）+ clippy + fmt
- 在 SUMMARY 中说明选择了方式 A 还是方式 B，并附上 Wave 1 vs include_pm=false 的 csv_export real-file benchmark 数值（粗略 1 次 quick 即可，详细 baseline 留给 Plan 04）
  </action>
  <verify>
    <automated>cargo test 2>&amp;1 | tail -10 ; grep -c "csv_include_performance_metrics" src/exporter/mod.rs ; grep -c "csv_include_performance_metrics" src/cli/run.rs ; grep -c "fn test_include_performance_metrics_false_csv_excludes_pm_columns" src/cli/run.rs ; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub fn csv_include_performance_metrics" src/exporter/mod.rs` 输出 至少 `1`
    - `grep -c "csv_include_performance_metrics" src/cli/run.rs` 输出 至少 `1`（热循环消费此标志）
    - `grep -c "fn test_include_performance_metrics_false_csv_excludes_pm_columns" src/cli/run.rs` 输出 `1`
    - `cargo test` 退出码 `0`，全套 ≥ 629 测试通过（包含本任务新增的集成测试）
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
    - SUMMARY 文件记录方式 A/B 选择 + 一次 quick benchmark 数值
  </acceptance_criteria>
  <done>cli/run.rs 热循环根据 include_performance_metrics 决定调用路径，集成测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config → Config struct | 用户输入 include_performance_metrics 值经 serde + parse_bool 校验 |
| --set CLI args → apply_one | --set 字符串经 parse_bool 校验，非法值返回结构化错误 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | apply_one("exporter.csv.include_performance_metrics", "<bad>") | mitigate | parse_bool 拒绝非 true/false/0/1/yes/no 值，返回 ConfigError::InvalidValue（test_apply_one_csv_include_performance_metrics_invalid 验证） |
| T-04-06 | Information disclosure | include_performance_metrics=false 输出列减少 | accept | 用户显式选择，CSV header 反映实际列；schema 不固定属于已设计行为（D-06） |
| T-04-07 | Repudiation | 默认值变更导致历史输出格式破坏 | mitigate | 默认值锁定 `true`，serde default + new() 默认 + Default impl 三处 + 单元测试 test_csv_default_include_pm_true_keeps_existing_behavior 验证 |
</threat_model>

<verification>
- `cargo test`（629+ 全套 + 4 config 测试 + 3 csv 测试 + 1 cli 集成测试 = 637+ 通过）退出码 `0`
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt -- --check` 无 diff
- `--set exporter.csv.include_performance_metrics=false` 命令行覆盖路径有单元测试覆盖
- TOML 默认 + 显式 false 双路径都有测试覆盖
</verification>

<success_criteria>
- src/config.rs 含 `pub include_performance_metrics: bool` 字段，默认 true
- src/exporter/csv.rs 含 `pub(crate) include_performance_metrics: bool` 字段，默认 true
- src/exporter/mod.rs 含 `pub fn csv_include_performance_metrics()` 方法
- src/cli/run.rs 热循环消费此标志（至少作为常量传递）
- include=false 时 header 和数据行均跳过 exectime/rowcount/exec_id 三列
- 所有现有测试 + 8 个新增测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04-csv/04-03-SUMMARY.md`，记录：
1. 上游 PerformanceMetrics 字段可见性结论（方式 A 还是 B）
2. include_performance_metrics=false 下 csv_export real-file 一次 quick benchmark 数值
3. 与 Wave 1 默认（include=true）的对比
4. 新增测试清单（4 + 3 + 1 = 8 个）
5. 任何编译/clippy 修复细节（如 PerformanceMetrics 构造方式调整）
</output>
