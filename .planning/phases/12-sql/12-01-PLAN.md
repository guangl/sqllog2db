---
phase: 12-sql
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/sql_fingerprint.rs  # 同文件重构 fingerprint() + 新增 normalize_template() + 共享 scan_sql_bytes()
autonomous: true
requirements:
  - TMPL-01

must_haves:
  truths:
    - "src/features/sql_fingerprint.rs 中新增 pub fn normalize_template(sql: &str) -> String"
    - "src/features/sql_fingerprint.rs 中存在私有 fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String 供 fingerprint() 与 normalize_template() 共享底层字节扫描循环（D-07/D-08）"
    - "src/features/sql_fingerprint.rs 中存在私有 enum ScanMode { Fingerprint, Normalize }（采用枚举参数化避免单态化膨胀，RESEARCH.md Q2 推荐）"
    - "pub fn fingerprint(sql: &str) -> String 公共签名不变，函数体重构为 scan_sql_bytes(sql, ScanMode::Fingerprint) 的薄包装"
    - "pub fn normalize_template(sql: &str) -> String 函数体为 scan_sql_bytes(sql, ScanMode::Normalize) 的薄包装"
    - "调用 normalize_template 对包含单行注释 `--` 的 SQL 去除注释段且保留结尾换行前的内容"
    - "调用 normalize_template 对包含多行注释 `/* ... */` 的 SQL 将注释块替换为单空格"
    - "对 `SELECT * FROM t WHERE id IN (1, 2, 3)` 和 `SELECT * FROM t WHERE id IN (10, 20, 30, 40)` 调用 normalize_template 返回相同的模板 key"
    - "对 `SELECT * FROM t WHERE name IN ('a', 'b')` 与 `IN ('xx', 'yy', 'zz')` 调用 normalize_template 返回相同模板 key（D-02）"
    - "select FROM t where id = 1 与 SELECT from T WHERE id = 2 经 normalize_template 后关键字均为大写形式"
    - "`WHERE col = '-- not a comment'` 经 normalize_template 后字符串字面量原文保留（D-15）"
    - "`SELECT  *  FROM  t` 经 normalize_template 后连续空白折叠为单空格"
    - "现有 cargo test fingerprint 全部测试（9 项）在重构后零回归"
    - "cargo test 中针对 normalize_template 的 7 项子测试全部通过"
    - "cargo clippy --all-targets -- -D warnings 通过且新增代码零 warning"
  artifacts:
    - path: "src/features/sql_fingerprint.rs"
      provides: "ScanMode 枚举 + scan_sql_bytes() 共享扫描引擎 + fingerprint() 重构为薄包装 + normalize_template() 新增 + 单元测试"
      contains: "pub fn normalize_template"
  key_links:
    - from: "src/features/sql_fingerprint.rs"
      to: "memchr crate"
      via: "memchr::memchr / memchr::memmem::find"
      pattern: "memchr::(memchr|memmem)"
    - from: "pub fn fingerprint()"
      to: "fn scan_sql_bytes()"
      via: "调用 scan_sql_bytes(sql, ScanMode::Fingerprint)（D-07 强制共享）"
      pattern: "scan_sql_bytes\\(.*ScanMode::Fingerprint"
    - from: "pub fn normalize_template()"
      to: "fn scan_sql_bytes()"
      via: "调用 scan_sql_bytes(sql, ScanMode::Normalize)（D-07 强制共享）"
      pattern: "scan_sql_bytes\\(.*ScanMode::Normalize"
    - from: "scan_sql_bytes()"
      to: "NEEDS_SPECIAL_NORM 字节表（含 `'`、空白、ASCII 数字、`-`、`/`）"
      via: "统一查找表 + 在 match arm 内按 mode 分发"
      pattern: "NEEDS_SPECIAL_NORM"
---

<objective>
重构 src/features/sql_fingerprint.rs：抽取 `fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String` 作为共享字节扫描引擎，配套私有 `enum ScanMode { Fingerprint, Normalize }`；将现有 `pub fn fingerprint()` 重构为 `scan_sql_bytes(sql, ScanMode::Fingerprint)` 的薄包装（公共签名不变、行为不变），并新增 `pub fn normalize_template(sql: &str) -> String` 同样作为薄包装 `scan_sql_bytes(sql, ScanMode::Normalize)`。`normalize_template` 完成 D-01 至 D-16 全部锁定决策定义的四项变换（注释去除、IN 列表折叠、关键字大小写统一、空白折叠），并保留字符串字面量原文。

Purpose: 满足 D-07/D-08 的强制共享语义——`NEEDS_SPECIAL_NORM` 字节表、memchr SIMD 查找、字面量跳过等底层逻辑仅写一次；为 TMPL-01 提供稳定的模板 key 生成函数。Phase 13 的 `TemplateAggregator::observe()` 将以 normalize_template 返回值作为聚合 key（D-14）。
Output: src/features/sql_fingerprint.rs 重构为 `enum ScanMode` + 私有共享引擎 `scan_sql_bytes` + 两个公共薄包装 (`fingerprint` 重构 / `normalize_template` 新增) + 私有辅助 (`prev_is_ident_byte` 复用 / `is_keyword` 新增 / `is_ident_byte` 新增) + `#[cfg(test)] mod tests` 含原 fingerprint 9 项测试零回归 + 7 项 normalize_template 新增子测试。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sql/12-CONTEXT.md
@.planning/phases/12-sql/12-RESEARCH.md
@.planning/phases/12-sql/12-PATTERNS.md
@.planning/phases/12-sql/12-VALIDATION.md
@src/features/sql_fingerprint.rs
</context>

<interfaces>
<!-- 现有 fingerprint() 暴露的内部约定，重构必须遵循 -->

From src/features/sql_fingerprint.rs:
- `const NEEDS_SPECIAL: [bool; 256]` — fingerprint() 原查找表（本 plan 重命名为 NEEDS_SPECIAL_NORM 并扩展，与 fingerprint 与 normalize_template 共享；fingerprint 路径下多分发的两个新字节 `-`/`/` 走默认 push-as-is 分支，行为不变）
- `pub fn fingerprint(sql: &str) -> String` — 公共签名不变；函数体改为 `scan_sql_bytes(sql, ScanMode::Fingerprint)`
- `fn prev_is_ident_byte(out: &[u8]) -> bool` — 标识符字节边界辅助（复用）

新增导出（公共）:
- `pub fn normalize_template(sql: &str) -> String` — 函数体为 `scan_sql_bytes(sql, ScanMode::Normalize)`

新增私有项（不导出）:
- `enum ScanMode { Fingerprint, Normalize }`
- `fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String`
- `fn is_keyword(word: &[u8]) -> bool`
- `fn is_ident_byte(b: u8) -> bool`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 引入共享扫描基础（NEEDS_SPECIAL_NORM 字节表 + is_keyword + is_ident_byte 辅助）</name>
  <files>src/features/sql_fingerprint.rs</files>
  <read_first>
    - src/features/sql_fingerprint.rs（完整文件，重点 lines 1-18 NEEDS_SPECIAL 与 lines 106-109 prev_is_ident_byte）
    - .planning/phases/12-sql/12-CONTEXT.md（D-04/D-07/D-08）
    - .planning/phases/12-sql/12-PATTERNS.md（Pattern 2 关键字匹配、§NEEDS_SPECIAL 方案 A 注解）
    - .planning/phases/12-sql/12-RESEARCH.md（Pitfall 4、Pitfall 5、§Open Questions Q2 枚举推荐）
  </read_first>
  <behavior>
    - 将原 `NEEDS_SPECIAL` 重命名为 `NEEDS_SPECIAL_NORM`：在原集合（`'`、空白、ASCII 数字）基础上额外将 `b'-'` 和 `b'/'` 标记为 true。该表由 fingerprint 与 normalize_template 共享（D-08 单一事实来源）。fingerprint 路径下，`-` 和 `/` 字节落入默认 push-as-is 分支，输出字节序列与原 fingerprint 完全一致——行为零回归。
    - 新增私有 `fn is_keyword(word: &[u8]) -> bool`：对输入 word 执行 ASCII case-insensitive 匹配，识别 SQL DML/DDL 关键字集合：SELECT, FROM, WHERE, AND, OR, JOIN, ON, AS, INSERT, UPDATE, DELETE, INTO, VALUES, SET, GROUP, ORDER, BY, HAVING, UNION, DISTINCT, LIMIT, CREATE, DROP, ALTER, IN, NOT, NULL, IS, BETWEEN, LIKE, EXISTS, CASE, WHEN, THEN, ELSE, END。实现：长度 > 8 直接返回 false（最长关键字 DISTINCT/BETWEEN 为 7-8 字节）；其余在栈数组上做 to_ascii_uppercase 后 `matches!` 字节匹配（RESEARCH.md Pattern 2 + Assumption A2 允许 match 而非 phf）。
    - 新增私有 `fn is_ident_byte(b: u8) -> bool`：返回 `b.is_ascii_alphanumeric() || b == b'_' || b == b'.'`（与 prev_is_ident_byte 语义一致，但接受单字节用于 word 边界后向检测）。
    - 本 Task 不修改 fingerprint() 函数体，也不新增 normalize_template；仅完成共享基础设施。
  </behavior>
  <action>
将 src/features/sql_fingerprint.rs 中的 `const NEEDS_SPECIAL: [bool; 256]` 改名为 `const NEEDS_SPECIAL_NORM: [bool; 256]`，并在初始化块中额外将 `b'-' as usize` 与 `b'/' as usize` 置为 true（保持原 const-fn 风格）。同步更新 `fingerprint()` 函数体内所有 `NEEDS_SPECIAL` 引用为 `NEEDS_SPECIAL_NORM`。

为确认 fingerprint 行为不变：原 `NEEDS_SPECIAL` 命中 `-`/`/` 的分支不存在；在共享表下，fingerprint 主循环遇到 `-`/`/` 时落入末尾的默认 push-as-is 分支（即 `b => { out.push(b); i += 1; }` 那个 arm），与原行为完全等价（原行为是批量复制路径直接 push）。这是合法重命名，不改变 fingerprint 语义。

在 prev_is_ident_byte 函数附近新增：
- `fn is_keyword(word: &[u8]) -> bool`：栈数组（≤ 8 字节）大写化 + matches! 字节字面量匹配，覆盖 §behavior 列出的所有关键字。
- `fn is_ident_byte(b: u8) -> bool`：单字节判定。

不修改任何现有公共/私有项的签名。本任务仅完成基础设施重命名与新私有项添加，不写入 scan_sql_bytes 或 normalize_template。
  </action>
  <verify>
    <automated>cargo build -p sqllog2db 2>&amp;1 | grep -E 'error' || true; cargo test --package sqllog2db --lib features::sql_fingerprint::tests -- --nocapture 2>&amp;1 | tail -20; cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -v '^#' src/features/sql_fingerprint.rs | grep -c 'NEEDS_SPECIAL_NORM'` ≥ 3（声明 + fingerprint 内引用 + 后续 Task 2 引用）。
    - 源断言：`grep -v '^#' src/features/sql_fingerprint.rs | grep -c 'NEEDS_SPECIAL\b'` 为 0（确保原名已全部替换，无遗漏）。
    - 源断言：`grep -E '^fn is_keyword' src/features/sql_fingerprint.rs` 输出 1 行。
    - 源断言：`grep -E '^fn is_ident_byte' src/features/sql_fingerprint.rs` 输出 1 行。
    - 源断言：`NEEDS_SPECIAL_NORM` 初始化代码块中存在 `b'-'` 与 `b'/'` 字面量。
    - 行为断言：现有 `cargo test --package sqllog2db --lib features::sql_fingerprint::tests` 全部 9 项测试通过，且与重构前输出完全一致（fingerprint 行为零回归）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>NEEDS_SPECIAL_NORM 重命名扩展完成；is_keyword、is_ident_byte 两项私有定义存在；原 fingerprint 9 项测试全绿；clippy 零 warning。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 抽取共享引擎 scan_sql_bytes(mode) + 重构 fingerprint() 为薄包装 + 新增 normalize_template() 为薄包装</name>
  <files>src/features/sql_fingerprint.rs</files>
  <read_first>
    - src/features/sql_fingerprint.rs（含 Task 1 新增的 NEEDS_SPECIAL_NORM / is_keyword / is_ident_byte）
    - .planning/phases/12-sql/12-CONTEXT.md（D-01 至 D-16 全部决策原文，重点 D-07 / D-08 强制共享语义）
    - .planning/phases/12-sql/12-RESEARCH.md（Pattern 1-5、Pitfall 1-5、§Code Examples、§Open Questions Q2 枚举推荐避免单态化膨胀）
    - .planning/phases/12-sql/12-PATTERNS.md（§Pattern Assignments / sql_fingerprint.rs 节、§Shared Patterns）
  </read_first>
  <behavior>
    - **共享引擎 `fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String`（私有）**：将原 fingerprint 主循环结构（外层 while-loop + 内层 bulk-copy 使用 NEEDS_SPECIAL_NORM + match 分发 + 末尾 String::from_utf8 + trim_ascii）整体下沉为该函数。所有 mode 分支差异通过 `match mode { ScanMode::Fingerprint => ..., ScanMode::Normalize => ... }` 在各 match arm 内部判定，确保 fingerprint 与 normalize_template 共享同一个 while-loop 框架、同一个 NEEDS_SPECIAL_NORM 查找表、同一个 memchr 字面量跳过逻辑、同一个 trim_ascii 收尾逻辑（D-07/D-08）。
    - **`enum ScanMode { Fingerprint, Normalize }`（私有）**：仅两个无字段 variant；采用枚举而非泛型闭包（RESEARCH.md §Open Questions Q2 推荐：避免单态化膨胀，保持 inline 友好）。
    - **`pub fn fingerprint(sql: &str) -> String` 重构为薄包装**：函数体仅一行 `scan_sql_bytes(sql, ScanMode::Fingerprint)`。保留原 doc 注释、`#[must_use]` 属性、公共签名。该重构必须使原 9 项 fingerprint 测试零回归。
    - **`pub fn normalize_template(sql: &str) -> String` 新增**：函数体仅一行 `scan_sql_bytes(sql, ScanMode::Normalize)`。添加 `#[must_use]` 与 doc 注释（参照 fingerprint 风格）。
    - **scan_sql_bytes 内 match arm 的 mode 分发规则**：
      * `b'\''` 字符串字面量分支：
        - Fingerprint mode：push `?`，然后 memchr 跳到闭合引号，处理 `''` 转义（与原 fingerprint 一致）。
        - Normalize mode：保留原文——`literal_start = i`，memchr 跳到闭合引号 + `''` 转义处理后，`out.extend_from_slice(&bytes[literal_start..i])`（D-15）。
        - 该分支始终先于注释分发（D-15 字面量内 `--` 不视为注释）。
      * `b'-'` 分支（仅 Normalize 触发处理逻辑；Fingerprint 走默认 push-as-is）：当 `i + 1 < len && bytes[i + 1] == b'-'` 时，用 memchr::memchr 找 `\n`；找到则 `i += rel + 1`，否则 `i = len`。out 不写入任何字节（D-16）。Fingerprint mode 下该字节落入末尾默认分支 `out.push(b); i += 1;`（行为与原 fingerprint 等价）。
      * `b'/'` 分支（仅 Normalize 触发处理逻辑；Fingerprint 走默认 push-as-is）：当 `i + 1 < len && bytes[i + 1] == b'*'` 时，`i += 2`，用 memchr::memmem::find(&bytes[i..], b"*/") 跳过注释体；未闭合则 `i = len`。若 out 末尾不是空格则 push 单空格（D-16，避免 token 粘连）。Fingerprint mode 下该字节落入末尾默认分支。
      * 字母字节分支（仅 Normalize 引入；Fingerprint mode 此分支无操作走默认 push-as-is）：
        - 前向边界检查：`prev_is_ident_byte(&out) == false`。若 false（即处于标识符中部，如 outer_join 的 outer），跳过关键字处理，走默认 push-as-is（Pitfall 4）。
        - 向前消费完整 word（while is_ident_byte 步进）；后向边界检查：`word_end == len || !is_ident_byte(bytes[word_end])`。若中部，同样不大写化。
        - 通过边界后调用 `is_keyword(word)`：是关键字则循环 push 大写字节（`b.to_ascii_uppercase()`），否则原样写入 word。
        - **IN 列表折叠（D-01/D-02/D-03，RESEARCH.md Pattern 5 策略 A 主循环内联）**：若大写化结果等于 `b"IN"` 且通过双向边界，则先 push `IN`；向前 lookahead 跳过空白；若下一非空白字节是 `b'('`，扫描到匹配 `)` 之间的全部内容（维护括号深度计数器：嵌套 `(` 深度+1、`)` 深度-1，归零即停止）。括号区间预扫描：若内部出现独立关键字 `SELECT` 或 `FROM`（子查询），放弃折叠保留原文（RESEARCH.md §Open Questions Q1 推荐）；否则 push ` (?)` 并将 i 设为 `)` 之后。
      * 数字字节分支（`b.is_ascii_digit()`）：
        - Fingerprint mode：若 `!prev_is_ident_byte(&out)` 则 push `?` 并消费连续数字+`.`（原行为）；否则走默认 push-as-is。
        - Normalize mode：**不**替换为 `?`（与 fingerprint 核心差异，研究 Pattern 1）；仅 push 该字节并 `i += 1`，让下一轮 bulk-copy 接管。
      * 空白字节分支（`b.is_ascii_whitespace()`）：两 mode 共用——折叠为单空格（与原 fingerprint 一致）。
      * 默认分支 `b => { out.push(b); i += 1; }`：两 mode 共用兜底（fingerprint mode 下处理 `-`/`/`/字母三类被新表标特殊但 fingerprint 不需特殊处理的字节）。
    - **末尾收尾**：scan_sql_bytes 末尾 `String::from_utf8(out).expect("scan_sql_bytes: invalid UTF-8")` + `trim_ascii` 与原 fingerprint lines 95-102 完全一致（PATTERNS.md §Shared Patterns / UTF-8 安全输出）。
    - **函数长度约束**：CLAUDE.md 要求函数 ≤ 40 行。`scan_sql_bytes` 主体若超长，必须拆分为私有辅助函数：候选 `fn copy_string_literal_mode(bytes, i, out, mode) -> usize`、`fn skip_line_comment(bytes, i) -> usize`、`fn skip_block_comment(bytes, i, out) -> usize`、`fn handle_word_mode(bytes, i, out, mode) -> usize`、`fn try_fold_in_list(bytes, cursor, out) -> Option<usize>`。每个辅助函数 ≤ 40 行。scan_sql_bytes 自身保持为循环 + match 分发骨架。
    - **薄包装行**：`pub fn fingerprint(sql: &str) -> String { scan_sql_bytes(sql, ScanMode::Fingerprint) }`、`pub fn normalize_template(sql: &str) -> String { scan_sql_bytes(sql, ScanMode::Normalize) }`。两者公共签名与 doc 注释完整保留。
    - Plan 02 在 src/features/mod.rs 中统一新增 `pub use sql_fingerprint::normalize_template`；本 plan 仅声明 `pub fn`，不动 mod.rs。
  </behavior>
  <action>
在 src/features/sql_fingerprint.rs 中执行三件事，**在同一个任务内完成**（D-07 强制共享：fingerprint 与 normalize_template 必须共享底层扫描循环）：

1. **新增私有 `enum ScanMode { Fingerprint, Normalize }`** 紧接 NEEDS_SPECIAL_NORM 之后定义。

2. **抽取共享私有 `fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String`**：将现 fingerprint() 主循环（lines 28-103 的函数体）整体下沉到该函数，按 §behavior 的 match arm 分发规则改造——所有 mode-specific 差异（字面量保留/替换、注释跳过、关键字大写化、IN 折叠、数字处理）均在 match arm 内通过 `match mode` 分发，外层 while-loop / NEEDS_SPECIAL_NORM 查找 / memchr 字面量跳过 / trim_ascii 收尾必须只写一次。

3. **重构 `pub fn fingerprint(sql: &str) -> String`**：保留原 doc 注释和 `#[must_use]`，函数体改为单行 `scan_sql_bytes(sql, ScanMode::Fingerprint)`。验证标准：原 9 项 fingerprint 测试零回归。

4. **新增 `pub fn normalize_template(sql: &str) -> String`**：加 `#[must_use]` 和 doc 注释，函数体为单行 `scan_sql_bytes(sql, ScanMode::Normalize)`。

5. **拆分辅助函数（如 scan_sql_bytes 超 40 行）**：抽取 copy_string_literal_mode / skip_line_comment / skip_block_comment / handle_word_mode / try_fold_in_list 等私有辅助，每个 ≤ 40 行。scan_sql_bytes 保持为骨架。

6. **新增测试**：在 `#[cfg(test)] mod tests` 中新增 7 项 normalize_template 子测试（详见 acceptance_criteria）；保留原 9 项 fingerprint 测试不变。

关键决策回顾：
- 采用 enum + match 而非泛型闭包：RESEARCH.md §Open Questions Q2 推荐，避免单态化膨胀，保持 inline 友好。
- NEEDS_SPECIAL_NORM 统一扩展表而非双表：D-08 单一事实来源；fingerprint 路径下 `-`/`/` 落入默认分支语义不变。
- 字符串字面量优先于注释分发：D-15 字面量内 `--` 不视为注释。
- 关键字双向边界检查：Pitfall 4 防止 outer_join 中的 outer 被大写化。
- IN 折叠 + 子查询回退：RESEARCH.md Q1 推荐策略。
  </action>
  <verify>
    <automated>cargo test --package sqllog2db --lib features::sql_fingerprint -- --nocapture 2>&amp;1 | tail -60; cargo clippy --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -E '^enum ScanMode' src/features/sql_fingerprint.rs` 输出 1 行（私有枚举存在）。
    - 源断言：`grep -E '^fn scan_sql_bytes' src/features/sql_fingerprint.rs` 输出 1 行（私有共享引擎存在）。
    - 源断言：`grep -E '^pub fn fingerprint' src/features/sql_fingerprint.rs` 输出 1 行（公共签名保留）。
    - 源断言：`grep -E '^pub fn normalize_template' src/features/sql_fingerprint.rs` 输出 1 行（新增公共函数）。
    - 源断言（强制共享 D-07）：`grep -c 'scan_sql_bytes(sql, ScanMode::Fingerprint)' src/features/sql_fingerprint.rs` ≥ 1（fingerprint 调用共享引擎）。
    - 源断言（强制共享 D-07）：`grep -c 'scan_sql_bytes(sql, ScanMode::Normalize)' src/features/sql_fingerprint.rs` ≥ 1（normalize_template 调用共享引擎）。
    - 源断言（薄包装）：`pub fn fingerprint` 函数体（从 `{` 到匹配 `}`）行数 ≤ 4（含大括号），即仅薄包装一行 + 大括号。
    - 源断言（薄包装）：`pub fn normalize_template` 函数体行数 ≤ 4。
    - 源断言：scan_sql_bytes 主函数体行数 ≤ 40（CLAUDE.md 函数长度约束）。
    - 源断言：每个新增私有辅助函数体行数 ≤ 40。
    - **行为断言（fingerprint 零回归——D-07 关键验收点）**：`cargo test --package sqllog2db --lib features::sql_fingerprint::tests` 中原 9 项 fingerprint 测试（test_string_literal_replaced / test_number_literal_replaced / test_decimal_literal_replaced / test_identifier_with_digit_preserved / test_whitespace_collapsed / test_escaped_quote_in_string / test_multiple_literals / test_empty_string / test_same_fingerprint_for_different_values）全部通过。
    - 行为断言（normalize_template 新增 7 项子测试在 #[cfg(test)] mod tests 内）：
      * `normalize_template("-- comment\nSELECT 1")` 返回 `"SELECT 1"`（单行注释去除，D-16；数字 1 保留不替换）。
      * `normalize_template("/* multi */ SELECT 1")` 返回 `"SELECT 1"`（多行注释替换为空格，前导空格被 trim_ascii 移除）。
      * `normalize_template("SELECT * FROM t WHERE id IN (1, 2, 3)")` 与 `normalize_template("SELECT * FROM t WHERE id IN (10, 20, 30, 40)")` 返回相同 String（D-01/D-03）。
      * `normalize_template("SELECT * FROM t WHERE name IN ('a', 'b')")` 与 `normalize_template("SELECT * FROM t WHERE name IN ('xx', 'yy', 'zz')")` 返回相同 String（D-02）。
      * `normalize_template("select * from t where id = 1")` 中 select / from / where 三个关键字大写化结果出现在返回值中（D-04）。
      * `normalize_template("SELECT a FROM outer_join_t")` 返回值保留 `outer_join_t` 原样（不大写化 `OUTER`，Pitfall 4）。
      * `normalize_template("WHERE col = '-- not a comment'")` 返回值字面量 `'-- not a comment'` 原样保留（D-15）。
      * `normalize_template("SELECT  *  FROM  t")` 返回值连续空白折叠为单空格。
    - 命令：`cargo test fingerprint` 全部通过（即 §read_first 中检查器要求："cargo test fingerprint still passes after refactor"）。
    - 命令：`cargo test features::sql_fingerprint` 全部通过（含原 9 项 fingerprint + 新增 7+ 项 normalize_template）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>enum ScanMode + fn scan_sql_bytes 共享引擎完成；pub fn fingerprint 重构为薄包装且原 9 项测试零回归；pub fn normalize_template 新增为薄包装且 7 项行为子测试全绿；D-07/D-08 强制共享语义满足（grep 验证两个公共函数均调用 scan_sql_bytes）；CLAUDE.md ≤ 40 行约束满足；clippy 零 warning。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 日志文件 → 解析器 | 外部 SQL 日志文本，可能含恶意构造的注释/字面量边界 |
| 解析器 → scan_sql_bytes | 已通过 dm-database-parser-sqllog 解析的 sql 字符串切片，UTF-8 已保证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | scan_sql_bytes 字节扫描（normalize 路径） | mitigate | 字符串字面量优先分发（D-15），注释检测只能发生在字面量之外；测试 `'-- not a comment'` 反例覆盖 |
| T-12-02 | Denial of Service | 未闭合 `/* */` 注释 | mitigate | 未找到 `*/` 时 i = len，主循环立即结束，无死循环风险；测试需含未闭合注释样本 |
| T-12-03 | Information Disclosure | 输出 buffer UTF-8 校验 | mitigate | from_utf8 expect 论证：所有插入字节均为 ASCII（≤0x7F），不破坏多字节 UTF-8 序列；与原 fingerprint() 论证一致，重构后由 scan_sql_bytes 统一论证 |
| T-12-04 | Tampering | fingerprint 行为回归 | mitigate | D-07 强制共享重构后，原 9 项 fingerprint 测试必须零回归；Task 2 acceptance_criteria 显式列举所有原测试名作验收点 |
| T-12-SC | Tampering | 无 npm/pip/cargo install | accept | 本阶段不引入任何新依赖（RESEARCH.md 已确认），跳过包合法性审计 |
</threat_model>

<verification>
- `cargo test fingerprint` 全部 9 项通过（D-07 重构后零回归——检查器明确要求的验收点）。
- `cargo test features::sql_fingerprint` 整体通过（含原 9 项 fingerprint + normalize_template 至少 7 项新增）。
- `cargo clippy --all-targets -- -D warnings` 零 warning。
- `cargo fmt` 已执行，无格式差异。
- 源码 grep 验证：两个公共函数均通过 `scan_sql_bytes(sql, ScanMode::...)` 调用共享引擎，D-07/D-08 强制共享语义满足。
</verification>

<success_criteria>
- enum ScanMode { Fingerprint, Normalize } 私有定义存在。
- 私有 fn scan_sql_bytes(sql: &str, mode: ScanMode) -> String 作为 fingerprint 与 normalize_template 的共享底层扫描引擎存在（D-07/D-08）。
- pub fn fingerprint 公共签名不变，函数体重构为 `scan_sql_bytes(sql, ScanMode::Fingerprint)` 薄包装，行为零回归（原 9 项测试全绿）。
- pub fn normalize_template 已声明并实现为 `scan_sql_bytes(sql, ScanMode::Normalize)` 薄包装，覆盖 D-01 至 D-16 全部决策。
- 7 项 normalize_template 行为子测试全部通过（注释去除 / IN 折叠数字 / IN 折叠字符串 / 关键字大写 / 字面量内注释保护 / 空白折叠 / 关键字边界 outer_join_t）。
- 共享扫描结构通过 NEEDS_SPECIAL_NORM 字节表 + enum ScanMode 参数化实现，单一事实来源（D-08）。
- 所有新增函数体 ≤ 40 行（遵守 CLAUDE.md 全局约束）。
</success_criteria>

<output>
Create `.planning/phases/12-sql/12-01-SUMMARY.md` when done
</output>
