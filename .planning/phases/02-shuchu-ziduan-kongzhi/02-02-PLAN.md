---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/exporter/csv.rs
autonomous: true
requirements:
  - FIELD-01
must_haves:
  truths:
    - "CsvExporter 有 ordered_indices: Vec<usize> 字段，默认值 [0..14]"
    - "build_header() 按 ordered_indices 顺序写列名（不再用 field_mask.is_active(i) 过滤原始顺序）"
    - "write_record_preparsed() 投影路径按 ordered_indices 用 match idx 分发写入"
    - "write_record() 兼容路径同步传入 ordered_indices"
    - "全量掩码快速路径（FieldMask::ALL）保留不变"
    - "normalized_sql（idx=14）在 normalize=false 时被跳过，与现有逻辑一致（D-03）"
    - "test_csv_field_order 和 test_csv_field_order_partial 通过"
  artifacts:
    - path: "src/exporter/csv.rs"
      provides: "有序字段 CSV 导出"
      contains: "ordered_indices"
  key_links:
    - from: "CsvExporter::build_header()"
      to: "self.ordered_indices"
      via: "for &idx in &self.ordered_indices"
      pattern: "for &idx in &self\\.ordered_indices"
    - from: "write_record_preparsed() 投影路径"
      to: "ordered_indices"
      via: "for &idx in ordered_indices { match idx {"
      pattern: "for &idx in ordered_indices"
---

<objective>
给 CsvExporter 添加 ordered_indices: Vec<usize> 字段，并将 build_header() 和 write_record_preparsed() 的投影路径改为按有序索引写出。

Purpose: 让 CSV 导出的列顺序完全跟随用户在 config.toml 中的 fields 配置顺序（D-01）。
Output: src/exporter/csv.rs 修改完毕，带集成测试，全量路径性能不回退。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-CONTEXT.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-01-SUMMARY.md

<interfaces>
<!-- 从 src/exporter/csv.rs 提取的现有接口，执行时直接使用 -->

CsvExporter 结构体（当前字段，需添加 ordered_indices）：
```rust
pub struct CsvExporter {
    path: PathBuf,
    overwrite: bool,
    append: bool,
    writer: Option<BufWriter<File>>,
    stats: ExportStats,
    itoa_buf: itoa::Buffer,
    line_buf: Vec<u8>,
    pub(crate) normalize: bool,
    pub(crate) field_mask: crate::features::FieldMask,
    // 需新增：
    // pub(crate) ordered_indices: Vec<usize>,
}
```

CsvExporter::new() 当前返回（需添加 ordered_indices 初始化）：
```rust
pub fn new(path: impl AsRef<Path>) -> Self {
    Self {
        // ...
        normalize: true,
        field_mask: crate::features::FieldMask::ALL,
        // 新增: ordered_indices: (0..crate::features::FIELD_NAMES.len()).collect(),
    }
}
```

write_record_preparsed() 签名（需新增 ordered_indices 参数）：
```rust
fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    writer: &mut BufWriter<File>,
    path: &Path,
    normalize: bool,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    // 需新增: ordered_indices: &[usize],
) -> Result<()>
```

write_record() 当前调用 write_record_preparsed（需同步添加 ordered_indices 参数）：
```rust
fn write_record(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    writer: &mut BufWriter<File>,
    path: &Path,
    normalize: bool,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    // 需新增: ordered_indices: &[usize],
) -> Result<()>
```

build_header() 当前逻辑（需改为按 ordered_indices）：
```rust
fn build_header(&self) -> Vec<u8> {
    use crate::features::FIELD_NAMES;
    let mut header = Vec::with_capacity(128);
    let mut first = true;
    for (i, name) in FIELD_NAMES.iter().enumerate() {
        if i == 14 && !self.normalize { continue; }
        if self.field_mask.is_active(i) {
            if !first { header.push(b','); }
            first = false;
            header.extend_from_slice(name.as_bytes());
        }
    }
    header.push(b'\n');
    header
}
```

全量掩码快速路径入口（保留不变）：
```rust
if field_mask == crate::features::FieldMask::ALL {
    // 全量写入逻辑，不经过 ordered_indices...
} else {
    // 投影路径，改为 match idx 分发
}
```

现有测试辅助函数（write_test_log），约在 lines 448-460，测试中复用：
```rust
// fn write_test_log(exporter, log_line) -> Result<String>
// 写入单条记录并返回 CSV 文件内容
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 ordered_indices 字段并修改 build_header()</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（完整文件：结构体定义、new()、build_header()、测试模块）
    - src/features/mod.rs（FIELD_NAMES 常量和 ordered_field_indices() 方法，确认返回值格式）
  </read_first>
  <behavior>
    - test_csv_header_field_order：ordered_indices=[10,4] (sql,username)，build_header() 输出 "sql,username\n"
    - test_csv_header_full_order：ordered_indices 为默认全量 [0..14]（normalize=true），header 包含全部 15 列，顺序与 FIELD_NAMES 一致
    - test_csv_header_no_normalized_sql：ordered_indices 包含 idx=14 但 normalize=false，header 中不含 "normalized_sql"
  </behavior>
  <action>
1. 在 CsvExporter 结构体中，在 `pub(crate) field_mask` 行之后添加：
   ```rust
   pub(crate) ordered_indices: Vec<usize>,
   ```

2. 在 CsvExporter::new() 中，在 `field_mask: crate::features::FieldMask::ALL,` 之后添加：
   ```rust
   ordered_indices: (0..crate::features::FIELD_NAMES.len()).collect(),
   ```

3. 将 build_header() 方法完整替换为：
   ```rust
   fn build_header(&self) -> Vec<u8> {
       use crate::features::FIELD_NAMES;
       let mut header = Vec::with_capacity(128);
       let mut first = true;
       for &idx in &self.ordered_indices {
           // idx 14 (normalized_sql) 在 normalize=false 时跳过（与全量路径行为一致）
           if idx == 14 && !self.normalize {
               continue;
           }
           if !first {
               header.push(b',');
           }
           first = false;
           header.extend_from_slice(FIELD_NAMES[idx].as_bytes());
       }
       header.push(b'\n');
       header
   }
   ```

4. 在 tests 模块中追加（直接在现有 CsvExporter 测试之后）：
   ```rust
   #[test]
   fn test_csv_header_field_order() {
       use crate::features::FieldMask;
       let dir = tempfile::TempDir::new().unwrap();
       let path = dir.path().join("out.csv");
       let mut exporter = CsvExporter::new(&path);
       exporter.field_mask = FieldMask::from_names(&["sql".to_string(), "username".to_string()]).unwrap();
       exporter.ordered_indices = vec![10, 4]; // sql=10, username=4
       exporter.initialize().unwrap();
       let content = std::fs::read_to_string(&path).unwrap();
       let header_line = content.lines().next().unwrap();
       assert_eq!(header_line, "sql,username");
   }

   #[test]
   fn test_csv_header_full_order() {
       use crate::features::FIELD_NAMES;
       let dir = tempfile::TempDir::new().unwrap();
       let path = dir.path().join("out.csv");
       let mut exporter = CsvExporter::new(&path);
       exporter.normalize = true;
       // ordered_indices 默认全量 [0..14]，无需修改
       exporter.initialize().unwrap();
       let content = std::fs::read_to_string(&path).unwrap();
       let header_line = content.lines().next().unwrap();
       let expected: Vec<&str> = FIELD_NAMES.to_vec();
       assert_eq!(header_line, expected.join(","));
   }

   #[test]
   fn test_csv_header_no_normalized_sql_when_normalize_false() {
       let dir = tempfile::TempDir::new().unwrap();
       let path = dir.path().join("out.csv");
       let mut exporter = CsvExporter::new(&path);
       exporter.normalize = false;
       // ordered_indices 默认全量，但 idx=14 应被跳过
       exporter.initialize().unwrap();
       let content = std::fs::read_to_string(&path).unwrap();
       let header_line = content.lines().next().unwrap();
       assert!(!header_line.contains("normalized_sql"));
       assert!(header_line.contains("sql")); // idx=10 的 "sql" 字段仍存在
   }
   ```
  </action>
  <verify>
    <automated>cargo test -q test_csv_header 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/csv.rs 包含 `pub(crate) ordered_indices: Vec<usize>` 字符串
    - src/exporter/csv.rs 包含 `for &idx in &self.ordered_indices` 字符串（build_header 新实现）
    - src/exporter/csv.rs 不再包含 `self.field_mask.is_active(i)` 字符串（build_header 旧逻辑已删除）
    - cargo test -q test_csv_header 全部通过（3 个测试绿）
    - cargo clippy --all-targets -- -D warnings 零警告
  </acceptance_criteria>
  <done>ordered_indices 字段已添加，build_header() 已改为按有序索引写列名，3 个 header 测试全部通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修改 write_record_preparsed() 投影路径</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（重点：write_record_preparsed() 函数签名和 else 分支投影路径，约 lines 76-243；write_record() 兼容路径约 lines 245-271；export_one_preparsed() 调用点）
  </read_first>
  <behavior>
    - test_csv_field_order：ordered_indices=[10,4]（sql,username），写入一条记录后 CSV 数据行只含 sql 和 username 两列，顺序正确
    - test_csv_field_order_partial：ordered_indices=[0,10,13]（ts,sql,exec_id），数据行按此顺序输出
    - test_csv_field_order_normalized_sql_skipped：ordered_indices 含 idx=14，但 normalize=false，数据行中不含 normalized_sql 值
  </behavior>
  <action>
1. 在 write_record_preparsed() 函数签名中，在 `field_mask: crate::features::FieldMask,` 参数之后新增：
   ```rust
   ordered_indices: &[usize],
   ```

2. 将 write_record_preparsed() 中 `} else {` 投影路径块（当前约 lines 142-233，用 `if field_mask.is_active(N)` 实现）完整替换为：
   ```rust
   } else {
       // 投影路径：按 ordered_indices 指定的字段顺序写入
       let mut need_sep = false;

       macro_rules! w_sep {
           () => {
               if need_sep {
                   line_buf.push(b',');
               }
               need_sep = true;
           };
       }

       let has_metrics = pm.exec_id != 0 || pm.exectime > 0.0;
       for &idx in ordered_indices {
           match idx {
               0 => {
                   w_sep!();
                   line_buf.extend_from_slice(sqllog.ts.as_ref().as_bytes());
               }
               1 => {
                   w_sep!();
                   line_buf.extend_from_slice(itoa_buf.format(meta.ep).as_bytes());
               }
               2 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.sess_id.as_ref().as_bytes());
               }
               3 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.thrd_id.as_ref().as_bytes());
               }
               4 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.username.as_ref().as_bytes());
               }
               5 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.trxid.as_ref().as_bytes());
               }
               6 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.statement.as_ref().as_bytes());
               }
               7 => {
                   w_sep!();
                   line_buf.extend_from_slice(meta.appname.as_ref().as_bytes());
               }
               8 => {
                   w_sep!();
                   line_buf.extend_from_slice(
                       strip_ip_prefix(meta.client_ip.as_ref()).as_bytes(),
                   );
               }
               9 => {
                   w_sep!();
                   if let Some(tag) = &sqllog.tag {
                       line_buf.extend_from_slice(tag.as_ref().as_bytes());
                   }
               }
               10 => {
                   w_sep!();
                   line_buf.push(b'"');
                   write_csv_escaped(line_buf, pm.sql.as_bytes());
                   line_buf.push(b'"');
               }
               11 => {
                   w_sep!();
                   if has_metrics {
                       line_buf.extend_from_slice(
                           itoa_buf.format(f32_ms_to_i64(pm.exectime)).as_bytes(),
                       );
                   }
               }
               12 => {
                   w_sep!();
                   if has_metrics {
                       line_buf.extend_from_slice(
                           itoa_buf.format(i64::from(pm.rowcount)).as_bytes(),
                       );
                   }
               }
               13 => {
                   w_sep!();
                   if has_metrics {
                       line_buf.extend_from_slice(itoa_buf.format(pm.exec_id).as_bytes());
                   }
               }
               14 => {
                   // D-03：normalize=false 时跳过 normalized_sql，与 header 逻辑一致
                   if normalize {
                       w_sep!();
                       if let Some(ns) = normalized_sql {
                           line_buf.push(b'"');
                           write_csv_escaped(line_buf, ns.as_bytes());
                           line_buf.push(b'"');
                       }
                   }
               }
               _ => {}
           }
       }
       // 消费 need_sep，避免"最后一次赋值从未被读取"的编译警告
       let _ = need_sep;
   }
   ```

3. 在 write_record() 函数签名中同样添加 `ordered_indices: &[usize]` 参数，并将内部对 write_record_preparsed() 的调用添加 `ordered_indices` 参数传入。

4. 在所有调用 write_record_preparsed() 和 write_record() 的地方（export_one_preparsed、export、export_one_normalized 等 Exporter impl 方法）中，传入 `&self.ordered_indices`。

5. 在 tests 模块中追加字段顺序数据行测试（使用现有 write_test_log 辅助函数，或自行写入一条记录后读取 CSV 内容验证数据行）：
   ```rust
   #[test]
   fn test_csv_field_order() {
       // 验证数据行按 ordered_indices=[10,4] 顺序输出（sql, username 两列）
       use crate::features::FieldMask;
       use dm_database_parser_sqllog::LogParser;

       let dir = tempfile::TempDir::new().unwrap();
       let log = dir.path().join("t.log");
       std::fs::write(
           &log,
           "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:testuser trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT 1. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n",
       )
       .unwrap();

       let out = dir.path().join("out.csv");
       let mut exporter = CsvExporter::new(&out);
       exporter.normalize = false;
       exporter.field_mask = FieldMask::from_names(&["sql".to_string(), "username".to_string()]).unwrap();
       exporter.ordered_indices = vec![10, 4]; // sql=10, username=4
       exporter.initialize().unwrap();

       let parser = LogParser::from_path(log.to_str().unwrap()).unwrap();
       for record in parser.iter().flatten() {
           exporter.export(&record).unwrap();
       }
       exporter.finalize().unwrap();

       let content = std::fs::read_to_string(&out).unwrap();
       let mut lines = content.lines();
       let header = lines.next().unwrap();
       let data = lines.next().unwrap();

       assert_eq!(header, "sql,username");
       // 数据行第一列是 sql 内容（含引号），第二列是 username=testuser
       assert!(data.ends_with(",testuser"), "data line: {data}");
   }

   #[test]
   fn test_csv_field_order_normalized_sql_skipped_when_normalize_false() {
       use crate::features::FieldMask;
       use dm_database_parser_sqllog::LogParser;

       let dir = tempfile::TempDir::new().unwrap();
       let log = dir.path().join("t.log");
       std::fs::write(
           &log,
           "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT 1. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n",
       )
       .unwrap();

       let out = dir.path().join("out.csv");
       let mut exporter = CsvExporter::new(&out);
       exporter.normalize = false;
       // ordered_indices 含 14（normalized_sql），但 normalize=false 时应跳过（D-03）
       exporter.ordered_indices = vec![10, 14]; // sql, normalized_sql（后者被跳过）
       exporter.field_mask = FieldMask::from_names(&["sql".to_string()]).unwrap();
       exporter.initialize().unwrap();

       let parser = LogParser::from_path(log.to_str().unwrap()).unwrap();
       for record in parser.iter().flatten() {
           exporter.export(&record).unwrap();
       }
       exporter.finalize().unwrap();

       let content = std::fs::read_to_string(&out).unwrap();
       let header = content.lines().next().unwrap();
       // normalize=false 时 normalized_sql 不出现在 header 或数据中
       assert!(!header.contains("normalized_sql"), "header: {header}");
   }
   ```
  </action>
  <verify>
    <automated>cargo test -q test_csv_field_order 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/csv.rs 包含 `for &idx in ordered_indices` 字符串（投影路径新实现）
    - src/exporter/csv.rs 不再包含 `field_mask.is_active(0)` 字符串（旧投影路径已删除）
    - src/exporter/csv.rs 中 write_record_preparsed 函数签名包含 `ordered_indices: &[usize]`
    - cargo test -q test_csv_field_order 全部通过（2 个测试绿）
    - cargo test -q 2>&1 | tail -5 全量测试套件通过（无 FAILED）
    - cargo clippy --all-targets -- -D warnings 零警告
  </acceptance_criteria>
  <done>投影路径已改为按 ordered_indices 分发，2 个数据行测试全部通过，全量测试套件绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ordered_indices→CSV | ordered_indices 在 exporter 初始化前赋值，值范围 [0,14]，来自 ordered_field_indices() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | write_record_preparsed idx 分发 | accept | ordered_indices 索引值 [0,14] 由 FIELD_NAMES.iter().position() 产生，`_ => {}` arm 处理越界安全退化 |
</threat_model>

<verification>
cargo test -q 2>&1 | tail -10
cargo clippy --all-targets -- -D warnings
</verification>

<success_criteria>
- CsvExporter 有 ordered_indices 字段，默认 [0..14]
- build_header() 按 ordered_indices 遍历，test_csv_header_* 3 个测试通过
- write_record_preparsed() 投影路径改为 match idx 分发，test_csv_field_order* 2 个测试通过
- normalize=false 时 idx=14 被跳过（D-03）
- 全量掩码快速路径（FieldMask::ALL 分支）代码未动，性能不回退
</success_criteria>

<output>
完成后创建 .planning/phases/02-shuchu-ziduan-kongzhi/02-02-SUMMARY.md
</output>
