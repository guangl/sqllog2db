---
phase: 03-profiling-benchmarking
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - benches/baselines/csv_export
  - benches/baselines/sqlite_export
  - benches/BENCHMARKS.md
  - docs/flamegraphs/.gitkeep
  - docs/flamegraphs/csv_export_real.svg
autonomous: false
requirements: [PERF-01]
tags: [benchmark, baseline, flamegraph, documentation]

must_haves:
  truths:
    - "benches/baselines/csv_export/ 下存在以 v1.0 命名的 baseline JSON（Phase 4/5 可对比）"
    - "benches/baselines/sqlite_export/ 下存在以 v1.0 命名的 baseline JSON"
    - "benches/BENCHMARKS.md 文档头部版本更新为 v1.0，commit date 更新为 2026-04-26"
    - "benches/BENCHMARKS.md 新增 ### CSV real-file export 与 ### SQLite real-file export 两节，包含实测时间和换算吞吐"
    - "benches/BENCHMARKS.md 中 opt-level 描述更新为 opt-level=3（与当前 Cargo.toml 一致），不再写 opt-level=z"
    - "docs/flamegraphs/csv_export_real.svg 存在且可在浏览器打开，热路径函数名清晰可读（非 unknown）"
  artifacts:
    - path: "benches/baselines/csv_export/v1.0/"
      provides: "criterion CSV v1.0 baseline JSON 数据集"
      contains: "estimates.json"
    - path: "benches/baselines/sqlite_export/v1.0/"
      provides: "criterion SQLite v1.0 baseline JSON 数据集"
      contains: "estimates.json"
    - path: "benches/BENCHMARKS.md"
      provides: "v1.0 性能基准报告（含 synthetic + real-file 数值，Phase 4/5 优化目标参照）"
      contains: "v1.0"
    - path: "docs/flamegraphs/csv_export_real.svg"
      provides: "CSV real-file 热路径火焰图（人工验证有符号）"
      min_size_bytes: 50000
  key_links:
    - from: "Phase 4/5 优化"
      to: "benches/baselines/csv_export/v1.0/"
      via: "criterion --baseline v1.0"
      pattern: "CRITERION_HOME=benches/baselines cargo bench .* --baseline v1.0"
    - from: "BENCHMARKS.md"
      to: "v1.0 实际数值"
      via: "Markdown 表格"
      pattern: "1\\.5[0-9]+ M/s|csv_export_real"
    - from: "docs/flamegraphs/csv_export_real.svg"
      to: "Cargo.toml [profile.flamegraph]"
      via: "cargo flamegraph --profile flamegraph"
      pattern: "--profile flamegraph"
---

<objective>
采集 v1.0 完整 baseline（synthetic + real-file，CSV + SQLite），生成 flamegraph SVG，并将所有数值与采集方法写入 BENCHMARKS.md，构成 Phase 4/5 优化的决策基准。

Purpose: 没有可复现的 v1.0 基准，Phase 4/5 的"提升 ≥10%"无从验证。BENCHMARKS.md 当前记录的是 v0.5.0（2026-04-04，opt-level=z）的数据，与当前 v0.10.7（opt-level=3）已不可直接比较。flamegraph 是 PERF-01 中"指出热路径占比最高函数调用链"的唯一可视化产出。

Output:
- benches/baselines/csv_export/v1.0/ 与 benches/baselines/sqlite_export/v1.0/ 完整 JSON 数据集
- docs/flamegraphs/csv_export_real.svg 火焰图（带符号）
- 更新后的 benches/BENCHMARKS.md，标注 v1.0 数值 + real-file 章节 + 反映当前 release profile
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-profiling-benchmarking/03-RESEARCH.md
@.planning/phases/03-profiling-benchmarking/03-PATTERNS.md
@.planning/phases/03-profiling-benchmarking/03-01-SUMMARY.md
@.planning/phases/03-profiling-benchmarking/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Wave 1 已完成的产物（前置条件） -->

```toml
# Cargo.toml — 由 Plan 01 添加
[profile.flamegraph]
inherits = "release"
debug = true
strip = "none"
```

```rust
// benches/bench_csv.rs 与 benches/bench_sqlite.rs 已注册的 benchmark 函数
criterion_group!(benches, bench_csv_export, bench_csv_real_file);
criterion_group!(benches, bench_sqlite_export, bench_sqlite_real_file);
```

<!-- 现有 benches/BENCHMARKS.md 结构 -->
- 文档头部：branch + commit date + 版本 + 硬件 + opt-level
- "How to reproduce" 章节
- "How to compare against this baseline" 章节
- "Baseline numbers" 章节，含 ### CSV/JSONL/SQLite/Filter 四节
- "Performance rules" 章节（硬性时间上限）

<!-- 注意：当前 BENCHMARKS.md 仍含 JSONL 章节，但项目已无 bench_jsonl benchmark（Cargo.toml 中只有 bench_csv/bench_sqlite/bench_filters 三个 [[bench]]）。更新时需删除 JSONL 章节与 Performance rules 中的 jsonl_export 行。 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 采集 v1.0 baseline JSON（CSV + SQLite，synthetic + real-file）</name>
  <files>benches/baselines/csv_export, benches/baselines/sqlite_export</files>
  <read_first>
    - benches/bench_csv.rs （确认 Plan 01 已添加 bench_csv_real_file）
    - benches/bench_sqlite.rs （确认 Plan 02 已添加 bench_sqlite_real_file）
    - benches/BENCHMARKS.md （查看现有 baseline 数值用于交叉核对）
    - .planning/phases/03-profiling-benchmarking/03-RESEARCH.md （Pattern 1 章节，确认 CRITERION_HOME 用法）
  </read_first>
  <action>
    依次执行以下命令采集 v1.0 baseline。每条命令必须显式设置 `CRITERION_HOME=benches/baselines`，否则会写到 `target/criterion/` 而非 baseline 目录。

**Step 1：CSV 完整采集（synthetic + real-file）**
```bash
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline v1.0
```
预期耗时：synthetic 部分 ~1 分钟；real-file 部分 ~10-15 分钟（10 次 sample × ~60s/iter，含 538MB 文件解析）。

**Step 2：SQLite 完整采集（synthetic + real-file）**
```bash
CRITERION_HOME=benches/baselines cargo bench --bench bench_sqlite -- --save-baseline v1.0
```
预期耗时：synthetic 部分 ~1 分钟；real-file 部分 ~15-20 分钟（5 次 sample × ~120s/iter）。

**Step 3：验证 baseline 落盘**
```bash
ls -la benches/baselines/csv_export/v1.0/ 2>&1
ls -la benches/baselines/sqlite_export/v1.0/ 2>&1
ls -la benches/baselines/csv_export_real/v1.0/ 2>&1
ls -la benches/baselines/sqlite_export_real/v1.0/ 2>&1
```
每个目录下应至少包含 `estimates.json`、`sample.json` 等 criterion 元数据。

**Step 4：抓取关键吞吐数值用于 BENCHMARKS.md 更新**

执行后，记录以下数值（从命令行输出 `time: [low median high]` 和 `thrpt: [...]` 行抓取，必要时也可从 estimates.json 读取 `mean.point_estimate`）：
- `csv_export/1000` 中位时间
- `csv_export/10000` 中位时间
- `csv_export/50000` 中位时间
- `csv_export_real/real_file` 中位时间（含真实文件总耗时，秒级）
- `sqlite_export/1000` 中位时间
- `sqlite_export/10000` 中位时间
- `sqlite_export/50000` 中位时间
- `sqlite_export_real/real_file` 中位时间

将这些数值写入临时备忘（如 `target/v1.0-baseline-numbers.txt`），供 Task 3 使用。

**注意事项：**
- 在执行 benchmark 期间避免运行其他 CPU/IO 密集任务，确保数值稳定
- 若 sqllogs/ 不存在，real-file group 会自动 skip（Plan 01/02 已实现），synthetic baseline 仍会落盘；此情况下 Task 3 中 real-file 章节标注 "skipped (sqllogs/ not present)"
- 不要修改任何 .rs 源文件（仅运行 benchmark）
- 若发现某次采样异常（criterion 报告 outliers > 30%），允许重跑该单一 benchmark：`CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- csv_export/10000 --save-baseline v1.0`
  </action>
  <verify>
    <automated>ls benches/baselines/csv_export/v1.0/estimates.json benches/baselines/sqlite_export/v1.0/estimates.json 2>&1 | grep -c "estimates.json" | grep -q "^2$"</automated>
  </verify>
  <acceptance_criteria>
    - `ls benches/baselines/csv_export/v1.0/estimates.json` 返回 0
    - `ls benches/baselines/sqlite_export/v1.0/estimates.json` 返回 0
    - `find benches/baselines -name "v1.0" -type d | wc -l` 至少为 2（synthetic 两个），sqllogs/ 存在时为 4（含 real-file 两个）
    - `find benches/baselines -path "*/v1.0/sample.json" | wc -l` 至少为 2
    - 命令行输出未包含 `error:` 或 `panicked at`
    - 关键吞吐数值已记录（写入 target/v1.0-baseline-numbers.txt 或终端输出可读取）
  </acceptance_criteria>
  <done>v1.0 baseline JSON 已落盘到 benches/baselines/{csv,sqlite}_export/v1.0/；real-file baseline 在 sqllogs/ 存在时也已落盘；关键吞吐数值已被记录用于后续文档更新。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 生成 CSV real-file flamegraph 并人工核验符号可读性</name>
  <files>docs/flamegraphs/csv_export_real.svg, docs/flamegraphs/.gitkeep</files>
  <read_first>
    - Cargo.toml （确认 Plan 01 已添加 [profile.flamegraph]，否则 flamegraph 无符号）
    - .planning/phases/03-profiling-benchmarking/03-RESEARCH.md （Pitfall 2 + 3 章节，了解 strip 与 sudo 要求）
    - .planning/phases/03-profiling-benchmarking/03-VALIDATION.md （Manual-Only Verifications 章节）
  </read_first>
  <action>
    Claude 自动执行下列命令，然后暂停等待用户人工核验 SVG 符号可读性。

**Step 1：创建 docs/flamegraphs/ 目录与 .gitkeep（保证目录被 git 追踪）**
```bash
mkdir -p docs/flamegraphs && touch docs/flamegraphs/.gitkeep
```

**Step 2：用 cargo-flamegraph + dtrace 采集 CSV real-file 火焰图（首选路径，macOS 需 sudo）**
```bash
sudo cargo flamegraph --profile flamegraph --bench bench_csv \
  --output docs/flamegraphs/csv_export_real.svg \
  -- --bench --profile-time 15 csv_export_real/real_file
```
关键参数说明（必须严格按此组合，缺一会导致符号丢失或采样错误）：
- `--profile flamegraph` — 使用 Plan 01 在 Cargo.toml 添加的 profile（`strip="none"` + `debug=true`），不能用 `--release`
- `--bench bench_csv` — 限定到 CSV bench（避免 sqlite 干扰）
- `--profile-time 15` — criterion profiler-friendly 模式，跳过统计逻辑直接采样 15 秒
- `csv_export_real/real_file` — 限定 benchmark filter，只采样 real-file 路径
- `--output docs/flamegraphs/csv_export_real.svg` — 输出路径

**Step 3：若 Step 2 失败（macOS SIP 限制 / 用户拒绝 sudo），回退到 samply（无需 sudo）**
```bash
samply setup -y  # 若提示未 codesign
samply record --save-only --output docs/flamegraphs/csv_export_real.json -- \
  cargo bench --profile flamegraph --bench bench_csv \
  -- --profile-time 15 csv_export_real/real_file
```
然后在终端打印提示：
```
samply JSON 已生成。运行以下命令在浏览器查看：
  samply load docs/flamegraphs/csv_export_real.json
请在浏览器中按 how-to-verify 步骤检查符号可读性。
```

**Step 4：基本完整性验证（自动）**
```bash
ls -la docs/flamegraphs/
wc -c docs/flamegraphs/csv_export_real.svg 2>/dev/null || wc -c docs/flamegraphs/csv_export_real.json
```
SVG 应 > 50KB；samply JSON 通常 > 100KB。

**关键约束：**
- 必须使用 `--profile flamegraph`（Plan 01 产出），不能用 `release` 否则符号被 strip
- 不要修改 Cargo.toml 或 bench 文件（仅采样）
- sqllogs/ 必须存在（否则 real-file bench skip，无法采集真实热路径）；若缺失，提示用户准备日志后再执行
- SVG 输出路径固定为 `docs/flamegraphs/csv_export_real.svg`，Task 3 BENCHMARKS.md 引用此路径

**完成上述步骤后，向用户展示以下人工核验指引：**

1. 检查 SVG 文件存在且大小 > 50KB：
   ```bash
   ls -la docs/flamegraphs/csv_export_real.svg && wc -c docs/flamegraphs/csv_export_real.svg
   ```
2. 用浏览器打开 SVG（macOS）：
   ```bash
   open docs/flamegraphs/csv_export_real.svg
   ```
3. 视觉检查（关键，无法自动化）：
   - 火焰图顶部可看到 `cargo bench` / `criterion` / `handle_run` 等帧
   - 中间可看到 sqllog2db 的函数名（例如 `parse_line`、`write_csv_record`、`SqllogParser`、`exporter::csv` 等）
   - **不能** 看到大量 `[unknown]` 或 `??` 帧（若大量出现则 strip 配置未生效，需检查 Cargo.toml `[profile.flamegraph]` 的 `strip="none"`）
   - 能识别出占比最高的 1-2 个函数（解析层 / 格式化层 / I/O 层）
4. 在 resume-signal 中报告占比最高的 top 3 函数名（用于 Task 3 写入 BENCHMARKS.md 的"Hot-path observation"小节）
5. 若使用 samply 回退路径，运行 `samply load docs/flamegraphs/csv_export_real.json` 后在浏览器中按上述 3 项检查
  </action>
  <verify>
    <automated>ls docs/flamegraphs/csv_export_real.svg 2>/dev/null && [ "$(wc -c < docs/flamegraphs/csv_export_real.svg)" -gt 50000 ] || (ls docs/flamegraphs/csv_export_real.json 2>/dev/null && [ "$(wc -c < docs/flamegraphs/csv_export_real.json)" -gt 50000 ])</automated>
    <manual>用户在浏览器打开 SVG/samply 视图，确认函数符号可读（非 unknown），并在 resume-signal 中报告 top 3 热路径函数名</manual>
  </verify>
  <acceptance_criteria>
    - `ls docs/flamegraphs/csv_export_real.svg` 返回 0（或 .json + 用户确认 samply 路径已用）
    - `wc -c docs/flamegraphs/csv_export_real.svg`（或 .json）输出 > 50000
    - `ls docs/flamegraphs/.gitkeep` 返回 0
    - 用户确认浏览器中可见 sqllog2db 内部函数名（非全 unknown）
    - 用户在 resume-signal 中报告 top 3 热路径函数名（用于下一任务）
  </acceptance_criteria>
  <done>docs/flamegraphs/csv_export_real.svg（或 .json）已生成且 > 50KB，用户已人工确认函数符号可读，并提供 top 3 热路径函数名供 Task 3 写入 BENCHMARKS.md。</done>
  <resume-signal>输入 "approved: top3=fn_a,fn_b,fn_c"（其中 fn_x 是火焰图中观察到的占比最高的函数名）；或 "issues: <描述>" 报告问题</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 更新 benches/BENCHMARKS.md 为 v1.0 基准报告</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md （完整文件，了解现有结构，作为骨架）
    - Cargo.toml （确认当前版本为 0.10.7，[profile.release] 是 opt-level=3，[[bench]] 只有 csv/sqlite/filters 三项）
    - benches/baselines/csv_export/v1.0/estimates.json （Task 1 产出，读取 mean.point_estimate 用于交叉核对吞吐数值）
    - benches/baselines/sqlite_export/v1.0/estimates.json （同上）
    - target/v1.0-baseline-numbers.txt （Task 1 抓取的关键数值；若不存在则改用 estimates.json）
    - .planning/phases/03-profiling-benchmarking/03-PATTERNS.md （BENCHMARKS.md 章节，确认更新规则）
  </read_first>
  <action>
    重写 `benches/BENCHMARKS.md` 内容，遵循以下精确要求：

**1. 文档头部** — 替换为：
```markdown
# Performance Baselines

Recorded on branch `main`, commit date 2026-04-26 (v1.0 / package version 0.10.7).
Machine: Apple Silicon (Darwin 25.4.0), release build (`opt-level=3`, LTO=fat, strip=symbols, panic=abort).
Synthetic log lines ≈ 170 bytes/record (realistic DaMeng SQL log format).
Real-file inputs: `sqllogs/` 下 269MB × 2 个真实达梦日志文件（合计 ~538MB，约 800 万条记录量级）。
CSV synthetic output goes to `/dev/null` (measures parse + serialization, no disk I/O).
SQLite synthetic output goes to a real file (`target/bench_sqlite/bench.db`) with `JOURNAL_MODE=OFF SYNCHRONOUS=OFF`.
Real-file benchmarks 使用独立 `target/bench_{csv,sqlite}_real/` 目录，CI 缺 `sqllogs/` 时自动 skip。
```

**2. "How to reproduce" 章节** — 替换为（注意删除 jsonl 行，因 Cargo.toml 已无 bench_jsonl）：
```markdown
## How to reproduce

```bash
# Synthetic + real-file（real-file 在 sqllogs/ 缺失时自动 skip）
cargo bench --bench bench_csv
cargo bench --bench bench_sqlite
cargo bench --bench bench_filters

# 全套
cargo bench
```
```

**3. "How to compare against this baseline" 章节** — 替换为：
```markdown
## How to compare against this baseline

baseline JSON 数据存档在 `benches/baselines/`，criterion 通过 `CRITERION_HOME` 环境变量定位。

```bash
# 对比当前修改与 v1.0 baseline
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline v1.0
CRITERION_HOME=benches/baselines cargo bench --bench bench_sqlite -- --baseline v1.0

# 保存新的 named baseline（例如 Phase 4 优化后）
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --save-baseline phase4
```

criterion 输出会标注 "Performance has improved" / "Performance has regressed" / "No change in performance detected"。
```

**4. "Baseline numbers" 章节** — 完全重写，使用 Task 1 抓取的实测数值。结构如下（用实测数值替换 `<填入>` 占位符）：

```markdown
## Baseline numbers (v1.0)

时间为 median（取 `[low median high]` 区间中位）。Throughput = records / median time。

### CSV synthetic export (→ /dev/null)

| Records | Median time | Throughput |
|--------:|------------:|-----------:|
|   1 000 |    <填入>   |  <填入>    |
|  10 000 |    <填入>   |  <填入>    |
|  50 000 |    <填入>   |  <填入>    |

### CSV real-file export (→ /dev/null, 538MB 真实日志)

| Input        | Median time | Approx throughput |
|--------------|------------:|------------------:|
| sqllogs/ (538MB, 2 文件) | <填入>s | <填入> M records/s（按粗略记录数估算） |

> 备注：real-file 未预扫描记录数，吞吐为粗略估算。Phase 4/5 对比时以 median time 为准（吞吐仅作参考）。

### SQLite synthetic export (→ target/bench_sqlite/bench.db, JOURNAL_MODE=OFF SYNCHRONOUS=OFF)

| Records | Median time | Throughput |
|--------:|------------:|-----------:|
|   1 000 |    <填入>   |  <填入>    |
|  10 000 |    <填入>   |  <填入>    |
|  50 000 |    <填入>   |  <填入>    |

### SQLite real-file export (→ target/bench_sqlite_real/bench.db)

| Input        | Median time | Approx throughput |
|--------------|------------:|------------------:|
| sqllogs/ (538MB, 2 文件) | <填入>s | <填入> M records/s（粗略估算）|

### Filter pipeline (10 000 records, CSV → /dev/null)

> 沿用旧表（filter 部分本 phase 未重新采集，与 v1.0 同源代码无变化）。Phase 4/5 完成后重新采集。

| Scenario              | Median time | Throughput    | Notes |
|-----------------------|------------:|--------------:|-------|
| `no_pipeline`         |   2.10 ms   |   4.75 M/s    | Fast path — no filter overhead |
| `pipeline_passthrough`|   2.77 ms   |   3.62 M/s    | All records pass; overhead = pipeline dispatch |
| `trxid_small`         |   1.08 ms   |   9.30 M/s    | 10 IDs in HashSet; ~0.1% pass |
| `trxid_large`         |   1.30 ms   |   7.70 M/s    | 1 000 IDs in HashSet; ~10% pass |
| `indicator_prescan`   |   2.12 ms   |   4.72 M/s    | Two-pass (pre-scan + main) |
```

**5. 新增章节："Hot-path observation (flamegraph)"** — 紧接 Baseline numbers 之后：

```markdown
## Hot-path observation (flamegraph)

flamegraph SVG: `docs/flamegraphs/csv_export_real.svg`（CSV real-file 采样 15s）

采集命令：
```bash
sudo cargo flamegraph --profile flamegraph --bench bench_csv \
  --output docs/flamegraphs/csv_export_real.svg \
  -- --bench --profile-time 15 csv_export_real/real_file
```

Top 3 占比函数（来自 v1.0 火焰图人工观察）：
1. `<填入 Task 2 用户报告的 fn_a>`
2. `<填入 fn_b>`
3. `<填入 fn_c>`

> 这些函数是 Phase 4 (CSV 优化) 的优先目标。Phase 5 SQLite 优化可重新采集 `sqlite_export_real/real_file` 的火焰图。
```

**6. "Performance rules" 章节** — 删除 jsonl_export 行，并基于 v1.0 实测数值更新硬限（取 median × 1.05 作为容差）：

```markdown
## Performance rules

新增功能或优化必须满足（±5% 容差以吸收测量噪声）：

| Benchmark                       | Hard limit (v1.0 median × 1.05) |
|---------------------------------|---------------------------------|
| `csv_export/10000`              | <填入 median × 1.05>            |
| `sqlite_export/10000`           | <填入 median × 1.05>            |
| `csv_export_real/real_file`     | <填入 median × 1.05>            |
| `sqlite_export_real/real_file`  | <填入 median × 1.05>            |
| `filters/no_pipeline`           | ≤ 2.21 ms                       |
| `filters/pipeline_passthrough`  | ≤ 2.91 ms                       |
```

**关键约束：**
- 所有 `<填入>` 占位符必须替换为 Task 1 实测的真实数值；若 real-file baseline 因 sqllogs/ 缺失而 skip，则在该行注明 "skipped (sqllogs/ not present)"
- 不要保留任何 JSONL 引用（项目已无 bench_jsonl，Cargo.toml 中也未注册）
- 不要保留旧版本 v0.5.0 / opt-level=z 的描述
- 若 Task 2 用户报告中无 top 3 函数名（输入 issues），改为写 "TBD pending re-capture（见 Plan/Phase 注释）"
- 文档总长度应在 100-180 行之间（精简而完整）
  </action>
  <verify>
    <automated>grep -c "v1.0" benches/BENCHMARKS.md | awk '$1>=2' | grep -q . && grep -c "opt-level=3" benches/BENCHMARKS.md | grep -q "^1$" && grep -c "csv_export_real" benches/BENCHMARKS.md | awk '$1>=2' | grep -q . && grep -c "sqlite_export_real" benches/BENCHMARKS.md | awk '$1>=2' | grep -q . && grep -c "Hot-path observation" benches/BENCHMARKS.md | grep -q "^1$" && ! grep -q "JSONL\|jsonl_export\|opt-level=z" benches/BENCHMARKS.md && ! grep -q "<填入>" benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v1.0" benches/BENCHMARKS.md` 输出 ≥ 2（头部 + Baseline numbers 标题）
    - `grep -c "opt-level=3" benches/BENCHMARKS.md` 输出 1（已更新；不再 opt-level=z）
    - `grep -c "csv_export_real" benches/BENCHMARKS.md` 输出 ≥ 2（章节 + Performance rules）
    - `grep -c "sqlite_export_real" benches/BENCHMARKS.md` 输出 ≥ 2
    - `grep -c "Hot-path observation" benches/BENCHMARKS.md` 输出 1
    - `grep -c "JSONL\|jsonl_export" benches/BENCHMARKS.md` 输出 0（旧 JSONL 引用已删除）
    - `grep -c "opt-level=z" benches/BENCHMARKS.md` 输出 0（旧 profile 描述已替换）
    - `grep -c "<填入>" benches/BENCHMARKS.md` 输出 0（所有占位符已替换为实测值或 "skipped"）
    - `wc -l benches/BENCHMARKS.md` 输出在 100-180 之间
    - 文件可作为 markdown 渲染（无语法错误，表格列对齐）
  </acceptance_criteria>
  <done>BENCHMARKS.md 已重写为 v1.0 报告，包含 synthetic + real-file 数值、采集方法、flamegraph 路径、Top 3 热路径、更新后的 Performance rules；JSONL 旧痕和 opt-level=z 描述已清除；无未替换占位符。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark 输出 → 版本控制 | baseline JSON 与 BENCHMARKS.md 提交到 git，需确保不含敏感信息 |
| flamegraph SVG → 版本控制 | docs/flamegraphs/ 中的 SVG 文件需检查不含真实数据 |
| sudo cargo flamegraph | macOS dtrace 需要 root 权限，仅本地开发者执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | I (Information Disclosure) | flamegraph SVG 提交到仓库 | mitigate | flamegraph 仅显示函数符号与计数，不含日志记录内容；human-verify 任务中人工确认 SVG 不含敏感字符串 |
| T-03-08 | E (Elevation of Privilege) | sudo cargo flamegraph | mitigate | 命令限定在本地开发机执行，不在 CI 自动运行；Plan 提供 samply 无 sudo 回退路径，降低 root 暴露面 |
| T-03-09 | T (Tampering) | benches/baselines/v1.0/ JSON 数据 | accept | 提交到 git 后受 commit 签名保护；后续 Phase 4/5 通过 --baseline v1.0 对比，篡改会立即触发数值不一致告警 |
| T-03-10 | R (Repudiation) | BENCHMARKS.md 数值来源不可追溯 | mitigate | 文档头部明确记录 commit date + 版本 + 机器配置 + opt-level；baseline JSON 同时提交，可由任何开发者用同 commit 复现 |
</threat_model>

<verification>
- `ls benches/baselines/csv_export/v1.0/estimates.json benches/baselines/sqlite_export/v1.0/estimates.json` 全部存在
- `ls docs/flamegraphs/csv_export_real.svg`（或 .json + 用户确认 samply）存在
- `grep -c "v1.0" benches/BENCHMARKS.md` ≥ 2
- 用户在 Task 2 中已确认火焰图函数名可读
- `cargo bench --bench bench_csv -- --baseline v1.0 csv_export/10000` 能成功对比（验证 baseline JSON 可被 criterion 读取）
</verification>

<success_criteria>
1. v1.0 baseline JSON 已提交到 benches/baselines/，criterion 可用 --baseline v1.0 引用
2. flamegraph SVG 已生成且函数符号可读（人工核验通过）
3. BENCHMARKS.md 已更新为 v1.0 报告，含 synthetic + real-file 数值 + 热路径观察 + 反映当前 release profile
4. JSONL 旧引用与 opt-level=z 描述全部清除
5. Phase 4/5 任何执行者只需读 BENCHMARKS.md 即可知道优化目标和对比命令
</success_criteria>

<output>
After completion, create `.planning/phases/03-profiling-benchmarking/03-03-SUMMARY.md`
</output>
