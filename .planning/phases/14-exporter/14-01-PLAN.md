---
phase: 14-exporter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exporter/mod.rs
  - src/features/mod.rs
autonomous: true
requirements:
  - TMPL-04
tags:
  - exporter
  - trait
  - rust
must_haves:
  truths:
    - "Exporter trait 提供 write_template_stats() 第四生命周期方法（默认 no-op 实现，向后兼容）"
    - "ExporterKind 对三个 variant（Csv/Sqlite/DryRun）静态分发 write_template_stats()，无虚表开销"
    - "ExporterManager::write_template_stats() 作为 run.rs 的唯一外部调用点（D-02）"
    - "DryRunExporter::write_template_stats() 覆盖默认实现，仅打 info! 日志，不产生任何文件（D-05）"
    - "TemplateStats 从 features 模块 pub 重导出，下游可直接用 crate::features::TemplateStats 引用"
  artifacts:
    - path: "src/exporter/mod.rs"
      provides: "trait method + ExporterKind dispatch arm + ExporterManager external API + DryRunExporter override"
      contains: "fn write_template_stats"
    - path: "src/features/mod.rs"
      provides: "TemplateStats pub re-export"
      contains: "pub use template_aggregator::TemplateStats"
  key_links:
    - from: "Exporter trait"
      to: "ExporterKind"
      via: "match arm 透传"
      pattern: "Self::Csv\\(e\\) => e\\.write_template_stats"
    - from: "ExporterManager::write_template_stats"
      to: "self.exporter.write_template_stats"
      via: "委托调用"
      pattern: "self\\.exporter\\.write_template_stats"
---

<objective>
扩展 `Exporter` trait 增加第四生命周期段 `write_template_stats()`，在所有 `ExporterKind` variant 上透传，并通过 `ExporterManager` 暴露为唯一公共调用点。同时为 `DryRunExporter` 实现 no-op 覆盖，并在 `features/mod.rs` 暴露 `TemplateStats` 类型，供 trait 签名引用。

Purpose: 为 Phase 14 后续 plan（SqliteExporter / CsvExporter / run.rs 接入）建立公共 trait 骨架。Plan 02/03 必须基于此 trait 才能各自实现 write_template_stats。
Output: 修改 `src/exporter/mod.rs` + `src/features/mod.rs`，通过 `cargo build` 与默认实现 + DryRun 覆盖的单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-exporter/14-CONTEXT.md
@.planning/phases/14-exporter/14-RESEARCH.md
@.planning/phases/14-exporter/14-PATTERNS.md
@.planning/phases/14-exporter/14-VALIDATION.md
@src/exporter/mod.rs
@src/features/mod.rs
@src/features/template_aggregator.rs

<interfaces>
<!-- 关键已有类型，executor 必须按此结构使用，不要再去 codebase 探索 -->

From src/features/template_aggregator.rs:
- `pub struct TemplateStats { template_key: String, count: u64, avg_us: u64, min_us: u64, max_us: u64, p50_us: u64, p95_us: u64, p99_us: u64, first_seen: String, last_seen: String }`
- `pub struct TemplateAggregator` 已存在（无需修改）

From src/exporter/mod.rs:
- `pub trait Exporter { fn initialize(); fn export(); fn export_one_normalized(...); fn export_one_preparsed(...); fn finalize(); fn stats_snapshot() }`
- `pub enum ExporterKind { Csv(CsvExporter), Sqlite(SqliteExporter), DryRun(DryRunExporter) }`
- `pub struct ExporterManager { exporter: ExporterKind }`
- 现有默认方法模式：`fn export_one_normalized(...) { let _ = normalized; self.export(sqllog) }`
- `DryRunExporter` 实现 `Exporter`（现有 finalize 返回 `Ok(())`）

From src/error/:
- `Result<T> = std::result::Result<T, Error>`（已通过 `use crate::error::{Error, Result}` 引入）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 features/mod.rs 暴露 TemplateStats 类型</name>
  <files>src/features/mod.rs</files>
  <read_first>
    - src/features/mod.rs（确认现有 pub use 模式，本文件已 `pub use template_aggregator::TemplateAggregator;`）
    - src/features/template_aggregator.rs（确认 `TemplateStats` 是 `pub struct`，已 derive `Serialize`）
  </read_first>
  <behavior>
    - Test 1: 编译期可见性 — 在 `src/features/mod.rs` 中新增 `pub use template_aggregator::TemplateStats;`，使外部模块可用 `crate::features::TemplateStats` 引用该类型
    - Test 2: `cargo build` 通过（结构性更改，不破坏现有 API）
  </behavior>
  <action>
    在 `src/features/mod.rs` 中找到现有的 `pub use template_aggregator::TemplateAggregator;` 行，紧随其后追加一行 `pub use template_aggregator::TemplateStats;`。

    不要修改 `template_aggregator.rs` 自身（`TemplateStats` 已是 `pub` 可见）。不要新增任何其它导出，不要改动其他模块的 re-export。
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "error|warning: unused"</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/mod.rs` 包含字面字符串 `pub use template_aggregator::TemplateStats;`（grep 可验证）
    - `cargo build` 退出码为 0，无新增 warning
    - `cargo test --no-run` 退出码为 0
  </acceptance_criteria>
  <done>
    `TemplateStats` 可通过 `crate::features::TemplateStats` 访问；不影响任何现有功能或测试。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 Exporter trait + ExporterKind + ExporterManager 中加入 write_template_stats() 接口，并为 DryRunExporter 覆盖</name>
  <files>src/exporter/mod.rs</files>
  <read_first>
    - src/exporter/mod.rs（当前 trait 定义、ExporterKind impl 块、ExporterManager impl 块、DryRunExporter impl 块全部位置）
    - .planning/phases/14-exporter/14-CONTEXT.md（D-01、D-02、D-05 锁定决策）
    - .planning/phases/14-exporter/14-PATTERNS.md（"Exporter trait 新增第四段" 部分的 4 个修改点模式）
  </read_first>
  <behavior>
    - Test 1（默认实现）：自定义 mock exporter 不覆盖 `write_template_stats`，调用 `write_template_stats(&[], None)` 返回 `Ok(())`，无 panic
    - Test 2（DryRunExporter 覆盖 no-op）：`DryRunExporter::default().write_template_stats(&stats, None)` 返回 `Ok(())`，且文件系统无任何文件创建（即不调用任何 `fs::create_*`）
    - Test 3（ExporterManager 委托）：用 `ExporterManager::dry_run()` 调用 `write_template_stats(&[], None)` 返回 `Ok(())`，验证调用链通畅
    - Test 4（ExporterKind 透传不 panic）：构造 CSV / Sqlite / DryRun 三个 variant，分别调用 `write_template_stats(&[], None)`，调用链无 panic（CSV/Sqlite 默认实现即 trait 默认 no-op，因 Plan 02/03 尚未实现具体覆盖）
  </behavior>
  <action>
    在 `src/exporter/mod.rs` 中按以下四个修改点添加代码（每处都参照 14-PATTERNS.md "src/exporter/mod.rs" 一节给出的现有模式）：

    1. **`Exporter` trait（约 L11-46）新增默认方法**：在 trait 块尾部、`stats_snapshot()` 之前或之后，添加方法签名 `fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], final_path: Option<&std::path::Path>) -> Result<()>`，默认实现体为 `let _ = (stats, final_path); Ok(())`（与同文件 `export_one_normalized` 的 `let _ = normalized;` 模式一致）。

    2. **`ExporterKind` impl 块（约 L57-114）新增透传方法**：在 `finalize()` 方法之后追加同签名的非 pub 方法，匹配三个 variant `Self::Csv(e) | Self::Sqlite(e) | Self::DryRun(e)` 各自调用 `e.write_template_stats(stats, final_path)`。方法可加 `#[inline]`，与 `export_one_preparsed` 风格一致。

    3. **`ExporterManager` impl 块（约 L193-306）新增 pub 外部方法**：在 `pub fn finalize(&mut self)` 之后追加同签名的 `pub` 方法，方法体仅为 `self.exporter.write_template_stats(stats, final_path)`。不加 `info!` wrapper（与 finalize 不同；DryRun/Sqlite/Csv 内部各自负责 logging）。

    4. **`DryRunExporter`（约 L143-178）impl Exporter 块新增覆盖**：在 `finalize()` 之后追加 `fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], _final_path: Option<&std::path::Path>) -> Result<()>`，方法体调用 `info!("Dry-run: would write {} template stats (no file written)", stats.len());` 并返回 `Ok(())`。绝对不创建任何文件（D-05）。

    注意：不要修改任何现有方法签名；不要 take/drop 任何字段；trait 默认实现签名中的类型路径用全路径 `crate::features::TemplateStats` 与 `std::path::Path`，避免在 trait 默认实现处新增 `use`（trait 默认实现可被外部 crate 看到，但本 crate 内部，无影响）。

    在 `mod.rs` 的现有 `#[cfg(test)] mod tests` 块内新增上述 4 项行为的单元测试函数：`test_default_write_template_stats_noop`、`test_dry_run_write_template_stats_noop`、`test_exporter_manager_write_template_stats_dry_run`、`test_exporter_kind_dispatch_write_template_stats`。

    所有测试不写文件即可证明 no-op：构造空 `Vec<TemplateStats>` 或长度 1 的样例（手工 literal）传入，返回 `Ok(())`。验证 DryRunExporter 的 stats（`stats_snapshot()`）不受调用影响（即 `exported` 计数不变化）。
  </action>
  <verify>
    <automated>cargo test --lib exporter::mod::tests --quiet 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "fn write_template_stats" src/exporter/mod.rs` 至少返回 4 处（trait 默认、ExporterKind 透传、ExporterManager pub、DryRunExporter 覆盖）
    - `grep -E "Self::Csv\\(e\\) => e\\.write_template_stats" src/exporter/mod.rs` 至少 1 处命中
    - `grep -E "pub fn write_template_stats" src/exporter/mod.rs` 至少 1 处命中（ExporterManager）
    - `grep -E "Dry-run: would write .* template stats" src/exporter/mod.rs` 至少 1 处命中
    - `cargo test test_default_write_template_stats_noop` 退出码 0
    - `cargo test test_dry_run_write_template_stats_noop` 退出码 0
    - `cargo test test_exporter_manager_write_template_stats_dry_run` 退出码 0
    - `cargo test test_exporter_kind_dispatch_write_template_stats` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - DryRunExporter 测试中调用 `write_template_stats` 后 `stats_snapshot().unwrap().exported` 值不变化（no-op 不影响导出计数）
  </acceptance_criteria>
  <done>
    Exporter trait 拥有 `write_template_stats` 默认 no-op 方法；ExporterKind 三个 variant 静态分发；ExporterManager 提供唯一公共调用点；DryRunExporter 覆盖为 info! + no-op；4 项单元测试全部通过；clippy 全绿；Plan 02/03 现在可以分别在 sqlite.rs / csv.rs 实现具体覆盖，无编译冲突。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增外部输入边界 | 本 plan 仅扩展内部 trait + 默认实现；`stats` 来自 `TemplateAggregator::finalize()`（内部生成），`final_path` 来自配置文件（已通过 `Config::validate_and_compile` 校验） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Exporter trait 默认实现 | accept | 默认实现为 no-op，无副作用；外部 crate 暂无消费此 trait |
| T-14-02 | Information Disclosure | DryRunExporter info! 日志 | accept | 仅打印模板数量（usize），不打印模板内容；与现有 dry-run 行为一致 |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装新包，无需 slopcheck |
</threat_model>

<verification>
- `cargo build` 通过
- `cargo test --lib exporter::mod::tests` 全部测试通过
- `cargo clippy --all-targets -- -D warnings` 通过
- 4 项新增测试函数存在且通过
</verification>

<success_criteria>
- Exporter trait 新增 `write_template_stats` 默认 no-op 方法
- ExporterKind 三个 variant 静态分发 `write_template_stats`
- ExporterManager 提供 `pub fn write_template_stats(...)`
- DryRunExporter 覆盖为 info! 日志 + no-op
- `crate::features::TemplateStats` 可在 trait 签名中被引用
- 全部测试与 clippy 通过
</success_criteria>

<output>
Create `.planning/phases/14-exporter/14-01-SUMMARY.md` when done with:
- Files modified（src/exporter/mod.rs, src/features/mod.rs）
- Trait signature actual text
- Test names added
- Affects: [exporter trait API]
- Provides: [write_template_stats public interface, TemplateStats re-export]
- Patterns: [trait 默认方法模式 + 静态分发透传]
</output>
