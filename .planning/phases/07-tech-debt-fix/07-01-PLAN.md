---
phase: 07-tech-debt-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
  - src/exporter/sqlite.rs
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
tags:
  - sqlite
  - sql-injection
  - error-handling
  - validation

must_haves:
  truths:
    - "用户配置非法 table_name（含特殊字符、首字符为数字、含非 ASCII、空字符串）时，cargo run -- validate 失败并返回 ConfigError::InvalidValue，错误信息包含 ^[a-zA-Z_][a-zA-Z0-9_]*$ 模式"
    - "用户配置合法 ASCII 标识符 table_name（如 sqllog_records、_tbl1、t）时,cargo run -- validate 通过"
    - "首次运行 SQLite 导出（DB 文件存在但表不存在，overwrite=false、append=false）时，initialize() 不打印 warn、不返回 Err、流程继续"
    - "运行时遭遇真实 SQLite 错误（非 'no such table'）时，initialize() 通过 log::warn! 输出到应用日志后继续运行，而非静默丢弃。**注：** 此处\"应用日志\"对应 [logging] file 配置路径（per CONTEXT.md D-02 locked decision，最小侵入性，不传入额外的 error writer 句柄）；ROADMAP Phase 7 成功标准 #2 所述 \"error log 文件\"在本计划语义下即指此应用日志路径——log::warn! 是项目里 SqliteExporter 已有的可观测错误通道，与 [error] file 的 per-record parse error log 互补但路径不同。"
    - "DROP TABLE / DELETE FROM / CREATE TABLE / INSERT INTO 四类 SQL 语句中，table_name 均使用双引号转义（形如 \"sqllog_records\"），SQL 注入向量被消除"
    - "全部 cargo test 通过（含新增的 6+ 项测试），cargo clippy --all-targets -- -D warnings 零警告，cargo fmt 无格式变更"
  artifacts:
    - path: "src/config.rs"
      provides: "SqliteExporter::validate() 含 ASCII 标识符校验"
      contains: "is_ascii_alphabetic"
    - path: "src/exporter/sqlite.rs"
      provides: "initialize() 显式 match DELETE 错误 + 4 处 DDL 双引号转义 + handle_delete_clear_result helper"
      contains: "no such table"
  key_links:
    - from: "src/config.rs::SqliteExporter::validate"
      to: "src/exporter/sqlite.rs DDL 字符串拼接"
      via: "validate 拒绝非 ASCII 标识符 → DDL 双引号转义安全（D-04 + D-06 协作）"
      pattern: "is_ascii_alphabetic.*is_ascii_alphanumeric"
    - from: "src/exporter/sqlite.rs::initialize match Err"
      to: "log::warn! 应用日志"
      via: "rusqlite::Error::SqliteFailure 模式匹配 + log::warn 宏调用 + handle_delete_clear_result helper 提取"
      pattern: "SqliteFailure.*no such table|log::warn|handle_delete_clear_result"
---

<objective>
修复 SQLite 导出器两处技术债：(1) DEBT-01 — `initialize()` 中 `let _ = conn.execute(DELETE FROM ...)` 静默错误改为显式 match，区分无害的"表不存在"与真实错误；(2) DEBT-02 — 在 `SqliteExporter::validate()` 添加 ASCII 标识符白名单校验，并将 4 条 DDL/DML 中的 `table_name` 全部用 SQLite 双引号转义。

Purpose: 让导出错误路径可观测（消除静默丢弃），消除 `table_name` 直接字符串拼接的 SQL 注入向量，同时保持最小侵入性（不改变 SqliteExporter 结构、不引入 regex crate、不增加新错误变体）。

Output: src/config.rs 与 src/exporter/sqlite.rs 修改完毕；新增至少 6 项单元测试（validate 4 项、DDL 转义 2 项、DEBT-01 错误处理 1 项）；clippy/fmt/test 三项检查全过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tech-debt-fix/07-CONTEXT.md
@.planning/phases/07-tech-debt-fix/07-PATTERNS.md
@src/config.rs
@src/exporter/sqlite.rs
@src/error.rs

<interfaces>
<!-- 关键签名与类型，executor 直接复用，无需再次探索 -->

From src/error.rs:
```rust
pub type Result<T> = std::result::Result<T, Error>;

pub enum Error {
    Config(#[from] ConfigError),
    Export(#[from] ExportError),
    // ...
}

pub enum ConfigError {
    InvalidValue {
        field: String,
        value: String,
        reason: String,
    },
    // ...
}
```

From src/config.rs (现有 SqliteExporter::validate 签名):
```rust
impl SqliteExporter {
    pub fn validate(&self) -> Result<()>;
}
```

From src/exporter/sqlite.rs (现有 initialize 关键签名):
```rust
impl SqliteExporter {
    pub fn new(database_url: String, table_name: String, overwrite: bool, append: bool) -> Self;
    fn build_insert_sql(table_name: &str, ordered_indices: &[usize]) -> String;
    fn build_create_sql(table_name: &str, ordered_indices: &[usize]) -> String;
}
impl Exporter for SqliteExporter {
    fn initialize(&mut self) -> Result<()>;
}
```

From rusqlite (executor 已熟悉，仅列匹配模式):
```rust
// 错误匹配模式（用于 DEBT-01）
match conn.execute(sql, []) {
    Ok(_) => {}
    Err(rusqlite::Error::SqliteFailure(_, Some(ref msg))) if msg.contains("no such table") => {
        // 静默
    }
    Err(e) => {
        log::warn!("sqlite clear failed: {e}");
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 SqliteExporter::validate() 添加 table_name ASCII 标识符校验（DEBT-02 D-04）</name>
  <files>src/config.rs</files>
  <read_first>
    - src/config.rs（必读，了解现有 validate 函数布局，特别是 lines 388–414 的 SqliteExporter::validate 完整实现）
    - src/error.rs（必读，确认 ConfigError::InvalidValue 三字段：field/value/reason，均为 String）
    - .planning/phases/07-tech-debt-fix/07-CONTEXT.md（必读，D-04 决策的精确规则与错误信息文本）
    - .planning/phases/07-tech-debt-fix/07-PATTERNS.md（必读，PATTERNS L144–158 给出 is_valid_ident 实现模板）
  </read_first>
  <behavior>
    - Test 1 (test_validate_sqlite_table_name_valid_simple): table_name="tbl" → validate() 返回 Ok(())
    - Test 2 (test_validate_sqlite_table_name_valid_underscore_prefix): table_name="_records" → validate() 返回 Ok(())
    - Test 3 (test_validate_sqlite_table_name_valid_with_digits): table_name="t1_log_2024" → validate() 返回 Ok(())
    - Test 4 (test_validate_sqlite_table_name_rejects_leading_digit): table_name="1tbl" → validate() 返回 Err，错误为 ConfigError::InvalidValue，field == "exporter.sqlite.table_name"，reason 含 "ASCII identifiers only"
    - Test 5 (test_validate_sqlite_table_name_rejects_special_char): table_name="tbl;DROP" → validate() 返回 Err，reason 含 "ASCII identifiers only"
    - Test 6 (test_validate_sqlite_table_name_rejects_quote): table_name="tbl\"x" → validate() 返回 Err
    - Test 7 (test_validate_sqlite_table_name_rejects_non_ascii): table_name="日志表" → validate() 返回 Err
    - Test 8 (test_validate_sqlite_table_name_rejects_space): table_name="my tbl" → validate() 返回 Err
    - 现有 test_validate_empty_sqlite_table_name 不应受影响（空字符串走早期返回路径）
  </behavior>
  <action>
    在 src/config.rs 中找到 `impl SqliteExporter { pub fn validate(&self) -> Result<()> { ... } }`（约 lines 388–414）。

    **修改步骤**（在现有 `if self.table_name.trim().is_empty()` 检查之后、`if self.batch_size == 0` 检查之前插入新代码块）：

    在 lines 397–402 的 `if self.table_name.trim().is_empty() { ... }` 块之后，新增以下代码：

    ```rust
    // ASCII 标识符校验：^[a-zA-Z_][a-zA-Z0-9_]*$（不引入 regex crate）
    let is_valid_ident = {
        let mut chars = self.table_name.chars();
        chars
            .next()
            .is_some_and(|c| c.is_ascii_alphabetic() || c == '_')
            && chars.all(|c| c.is_ascii_alphanumeric() || c == '_')
    };
    if !is_valid_ident {
        return Err(Error::Config(ConfigError::InvalidValue {
            field: "exporter.sqlite.table_name".to_string(),
            value: self.table_name.clone(),
            reason: "table name must match ^[a-zA-Z_][a-zA-Z0-9_]*$ (ASCII identifiers only)"
                .to_string(),
        }));
    }
    ```

    **要点：**
    - 使用 `is_some_and`（Rust 1.70+，本项目已在用）替代 PATTERNS 文件中示意的 `map_or(false, |c|...)` —— `is_some_and` 更简洁且 clippy 推荐。
    - 错误的 `field`、`reason` 字符串必须**逐字**与上面一致（测试断言会匹配 "ASCII identifiers only"）。
    - 不修改任何现有代码行；仅插入新块。
    - 函数体加上新增代码后总行数仍 < 40 行（约 35 行），符合 CLAUDE.md 函数长度规范。

    然后在 `mod tests` 区块内（约 lines 420+，紧跟 `test_validate_empty_sqlite_table_name` 之后）追加 8 个新测试，模板：

    ```rust
    #[test]
    fn test_validate_sqlite_table_name_valid_simple() {
        let mut cfg = default_config();
        cfg.exporter.sqlite = Some(SqliteExporter {
            database_url: "/tmp/x.db".into(),
            table_name: "tbl".into(),
            overwrite: true,
            append: false,
            batch_size: 10_000,
        });
        cfg.exporter.csv = None;
        assert!(cfg.validate().is_ok());
    }

    #[test]
    fn test_validate_sqlite_table_name_rejects_leading_digit() {
        let mut cfg = default_config();
        cfg.exporter.sqlite = Some(SqliteExporter {
            database_url: "/tmp/x.db".into(),
            table_name: "1tbl".into(),
            overwrite: true,
            append: false,
            batch_size: 10_000,
        });
        cfg.exporter.csv = None;
        let err = cfg.validate().unwrap_err();
        let msg = format!("{err}");
        assert!(msg.contains("ASCII identifiers only"), "actual: {msg}");
        assert!(msg.contains("exporter.sqlite.table_name"), "actual: {msg}");
    }

    // 其余 6 项按相同模板编写，分别测：
    //   - "_records" 通过
    //   - "t1_log_2024" 通过
    //   - "tbl;DROP" 拒绝
    //   - "tbl\"x" 拒绝
    //   - "日志表" 拒绝
    //   - "my tbl"（含空格）拒绝
    ```

    **注意：** 如果 `default_config()` 默认配置已含 csv exporter 但无 sqlite，需将 `cfg.exporter.csv = None` 才能让 sqlite validate 路径执行（参考现有 `test_validate_empty_sqlite_database_url` / `test_validate_empty_sqlite_table_name` 的写法 —— 先读这两个测试确认正确的设置方式，复用同一构造模式）。
  </action>
  <verify>
    <automated>cargo test --lib config::tests::test_validate_sqlite_table_name -- --nocapture 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "is_ascii_alphabetic" src/config.rs` 返回 ≥ 1
    - `grep -c "ASCII identifiers only" src/config.rs` 返回 ≥ 1（错误信息文本，且至少一处出现在生产代码而非仅测试）
    - `cargo test --lib config::tests::test_validate_sqlite_table_name 2>&1 | grep -c "test result: ok"` 返回 1
    - `cargo test --lib config::tests::test_validate_sqlite_table_name 2>&1 | grep -E "^test .* \.\.\. ok$" | wc -l` ≥ 8（8 个新测试全部通过）
    - `cargo test --lib config::tests::test_validate_empty_sqlite_table_name 2>&1 | grep -c "test result: ok"` 返回 1（既有测试不回归）
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "warning\|error"` 返回 0
  </acceptance_criteria>
  <done>
    - SqliteExporter::validate() 含 is_valid_ident 块与 InvalidValue 错误返回
    - 8 个新单元测试全部通过
    - 既有 validate 测试零回归
    - clippy 零警告
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 将 src/exporter/sqlite.rs 的 5 处 DDL/DML format! 改用双引号转义 table_name（DEBT-02 D-05）</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs（必读，定位全部 5 处 format! 调用，保留与现状对照修改）
    - .planning/phases/07-tech-debt-fix/07-CONTEXT.md（必读，D-05 与 D-06 给出转义规则与"validate 已保证安全因而无需运行时双引号自转义"的论证）
    - .planning/phases/07-tech-debt-fix/07-PATTERNS.md（必读，PATTERNS L86–105 列出 5 处 format! 修改前后对照）
  </read_first>
  <behavior>
    - Test 1 (test_sqlite_build_insert_sql_ordered): 修改后 sql 形如 `INSERT INTO "t" (sql, username) VALUES (?, ?)` —— 注意 `t` 外有双引号
    - Test 2 (test_sqlite_build_insert_sql_full_fast_path): 修改后 sql 形如 `INSERT INTO "t" VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
    - Test 3 (test_sqlite_build_create_sql_ordered): 修改后 sql 形如 `CREATE TABLE IF NOT EXISTS "t" (sql TEXT NOT NULL, username TEXT NOT NULL)`
    - Test 4（新增 test_sqlite_initialize_creates_quoted_table）: initialize() 后查询 `sqlite_master` 的 `sql` 列，应包含 `CREATE TABLE IF NOT EXISTS "<table_name>"` 子串
    - 所有现有的 SELECT COUNT/SELECT 查询（在 test 中）继续通过，因为 SELECT 路径未变（裸 table_name 在 SELECT 中合法，因为表名是普通标识符）
  </behavior>
  <action>
    在 src/exporter/sqlite.rs 中精确修改 5 处 format! 调用：

    **修改 1 — `new()` 中的 insert_sql 初始化（约 line 51–53）：**
    将
    ```rust
    let insert_sql = format!(
        "INSERT INTO {table_name} VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    );
    ```
    改为
    ```rust
    let insert_sql = format!(
        "INSERT INTO \"{table_name}\" VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    );
    ```

    **修改 2 — `build_insert_sql()` 全量快速路径（约 line 75–77）：**
    将
    ```rust
    return format!(
        "INSERT INTO {table_name} VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    );
    ```
    改为
    ```rust
    return format!(
        "INSERT INTO \"{table_name}\" VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    );
    ```

    **修改 3 — `build_insert_sql()` 投影路径（约 line 81–84）：**
    将
    ```rust
    format!(
        "INSERT INTO {table_name} ({}) VALUES ({placeholders})",
        cols.join(", ")
    )
    ```
    改为
    ```rust
    format!(
        "INSERT INTO \"{table_name}\" ({}) VALUES ({placeholders})",
        cols.join(", ")
    )
    ```

    **修改 4 — `build_create_sql()`（约 line 111–114）：**
    将
    ```rust
    format!(
        "CREATE TABLE IF NOT EXISTS {table_name} ({})",
        cols.join(", ")
    )
    ```
    改为
    ```rust
    format!(
        "CREATE TABLE IF NOT EXISTS \"{table_name}\" ({})",
        cols.join(", ")
    )
    ```

    **修改 5 — `initialize()` 中的 DROP TABLE（约 line 260）：**
    将
    ```rust
    conn.execute(&format!("DROP TABLE IF EXISTS {}", self.table_name), [])
    ```
    改为
    ```rust
    conn.execute(&format!("DROP TABLE IF EXISTS \"{}\"", self.table_name), [])
    ```

    **不要在本任务修改 line 265 的 `DELETE FROM`** —— 该行由 Task 3 与 DEBT-01 的 match 重构同步处理（避免两次任务都触碰同一行造成 conflict）。Task 3 会一次性把 DELETE 拼接改为 `format!("DELETE FROM \"{}\"", self.table_name)` 并包在 match 块里。

    **更新现有 3 项测试以匹配新输出：**

    `test_sqlite_build_insert_sql_ordered`（约 line 609）：
    ```rust
    assert_eq!(sql, "INSERT INTO \"t\" (sql, username) VALUES (?, ?)");
    ```

    `test_sqlite_build_create_sql_ordered`（约 line 615）：
    ```rust
    assert_eq!(
        sql,
        "CREATE TABLE IF NOT EXISTS \"t\" (sql TEXT NOT NULL, username TEXT NOT NULL)"
    );
    ```

    `test_sqlite_build_insert_sql_full_fast_path`（约 line 624）：
    ```rust
    assert_eq!(
        sql,
        "INSERT INTO \"t\" VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    );
    ```

    **新增 1 项测试**（追加在 `mod tests` 末尾）：
    ```rust
    #[test]
    fn test_sqlite_initialize_creates_quoted_table() {
        let dir = tempfile::TempDir::new().unwrap();
        let dbfile = dir.path().join("quoted.db");
        {
            let mut exporter = SqliteExporter::new(
                dbfile.to_string_lossy().into(),
                "my_records".to_string(),
                true,
                false,
            );
            exporter.initialize().unwrap();
            exporter.finalize().unwrap();
        }
        let conn = rusqlite::Connection::open(&dbfile).unwrap();
        let create_stmt: String = conn
            .query_row(
                "SELECT sql FROM sqlite_master WHERE type='table' AND name='my_records'",
                [],
                |r| r.get(0),
            )
            .unwrap();
        assert!(
            create_stmt.contains("CREATE TABLE IF NOT EXISTS \"my_records\""),
            "actual: {create_stmt}"
        );
    }
    ```

    **要点：**
    - 不要改任何 SELECT 语句（测试中的 `SELECT COUNT(*) FROM tbl` 等保持原样 —— 表名作为 token 无引号合法且测试已通过）
    - 不要改 batch_commit_if_needed 中的 `COMMIT; BEGIN`（无 table_name）
    - 不要改 `BEGIN TRANSACTION;` / `COMMIT;`（无 table_name）
    - format! 字符串中 `\"` 是转义后的字面双引号，rust raw string `r#"..."#` 在此处不必使用（保持与现有风格一致）
  </action>
  <verify>
    <automated>cargo test --lib exporter::sqlite::tests 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    <!-- 使用单引号包裹的字符串内嵌双引号字面量，避免反斜杠-双引号在 shell 内的歧义；executor 验证前可先 `cat src/exporter/sqlite.rs | grep -n 'INSERT INTO'` 人工确认 grep 命中目标行 -->
    - `grep -c '"INSERT INTO "' src/exporter/sqlite.rs` 返回 3（new + build_insert_sql 全量 + build_insert_sql 投影 —— 注意搜索串本身就含一个空格 + 字面量双引号 + 空格的形态）
    - `grep -c '"CREATE TABLE IF NOT EXISTS "' src/exporter/sqlite.rs` 返回 ≥ 1（生产代码 1 处；测试断言中可能再有 1 处）
    - `grep -c '"DROP TABLE IF EXISTS "' src/exporter/sqlite.rs` 返回 ≥ 1
    - `grep -v '^[[:space:]]*//' src/exporter/sqlite.rs | grep -cE 'INSERT INTO \{table_name\}[^"]'` 返回 0（旧的未转义 `INSERT INTO {table_name}` 形式应被全部替换；尾部排除 `\"` 避免误命中新形态）
    - `grep -v '^[[:space:]]*//' src/exporter/sqlite.rs | grep -cE 'DROP TABLE IF EXISTS \{\}[^"]'` 返回 0
    - `cargo test --lib exporter::sqlite::tests::test_sqlite_initialize_creates_quoted_table 2>&1 | grep -c "test result: ok"` 返回 1
    - `cargo test --lib exporter::sqlite::tests 2>&1 | grep -c "test result: ok"` 返回 1（全部 sqlite 测试通过）
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "warning\|error"` 返回 0
  </acceptance_criteria>
  <done>
    - 4 处 DDL format!（new、build_insert_sql×2、build_create_sql、DROP TABLE）已使用 `\"{table_name}\"` 双引号转义
    - 3 项现有 build_*_sql 测试断言已同步更新
    - 1 项新增 test_sqlite_initialize_creates_quoted_table 通过
    - 全部 sqlite 模块测试通过，clippy 零警告
    - DELETE FROM 行（line 265）保持原样等待 Task 3 处理
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 将 initialize() 的 DELETE FROM 静默错误改为显式 match 并应用双引号转义（DEBT-01 D-01/D-02 + 完成 DEBT-02 最后一处）</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs（必读，关注 line 263–266 的 else if 分支与 line 265 的 `let _ =` 行；同时确认 `initialize()` 当前函数体行数 41 行，决定 helper 提取的位置）
    - .planning/phases/07-tech-debt-fix/07-CONTEXT.md（必读，D-01/D-02/D-03 给出 match 三分支的精确语义）
    - .planning/phases/07-tech-debt-fix/07-PATTERNS.md（必读，PATTERNS L41–55 给出 match 块的完整模板）
  </read_first>
  <behavior>
    - Test 1 (test_sqlite_initialize_silent_when_table_missing): 全新空 DB 文件、overwrite=false、append=false → initialize() 返回 Ok(())，无 panic、无 Err 传播。预期 DELETE FROM 触发 "no such table" 但被静默吃掉
    - Test 2 (test_sqlite_initialize_succeeds_on_existing_table): 第一次 initialize/finalize 后再次以 overwrite=false、append=false initialize → DELETE FROM 成功执行（清空已有表），返回 Ok(())。后续 export 行数 = 本次写入数（不累计）
    - 现有 test_sqlite_basic_export / test_sqlite_overwrite_drops_existing_table / test_sqlite_append_mode 必须继续通过
    - sqlite.rs 中不应再有 `let _ = conn.execute` 模式（DEBT-01 收尾后 sqlite.rs 此模式应清零；项目其他文件中合法的 `let _ =` 用法不在本计划清理范围内）
  </behavior>
  <action>
    在 src/exporter/sqlite.rs 中精确修改 `initialize()` 的 else-if 分支（当前 line 263–266），并提取一个私有 helper 函数以保持 `initialize()` 函数体 ≤ 40 行（满足 CLAUDE.md 函数长度约束）。

    **背景：** 当前 initialize() 函数体约 41 行（lines 241–281）。直接把 `let _ = conn.execute(...)` 1 行展开为 9 行 match 块会让函数膨胀到约 49 行，违反 CLAUDE.md 40 行上限。解决方案：将 match 块封装为私有 helper 函数 `handle_delete_clear_result`，调用点保持单行，net delta ≈ 0 行。

    **步骤 1 — 在 `impl SqliteExporter { ... }` 块内（在 `build_create_sql` 之后、`impl Exporter for SqliteExporter` 之前的合适位置）新增私有 helper 函数：**

    ```rust
    /// Helper：处理 initialize() 中 DELETE FROM 的执行结果（D-01 软失败语义）。
    /// - Ok：成功清空，无副作用
    /// - SqliteFailure 且 msg 含 "no such table"：首次运行预期，静默
    /// - 其他 Err：通过 log::warn! 记录，函数返回但不向上传播错误（软失败）
    fn handle_delete_clear_result(result: rusqlite::Result<usize>, table_name: &str) {
        if let Err(rusqlite::Error::SqliteFailure(_, Some(ref msg))) = result {
            if msg.contains("no such table") {
                return;
            }
        }
        if let Err(e) = result {
            log::warn!("sqlite clear failed for table {table_name}: {e}");
        }
    }
    ```

    **要点：**
    - 函数签名取 `rusqlite::Result<usize>`，与 `conn.execute(sql, [])` 返回类型一致
    - 第一个 `if let` 用引用绑定 `ref msg` 提取错误信息，这一分支命中时直接 `return`，不再 fall-through
    - 第二个 `if let Err(e)` 是兜底：所有未在第一分支静默的错误都通过 `log::warn!` 记录
    - 包含 `table_name` 参数以使日志更具体（便于多表场景定位问题）
    - helper 函数自身约 9 行（含 doc 注释外），独立于 initialize()，不影响 initialize() 行数

    **步骤 2 — 修改 `initialize()` 的 else-if 分支：**

    **修改前（当前代码 line 263–266）：**
    ```rust
    } else if !self.append {
        let conn = self.conn.as_ref().unwrap();
        let _ = conn.execute(&format!("DELETE FROM {}", self.table_name), []);
    }
    ```

    **修改后：**
    ```rust
    } else if !self.append {
        let conn = self.conn.as_ref().unwrap();
        Self::handle_delete_clear_result(
            conn.execute(&format!("DELETE FROM \"{}\"", self.table_name), []),
            &self.table_name,
        );
    }
    ```

    **要点：**
    - 调用 `Self::handle_delete_clear_result(...)`（关联函数语法，因为它没有 `&self`）
    - DELETE FROM 字符串内的 `\"{}\"` 双引号是 DEBT-02 在本计划中的最后一处转义（与 Task 2 共同完成 D-05 全集）
    - 调用展开为 4 行（vs 原 1 行），net delta = +3 行；initialize() 函数体从 41 行变为 44 行 —— 仍超 40 行
    - **因此**：在步骤 1 提取 helper 后，还要把 `let conn = self.conn.as_ref().unwrap();` 与下面的调用合并为单行表达式：
      ```rust
      } else if !self.append {
          Self::handle_delete_clear_result(
              self.conn.as_ref().unwrap().execute(
                  &format!("DELETE FROM \"{}\"", self.table_name),
                  [],
              ),
              &self.table_name,
          );
      }
      ```
      这样去掉了 `let conn` 中间变量，分支体从原来的 3 行（含 let）变为 7 行；同时砍掉原 if 分支前面任何可省略的空行（如有）。
    - **如果**经过 helper 提取与中间变量合并后 initialize() 仍 > 40 行：进一步把 `if self.overwrite { ... } else if !self.append { ... }` 整个条件块也提取为单独的 helper（如 `fn prepare_target_table(&self) -> Result<()>`），把 initialize() 的主流程压回 ≤ 40 行。executor 应在编辑后立刻 `awk 'NR>=<start>&&NR<=<end>' src/exporter/sqlite.rs | wc -l` 实测函数行数确认。
    - `Err(rusqlite::Error::SqliteFailure(_, Some(ref msg))) if msg.contains("no such table")` 使用 guard 而非 extended_code 比对（D-01 specifics）—— 此 guard 已在 helper 中实现
    - `log::warn!` 宏可直接调用 —— 文件第 5 行 `use log::info;` 仅引入 info 别名；warn 通过 `log::warn!` 完整路径调用，无需新增 use（PATTERNS L54–55 已确认）
    - helper 中绝不返回 Err（这是软失败，D-01 明确要求继续运行）

    **新增 2 项测试**（追加在 `mod tests` 末尾）：

    ```rust
    #[test]
    fn test_sqlite_initialize_silent_when_table_missing() {
        let dir = tempfile::TempDir::new().unwrap();
        let dbfile = dir.path().join("missing_tbl.db");

        // overwrite=false 且 append=false → initialize 会走 DELETE FROM 分支
        // 由于 DB 全新，表不存在，DELETE 应触发 "no such table" 并被静默吃掉
        let mut exporter = SqliteExporter::new(
            dbfile.to_string_lossy().into(),
            "fresh_tbl".to_string(),
            false,
            false,
        );
        // 必须 Ok —— 不能因 "no such table" 返回 Err
        exporter.initialize().expect("initialize should silently succeed when table missing");
        exporter.finalize().unwrap();

        // 表应已被 CREATE TABLE IF NOT EXISTS 创建
        let conn = rusqlite::Connection::open(&dbfile).unwrap();
        let count: i64 = conn
            .query_row(
                "SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='fresh_tbl'",
                [],
                |r| r.get(0),
            )
            .unwrap();
        assert_eq!(count, 1, "table fresh_tbl should be created");
    }

    #[test]
    fn test_sqlite_initialize_clears_existing_table_via_delete() {
        let dir = tempfile::TempDir::new().unwrap();
        let logfile = dir.path().join("test.log");
        let dbfile = dir.path().join("clear.db");
        write_test_log(&logfile, 4);

        let parser = LogParser::from_path(logfile.to_str().unwrap()).unwrap();
        let records: Vec<_> = parser.iter().filter_map(std::result::Result::ok).collect();

        // 第一次 run：写入 4 条
        {
            let mut e =
                SqliteExporter::new(dbfile.to_string_lossy().into(), "clr_tbl".into(), false, false);
            e.initialize().unwrap();
            for r in &records {
                e.export(r).unwrap();
            }
            e.finalize().unwrap();
        }

        // 第二次 run：overwrite=false、append=false → 走 DELETE FROM 清空已有数据
        // 然后写入同样 4 条 —— 期望最终行数 == 4 而非 8
        {
            let mut e =
                SqliteExporter::new(dbfile.to_string_lossy().into(), "clr_tbl".into(), false, false);
            e.initialize().unwrap();
            for r in &records {
                e.export(r).unwrap();
            }
            e.finalize().unwrap();
        }

        let conn = rusqlite::Connection::open(&dbfile).unwrap();
        let count: i64 = conn
            .query_row("SELECT COUNT(*) FROM clr_tbl", [], |r| r.get(0))
            .unwrap();
        assert_eq!(count, 4, "DELETE FROM should clear previous rows; got {count}");
    }
    ```

    **关于 "warn 路径" 的可观测验证：** 注入 rusqlite 错误需要拦截 Connection 行为，复杂度高。退而求其次：通过本任务的两个测试 + 代码审查（grep 验证 `log::warn!("sqlite clear failed` 字面存在）证明 warn 分支被构造正确。Verification 阶段以 grep + 编译通过 + match/if-let 分支结构正确为准，不强求运行时触发 warn 分支的测试（D-01 对 warn 路径的要求是"被记录而非丢弃"，结构正确即满足）。
  </action>
  <verify>
    <automated>cargo test --lib exporter::sqlite::tests::test_sqlite_initialize 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    <!-- Blocker 1 修复：仅验证目标文件 sqlite.rs 中的目标模式，不做全项目扫描（项目其他文件中存在合法的 let _ = 用法，不在本计划清理范围） -->
    - `grep -c 'let _ = conn.execute' src/exporter/sqlite.rs` 返回 0（sqlite.rs 中 DEBT-01 静默 conn.execute 模式已消除）
    - `grep -n 'fn handle_delete_clear_result' src/exporter/sqlite.rs | wc -l` ≥ 1（Blocker 2 修复：helper 函数已提取，保证 initialize() ≤ 40 行）
    - `grep -cE 'rusqlite::Error::SqliteFailure.*Some\(ref msg\)' src/exporter/sqlite.rs` 返回 ≥ 1
    - `grep -c '"no such table"' src/exporter/sqlite.rs` 返回 ≥ 1
    - `grep -c 'log::warn!("sqlite clear failed' src/exporter/sqlite.rs` 返回 1
    - `grep -c '"DELETE FROM "' src/exporter/sqlite.rs` 返回 ≥ 1（DEBT-02 在 DELETE 处的双引号转义到位 —— 形态为 `"DELETE FROM "` 后接 `\"{}\"`）
    - `grep -v '^[[:space:]]*//' src/exporter/sqlite.rs | grep -cE 'DELETE FROM \{\}[^"]'` 返回 0（旧裸拼接 `DELETE FROM {}` 已无）
    - `cargo test --lib exporter::sqlite::tests::test_sqlite_initialize_silent_when_table_missing 2>&1 | grep -c "test result: ok"` 返回 1
    - `cargo test --lib exporter::sqlite::tests::test_sqlite_initialize_clears_existing_table_via_delete 2>&1 | grep -c "test result: ok"` 返回 1
    - `cargo test 2>&1 | tail -5 | grep -cE "test result: ok\."` ≥ 1（全项目 cargo test 全绿）
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -cE "warning|error"` 返回 0
    - `cargo fmt -- --check 2>&1 | wc -l` 返回 0
    - 函数长度自检：执行 `awk '/fn initialize/,/^    }/' src/exporter/sqlite.rs | wc -l` 应返回 ≤ 40（initialize() 函数体含签名行不超过 40 行；如超出则需进一步提取 prepare_target_table helper）
  </acceptance_criteria>
  <done>
    - line 265 的 `let _ = conn.execute(DELETE FROM ...)` 已替换为对 `Self::handle_delete_clear_result(...)` 的调用
    - 私有 helper `fn handle_delete_clear_result(result, table_name)` 已新增，封装"OK / no such table 静默 / 其他 Err warn"三分支语义
    - DELETE FROM 字符串使用 `\"{}\"` 双引号转义
    - initialize() 函数体仍 ≤ 40 行（满足 CLAUDE.md 函数长度约束）
    - 2 项新单元测试通过（silent + clear-existing）
    - 全项目 cargo test 通过，clippy 零警告，cargo fmt 无改动
    - sqlite.rs 中 `let _ = conn.execute` 模式已消除
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → config.toml → SqliteExporter | 用户配置文件中 `[exporter.sqlite].table_name` 字符串经 toml 解析后流入 SqliteExporter，最终被拼接进 5 处 SQL 字符串（DROP/DELETE/CREATE/INSERT × 全量与投影路径） |
| SqliteExporter → rusqlite::Connection | 拼接后的 SQL 字符串通过 `conn.execute()` 提交 SQLite，无 prepare 参数化（标识符不能用 `?` 占位符） |
| rusqlite::Connection → SQLite engine | SQLite 会执行任何语法合法的 SQL —— 信任边界依赖上游的输入校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `SqliteExporter::new`/`initialize`/`build_insert_sql`/`build_create_sql` 中 5 处 `format!("... {table_name} ...")` | mitigate | DEBT-02 修复：(a) `SqliteExporter::validate()` 强制 ASCII 标识符白名单 `^[a-zA-Z_][a-zA-Z0-9_]*$`，拒绝任何含分号、引号、反斜杠、空格、Unicode 的输入；(b) 所有 5 处 DDL/DML 字符串改用 SQLite 双引号标识符语法 `"{table_name}"` 转义。两层防御构成纵深：即使 validate 因配置加载顺序问题被绕过（如直接构造 SqliteExporter::new()），双引号仍能限制注入语法范围；validate 通过则双引号内的内容已被白名单证明为纯 ASCII alphanumeric+underscore，SQLite 标识符语义下绝无歧义解析。Task 1 实现 validate，Task 2 + Task 3 实现全部 5 处转义 |
| T-07-02 | Repudiation / Information Disclosure | `initialize()` line 265 的 `let _ = conn.execute(DELETE FROM ...)` | mitigate | DEBT-01 修复：将静默丢弃改为显式 `match`（封装在 `handle_delete_clear_result` helper 中），区分 (a) `SqliteFailure` + msg 含 "no such table"（首次运行的预期行为，静默）与 (b) 任何其他错误（通过 `log::warn!` 写入应用日志，由 `[logging] file` 配置路由到 `logs/sqllog2db.log`）。修复后任何真实 SQLite 错误都可在日志中追溯，运维可检测到表锁、磁盘满、损坏等异常。Task 3 实现 |
| T-07-03 | Denial of Service | `initialize()` 中 warn 分支后继续运行 | accept | D-01 明确决策：DELETE 失败属软失败（数据未清空但导出仍可进行 —— 后续 INSERT 会追加到已有数据上）。这是**有意行为**，理由：(a) 让用户优先看到数据而非中止；(b) 真实导出错误会在 `export()` 路径再次暴露并被 export 的 error log 路径捕获；(c) `[error] file` 路径已建立完善的 per-record 错误处理。残留风险：用户可能在 warn 被忽略时得到 stale + new 混合数据，但 ROADMAP 成功标准 #2 仅要求"错误信息写入 error log 而非被静默丢弃"，已满足。 |
| T-07-04 | Elevation of Privilege | 通过 `table_name` 注入触发跨表 DROP/UPDATE/SELECT 等任意 SQL | mitigate | 由 T-07-01 的 mitigation 完全覆盖：validate 的 ASCII 白名单使任何含分号或空格的 payload（如 `tbl; DROP TABLE secrets;`）在启动时即被拒绝；双引号转义使 SQLite 解析器将引号内的所有字符视为单一标识符 token，即使 validate 被绕过也无法注入新语句。 |

**Residual Risks:**
- T-07-03（DELETE 软失败可能导致 stale+new 数据混合）已被显式接受，由产品决策背书。
- 双引号自身在 SQLite 中可通过 `""` 双写转义，但 D-04 + D-06 的 ASCII 白名单已禁止 `"` 出现在 `table_name` 中，因此运行时无需额外的 `replace("\"", "\"\"")` 防御层。
</threat_model>

<verification>
**整体执行后验证脚本（executor 在 Task 3 完成后逐条执行）：**

```bash
# 1. 编译 + 测试 + lint + fmt 三连
cargo test 2>&1 | tail -5
cargo clippy --all-targets -- -D warnings 2>&1 | tail -5
cargo fmt -- --check

# 2. validate CLI 实际行为验证（DEBT-02 端到端）
# 创建临时合法 + 非法配置各一份，分别 cargo run -- validate
mkdir -p /tmp/sqllog2db-p7-test
cat > /tmp/sqllog2db-p7-test/legal.toml <<EOF
[sqllog]
directory = "./sqllogs"

[exporter.sqlite]
database_url = "/tmp/legal.db"
table_name = "valid_tbl"
overwrite = true
append = false
batch_size = 10000

[logging]
file = "/tmp/sqllog2db-p7-test/app.log"
level = "info"
retention_days = 7

[error]
file = "/tmp/sqllog2db-p7-test/err.log"
EOF

cat > /tmp/sqllog2db-p7-test/illegal.toml <<EOF
[sqllog]
directory = "./sqllogs"

[exporter.sqlite]
database_url = "/tmp/illegal.db"
table_name = "tbl;DROP TABLE x"
overwrite = true
append = false
batch_size = 10000

[logging]
file = "/tmp/sqllog2db-p7-test/app.log"
level = "info"
retention_days = 7

[error]
file = "/tmp/sqllog2db-p7-test/err.log"
EOF

# 期望：legal 通过，illegal 报错并退出非零，错误信息含 "ASCII identifiers only"
cargo run -- validate -c /tmp/sqllog2db-p7-test/legal.toml
echo "Exit: $?"  # 期望 0
cargo run -- validate -c /tmp/sqllog2db-p7-test/illegal.toml
echo "Exit: $?"  # 期望非 0

# 3. sqlite.rs 中静默 conn.execute 模式清零检查（仅目标文件，不全项目）
grep -c 'let _ = conn.execute' src/exporter/sqlite.rs
# 期望: 0

# 4. helper 函数存在性
grep -n 'fn handle_delete_clear_result' src/exporter/sqlite.rs
# 期望: 至少 1 行命中（函数已提取）

# 5. 双引号转义 grep 计数总览（使用单引号包裹的字符串，避免 \" 转义歧义）
grep -cE '"INSERT INTO "|"CREATE TABLE IF NOT EXISTS "|"DROP TABLE IF EXISTS "|"DELETE FROM "' src/exporter/sqlite.rs
# 期望: ≥ 5（4 个 DDL 形态 + INSERT 投影路径 = 5 个 format! 实例）

# 6. initialize() 函数体长度自检（CLAUDE.md ≤ 40 行约束）
awk '/fn initialize/,/^    }/' src/exporter/sqlite.rs | wc -l
# 期望: ≤ 40
```
</verification>

<success_criteria>
全部 4 条 ROADMAP Phase 7 成功标准必须全部为 TRUE：

1. **首次运行无害错误静默继续**：DB 文件存在但表不存在的首次 `cargo run -- run -c config.toml` 不打印 warn、不中止。由 Task 3 + test_sqlite_initialize_silent_when_table_missing 保证。

2. **真实错误写入 error log**：DELETE 触发非 "no such table" 错误时通过 `log::warn!` 写入应用日志（由 `[logging] file` 配置路由）。**注：** ROADMAP 成功标准 #2 措辞为 "error log"，本计划在 must_haves.truths 第 4 条与本条均明确这是 [logging] file 路径（per CONTEXT.md D-02 锁定决策——最小侵入性，不传入额外的 error writer 句柄）。由 Task 3 的 helper 函数 `handle_delete_clear_result` 中的 `if let Err(e)` 分支 + grep 验证 `log::warn!("sqlite clear failed` 字面存在保证。

3. **非法 table_name 启动报错**：`cargo run -- validate -c <含特殊字符的 config>` 退出码非 0，错误信息含 `exporter.sqlite.table_name` 与 `ASCII identifiers only`。由 Task 1 的 validate 新增校验 + verification 脚本第 2 步 e2e 验证保证。

4. **DDL 全量双引号转义**：grep 验证 `src/exporter/sqlite.rs` 中 INSERT/CREATE/DROP/DELETE 四类语句全部使用 `\"{table_name}\"` 形式（共 5 个 format! 实例：new + build_insert_sql×2 + build_create_sql + DROP + DELETE）。由 Task 2 + Task 3 协作完成，acceptance_criteria 的 grep 计数保证。

工程质量门：
- `cargo test` 全绿（含至少 11 项新增测试：8 项 validate + 1 项 quoted-table + 2 项 silent/clear）
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt -- --check` 无改动
- sqlite.rs 中 `let _ = conn.execute` 模式清零（`grep -c 'let _ = conn.execute' src/exporter/sqlite.rs` == 0）
- initialize() 函数体 ≤ 40 行（CLAUDE.md 函数长度规范）
</success_criteria>

<output>
执行完毕后，创建 `.planning/phases/07-tech-debt-fix/07-01-SUMMARY.md`，包含：
- 实际修改的行数与位置（含 helper 函数 handle_delete_clear_result 的提取位置与行数）
- 新增测试列表与全部 cargo test 输出
- clippy / fmt 输出快照
- ROADMAP 4 条成功标准的逐条 TRUE 证据（命令 + 输出）
- ConfigError 与 log::warn! 的实际样例输出（如 cargo run -- validate 的 stderr 截取）
- initialize() 函数体最终行数（证明 ≤ 40 行）
</output>
</content>
</invoke>