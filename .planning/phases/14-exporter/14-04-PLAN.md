---
phase: 14-exporter
plan: 04
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
files_modified:
  - src/cli/run.rs
autonomous: true
requirements:
  - TMPL-04
tags:
  - cli
  - orchestration
  - rust
must_haves:
  truths:
    - "顺序路径在 exporter_manager.finalize() 之后、template_stats 计算之后调用 exporter_manager.write_template_stats(stats, None)"
    - "并行路径在 process_csv_parallel() 返回、parallel_agg.finalize() 之后用临时 ExporterManager::from_csv 调用 write_template_stats(stats, Some(csv_cfg.file))"
    - "未启用模板统计（template_agg/parallel_agg 为 None）时不调用 write_template_stats，未生成 sql_templates 表或伴随文件（成功标准 SC-4 / TMPL-04-D）"
    - "数据完整性：write_template_stats 调用严格位于主导出 finalize 之后；主导出中断（interrupted=true 或错误）时 template_stats 为 None（中断时 template_agg 不调用 finalize），无伴随文件生成（成功标准 SC-3 / D-11）"
    - "Err 沿调用链向上传播，整体退出码非零（D-04）"
  artifacts:
    - path: "src/cli/run.rs"
      provides: "两条路径 write_template_stats 调用点 + 1 项集成测试"
      contains: "exporter_manager.write_template_stats"
    - path: "src/cli/run.rs"
      provides: "并行路径临时 ExporterManager 构造"
      contains: "ExporterManager::from_csv"
  key_links:
    - from: "顺序路径"
      to: "exporter_manager.write_template_stats"
      via: "finalize 后顺序调用"
      pattern: "exporter_manager\\.write_template_stats\\(stats, None\\)"
    - from: "并行路径"
      to: "ExporterManager::from_csv + write_template_stats"
      via: "临时 manager + final_path"
      pattern: "ExporterManager::from_csv"
---

<objective>
在 `src/cli/run.rs` 的两条执行路径（顺序 ~L886、并行 ~L791）插入 `exporter_manager.write_template_stats(...)` 调用。顺序路径直接调用既有 `exporter_manager`；并行路径在 `handle_run()` 主线程构造临时 `ExporterManager::from_csv` 调用 write_template_stats（D-02：唯一调用点必须经 ExporterManager；D-03：传 final_path=Some(csv_cfg.file)）。同时新增 1 项集成测试验证 `template_analysis.enabled = false` 时不生成 sql_templates 表/伴随文件（成功标准 SC-4）。

Purpose: 完成 Phase 14 集成接入，让 TMPL-04 端到端在 `cargo run -- run -c config.toml` 中生效；TMPL-04-C、TMPL-04-D 测试映射。
Output: `src/cli/run.rs` 顺序/并行两处插入 + 1 个集成测试；`cargo test integration` 全绿；`cargo run` 实际 e2e 行为符合 ROADMAP 全部 4 条成功标准。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-exporter/14-CONTEXT.md
@.planning/phases/14-exporter/14-RESEARCH.md
@.planning/phases/14-exporter/14-PATTERNS.md
@.planning/phases/14-exporter/14-VALIDATION.md
@.planning/phases/14-exporter/14-01-SUMMARY.md
@.planning/phases/14-exporter/14-02-SUMMARY.md
@.planning/phases/14-exporter/14-03-SUMMARY.md
@src/cli/run.rs
@src/exporter/mod.rs

<interfaces>
<!-- 关键已有代码，executor 必须按此使用，不要再去 codebase 探索 -->

From src/cli/run.rs（现有代码 / 已确认行号）:
- L5：`use crate::exporter::{CsvExporter, ExporterManager};` 已存在
- L17：`use std::path::{Path, PathBuf};` 已存在
- 并行路径（L771-L807）：返回 `(processed_files, parallel_skipped, parallel_agg)`；L791-L795 是 template_stats 计算点
  - `let template_stats = parallel_agg.map(TemplateAggregator::finalize);`
  - `if let Some(ref stats) = template_stats { info!("Template analysis: {} unique templates", stats.len()); }` ← 在此 if 块内插入
- 顺序路径（L808-L896）：`exporter_manager` 在 scope 内可变；L886-L895 是 finalize + template_stats 段
  - `exporter_manager.finalize()?;`
  - `if !quiet { exporter_manager.log_stats(); }`
  - `let template_stats = template_agg.map(TemplateAggregator::finalize);`
  - `if let Some(ref stats) = template_stats { info!("Template analysis: {} unique templates", stats.len()); }` ← 在此 if 块内插入
- `final_cfg: &Config`（函数参数）：可访问 `final_cfg.exporter.csv.as_ref()` 得 `Option<&CsvExporterCfg>`，其字段 `file: String`

From src/exporter/mod.rs（Plan 01 完成后）:
- `impl ExporterManager { pub fn from_csv(exporter: CsvExporter) -> Self; pub fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], final_path: Option<&std::path::Path>) -> Result<()> }`
- `CsvExporter::new(path: impl AsRef<Path>) -> Self`
- ExporterKind dispatch 已覆盖 Csv/Sqlite/DryRun 三种

From Plan 02/03:
- SqliteExporter::write_template_stats 实现完整（顺序路径会用到）
- CsvExporter::write_template_stats 实现完整（顺序路径 + 并行路径都用到）
- DryRunExporter::write_template_stats 为 no-op + info!（dry-run 顺序路径会调用）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 run.rs 顺序与并行两条路径接入 write_template_stats() 调用，并新增 disabled-state 集成测试</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（必须读取至少 L1-L30 imports、L760-L900 两条路径区域、L900-end handle_run 末尾、tests 模块结构 + 至少一个已有集成测试如 `test_run_basic_csv_export`）
    - src/exporter/mod.rs（确认 Plan 01 完成的 ExporterManager::from_csv 与 write_template_stats 签名）
    - .planning/phases/14-exporter/14-CONTEXT.md（D-02、D-03、D-04、D-11 决策）
    - .planning/phases/14-exporter/14-RESEARCH.md（"并行路径的关键设计决策" 与 "Open Question 1" 的临时 ExporterManager 推荐方案）
    - .planning/phases/14-exporter/14-PATTERNS.md（src/cli/run.rs 一节，含顺序/并行插入示例）
    - .planning/phases/14-exporter/14-01-SUMMARY.md
    - .planning/phases/14-exporter/14-02-SUMMARY.md
    - .planning/phases/14-exporter/14-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1（test_no_template_stats_when_disabled，TMPL-04-D 集成）：用 tempfile 准备一个简单的 .log 文件（至少 1 行可解析的达梦日志记录），构造 Config（CSV exporter，`features.template_analysis.enabled = false` 或不存在 `[features.template_analysis]` 段），调用 `handle_run` 或同等 entry point；运行后验证：(a) 主 CSV 文件存在；(b) 同目录 `<basename>_templates.csv` 不存在。可同时测一个 SQLite 版本：验证 `sql_templates` 表不存在（`SELECT name FROM sqlite_master WHERE type='table' AND name='sql_templates'` 返回 0 行）
    - 已被 Plan 02 / Plan 03 覆盖的 enabled 情形（基础写入、overwrite、append、final_path 推导）不重复在 run.rs 测试中验证；本 plan 仅补充 disabled-state 的端到端集成验证
  </behavior>
  <action>
    在 `src/cli/run.rs` 中做 2 处插入 + 1 处新增测试：

    1. **顺序路径插入（约 L891-L895 的 if 块内）**：找到现有代码段：

       ```
       let template_stats = template_agg.map(TemplateAggregator::finalize);
       if let Some(ref stats) = template_stats {
           info!("Template analysis: {} unique templates", stats.len());
       }
       ```

       在该 `if let Some(ref stats) = template_stats { ... }` 块的 `info!(...)` 之后追加一行：`exporter_manager.write_template_stats(stats, None)?;`。`exporter_manager` 在此 scope 内仍可变（顺序路径分支声明为 `let mut exporter_manager = ...`）。`None` 作为 final_path：CsvExporter 用 self.path 推导（D-09 顺序路径分支），SqliteExporter 忽略此参数。

    2. **并行路径插入（约 L791-L795 的 if 块内）**：找到现有代码段：

       ```
       let template_stats = parallel_agg.map(TemplateAggregator::finalize);
       if let Some(ref stats) = template_stats {
           info!("Template analysis: {} unique templates", stats.len());
       }
       ```

       在该 if 块的 `info!(...)` 之后追加临时 ExporterManager 构造 + 调用（参照 14-PATTERNS.md run.rs 一节的"插入后（并行路径）"模板）。具体步骤：
       - `if let Some(csv_cfg) = final_cfg.exporter.csv.as_ref() { ... }`（并行路径仅在 CSV exporter 下激活，与现有 process_csv_parallel 触发条件一致；SQLite 走顺序路径）
       - 块内：`let tmp_csv = CsvExporter::new(&csv_cfg.file);`
       - `let mut tmp_em = ExporterManager::from_csv(tmp_csv);`
       - `tmp_em.write_template_stats(stats, Some(Path::new(&csv_cfg.file)))?;`
       - 注意：临时 CsvExporter 不调用 initialize/finalize；write_template_stats 实现内部不依赖 self.writer，只依赖 self.path（仅当 final_path=None 时 fallback）。本调用传 Some，self.path 仅作未使用 fallback 占位。
       - `Path` 已通过 L17 `use std::path::{Path, PathBuf};` 引入

    3. **新增集成测试 test_no_template_stats_when_disabled**：在 `src/cli/run.rs` 的 `#[cfg(test)] mod tests` 块内（参照已有集成测试模式）：
       - 用 tempfile::TempDir 建临时目录
       - 写入测试日志文件 `input.log`（1-3 条可解析的达梦日志，需含 EXECTIME 等元数据，可复用已有测试中的日志样本，例如：`"2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT 1. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n"`）
       - 构造 Config：`exporter.csv = Some(...)` 指向 `<tmp>/out.csv`；`sqllog.path = <tmp>` 指向日志目录；`features.template_analysis` 不设置或显式 `enabled = false`
       - 调用 handle_run（或现有测试已使用的 entry，如直接调用并附上 dry_run/quiet/限制参数），不传中断信号
       - 断言：`Path::new(<tmp>/out.csv).exists() == true`（主 CSV 写出成功）；`Path::new(<tmp>/out_templates.csv).exists() == false`（伴随文件未生成）

    禁止：
    - 改动 `template_stats` 的计算逻辑（保持 `template_agg.map(TemplateAggregator::finalize)`）
    - 把 write_template_stats 调用置于 `if let Some(ref stats)` 块外（破坏 disabled state 的天然保护）
    - 改动 process_csv_parallel 的内部 ExporterManager 流程（任何 task 内的 em 已 finalize/drop）
    - 用 unwrap()/expect() 替代 `?` 错误传播
    - 改动 handle_run 函数签名或返回类型
    - 在并行路径调用 tmp_em.initialize()（无必要，writer 不创建）
    - 改动 final_cfg 任何字段
  </action>
  <verify>
    <automated>cargo test --lib cli::run 2>&1 | tail -30 && cargo test test_no_template_stats_when_disabled 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "exporter_manager\\.write_template_stats\\(stats, None\\)" src/cli/run.rs` 命中 1 处（顺序路径）
    - `grep -nE "ExporterManager::from_csv" src/cli/run.rs` 至少命中 1 处（并行路径）
    - `grep -nE "tmp_em\\.write_template_stats\\(stats, Some\\(Path::new\\(&csv_cfg\\.file\\)\\)\\)" src/cli/run.rs` 命中 1 处（并行路径完整调用）
    - `grep -E "fn test_no_template_stats_when_disabled" src/cli/run.rs` 命中 1 处
    - `cargo test test_no_template_stats_when_disabled` 退出码 0
    - `cargo test --lib cli::run` 全部通过（无现有测试回归）
    - `cargo test` 整体退出码 0（含 exporter::sqlite、exporter::csv 在 Plan 02/03 新增的测试）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt` 后无 diff
    - 行为验证（人工 spot check 或测试输出）：disabled 状态下 stdout 不包含 "sql_templates: .* rows written" 或 "Template companion CSV written"
  </acceptance_criteria>
  <done>
    顺序与并行两条路径均在 finalize 后正确调用 write_template_stats；disabled 集成测试通过；并行路径用临时 ExporterManager 满足 D-02 唯一调用点要求；clippy + 全部测试通过；e2e 行为：(1) SQLite 模式生成 sql_templates 表；(2) CSV 顺序模式生成 *_templates.csv；(3) CSV 并行模式生成最终 final_path 同目录的 *_templates.csv；(4) disabled 状态无 sql_templates 表/伴随文件——四条 ROADMAP 成功标准全部达成。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 配置文件 → 调用路径选择 | `final_cfg.exporter.csv` / `final_cfg.exporter.sqlite` 由 Config::validate_and_compile 校验，分支选择基于已校验配置 |
| 临时 ExporterManager 生命周期 | 并行路径构造的 tmp_em 在 if 块结束时 drop，自然释放文件句柄 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | 数据完整性（中断时不写伴随） | mitigate | write_template_stats 仅在 `if let Some(ref stats) = template_stats` 块内调用；中断/错误时 template_agg 不调用 finalize，stats 为 None，自动跳过（D-11 天然结构保证） |
| T-14-10 | Denial of Service | 并行路径临时 ExporterManager 资源 | accept | tmp_em 仅持有 PathBuf 与轻量结构，无打开的 writer/conn；scope 结束立即 drop |
| T-14-11 | Tampering | 并行路径 final_path 推导 | mitigate | 严格用 `Path::new(&csv_cfg.file)`（来自校验过的配置），不接受运行时变量 |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装新包 |
</threat_model>

<verification>
- `cargo test` 全绿（含 Phase 12/13 已有测试不退化、Plan 02/03 新增测试通过、本 plan 新增测试通过）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt` 后无 diff
- 手动验证（可选）：`cargo run -- run -c <enable-template-toml>` 后 SQLite/CSV 输出符合预期；`cargo run -- run -c <disable-template-toml>` 后无 sql_templates 表 / 伴随文件
</verification>

<success_criteria>
- ROADMAP SC-1：SQLite 导出后 sql_templates 表存在且包含全 10 列（由 Plan 02 + 本 plan 顺序接入保证）
- ROADMAP SC-2：CSV 导出后伴随文件存在且列结构一致（由 Plan 03 + 本 plan 顺序/并行接入保证）
- ROADMAP SC-3：write_template_stats 在主 exporter finalize 之后调用；中断时不被调用（由本 plan 调用点位置保证）
- ROADMAP SC-4：disabled 状态不创建表/伴随文件（由本 plan 新增集成测试覆盖）
- 全部测试与 clippy 通过
</success_criteria>

<output>
Create `.planning/phases/14-exporter/14-04-SUMMARY.md` when done with:
- Files modified（src/cli/run.rs）
- 顺序路径插入位置（行号）actual diff
- 并行路径插入位置（行号）actual diff
- Test name + status
- Affects: [handle_run orchestration]
- Provides: [end-to-end TMPL-04 wiring]
- Patterns: [conditional Some check + 临时 ExporterManager 构造]
</output>
