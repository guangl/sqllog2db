---
phase: 05-sqlite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
  - benches/bench_sqlite.rs
autonomous: true
requirements:
  - PERF-04
  - PERF-06

must_haves:
  truths:
    - "config.toml 可包含 batch_size 字段，SqliteExporter 反序列化后字段值生效"
    - "缺省 batch_size 时 serde 自动填 10_000，与现有 config.toml 向后兼容"
    - "bench_sqlite 包含 sqlite_single_row group，可用 make_config(..., 1) 触发单行提交对照"
    - "cargo test --lib -- config 全部通过（含 SqliteExporter 结构体字面量修复）"
  artifacts:
    - path: "src/config.rs"
      provides: "SqliteExporter.batch_size: usize 字段 + default_sqlite_batch_size() 函数"
      contains: "default_sqlite_batch_size"
    - path: "benches/bench_sqlite.rs"
      provides: "bench_sqlite_single_row 函数 + make_config batch_size 参数"
      contains: "bench_sqlite_single_row"
  key_links:
    - from: "src/config.rs SqliteExporter"
      to: "src/exporter/sqlite.rs SqliteExporter::from_config()"
      via: "config.batch_size 赋值给 exporter.batch_size（Plan 02 实现）"
      pattern: "batch_size"
    - from: "benches/bench_sqlite.rs make_config"
      to: "criterion sqlite_single_row group"
      via: "make_config(&sqllog_dir, &bench_dir, 1)"
      pattern: "sqlite_single_row"
---

<objective>
在 config.rs 为 SqliteExporter 添加 batch_size 配置字段，并在 bench_sqlite.rs 添加单行提交对照 benchmark group。

Purpose: batch_size 是 Plan 02 批量事务实现的配置入口；sqlite_single_row benchmark 是 PERF-04 的可量化对比基线。两项变更文件不重叠，本计划先行完成作为 Wave 1，Plan 02 依赖产出。

Output: config.rs 含 batch_size 字段（serde default 10_000）；bench_sqlite.rs 含 bench_sqlite_single_row 函数 + make_config 签名更新 + criterion_group! 注册。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sqlite/05-RESEARCH.md
@.planning/phases/05-sqlite/05-PATTERNS.md

<interfaces>
<!-- 当前 config.rs SqliteExporter 结构体（lines 342-351） -->
```rust
#[derive(Debug, Deserialize, Clone)]
pub struct SqliteExporter {
    pub database_url: String,
    #[serde(default = "default_table_name")]
    pub table_name: String,
    #[serde(default = "default_true")]
    pub overwrite: bool,
    #[serde(default)]
    pub append: bool,
}

impl Default for SqliteExporter {
    fn default() -> Self {
        Self {
            database_url: "export/sqllog2db.db".to_string(),
            table_name: "sqllog_records".to_string(),
            overwrite: true,
            append: false,
        }
    }
}
```

<!-- 现有 serde default 函数模式（config.rs） -->
```rust
fn default_table_name() -> String { "sqllog_records".to_string() }
fn default_true() -> bool { true }
// 新增遵循同一命名约定：default_{字段名}()
```

<!-- apply_one() 中 exporter.sqlite.append 分支之后插入（config.rs lines 170-175） -->
```rust
"exporter.sqlite.append" => {
    self.exporter
        .sqlite
        .get_or_insert_with(Default::default)
        .append = parse_bool(value)?;
}
// 在此行之后插入 "exporter.sqlite.batch_size" 分支
```

<!-- 现有 bench_sqlite.rs make_config 签名（line 33） -->
```rust
fn make_config(sqllog_dir: &Path, bench_dir: &Path) -> Config
```

<!-- 现有 criterion_group! 宏（line 133） -->
```rust
criterion_group!(benches, bench_sqlite_export, bench_sqlite_real_file);
```

<!-- handle_run 调用签名（bench_sqlite.rs lines 77-90） -->
```rust
handle_run(
    cfg,
    None,
    false,
    true,  // quiet=true
    &Arc::new(AtomicBool::new(false)),
    80,
    false,
    None,
    1,
)
.unwrap();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.rs — SqliteExporter 新增 batch_size 字段</name>
  <files>src/config.rs</files>
  <read_first>
    - src/config.rs (必读：了解完整结构体定义、Default impl、apply_one 方法、现有测试中 SqliteExporter 字面量位置)
  </read_first>
  <behavior>
    - SqliteExporter 结构体：新增 `pub batch_size: usize` 字段，位于 `append` 之后，带 `#[serde(default = "default_sqlite_batch_size")]`
    - 新增私有函数 `fn default_sqlite_batch_size() -> usize { 10_000 }`（紧跟 `default_table_name()` 之后）
    - `Default for SqliteExporter`：新增 `batch_size: 10_000`
    - `apply_one()` 方法：在 `"exporter.sqlite.append"` 分支之后新增 `"exporter.sqlite.batch_size"` 分支，解析方式：`value.parse::<usize>().map_err(...)`，错误包装为 `ConfigError::InvalidValue { field: "exporter.sqlite.batch_size", ... reason: "expected a positive integer" }`
    - 修复编译错误：全文搜索所有 `SqliteExporter {` 结构体字面量（config.rs 测试中约 3 处，src/exporter/mod.rs 约 1 处），补全 `batch_size: 10_000` 或改为 `..SqliteExporter::default()`
  </behavior>
  <action>
    读取 config.rs 全文后执行以下修改：

    1. 在 `SqliteExporter` struct 中 `append` 字段之后插入：
       ```rust
       #[serde(default = "default_sqlite_batch_size")]
       pub batch_size: usize,
       ```

    2. 在 `fn default_table_name()` 之后插入：
       ```rust
       fn default_sqlite_batch_size() -> usize {
           10_000
       }
       ```

    3. 在 `impl Default for SqliteExporter` 的 `append: false,` 之后插入：
       ```rust
       batch_size: 10_000,
       ```

    4. 在 `apply_one()` 方法的 `"exporter.sqlite.append"` 分支之后（`_ => return Err(unknown())` 之前）插入：
       ```rust
       "exporter.sqlite.batch_size" => {
           let parsed = value.parse::<usize>().map_err(|_| {
               Error::Config(ConfigError::InvalidValue {
                   field: "exporter.sqlite.batch_size".to_string(),
                   value: value.to_string(),
                   reason: "expected a positive integer".to_string(),
               })
           })?;
           self.exporter
               .sqlite
               .get_or_insert_with(Default::default)
               .batch_size = parsed;
       }
       ```

    5. 搜索所有 `SqliteExporter {` 结构体字面量（config.rs 测试 + src/exporter/mod.rs），逐一补全 `batch_size: 10_000,`。不使用 `..Default::default()` 展开（保持字段可见性）。

    6. 在 `impl SqliteExporter` 的 `validate()` 方法中（或 `Config::validate()` 内调用 SqliteExporter 的 validate 路径），追加以下校验（在现有 validate 逻辑之后，返回 `Ok(())` 之前）：
       ```rust
       if self.batch_size == 0 {
           return Err(ConfigError::InvalidValue {
               field: "exporter.sqlite.batch_size".to_string(),
               value: "0".to_string(),
               reason: "batch_size must be greater than 0".to_string(),
           });
       }
       ```
       这可防止 `row_count % batch_size` 在运行时出现除以零（T-05-02 缓解）。

    完成后运行 `cargo clippy --all-targets -- -D warnings` 和 `cargo fmt`。
  </action>
  <verify>
    <automated>cargo test --lib -- config 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch_size" /Users/guang/Projects/sqllog2db/src/config.rs` 输出包含：`pub batch_size: usize`、`default_sqlite_batch_size`、`batch_size: 10_000`（Default impl）、`"exporter.sqlite.batch_size"`（apply_one 分支）
    - `grep -n "batch_size" /Users/guang/Projects/sqllog2db/src/exporter/mod.rs` 输出包含至少 1 行（结构体字面量修复）
    - `grep -n "batch_size == 0" /Users/guang/Projects/sqllog2db/src/config.rs` 输出至少 1 行（validate() 中 batch_size > 0 校验）
    - `cargo test --lib -- config` 退出码为 0，输出含 `test result: ok`
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无警告
  </acceptance_criteria>
  <done>config.rs 中 SqliteExporter 含 batch_size 字段，serde 默认 10_000，apply_one 可解析 "exporter.sqlite.batch_size"，所有现有测试通过，clippy 无警告</done>
</task>

<task type="auto">
  <name>Task 2: bench_sqlite.rs — make_config 增加 batch_size 参数 + 新增 sqlite_single_row group</name>
  <files>benches/bench_sqlite.rs</files>
  <behavior>
    - make_config 签名变更：从 `fn make_config(sqllog_dir: &Path, bench_dir: &Path) -> Config` 改为 `fn make_config(sqllog_dir: &Path, bench_dir: &Path, batch_size: usize) -> Config`；TOML 模板 `[exporter.sqlite]` 区块末尾追加 `batch_size = {batch_size}` 插值
    - 现有两处 make_config 调用（bench_sqlite_export、bench_sqlite_real_file）补充第三个参数 `10_000`，编译无变化
    - 新增 `fn bench_sqlite_single_row(c: &mut Criterion)` benchmark group：group 名为 "sqlite_single_row"，sample_size=10，规模 [1_000, 10_000]，调用 `make_config(..., 1)`（batch_size=1 触发单行提交路径），通过 handle_run() 运行全流程
    - criterion_group! 宏注册新增 bench_sqlite_single_row（三个函数均注册）
    - 编译后 `cargo build --bench bench_sqlite` 退出码为 0，无警告
  </behavior>
  <read_first>
    - benches/bench_sqlite.rs (必读：了解 make_config 当前签名、bench_sqlite_export 完整模式、criterion_group! 宏位置)
  </read_first>
  <action>
    读取 bench_sqlite.rs 全文后执行以下修改：

    1. 修改 `make_config` 函数签名为：
       ```rust
       fn make_config(sqllog_dir: &Path, bench_dir: &Path, batch_size: usize) -> Config
       ```
       在 TOML 模板的 `[exporter.sqlite]` 区块末尾追加：
       ```
       batch_size = {batch_size}
       ```
       并在 `format!` 的具名参数中加入 `batch_size = batch_size,`。

    2. 将 `bench_sqlite_export` 中的 `make_config(&sqllog_dir, &bench_dir)` 改为 `make_config(&sqllog_dir, &bench_dir, 10_000)`。

    3. 将 `bench_sqlite_real_file` 中的 `make_config(&real_dir, &bench_dir)` 改为 `make_config(&real_dir, &bench_dir, 10_000)`。

    4. 在 `bench_sqlite_real_file` 函数结尾之后、`criterion_group!` 之前插入新函数：
       ```rust
       fn bench_sqlite_single_row(c: &mut Criterion) {
           let bench_dir = PathBuf::from("target/bench_sqlite_single_row");
           let sqllog_dir = bench_dir.join("sqllogs");
           fs::create_dir_all(&sqllog_dir).unwrap();

           let mut group = c.benchmark_group("sqlite_single_row");
           // 单行提交每次需要 fsync，采样数尽量小以控制总时间
           group.sample_size(10);

           for &n in &[1_000usize, 10_000] {
               fs::write(sqllog_dir.join("bench.log"), synthetic_log(n)).unwrap();
               // batch_size=1 触发每条 INSERT 独立 BEGIN/COMMIT（单行提交对照组）
               let cfg = make_config(&sqllog_dir, &bench_dir, 1);

               group.throughput(Throughput::Elements(n as u64));
               group.bench_with_input(BenchmarkId::from_parameter(n), &cfg, |b, cfg| {
                   b.iter(|| {
                       handle_run(
                           cfg,
                           None,
                           false,
                           true,
                           &Arc::new(AtomicBool::new(false)),
                           80,
                           false,
                           None,
                           1,
                       )
                       .unwrap();
                   });
               });
           }

           group.finish();
       }
       ```

    5. 更新 `criterion_group!` 宏：
       ```rust
       criterion_group!(benches, bench_sqlite_export, bench_sqlite_single_row, bench_sqlite_real_file);
       ```

    完成后运行 `cargo clippy --all-targets -- -D warnings` 和 `cargo fmt`。
  </action>
  <verify>
    <automated>cargo build --bench bench_sqlite 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "batch_size" /Users/guang/Projects/sqllog2db/benches/bench_sqlite.rs` 输出包含：函数签名 `batch_size: usize`、TOML 模板行 `batch_size = {batch_size}`、`make_config(..., 10_000)` 调用（2 处）、`make_config(..., 1)` 调用（1 处，在 single_row 函数中）
    - `grep -n "sqlite_single_row" /Users/guang/Projects/sqllog2db/benches/bench_sqlite.rs` 输出包含：函数定义 `fn bench_sqlite_single_row` 和 `criterion_group!` 注册
    - `cargo build --bench bench_sqlite` 退出码为 0，无编译错误
    - `cargo clippy --all-targets -- -D warnings` 退出码为 0，无警告
  </acceptance_criteria>
  <done>bench_sqlite.rs 的 make_config 携带 batch_size 参数，sqlite_single_row benchmark group 已注册，编译通过，clippy 无警告</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → SqliteExporter | 用户可控的 batch_size 字段值，在 apply_one 中通过字符串解析进入程序 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | apply_one batch_size 解析 | mitigate | 用 `value.parse::<usize>()` 限定为正整数，解析失败返回 `ConfigError::InvalidValue`，不接受负数或非数字 |
| T-05-02 | Denial of Service | batch_size = 0 导致除以零 | mitigate | Plan 02 的 `batch_commit_if_needed` 使用 `row_count % batch_size`；在 config validate() 中增加 `batch_size > 0` 断言（Task 1 已通过 usize 类型排除负数，0 的校验在 Plan 02 validate() 中处理）|
| T-05-03 | Information Disclosure | bench_sqlite_single_row 在 target/ 生成 .db 文件 | accept | 仅本机 benchmark 产物，无 PII，target/ 已在 .gitignore |
</threat_model>

<verification>
Plan 01 完成后整体验证：

```bash
cargo test --lib -- config
cargo clippy --all-targets -- -D warnings
cargo build --bench bench_sqlite
```

三个命令均退出码为 0。
</verification>

<success_criteria>
- SqliteExporter.batch_size 字段存在，serde default 10_000，apply_one 支持 "exporter.sqlite.batch_size" key
- bench_sqlite.rs 的 make_config 签名含 batch_size: usize 参数
- sqlite_single_row benchmark group 已注册到 criterion_group!
- cargo test --lib -- config 全通过
- cargo clippy --all-targets -- -D warnings 零警告
- cargo build --bench bench_sqlite 编译成功
</success_criteria>

<output>
完成后创建 `.planning/phases/05-sqlite/05-01-SUMMARY.md`
</output>
