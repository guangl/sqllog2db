---
phase: 09-cli
plan: 05
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04]
files_modified:
  - src/config.rs
  - src/main.rs
  - src/cli/run.rs
  - benches/BENCHMARKS.md
autonomous: true
requirements: [PERF-11]
gap_closure: true

must_haves:
  truths:
    - "Config::validate_and_compile() 函数存在并返回 Result<Option<(CompiledMetaFilters, CompiledSqlFilters)>>"
    - "main.rs 中 run 子命令调用 validate_and_compile() 而非 validate()，并捕获编译结果"
    - "handle_run() 签名扩展，接收预编译的 Option<(CompiledMetaFilters, CompiledSqlFilters)> 参数"
    - "build_pipeline() 改为接收预编译的 CompiledMetaFilters，不再调用 try_from_meta()"
    - "FilterProcessor::try_new() 改为 FilterProcessor::new(compiled_meta, filter)，不再调用 try_from_meta()"
    - "run.rs 中 compiled_record_sql 改用传入的 CompiledSqlFilters，不再调用 try_from_sql_filters()"
    - "在 run 子命令完整执行路径中，每个 regex 字段只被 Regex::new() 调用一次"
    - "validate 子命令保持原 validate() 调用不变，验证结果丢弃（独立路径）"
    - "benches/BENCHMARKS.md 错误断言被修正为可观测的验证方法（计数 try_from_meta 调用次数而非旧 API 名）"
    - "cargo test 全部通过（≥651 测试），cargo clippy --all-targets -- -D warnings 无 error"
  artifacts:
    - path: "src/config.rs"
      provides: "Config::validate_and_compile 方法"
      contains: "pub fn validate_and_compile"
    - path: "src/main.rs"
      provides: "run 子命令调用 validate_and_compile 并传递结果到 handle_run"
      contains: "validate_and_compile"
    - path: "src/cli/run.rs"
      provides: "handle_run 接受预编译过滤器并传递给 build_pipeline；FilterProcessor::new 接受预编译参数"
      contains: "compiled_filters"
    - path: "benches/BENCHMARKS.md"
      provides: "更正后的双重编译验证断言"
      contains: "validate_and_compile"
  key_links:
    - from: "main.rs run 子命令分支"
      to: "Config::validate_and_compile"
      via: "let compiled = cfg.validate_and_compile()?"
      pattern: "validate_and_compile"
    - from: "main.rs"
      to: "handle_run"
      via: "传递 compiled 作为参数"
      pattern: "handle_run.*compiled"
    - from: "handle_run"
      to: "build_pipeline"
      via: "传递 compiled_meta 引用"
      pattern: "build_pipeline.*compiled"
    - from: "build_pipeline"
      to: "FilterProcessor::new"
      via: "传递克隆的 CompiledMetaFilters"
      pattern: "FilterProcessor::new"
    - from: "handle_run"
      to: "sql_record_filter"
      via: "从入参 compiled_sql 获取，不调用 try_from_sql_filters"
      pattern: "sql_record_filter"
---

<objective>
关闭 VERIFICATION.md SC-2 BLOCKER：实现 `Config::validate_and_compile()` 统一接口，将编译结果从 `main.rs` 一直贯通至 `build_pipeline` 与 SQL 过滤器初始化点，使 `run` 子命令路径中每个 regex 字段只调用一次 `Regex::new()`，彻底消除双重编译。

Purpose: ROADMAP SC-2 与 PERF-11 明确要求"`validate_and_compile()` 统一接口实现：regex 由单次编译结果同时用于验证与运行"。当前 `run` 路径中 `try_from_meta` 与 `try_from_sql_filters` 各被调用两次（一次在 `Config::validate()` 中丢弃结果，一次在 `FilterProcessor::try_new` / `handle_run` 中编译为运行使用）。本 Plan 通过引入 `validate_and_compile()` 并向下传递编译结果消除该重复。

Output: 更新后的 src/config.rs（新增 validate_and_compile）、src/main.rs（run 分支改用新方法并传参）、src/cli/run.rs（handle_run/build_pipeline/FilterProcessor 接受预编译参数），以及修正 benches/BENCHMARKS.md 中失效的"双重编译已消除"验证断言。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cli/09-CONTEXT.md
@.planning/phases/09-cli/09-PATTERNS.md
@.planning/phases/09-cli/09-VERIFICATION.md
@.planning/phases/09-cli/09-03-SUMMARY.md
@.planning/phases/09-cli/09-04-SUMMARY.md

<interfaces>
<!-- 现有已存在的接口（前 4 个 Plan 完成后的状态） -->

src/features/filters.rs 已存在接口（不修改）：
```rust
// 模块 src/features 已通过 pub use 导出
pub use filters::{CompiledMetaFilters, CompiledSqlFilters, FiltersFeature};

impl CompiledMetaFilters {
    pub fn try_from_meta(meta: &MetaFilters) -> crate::error::Result<Self>;
    pub fn has_any_filters(&self) -> bool;
    pub fn should_keep(&self, m: &RecordMeta<'_>) -> bool;
}

impl CompiledSqlFilters {
    pub fn try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self>;
    pub fn matches(&self, sql: &str) -> bool;
}

pub struct FiltersFeature {
    pub enable: bool,
    pub meta: MetaFilters,
    pub record_sql: SqlFilters,
    // ... 其他字段
}
impl FiltersFeature {
    pub fn has_filters(&self) -> bool;
    pub fn has_transaction_filters(&self) -> bool;
}
```

src/config.rs 当前 validate() 实现（L54-81，保留不变）：
```rust
pub fn validate(&self) -> Result<()> {
    self.logging.validate()?;
    self.exporter.validate()?;
    self.sqllog.validate()?;
    if let Some(filters) = &self.features.filters {
        if filters.enable {
            crate::features::filters::CompiledMetaFilters::try_from_meta(&filters.meta)?;
            crate::features::filters::CompiledSqlFilters::try_from_sql_filters(
                &filters.record_sql,
            )?;
        }
    }
    if let Some(names) = &self.features.fields {
        for name in names {
            if !crate::features::FIELD_NAMES.contains(&name.as_str()) {
                return Err(Error::Config(ConfigError::InvalidValue {
                    field: "features.fields".to_string(),
                    value: name.clone(),
                    reason: format!(
                        "unknown field '{name}'; valid fields: {}",
                        crate::features::FIELD_NAMES.join(", ")
                    ),
                }));
            }
        }
    }
    Ok(())
}
```
注意：`validate()` 必须保留——`validate` 子命令（main.rs:228）仍使用此方法。

src/main.rs 当前 run 分支调用（L177-224）：
```rust
let mut cfg = load_config(config)?;
// --output / --set / apply_date_range ...
cfg.validate()?;                       // ← 改为 validate_and_compile()
// ...
cli::run::handle_run(
    &cfg,
    *limit,
    *dry_run,
    cli.quiet,
    &interrupted,
    *progress_interval,
    *resume,
    state_file.as_deref(),
    jobs,
)                                       // ← 增加 compiled 参数
```

src/cli/run.rs 当前调用链：
```rust
fn build_pipeline(cfg: &Config) -> Result<Pipeline> {           // ← 改签名
    let mut pipeline = Pipeline::new();
    if let Some(f) = &cfg.features.filters {
        if f.has_filters() {
            pipeline.add(Box::new(FilterProcessor::try_new(f)?));  // ← 改用预编译
        }
    }
    Ok(pipeline)
}

impl FilterProcessor {
    fn try_new(filter: &crate::features::FiltersFeature) -> Result<Self> {
        let compiled_meta = CompiledMetaFilters::try_from_meta(&filter.meta)?;  // ← 删除编译
        // ...
    }
}

// handle_run 中（L671-677）：
let compiled_record_sql: Option<CompiledSqlFilters> = final_cfg
    .features
    .filters
    .as_ref()
    .filter(|f| f.enable && f.record_sql.has_filters())
    .map(|f| CompiledSqlFilters::try_from_sql_filters(&f.record_sql))  // ← 改用预编译
    .transpose()?;
```

特别注意 transaction filter 路径（run.rs L636-653）：当 `has_transaction_filters()` 为真时，`final_cfg` 被克隆并通过 `merge_found_trxids` 修改 `trxids`。此修改不影响 regex 字段（trxids 是 HashSet，不参与 regex 编译），所以预编译结果对 `final_cfg` 与原始 `cfg` 同样有效。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 src/config.rs 新增 validate_and_compile() 方法</name>
  <read_first>
    - src/config.rs（完整文件，重点看 L41-81 Config impl 块、L1-10 use 声明、L703-723 现有的 test_validate_invalid_regex_in_filters 测试模式）
    - src/features/mod.rs L1-3（确认 CompiledMetaFilters/CompiledSqlFilters 已通过 pub use 导出）
    - src/features/filters.rs L43-58（FiltersFeature 字段：enable / meta / record_sql 均为 pub）
    - .planning/phases/09-cli/09-VERIFICATION.md L131-150（CR-01 方案 B 推荐的方法签名）
  </read_first>
  <files>src/config.rs</files>
  <behavior>
    - Test 1: 默认 Config（无 filters）调用 validate_and_compile() 返回 Ok(None)
    - Test 2: features.filters 配置且 enable=true 且含合法 regex 时，返回 Ok(Some((meta, sql)))，且 meta/sql 内部 has_any_filters() 反映配置
    - Test 3: features.filters 配置但 enable=false 时，返回 Ok(None)（不编译，避免无效配置阻塞）
    - Test 4: features.filters 含非法 regex（如 "[invalid"）时，返回 Err，错误信息含字段名 "features.filters.usernames"
    - Test 5: logging.level=invalid 时，validate_and_compile() 返回 Err（继承 validate() 的所有非过滤器校验）
    - Test 6: features.fields 含未知字段名时，validate_and_compile() 返回 Err
    - Test 7: validate_and_compile() 与 validate() 在合法配置下均返回 Ok（同步行为，仅返回类型差异）
  </behavior>
  <action>
在 src/config.rs 的 `impl Config` 块中新增公开方法 `validate_and_compile`。位置：紧跟在现有 `pub fn validate(&self) -> Result<()>` 方法之后（L80 `}` 之后、L83 `pub fn apply_overrides` 之前）。

**方法实现（与 validate() 等价的非过滤器校验 + 返回编译结果）：**

```rust
/// 等价于 `validate()` 但额外返回已编译的过滤器对，供调用方复用，
/// 消除 `run` 子命令路径中 regex 的双重编译（per ROADMAP SC-2 / PERF-11）。
///
/// 返回值语义：
/// - `Ok(None)`：无过滤器配置 或 `features.filters.enable == false`
/// - `Ok(Some((meta, sql)))`：过滤器已编译，调用方可直接传递给 `build_pipeline`
/// - `Err(_)`：任意子校验失败（logging/exporter/sqllog/fields/正则编译）
pub fn validate_and_compile(
    &self,
) -> Result<Option<(crate::features::CompiledMetaFilters, crate::features::CompiledSqlFilters)>>
{
    self.logging.validate()?;
    self.exporter.validate()?;
    self.sqllog.validate()?;

    let compiled = if let Some(filters) = &self.features.filters {
        if filters.enable {
            let meta = crate::features::CompiledMetaFilters::try_from_meta(&filters.meta)?;
            let sql = crate::features::CompiledSqlFilters::try_from_sql_filters(
                &filters.record_sql,
            )?;
            Some((meta, sql))
        } else {
            None
        }
    } else {
        None
    };

    if let Some(names) = &self.features.fields {
        for name in names {
            if !crate::features::FIELD_NAMES.contains(&name.as_str()) {
                return Err(Error::Config(ConfigError::InvalidValue {
                    field: "features.fields".to_string(),
                    value: name.clone(),
                    reason: format!(
                        "unknown field '{name}'; valid fields: {}",
                        crate::features::FIELD_NAMES.join(", ")
                    ),
                }));
            }
        }
    }

    Ok(compiled)
}
```

**use 声明无需修改**：`crate::features::CompiledMetaFilters` 和 `crate::features::CompiledSqlFilters` 已通过 features/mod.rs 的 `pub use filters::{...}` 公开。函数体内使用全路径或新增 use 均可；本任务选择全路径以避免触动现有 use 顺序。

**保留 validate()**：不删除现有的 `pub fn validate(&self) -> Result<()>`。该方法仍由 `validate` 子命令（main.rs:228）调用，独立于 run 路径，不存在双重编译问题。

**新增单元测试（追加到 `#[cfg(test)] mod tests` 的末尾，L913 `}` 之前）：**

```rust
// ── validate_and_compile ───────────────────────────────────────
#[test]
fn test_validate_and_compile_default_returns_none() {
    let cfg = default_config();
    let result = cfg.validate_and_compile().expect("default config valid");
    assert!(result.is_none(), "默认 config 无 filters，应返回 None");
}

#[test]
fn test_validate_and_compile_filters_disabled_returns_none() {
    let toml = r#"
[sqllog]
path = "sqllogs"
[features.filters]
enable = false
usernames = ["^admin.*"]
[exporter.csv]
file = "out.csv"
"#;
    let cfg: Config = toml::from_str(toml).unwrap();
    let result = cfg.validate_and_compile().expect("config valid");
    assert!(result.is_none(), "filters.enable=false 时应返回 None");
}

#[test]
fn test_validate_and_compile_returns_compiled_pair() {
    let toml = r#"
[sqllog]
path = "sqllogs"
[features.filters]
enable = true
usernames = ["^admin.*"]
[exporter.csv]
file = "out.csv"
"#;
    let cfg: Config = toml::from_str(toml).unwrap();
    let result = cfg.validate_and_compile().expect("config valid");
    let pair = result.expect("filters.enable=true 应返回 Some");
    assert!(
        pair.0.has_any_filters(),
        "usernames 配置后 CompiledMetaFilters.has_any_filters 必为 true"
    );
}

#[test]
fn test_validate_and_compile_invalid_regex_returns_err() {
    let toml = r#"
[sqllog]
path = "sqllogs"
[features.filters]
enable = true
usernames = ["[invalid"]
[exporter.csv]
file = "out.csv"
"#;
    let cfg: Config = toml::from_str(toml).unwrap();
    let result = cfg.validate_and_compile();
    assert!(result.is_err());
    let err_msg = result.unwrap_err().to_string();
    assert!(
        err_msg.contains("features.filters.usernames"),
        "error should mention field name, got: {err_msg}"
    );
}

#[test]
fn test_validate_and_compile_invalid_log_level_returns_err() {
    let mut cfg = default_config();
    cfg.logging.level = "invalid".into();
    assert!(cfg.validate_and_compile().is_err());
}

#[test]
fn test_validate_and_compile_unknown_field_returns_err() {
    let mut cfg = default_config();
    cfg.features.fields = Some(vec!["nonexistent_field".into()]);
    assert!(cfg.validate_and_compile().is_err());
}

#[test]
fn test_validate_and_compile_matches_validate_on_ok() {
    // 合法配置：两个方法都应返回 Ok（行为等价，仅返回类型不同）
    let cfg = default_config();
    assert!(cfg.validate().is_ok());
    assert!(cfg.validate_and_compile().is_ok());
}
```

新方法签名与单元测试覆盖 4 个错误路径（regex 非法、log level 非法、字段名非法、未启用 filters）+ 2 个成功路径（默认 + 有 filter）+ 1 个等价性路径。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo build 2>&1 | grep -c "^error\[" | head -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test --lib config:: 2>&1 | tail -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn validate_and_compile" src/config.rs` 恰好返回 1 行
    - `grep -n "pub fn validate(&self)" src/config.rs` 仍恰好返回 1 行（旧方法未删除）
    - `cargo build 2>&1 | grep -c "^error\["` 输出 0
    - `cargo test --lib config::tests::test_validate_and_compile 2>&1 | grep "test result"` 显示 "7 passed; 0 failed"（7 个新测试全部通过）
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "^error"` 输出 0
    - `grep -n "validate_and_compile" src/config.rs | grep -v "^#\|//"` 至少返回 8 行（1 实现 + 7 测试调用）
  </acceptance_criteria>
  <done>Config 提供 validate_and_compile() 方法，返回 Result<Option<(CompiledMetaFilters, CompiledSqlFilters)>>，与 validate() 在错误路径上行为等价但额外暴露已编译结果；7 个新单元测试全部通过；clippy 干净；保留原 validate() 方法供 validate 子命令使用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.rs run 分支 + run.rs handle_run/build_pipeline/FilterProcessor 全链路接入预编译参数</name>
  <read_first>
    - src/main.rs L138-224（特别是 L177-224 的 Run 分支，确认 cfg.validate() 调用位置和 handle_run 的参数列表）
    - src/cli/run.rs L1-56（imports + build_pipeline + FilterProcessor::try_new）
    - src/cli/run.rs L603-680（handle_run 函数签名、final_cfg 派生逻辑、compiled_record_sql 当前的 try_from_sql_filters 调用）
    - src/cli/run.rs L833-890（现有集成测试 test_include_performance_metrics_false_csv_excludes_pm_columns，确认调用方式）
    - .planning/phases/09-cli/09-VERIFICATION.md L75-89（双重编译调用图）
  </read_first>
  <files>src/main.rs, src/cli/run.rs</files>
  <behavior>
    - Test 1: run 分支以合法配置调用时，每个 regex 字段在整个流程中只编译一次（行为不可直接观测，但通过保证调用点唯一性达成）
    - Test 2: build_pipeline 接受 Option<&CompiledMetaFilters> 参数；当输入 Some 时使用之，不再调用 try_from_meta
    - Test 3: handle_run 接受 Option<(CompiledMetaFilters, CompiledSqlFilters)>，当 transaction filter 改写 trxids 时仍使用同一编译结果（trxids 不影响 regex 编译）
    - Test 4: 现有集成测试 test_include_performance_metrics_false_csv_excludes_pm_columns 调整参数后通过，CSV header 行为不变
    - Test 5: 现有 651+ 测试无回归
  </behavior>
  <action>
本任务为单一原子改动：所有调用方同步更新以让 `validate_and_compile()` 的结果一路传递到 regex 实际消费点。分三处文件修改。

**修改 1：src/cli/run.rs — FilterProcessor::new 改签名（接受预编译 meta）**

将 `FilterProcessor::try_new` 替换为 `FilterProcessor::new`，接收已编译的 `CompiledMetaFilters` 与原始 `FiltersFeature`（仅取 start_ts/end_ts）：

```rust
impl FilterProcessor {
    /// 接受预编译的 `CompiledMetaFilters`（来自 `Config::validate_and_compile`），
    /// 避免在 `run` 路径中第二次调用 `Regex::new()`。
    fn new(
        compiled_meta: CompiledMetaFilters,
        filter: &crate::features::FiltersFeature,
    ) -> Self {
        let has_meta_filters = compiled_meta.has_any_filters();
        Self {
            compiled_meta,
            start_ts: filter.meta.start_ts.clone(),
            end_ts: filter.meta.end_ts.clone(),
            has_meta_filters,
        }
    }
}
```

**修改 2：src/cli/run.rs — build_pipeline 签名变化**

```rust
/// 构建处理器管线。
///
/// `compiled_meta` 由 `Config::validate_and_compile` 在主流程入口预编译；
/// 当输入 `None` 或 `has_filters() == false` 时返回空管线。
fn build_pipeline(
    cfg: &Config,
    compiled_meta: Option<CompiledMetaFilters>,
) -> Pipeline {
    let mut pipeline = Pipeline::new();
    if let (Some(f), Some(meta)) = (cfg.features.filters.as_ref(), compiled_meta) {
        if f.has_filters() {
            pipeline.add(Box::new(FilterProcessor::new(meta, f)));
        }
    }
    pipeline
}
```

注意：返回类型从 `Result<Pipeline>` 改为 `Pipeline`（不再有可失败操作；regex 编译已发生在 validate_and_compile 阶段）。

**修改 3：src/cli/run.rs — handle_run 接受预编译参数**

在 handle_run 函数签名末尾追加 `compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>` 参数：

```rust
pub fn handle_run(
    cfg: &Config,
    limit: Option<usize>,
    dry_run: bool,
    quiet: bool,
    interrupted: &Arc<AtomicBool>,
    progress_interval: u64,
    resume: bool,
    state_file_override: Option<&str>,
    jobs: usize,
    compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>,
) -> Result<()> {
    // 拆分入参：build_pipeline 消费 meta（Move），sql 保留供后续使用
    let (compiled_meta, compiled_sql) = match compiled_filters {
        Some((m, s)) => (Some(m), Some(s)),
        None => (None, None),
    };

    // ... 现有 log_files / resume_state / final_cfg 派生逻辑保持不变 ...

    // 改：build_pipeline 调用
    let pipeline = build_pipeline(final_cfg, compiled_meta);

    // ... field_mask / do_normalize / placeholder_override 保持不变 ...

    // 改：compiled_record_sql 来源——使用预编译结果而非再次调用 try_from_sql_filters
    let compiled_record_sql: Option<CompiledSqlFilters> = compiled_sql
        .filter(|_| {
            final_cfg
                .features
                .filters
                .as_ref()
                .is_some_and(|f| f.enable && f.record_sql.has_filters())
        });
    let sql_record_filter = compiled_record_sql.as_ref();

    // 后续代码保持不变 ...
}
```

注意点：
- `compiled_filters` 的 `Some` 包含一对元素；用 `match` 解构为两个 `Option`。
- `compiled_sql` 仅当 `final_cfg.features.filters` 存在且 `enable && record_sql.has_filters()` 时才用于 SQL 记录过滤；其他情况下传 None（与原行为一致）。
- `final_cfg` 在 transaction filter 路径中是 `cfg.clone()` 后的修改版（仅修改 `trxids` HashSet），不影响 regex 编译，因此使用上游预编译结果安全。

**修改 4：src/main.rs — Run 分支调用新方法并传递参数**

将（L177-224）的 Run 分支改为：

```rust
Some(cli::opts::Commands::Run {
    config,
    limit,
    dry_run,
    set,
    from,
    to,
    output,
    progress_interval,
    resume,
    state_file,
    jobs,
}) => {
    let mut cfg = load_config(config)?;
    let mut all_set = Vec::new();
    if let Some(out) = output {
        all_set.push(format!("exporter.csv.file={out}"));
    }
    all_set.extend_from_slice(set);
    cfg.apply_overrides(&all_set)?;
    apply_date_range(&mut cfg, from.as_deref(), to.as_deref());
    // 替换：validate() → validate_and_compile()
    let compiled_filters = cfg.validate_and_compile()?;

    apply_cli_flags_to_config(&mut cfg, cli.verbose, cli.quiet);
    logging::init_logging(&cfg.logging, false)?;
    info!("Application started");
    info!("Configuration validation passed");

    if !*dry_run {
        let pf = cli::preflight::check(&cfg);
        if pf.print_and_check() {
            std::process::exit(EXIT_CONFIG);
        }
    }

    let interrupted = Arc::new(AtomicBool::new(false));
    let interrupted_flag = Arc::clone(&interrupted);
    ctrlc::set_handler(move || {
        interrupted_flag.store(true, Ordering::Relaxed);
    })
    .ok();

    let jobs = jobs.unwrap_or_else(|| {
        std::thread::available_parallelism().map_or(1, std::num::NonZero::get)
    });
    cli::run::handle_run(
        &cfg,
        *limit,
        *dry_run,
        cli.quiet,
        &interrupted,
        *progress_interval,
        *resume,
        state_file.as_deref(),
        jobs,
        compiled_filters,            // 新增：传递预编译结果
    )
}
```

**修改 5：src/cli/run.rs — 集成测试 test_include_performance_metrics_false_csv_excludes_pm_columns 适配新签名**

文件末尾（L833-889）的测试调用 handle_run 时需要新增 `None`（无 filter 配置 → 无需预编译结果）：

```rust
handle_run(
    &cfg,
    None,
    false,
    true,
    &Arc::new(AtomicBool::new(false)),
    80,
    false,
    None,
    1,
    None,                            // 新增：compiled_filters
)
.unwrap();
```

**修改 6：src/cli/run.rs — 移除 build_pipeline 的 ? 传播**

handle_run 内 `let pipeline = build_pipeline(final_cfg)?;` 改为 `let pipeline = build_pipeline(final_cfg, compiled_meta);`（参数移到调用点，无 Result 返回，去掉 `?`）。

**修改 7：删除现有 FilterProcessor::try_new 的旧实现**

确保 `fn try_new` 整体被新的 `fn new` 替换，不留两个版本。clippy `dead_code` 不会触发，但保留旧名易混淆。

**完整性自检清单（执行后核对）：**

1. `grep -n "try_from_meta" src/cli/run.rs` → 0 行（FilterProcessor 不再编译 meta）
2. `grep -n "try_from_sql_filters" src/cli/run.rs` → 0 行（handle_run 不再编译 sql）
3. `grep -n "try_from_meta" src/config.rs` → 至少 2 行（旧 validate() 和新 validate_and_compile() 中各 1 次）
4. `grep -n "try_from_sql_filters" src/config.rs` → 至少 2 行（同上）
5. `grep -n "validate_and_compile" src/main.rs` → 至少 1 行
6. `grep -n "cfg.validate()\b" src/main.rs` → 至少 1 行（validate 子命令仍调用，L228）
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo build 2>&1 | grep -c "^error\["</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test 2>&1 | tail -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "try_from_meta\|try_from_sql_filters" src/cli/run.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "try_from_meta\|try_from_sql_filters" src/cli/run.rs | grep -v "^#\|//"` 返回 0 行（run.rs 中 regex 编译入口已全部下沉到 validate_and_compile）
    - `grep -n "validate_and_compile" src/main.rs` 返回至少 1 行（run 分支已切换）
    - `grep -n "cfg.validate()" src/main.rs` 返回至少 1 行（validate 子命令保留旧调用）
    - `grep -nE "fn handle_run\b" src/cli/run.rs` 显示签名末尾参数为 `compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>`
    - `grep -nE "fn build_pipeline\b" src/cli/run.rs` 显示签名为 `fn build_pipeline(cfg: &Config, compiled_meta: Option<CompiledMetaFilters>) -> Pipeline`（不再返回 Result）
    - `grep -nE "fn (new|try_new)\b" src/cli/run.rs | grep -i "compiled_meta\|filter"` 显示 FilterProcessor 构造函数接受 CompiledMetaFilters
    - `cargo build 2>&1 | grep -c "^error\["` 输出 0
    - `cargo test 2>&1 | grep "^test result"` 累计显示 ≥651 passed; 0 failed
    - `cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "^error"` 输出 0
  </acceptance_criteria>
  <done>main.rs run 分支调用 validate_and_compile() 并将结果传给 handle_run；handle_run 接受 Option<(CompiledMetaFilters, CompiledSqlFilters)> 并下传至 build_pipeline 和 sql_record_filter；run.rs 中 try_from_meta 与 try_from_sql_filters 调用清零；validate 子命令的 cfg.validate() 调用保留；全量测试通过，clippy 无 error。</done>
</task>

<task type="auto">
  <name>Task 3: 修正 benches/BENCHMARKS.md 中失效的双重编译验证断言</name>
  <read_first>
    - benches/BENCHMARKS.md L380-385（结论小节，特别是 L382 的失效断言）
    - benches/BENCHMARKS.md L315-385（Phase 9 全节，确保改动只针对断言行）
    - .planning/phases/09-cli/09-VERIFICATION.md L99-103（Anti-Patterns Found 记录）
  </read_first>
  <files>benches/BENCHMARKS.md</files>
  <action>
**问题**：BENCHMARKS.md L382 当前断言：

```markdown
- [x] 双重编译已消除：`grep -rn "from_meta\b" src/ | grep -v "try_from_meta"` 返回 0 个匹配；`compile_patterns` / `try_from_meta` / `try_from_sql_filters` 是唯一的 `Regex::new()` 入口
```

该断言只检测旧 API 名（`from_meta`，不带 `try_` 前缀），无法检测新 API（`try_from_meta`）被多次调用的情形。Verifier 已将其列为 Anti-Pattern（BLOCKER）。

**修复方案**：替换 L382 整行为可观测的新断言。新断言验证两点：(1) `validate_and_compile` 接口存在；(2) `run` 路径中 `try_from_meta` 和 `try_from_sql_filters` 仅作为定义/`validate*` 调用点出现，不出现在 `cli/run.rs` 中。

将 L382 行：

```markdown
- [x] 双重编译已消除：`grep -rn "from_meta\b" src/ | grep -v "try_from_meta"` 返回 0 个匹配；`compile_patterns` / `try_from_meta` / `try_from_sql_filters` 是唯一的 `Regex::new()` 入口
```

改为以下三行（替换为更强的可观测验证）：

```markdown
- [x] `validate_and_compile()` 统一接口存在：`grep -c "fn validate_and_compile" src/config.rs` ≥ 1
- [x] `run` 路径无重复 regex 编译：`grep -cE "try_from_meta|try_from_sql_filters" src/cli/run.rs` 返回 0（编译入口下沉至 `validate_and_compile`）
- [x] 旧 API 完全删除：`grep -rn "from_meta\b" src/ | grep -v "try_from_meta"` 返回 0 个匹配
```

其他行不动：日期、测量命令、对比维度表、hyperfine 原始输出、CLI 冷启动 3ms 等结论均保留。

**结果列表完整性**：修改后 L380 起 "### 结论" 下应有的复选项：

```markdown
### 结论

- [x] `validate_and_compile()` 统一接口存在：`grep -c "fn validate_and_compile" src/config.rs` ≥ 1
- [x] `run` 路径无重复 regex 编译：`grep -cE "try_from_meta|try_from_sql_filters" src/cli/run.rs` 返回 0（编译入口下沉至 `validate_and_compile`）
- [x] 旧 API 完全删除：`grep -rn "from_meta\b" src/ | grep -v "try_from_meta"` 返回 0 个匹配
- [x] update check 已后台化：`grep -n "thread::spawn" src/cli/update.rs` 确认存在（L68）
- [x] hyperfine 数据已记录（三对比维度）
- [x] CLI 冷启动 ≈ 3 ms，低于 50 ms 门控，PERF-11 验收通过
```

注意：复选项保留 `[x]` 表示已验证通过；这些断言在 Task 1+2 完成后均可独立用 grep 校验。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "validate_and_compile" benches/BENCHMARKS.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -cE "try_from_meta|try_from_sql_filters" src/cli/run.rs</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "fn validate_and_compile" src/config.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "validate_and_compile" benches/BENCHMARKS.md` ≥ 1（新断言已写入）
    - `grep -cE "try_from_meta|try_from_sql_filters" src/cli/run.rs` 返回 0（实际代码达成"无重复编译"承诺）
    - `grep -c "fn validate_and_compile" src/config.rs` ≥ 1（新接口实际存在）
    - `grep -rn "from_meta\b" src/ | grep -v "try_from_meta" | wc -l` 输出 0（旧 API 仍清零）
    - `grep -n "thread::spawn" src/cli/update.rs` 返回至少 1 行（update check 后台化保留）
    - `grep -c "Phase 9" benches/BENCHMARKS.md` ≥ 1（Phase 9 节仍存在）
  </acceptance_criteria>
  <done>benches/BENCHMARKS.md L382 的失效断言被替换为可独立 grep 验证的新断言：检测 validate_and_compile 接口存在、run.rs 中 try_from_meta/try_from_sql_filters 计数为 0、旧 API 清零；其他三对比维度数据与 hyperfine 原始输出均保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → validate_and_compile() → run 路径 | regex 字符串从 TOML 流入，单次编译后通过 main.rs → handle_run → build_pipeline → FilterProcessor 一路传递，无重新编译 |
| validate 子命令独立路径 | 不消费 compiled 结果；仍用 validate() 做语法检查，不影响 run 路径优化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | validate_and_compile 错误路径 | accept | 错误通过 `?` 从 main.rs 传播至顶层 `run()`，由 `exit_code_for` 映射到 EXIT_CONFIG，用户可见错误信息含字段名 |
| T-09-08 | Information Disclosure | compiled regex 在 transaction filter 路径中跨 cfg.clone() 边界传递 | accept | trxids 修改不涉及 regex；编译结果仅在单进程内传递，无外部边界 |
</threat_model>

<verification>
```bash
cd /Users/guang/Projects/sqllog2db

# 1. 编译 + 测试 + clippy
cargo build
cargo test
cargo clippy --all-targets -- -D warnings

# 2. 接口存在性
grep -n "fn validate_and_compile" src/config.rs
grep -n "validate_and_compile" src/main.rs

# 3. 双重编译消除（核心目标）
grep -cE "try_from_meta|try_from_sql_filters" src/cli/run.rs       # 期望 0
grep -cE "try_from_meta|try_from_sql_filters" src/config.rs        # 期望 ≥ 4（旧 validate + 新 validate_and_compile 各 2 次）
grep -rn "from_meta\b" src/ | grep -v "try_from_meta"              # 期望 0 行

# 4. validate 子命令保留
grep -n "cfg.validate()" src/main.rs

# 5. BENCHMARKS.md 断言更新
grep -c "validate_and_compile" benches/BENCHMARKS.md
```
</verification>

<success_criteria>
- `Config::validate_and_compile()` 方法存在，返回 `Result<Option<(CompiledMetaFilters, CompiledSqlFilters)>>`
- `main.rs` 的 run 分支调用 `validate_and_compile()` 并将结果传递给 `handle_run`
- `handle_run` 签名末尾新增 `compiled_filters: Option<(CompiledMetaFilters, CompiledSqlFilters)>`
- `build_pipeline` 改为接受 `Option<CompiledMetaFilters>`，返回 `Pipeline`（非 Result）
- `FilterProcessor::new(compiled_meta, filter)` 接受预编译参数
- `cli/run.rs` 中 `try_from_meta` 与 `try_from_sql_filters` 调用计数为 0
- `validate` 子命令仍调用 `cfg.validate()`，独立路径不受影响
- `benches/BENCHMARKS.md` 的"双重编译已消除"断言被替换为基于 `validate_and_compile` 存在性 + `run.rs` 调用清零的可观测断言
- `cargo test`：所有现有测试（≥651）+ Task 1 新增 7 个单元测试全部通过
- `cargo clippy --all-targets -- -D warnings`：0 error
- 验收 VERIFICATION.md SC-2 BLOCKER：grep 验证 `run` 命令路径中每个 regex 字段只被 `Regex::new()` 调用一次
</success_criteria>

<output>
完成后创建 `.planning/phases/09-cli/09-05-SUMMARY.md`，记录：
- validate_and_compile() 的实现要点和与原 validate() 的等价性
- handle_run/build_pipeline/FilterProcessor 签名变更前后对比
- benches/BENCHMARKS.md 修正后的可观测断言
- grep 验证证据：try_from_meta 在 run.rs 中清零、validate_and_compile 在 config.rs 中存在
- SC-2 验收闭环：双重编译彻底消除
</output>
