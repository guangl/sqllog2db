---
phase: "02"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/exporter/sqlite.rs
autonomous: true
requirements:
  - FIELD-01
must_haves:
  truths:
    - "SqliteExporter 有 ordered_indices: Vec<usize> 字段，默认值 [0..14]"
    - "build_create_sql() 按 ordered_indices 顺序建列（CREATE TABLE 列顺序与配置一致）"
    - "build_insert_sql() 按 ordered_indices 顺序构建 INSERT 语句"
    - "do_insert_preparsed() 投影路径按 ordered_indices 从全量 Value 数组选取值"
    - "initialize() 中调用点已改为传 &self.ordered_indices"
    - "全量掩码快速路径（FieldMask::ALL + params![]）保留不变"
    - "test_sqlite_field_order 通过"
  artifacts:
    - path: "src/exporter/sqlite.rs"
      provides: "有序字段 SQLite 导出"
      contains: "ordered_indices"
  key_links:
    - from: "SqliteExporter::initialize()"
      to: "build_insert_sql() / build_create_sql()"
      via: "&self.ordered_indices"
      pattern: "build_insert_sql.*ordered_indices"
    - from: "do_insert_preparsed() 投影路径"
      to: "ordered_indices"
      via: "ordered_indices.iter().map(|&i| &all[i])"
      pattern: "ordered_indices\\.iter\\(\\)\\.map"
---

<objective>
给 SqliteExporter 添加 ordered_indices: Vec<usize> 字段，并将 build_create_sql()、build_insert_sql()、do_insert_preparsed() 的投影路径改为按有序索引操作。

Purpose: 让 SQLite 导出的 CREATE TABLE 列顺序和数据插入顺序完全跟随用户 fields 配置（D-01）。
Output: src/exporter/sqlite.rs 修改完毕，带集成测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-CONTEXT.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-01-SUMMARY.md

<interfaces>
<!-- 从 src/exporter/sqlite.rs 提取的现有接口，执行时直接使用 -->

SqliteExporter 结构体（当前字段，需添加 ordered_indices）：
```rust
pub struct SqliteExporter {
    database_url: String,
    table_name: String,
    insert_sql: String,
    overwrite: bool,
    append: bool,
    conn: Option<Connection>,
    stats: ExportStats,
    pub(super) normalize: bool,
    pub(super) field_mask: crate::features::FieldMask,
    // 需新增：
    // pub(super) ordered_indices: Vec<usize>,
}
```

SqliteExporter::new() 当前（需添加 ordered_indices 初始化）：
```rust
pub fn new(database_url: String, table_name: String, overwrite: bool, append: bool) -> Self {
    let insert_sql = format!("INSERT INTO {table_name} VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)");
    Self {
        // ...
        normalize: true,
        field_mask: crate::features::FieldMask::ALL,
        // 需新增: ordered_indices: (0..crate::features::FIELD_NAMES.len()).collect(),
    }
}
```

build_insert_sql() 当前签名（需改为接受 &[usize]）：
```rust
fn build_insert_sql(table_name: &str, field_mask: crate::features::FieldMask) -> String
```

build_create_sql() 当前签名（需改为接受 &[usize]）：
```rust
fn build_create_sql(table_name: &str, field_mask: crate::features::FieldMask) -> String
// 内部 COL_TYPES 常量（15 个类型，与 FIELD_NAMES 索引对齐）：
// ["TEXT NOT NULL","INTEGER NOT NULL","TEXT NOT NULL","TEXT NOT NULL",
//  "TEXT NOT NULL","TEXT NOT NULL","TEXT","TEXT","TEXT","TEXT",
//  "TEXT NOT NULL","REAL","INTEGER","INTEGER","TEXT"]
```

do_insert_preparsed() 当前投影路径（需改为按 ordered_indices 选取）：
```rust
let all: [Value; 15] = [ /* 15 个字段的 Value */ ];
let selected: Vec<Value> = all.into_iter().enumerate()
    .filter(|(i, _)| field_mask.is_active(*i))
    .map(|(_, v)| v)
    .collect();
stmt.execute(rusqlite::params_from_iter(selected))?;
// 改后：
let selected: Vec<&Value> = ordered_indices.iter().map(|&i| &all[i]).collect();
stmt.execute(rusqlite::params_from_iter(selected))?;
```

do_insert_preparsed() 当前签名（需新增 ordered_indices 参数）：
```rust
fn do_insert_preparsed(
    stmt: &mut rusqlite::CachedStatement<'_>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    // 需新增: ordered_indices: &[usize],
) -> std::result::Result<(), rusqlite::Error>
```

do_insert() 兼容路径（同步新增参数）：
```rust
fn do_insert(
    stmt: &mut rusqlite::CachedStatement<'_>,
    sqllog: &Sqllog<'_>,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    // 需新增: ordered_indices: &[usize],
) -> std::result::Result<(), rusqlite::Error>
```

initialize() 调用点（需从 self.field_mask 改为 &self.ordered_indices）：
```rust
// 当前（line 241-244）：
self.insert_sql = Self::build_insert_sql(&self.table_name, self.field_mask);
let create_sql = Self::build_create_sql(&self.table_name, self.field_mask);
// 改后：
self.insert_sql = Self::build_insert_sql(&self.table_name, &self.ordered_indices);
let create_sql = Self::build_create_sql(&self.table_name, &self.ordered_indices);
```

现有测试辅助函数（约 lines 331-342）：
```rust
fn write_test_log(exporter: &mut SqliteExporter, log_line: &str) -> crate::error::Result<()>
// 写入一条记录到 SQLite
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 ordered_indices 字段并修改 CREATE/INSERT SQL 构建逻辑</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs（完整文件：结构体、new()、build_insert_sql()、build_create_sql()、initialize() 调用点、do_insert_preparsed()、Exporter impl 各方法、测试模块）
    - src/features/mod.rs（FIELD_NAMES 常量确认索引，ordered_field_indices() 返回格式）
  </read_first>
  <behavior>
    - test_sqlite_build_insert_sql_ordered：ordered_indices=[10,4]（sql,username），build_insert_sql 返回 "INSERT INTO t (sql, username) VALUES (?, ?)"
    - test_sqlite_build_create_sql_ordered：ordered_indices=[10,4]，build_create_sql 返回 "CREATE TABLE IF NOT EXISTS t (sql TEXT NOT NULL, username TEXT NOT NULL)"
    - test_sqlite_build_insert_sql_full_fast_path：ordered_indices 长度=15，build_insert_sql 返回全量 "INSERT INTO t VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
    - test_sqlite_field_order：ordered_indices=[10,4]，初始化后写入一条记录，SELECT sql, username FROM t 返回正确数据
  </behavior>
  <action>
1. 在 SqliteExporter 结构体中，在 `pub(super) field_mask` 行之后添加：
   ```rust
   pub(super) ordered_indices: Vec<usize>,
   ```

2. 在 SqliteExporter::new() 中，在 `field_mask: crate::features::FieldMask::ALL,` 之后添加：
   ```rust
   ordered_indices: (0..crate::features::FIELD_NAMES.len()).collect(),
   ```

3. 将 build_insert_sql() 函数签名和实现完整替换为：
   ```rust
   /// 根据有序字段索引列表生成 INSERT SQL
   fn build_insert_sql(table_name: &str, ordered_indices: &[usize]) -> String {
       use crate::features::FIELD_NAMES;
       if ordered_indices.len() == FIELD_NAMES.len() {
           // 全量快速路径：与 new() 的默认 insert_sql 一致
           return format!(
               "INSERT INTO {table_name} VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
           );
       }
       let cols: Vec<&str> = ordered_indices.iter().map(|&i| FIELD_NAMES[i]).collect();
       let placeholders = vec!["?"; ordered_indices.len()].join(", ");
       format!(
           "INSERT INTO {table_name} ({}) VALUES ({placeholders})",
           cols.join(", ")
       )
   }
   ```

4. 将 build_create_sql() 函数签名和实现完整替换为：
   ```rust
   /// 根据有序字段索引列表生成 CREATE TABLE SQL
   fn build_create_sql(table_name: &str, ordered_indices: &[usize]) -> String {
       use crate::features::FIELD_NAMES;
       const COL_TYPES: &[&str] = &[
           "TEXT NOT NULL",    // ts        0
           "INTEGER NOT NULL", // ep        1
           "TEXT NOT NULL",    // sess_id   2
           "TEXT NOT NULL",    // thrd_id   3
           "TEXT NOT NULL",    // username  4
           "TEXT NOT NULL",    // trx_id    5
           "TEXT",             // statement 6
           "TEXT",             // appname   7
           "TEXT",             // client_ip 8
           "TEXT",             // tag       9
           "TEXT NOT NULL",    // sql       10
           "REAL",             // exec_time_ms 11
           "INTEGER",          // row_count 12
           "INTEGER",          // exec_id   13
           "TEXT",             // normalized_sql 14
       ];
       let cols: Vec<String> = ordered_indices
           .iter()
           .map(|&i| format!("{} {}", FIELD_NAMES[i], COL_TYPES[i]))
           .collect();
       format!(
           "CREATE TABLE IF NOT EXISTS {table_name} ({})",
           cols.join(", ")
       )
   }
   ```

5. 在 initialize() 中将调用点从 `self.field_mask` 改为 `&self.ordered_indices`：
   ```rust
   self.insert_sql = Self::build_insert_sql(&self.table_name, &self.ordered_indices);
   let create_sql = Self::build_create_sql(&self.table_name, &self.ordered_indices);
   ```

6. 在 do_insert_preparsed() 签名中，在 `field_mask: crate::features::FieldMask,` 之后新增：
   ```rust
   ordered_indices: &[usize],
   ```

7. 将 do_insert_preparsed() 内投影路径（当前 let selected: Vec<Value> ... filter...map 部分）替换为：
   ```rust
   // 投影路径：按有序索引从全量数组中选取（使用引用，避免 move）
   use rusqlite::types::Value;
   let all: [Value; 15] = [
       // 与现有完全一致的 15 个 Value 构建，不修改
       Value::Text(sqllog.ts.as_ref().to_string()),
       Value::Integer(i64::from(meta.ep)),
       Value::Text(meta.sess_id.as_ref().to_string()),
       Value::Text(meta.thrd_id.as_ref().to_string()),
       Value::Text(meta.username.as_ref().to_string()),
       Value::Text(meta.trxid.as_ref().to_string()),
       Value::Text(meta.statement.as_ref().to_string()),
       Value::Text(meta.appname.as_ref().to_string()),
       Value::Text(strip_ip_prefix(meta.client_ip.as_ref()).to_string()),
       sqllog
           .tag
           .as_deref()
           .map_or(Value::Null, |t| Value::Text(t.to_string())),
       Value::Text(pm.sql.as_ref().to_string()),
       exec_time.map_or(Value::Null, |v| Value::Real(f64::from(v))),
       row_count.map_or(Value::Null, |v| Value::Integer(i64::from(v))),
       exec_id.map_or(Value::Null, Value::Integer),
       normalized_sql.map_or(Value::Null, |s| Value::Text(s.to_string())),
   ];
   let selected: Vec<&Value> = ordered_indices.iter().map(|&i| &all[i]).collect();
   stmt.execute(rusqlite::params_from_iter(selected))?;
   ```

8. 在 do_insert() 兼容路径签名中同步添加 `ordered_indices: &[usize]` 参数，并传入 do_insert_preparsed()。

9. 在所有调用 do_insert() 和 do_insert_preparsed() 的 Exporter impl 方法（export()、export_one_normalized()、export_one_preparsed()）中，传入 `&self.ordered_indices`。

10. 在 tests 模块中追加：
    ```rust
    #[test]
    fn test_sqlite_build_insert_sql_ordered() {
        let sql = SqliteExporter::build_insert_sql("t", &[10, 4]);
        assert_eq!(sql, "INSERT INTO t (sql, username) VALUES (?, ?)");
    }

    #[test]
    fn test_sqlite_build_create_sql_ordered() {
        let sql = SqliteExporter::build_create_sql("t", &[10, 4]);
        assert_eq!(sql, "CREATE TABLE IF NOT EXISTS t (sql TEXT NOT NULL, username TEXT NOT NULL)");
    }

    #[test]
    fn test_sqlite_build_insert_sql_full_fast_path() {
        let all_indices: Vec<usize> = (0..15).collect();
        let sql = SqliteExporter::build_insert_sql("t", &all_indices);
        assert_eq!(
            sql,
            "INSERT INTO t VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)"
        );
    }

    #[test]
    fn test_sqlite_field_order() {
        use crate::features::FieldMask;
        use dm_database_parser_sqllog::LogParser;

        let dir = tempfile::TempDir::new().unwrap();
        let log = dir.path().join("t.log");
        std::fs::write(
            &log,
            "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:testuser trxid:1 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT 42. EXECTIME: 5(ms) ROWCOUNT: 2(rows) EXEC_ID: 99.\n",
        )
        .unwrap();

        let db = dir.path().join("out.db");
        let mut exporter = SqliteExporter::new(
            db.to_str().unwrap().to_string(),
            "records".to_string(),
            true,
            false,
        );
        exporter.normalize = false;
        exporter.field_mask = FieldMask::from_names(&["sql".to_string(), "username".to_string()]).unwrap();
        exporter.ordered_indices = vec![10, 4]; // sql=10, username=4
        exporter.initialize().unwrap();

        let parser = LogParser::from_path(log.to_str().unwrap()).unwrap();
        for record in parser.iter().flatten() {
            exporter.export(&record).unwrap();
        }
        exporter.finalize().unwrap();

        let conn = rusqlite::Connection::open(&db).unwrap();
        let (sql_val, username_val): (String, String) = conn
            .query_row("SELECT sql, username FROM records", [], |row| {
                Ok((row.get(0)?, row.get(1)?))
            })
            .unwrap();

        assert!(sql_val.contains("SELECT 42"), "sql_val: {sql_val}");
        assert_eq!(username_val, "testuser");
    }
    ```
  </action>
  <verify>
    <automated>cargo test -q test_sqlite_field_order 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/sqlite.rs 包含 `pub(super) ordered_indices: Vec<usize>` 字符串
    - src/exporter/sqlite.rs 中 build_insert_sql 函数签名包含 `ordered_indices: &[usize]`（不含 field_mask 参数）
    - src/exporter/sqlite.rs 中 build_create_sql 函数签名包含 `ordered_indices: &[usize]`（不含 field_mask 参数）
    - src/exporter/sqlite.rs 包含 `ordered_indices.iter().map(|&i| &all[i])` 字符串（投影路径新实现）
    - cargo test -q test_sqlite_build_insert_sql_ordered 输出 "1 passed"
    - cargo test -q test_sqlite_build_create_sql_ordered 输出 "1 passed"
    - cargo test -q test_sqlite_build_insert_sql_full_fast_path 输出 "1 passed"
    - cargo test -q test_sqlite_field_order 输出 "1 passed"
    - cargo test -q 2>&1 | tail -5 全量测试套件通过（无 FAILED）
    - cargo clippy --all-targets -- -D warnings 零警告
  </acceptance_criteria>
  <done>SqliteExporter 的 CREATE/INSERT/Value 选取全部改为 ordered_indices，4 个新测试全部通过，全量套件绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ordered_indices→SQLite | ordered_indices 索引范围 [0,14]，用于数组索引访问 [Value; 15]；越界已由 ordered_field_indices() 的 position() 调用天然避免 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | build_insert_sql 列名 | accept | 列名来自 FIELD_NAMES[i]（静态常量），不含用户输入，无 SQL 注入风险 |
| T-02-04 | Tampering | do_insert_preparsed 索引访问 | accept | all[i] 中 i 值来自 ordered_field_indices()，天然在 [0,14] 范围内 |
</threat_model>

<verification>
cargo test -q 2>&1 | tail -10
cargo clippy --all-targets -- -D warnings
</verification>

<success_criteria>
- SqliteExporter 有 ordered_indices 字段，默认 [0..14]
- build_insert_sql / build_create_sql 签名改为接受 &[usize]
- do_insert_preparsed 投影路径按 ordered_indices 选取 &Value
- 4 个新增单元/集成测试全部通过
- 全量 cargo test 绿，clippy 零警告
</success_criteria>

<output>
完成后创建 .planning/phases/02-shuchu-ziduan-kongzhi/02-03-SUMMARY.md
</output>
