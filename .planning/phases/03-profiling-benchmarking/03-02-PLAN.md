---
phase: 03-profiling-benchmarking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/bench_sqlite.rs
autonomous: true
requirements: [PERF-01]
tags: [benchmark, criterion, sqlite]

must_haves:
  truths:
    - "执行 cargo bench --bench bench_sqlite 后输出包含 sqlite_export_real benchmark group"
    - "bench_sqlite_real_file 函数在 sqllogs/ 目录缺失时打印 skip 信息并直接 return（不 panic）"
    - "criterion_group! 宏同时注册 bench_sqlite_export 和 bench_sqlite_real_file"
    - "real-file SQLite 输出写到 target/bench_sqlite_real/bench.db（而非 synthetic 共用的 target/bench_sqlite）"
  artifacts:
    - path: "benches/bench_sqlite.rs"
      provides: "SQLite real-file benchmark group（PERF-01 real-file 测量）"
      contains: "fn bench_sqlite_real_file"
  key_links:
    - from: "bench_sqlite.rs bench_sqlite_real_file"
      to: "criterion_group! 宏"
      via: "criterion 注册"
      pattern: "criterion_group!\\(benches, bench_sqlite_export, bench_sqlite_real_file\\)"
    - from: "bench_sqlite_real_file"
      to: "make_config(&real_dir, &bench_dir)"
      via: "config 构造"
      pattern: "make_config\\(&real_dir, &bench_dir\\)"
---

<objective>
为 SQLite 导出建立 real-file benchmark 路径，与 CSV real-file benchmark 并列，构成 PERF-01 中"在 CI 环境可重复运行"的完整 benchmark 矩阵。

Purpose: SQLite 导出涉及实际文件 I/O、PRAGMA 设置（JOURNAL_MODE=OFF / SYNCHRONOUS=OFF）、单事务 INSERT 等多重慢点，synthetic benchmark（~1.45M rec/s）与真实 1.1GB 文件的实际表现差距更大。Phase 5 SQLite 优化（批量事务、WAL、prepared statement 复用）必须以真实文件吞吐为对照基准。

Output:
- benches/bench_sqlite.rs 新增 bench_sqlite_real_file 函数与 criterion_group 注册
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-profiling-benchmarking/03-RESEARCH.md
@.planning/phases/03-profiling-benchmarking/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- handle_run 调用签名（同 bench_csv），bench 中固定 quiet=true。 -->

```rust
pub fn handle_run(
    cfg: &Config,
    output_path_override: Option<PathBuf>,
    verbose: bool,
    quiet: bool,
    shutdown: &Arc<AtomicBool>,
    terminal_width: usize,
    dry_run: bool,
    filter_override: Option<String>,
    threads: usize,
) -> Result<(), Error>;
```

<!-- bench_sqlite.rs 现有 make_config 写出 SQLite 专用配置（database_url + table_name），SQLite 必须写真实文件，不能用 /dev/null -->

```toml
[exporter.sqlite]
database_url = "{dir}/bench.db"
table_name = "sqllogs"
overwrite = true
append = false
```

<!-- 真实日志文件状态：sqllogs/ 下已有 269MB × 2 个 .log 文件，总计 ~538MB -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 bench_sqlite.rs 中新增 sqlite_export_real benchmark group</name>
  <files>benches/bench_sqlite.rs</files>
  <read_first>
    - benches/bench_sqlite.rs （完整文件，确认现有 import、make_config、bench_sqlite_export 与 criterion_group! 结构）
    - benches/bench_csv.rs （参考其现有 bench_csv_export 的写法，与 bench_sqlite 结构一致）
    - .planning/phases/03-profiling-benchmarking/03-PATTERNS.md （real-file group 差异点章节，特别是 sample_size=5 和 bench_dir 路径不同）
    - .planning/phases/03-profiling-benchmarking/03-RESEARCH.md （Pitfall 1 + 2 章节）
  </read_first>
  <action>
    在 `benches/bench_sqlite.rs` 中作如下修改：

1. **新增导入：** 在文件顶部 import 块加入 `use std::time::Duration;`（用于 `measurement_time`）。其余 import 已齐全。

2. **新增函数 `bench_sqlite_real_file`，紧接现有 `bench_sqlite_export` 函数之后（criterion_group! 宏之前）。完整代码如下，逐字写入：**

```rust
fn bench_sqlite_real_file(c: &mut Criterion) {
    let real_dir = PathBuf::from("sqllogs");
    if !real_dir.exists() {
        eprintln!("sqllogs/ not found, skipping sqlite_export_real benchmark");
        return;
    }

    // 独立 bench_dir，避免与 synthetic bench_sqlite 的 bench.db 冲突
    let bench_dir = PathBuf::from("target/bench_sqlite_real");
    fs::create_dir_all(&bench_dir).unwrap();
    let cfg = make_config(&real_dir, &bench_dir);

    let mut group = c.benchmark_group("sqlite_export_real");
    // 真实文件 + SQLite 双重慢，进一步减少采样次数
    group.sample_size(5);
    group.measurement_time(Duration::from_secs(120));
    // 记录数未预扫描，省略 Throughput::Elements，仅记录绝对时间
    group.bench_function("real_file", |b| {
        b.iter(|| {
            handle_run(
                &cfg,
                None,
                false,
                true, // quiet=true：排除进度条 I/O
                &Arc::new(AtomicBool::new(false)),
                80,
                false,
                None,
                1,
            )
            .unwrap();
        });
    });
    group.finish();
}
```

3. **更新文件最末尾的 `criterion_group!` 宏**，将原来的：
```rust
criterion_group!(benches, bench_sqlite_export);
```
替换为：
```rust
criterion_group!(benches, bench_sqlite_export, bench_sqlite_real_file);
```

关键约束：
- 复用现有 `make_config(&real_dir, &bench_dir)`，不要重新定义
- `sqllog_dir` 必须传 `PathBuf::from("sqllogs")`（项目根真实日志目录）
- `bench_dir` 必须用 `target/bench_sqlite_real`（与 synthetic 的 `target/bench_sqlite` 隔离，避免 bench.db 互相覆盖污染计时）
- `sample_size(5)` + `measurement_time(120s)`：SQLite real-file 比 CSV 更慢（fsync-off WAL-off 单事务也只有 ~1.45M rec/s），需更长测量窗口
- handle_run 第 4 参数 `quiet` 必须为 `true`
- 不要新建 `[[bench]]` 条目，沿用现有 `bench_sqlite` 条目
- CI skip 模式（`real_dir.exists()` 检查 + return）必不可少
- 保留现有 `bench_sqlite_export` 函数和 synthetic 测量逻辑不变
- `make_config` 中 `overwrite=true`，每次迭代会 drop+recreate 表（保证 clean slate）
  </action>
  <verify>
    <automated>cargo build --release --bench bench_sqlite 2>&1 | tail -5 && grep -c "fn bench_sqlite_real_file" benches/bench_sqlite.rs | grep -q "^1$" && grep -c "sqlite_export_real" benches/bench_sqlite.rs | grep -q "^1$" && grep -c "bench_sqlite_export, bench_sqlite_real_file" benches/bench_sqlite.rs | grep -q "^1$" && cargo bench --bench bench_sqlite -- --list 2>&1 | grep -q "sqlite_export_real"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_sqlite_real_file" benches/bench_sqlite.rs` 输出 `1`
    - `grep -c "sqlite_export_real" benches/bench_sqlite.rs` 输出 `1`
    - `grep -c "if !real_dir.exists()" benches/bench_sqlite.rs` 输出 `1`
    - `grep -c "PathBuf::from(\"sqllogs\")" benches/bench_sqlite.rs` 输出 `1`
    - `grep -c "target/bench_sqlite_real" benches/bench_sqlite.rs` 输出 `1`（与 synthetic 路径隔离）
    - `grep -c "bench_sqlite_export, bench_sqlite_real_file" benches/bench_sqlite.rs` 输出 `1`
    - `grep -c "sample_size(5)" benches/bench_sqlite.rs` 输出 `1`（real-file 专用低采样）
    - `cargo bench --bench bench_sqlite -- --list` 输出包含 `sqlite_export_real/real_file`
    - `cargo build --release --bench bench_sqlite` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - 现有 `bench_sqlite_export` 函数仍存在（`grep -c "fn bench_sqlite_export" benches/bench_sqlite.rs` 输出 `1`）
  </acceptance_criteria>
  <done>bench_sqlite.rs 新增 bench_sqlite_real_file 函数；criterion_group! 已注册两个函数；`cargo bench --bench bench_sqlite -- --list` 列出 4 个 benchmark（sqlite_export 的 3 个 + sqlite_export_real/real_file）；clippy 零警告。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → benchmark | sqllogs/ 真实日志由本地用户提供，benchmark 仅以只读方式读取 |
| benchmark 输出 → 本地磁盘 | bench.db 写入 target/bench_sqlite_real/，本地非持久产物 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I (Information Disclosure) | benches/bench_sqlite.rs 真实日志读取 | accept | sqllogs/ 已 .gitignore；bench.db 写 target/（gitignored），无外泄路径 |
| T-03-05 | T (Tampering) | bench_sqlite_real_file 与 synthetic 共用 bench.db | mitigate | 使用独立 bench_dir=`target/bench_sqlite_real`，物理隔离两个 benchmark group 的数据库文件，避免互相覆盖污染计时 |
| T-03-06 | D (Denial of Service) | real-file SQLite bench 长时运行 | mitigate | sample_size=5 + measurement_time=120s 控制单次 cargo bench 总耗时；CI 缺文件时安全 skip |
</threat_model>

<verification>
- `cargo build --release --bench bench_sqlite` 成功
- `cargo bench --bench bench_sqlite -- --list` 输出包含 `sqlite_export_real/real_file`
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 通过
</verification>

<success_criteria>
- bench_sqlite.rs 新增 bench_sqlite_real_file 函数并在 criterion_group! 中注册
- real-file group 使用独立 target/bench_sqlite_real 目录，与 synthetic 物理隔离
- sqllogs/ 缺失环境下 bench 不 panic（CI skip 模式生效）
- 项目规范全部通过（clippy 零警告 + fmt 无 diff）
</success_criteria>

<output>
After completion, create `.planning/phases/03-profiling-benchmarking/03-02-SUMMARY.md`
</output>
