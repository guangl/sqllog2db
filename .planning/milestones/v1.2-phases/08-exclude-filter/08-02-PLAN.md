---
phase: 08-exclude-filter
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/cli/run.rs
  - src/cli/init.rs
autonomous: true
requirements:
  - FILTER-03

must_haves:
  truths:
    - "纯 exclude 配置（无任何 include 字段）时，FilterProcessor 正确激活 meta 检查路径（has_meta_filters = true）"
    - "cargo run -- init 生成的配置文件在每个 include 字段注释下方包含对应 exclude_* 注释示例"
    - "cargo run -- validate 对含 exclude_* 字段的合法配置成功通过"
    - "cargo test 全量通过，无回归"
  artifacts:
    - path: "src/cli/run.rs"
      provides: "FilterProcessor::new() 使用 has_any_filters() 预计算 has_meta_filters"
      contains: "has_any_filters"
    - path: "src/cli/init.rs"
      provides: "verbose + minimal 两个模板均含 exclude_* 注释示例"
      contains: "exclude_usernames"
  key_links:
    - from: "FilterProcessor::new()"
      to: "compiled_meta.has_any_filters()"
      via: "let has_meta_filters = compiled_meta.has_any_filters();"
      pattern: "has_any_filters"
    - from: "CONFIG_TEMPLATE_ZH / CONFIG_TEMPLATE_EN"
      to: "exclude_* 注释块"
      via: "每个 include 注释行之后的 exclude 对应注释"
      pattern: "exclude_usernames"
---

<objective>
在 Plan 01 完成 `filters.rs` 核心逻辑之后，完成以下两处收尾工作：

1. **`src/cli/run.rs`**：`FilterProcessor::new()` 中将 `compiled_meta.has_filters()` 改为 `compiled_meta.has_any_filters()`（D-05），确保纯 exclude 配置也能激活元数据过滤路径（per D-05）。

2. **`src/cli/init.rs`**：在 verbose（中文）和 minimal（英文）两个配置模板的 meta filters 注释区域，为 7 个字段（client_ips / usernames / sess_ids / thrd_ids / statements / appnames / tags）各插入一对 exclude_* 注释示例（D-09）。同步将英文模板中旧的 "substring match" 注释更正为 "regex match"（D-09 范围内）。

Purpose: 激活 FilterProcessor 对纯 exclude 场景的正确路由；让 `cargo run -- init` 生成的配置文件引导用户发现并使用 exclude 功能。

Output: 修改后的 `src/cli/run.rs` 和 `src/cli/init.rs`。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-exclude-filter/08-CONTEXT.md
@.planning/phases/08-exclude-filter/08-01-SUMMARY.md

<interfaces>
<!-- 从 src/cli/run.rs 提取，执行前须读取当前状态 -->

FilterProcessor::new() 当前实现（L44-55）：
```rust
impl FilterProcessor {
    fn new(filter: &crate::features::FiltersFeature) -> Self {
        let compiled_meta = CompiledMetaFilters::from_meta(&filter.meta);
        let has_meta_filters = compiled_meta.has_filters();  // ← 改为 has_any_filters()
        Self {
            compiled_meta,
            start_ts: filter.meta.start_ts.clone(),
            end_ts: filter.meta.end_ts.clone(),
            has_meta_filters,
        }
    }
}
```

需要更改的唯一一行（D-05）：
```rust
// Before:
let has_meta_filters = compiled_meta.has_filters();
// After:
let has_meta_filters = compiled_meta.has_any_filters();
```

<!-- init.rs 当前模板区域（L88-106 ZH，L168-185 EN） -->
ZH 模板当前 meta filters 注释块（按字段顺序，需在每个 include 行后插入 exclude 注释）：
```toml
# 过滤指定的客户端 IP（支持模糊匹配）
# client_ips = ["127.0.0.1", "192.168"]
# 过滤指定的用户名（支持模糊匹配）
# usernames = ["SYSDBA"]
...
# 过滤指定的会话 ID（支持模糊匹配）
# sess_ids = ["0x7f41435437a8"]
# 过滤指定的线程 ID（支持模糊匹配）
# thrd_ids = ["2188515"]
# 过滤指定的语句类型（支持模糊匹配）
# statements = ["INS", "UPD", "DEL"]
# 过滤指定的应用名称（支持模糊匹配）
# appnames = ["DMSQL"]
```

EN 模板当前 meta filters 注释块（L168-185）：
```toml
# Filter by client IPs (substring match)
# client_ips = ["127.0.0.1", "192.168"]
# Filter by usernames (substring match)
# usernames = ["SYSDBA"]
...
# Filter by session IDs (substring match)
# sess_ids = ["0x7f41435437a8"]
# Filter by thread IDs (substring match)
# thrd_ids = ["2188515"]
# Filter by statement types (substring match)
# statements = ["INS", "UPD", "DEL"]
# Filter by application names (substring match)
# appnames = ["DMSQL"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 FilterProcessor::new() 调用 has_any_filters()</name>
  <files>src/cli/run.rs</files>

  <read_first>
    - src/cli/run.rs（完整文件前 60 行——定位 FilterProcessor::new() 的当前状态）
    - .planning/phases/08-exclude-filter/08-01-SUMMARY.md（确认 has_any_filters() 已在 Plan 01 中实现）
  </read_first>

  <action>
在 `src/cli/run.rs` 的 `FilterProcessor::new()` 实现中（约 L47），将：

```rust
let has_meta_filters = compiled_meta.has_filters();
```

改为：

```rust
let has_meta_filters = compiled_meta.has_any_filters();
```

这是本文件唯一需要修改的一行（per D-05）。`has_any_filters()` 在 Plan 01 中已由 `CompiledMetaFilters` 新增，检查 include 或 exclude 任一字段非空。

同时更新 `FilterProcessor` 的 `has_meta_filters` 字段注释（正上方的文档注释），将：
```rust
/// 预计算：`compiled_meta.has_filters()` 的结果，避免热路径重复检查
```
改为：
```rust
/// 预计算：`compiled_meta.has_any_filters()` 的结果（include 或 exclude 任一），避免热路径重复检查
```
  </action>

  <acceptance_criteria>
    - `grep "has_any_filters" src/cli/run.rs` 输出包含 `has_any_filters()` 的行
    - `grep "has_filters()" src/cli/run.rs` 不再包含对 `compiled_meta.has_filters()` 的调用（仅保留注释中的旧名称引用可以不计）
    - `cargo build 2>&1 | grep "^error"` 无输出
    - `cargo test -p sqllog2db 2>&1 | grep "test result"` 输出 `test result: ok`
  </acceptance_criteria>

  <verify>
    <automated>cargo build 2>&1 | grep "^error" | head -5; cargo test -p sqllog2db 2>&1 | grep "test result"</automated>
  </verify>

  <done>
    - run.rs 中 has_meta_filters 预计算改用 has_any_filters()
    - 字段注释同步更新
    - 编译通过，测试无回归
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 init.rs 配置模板，插入 exclude_* 注释示例</name>
  <files>src/cli/init.rs</files>

  <read_first>
    - src/cli/init.rs（完整文件——必须读取当前 ZH 和 EN 模板的精确内容，确定插入位置）
    - .planning/phases/08-exclude-filter/08-CONTEXT.md（D-09 决策：每个 include 字段注释下方紧跟对应 exclude 注释）
  </read_first>

  <action>
**ZH 模板（CONFIG_TEMPLATE_ZH）修改**

在 ZH 模板的 meta filters 注释区域，对 7 个字段逐一在 include 注释行下方插入 exclude 对应注释。最终目标格式（每对 include + exclude 紧邻）：

```toml
# 过滤指定的客户端 IP（支持正则匹配）
# client_ips = ["127.0.0.1", "192\\.168"]
# 排除指定的客户端 IP（OR veto：任一命中则丢弃该记录）
# exclude_client_ips = ["^10\\.0", "^172\\.16"]

# 过滤指定的用户名（支持正则匹配）
# usernames = ["SYSDBA"]
# 排除指定的用户名（OR veto：任一命中则丢弃该记录）
# exclude_usernames = ["guest", "^anon"]

# 过滤指定的会话 ID（支持正则匹配）
# sess_ids = ["0x7f41435437a8"]
# 排除指定的会话 ID（OR veto：任一命中则丢弃该记录）
# exclude_sess_ids = ["^0x0000"]

# 过滤指定的线程 ID（支持正则匹配）
# thrd_ids = ["2188515"]
# 排除指定的线程 ID（OR veto：任一命中则丢弃该记录）
# exclude_thrd_ids = ["^0$"]

# 过滤指定的语句类型（支持正则匹配）
# statements = ["INS", "UPD", "DEL"]
# 排除指定的语句类型（OR veto：任一命中则丢弃该记录）
# exclude_statements = ["SEL", "SET"]

# 过滤指定的应用名称（支持正则匹配）
# appnames = ["DMSQL"]
# 排除指定的应用名称（OR veto：任一命中则丢弃该记录）
# exclude_appnames = ["monitor", "health"]

# 过滤指定的 tag（支持正则匹配）
# tags = ["\\[SEL\\]"]
# 排除指定的 tag（OR veto：任一命中则丢弃该记录）
# exclude_tags = ["\\[SET\\]", "\\[OTH\\]"]
```

注意：
- ZH 模板中原注释为"支持模糊匹配"，因实际功能为正则匹配，按 D-09 同步更正为"支持正则匹配"
- ZH 模板原来没有 tags 字段注释，按 D-09 补充

**EN 模板（CONFIG_TEMPLATE_EN）修改**

在 EN 模板的 meta filters 注释区域，对 7 个字段逐一在 include 注释行下方插入 exclude 对应注释。同时将旧注释 "(substring match)" 更正为 "(regex match)"（D-09 范围内）。最终目标格式：

```toml
# Filter by client IPs (regex match)
# client_ips = ["127.0.0.1", "192\\.168"]
# Exclude by client IPs (OR veto: any match drops the record)
# exclude_client_ips = ["^10\\.0", "^172\\.16"]

# Filter by usernames (regex match)
# usernames = ["SYSDBA"]
# Exclude by usernames (OR veto: any match drops the record)
# exclude_usernames = ["guest", "^anon"]

# Filter by session IDs (regex match)
# sess_ids = ["0x7f41435437a8"]
# Exclude by session IDs (OR veto: any match drops the record)
# exclude_sess_ids = ["^0x0000"]

# Filter by thread IDs (regex match)
# thrd_ids = ["2188515"]
# Exclude by thread IDs (OR veto: any match drops the record)
# exclude_thrd_ids = ["^0$"]

# Filter by statement types (regex match)
# statements = ["INS", "UPD", "DEL"]
# Exclude by statement types (OR veto: any match drops the record)
# exclude_statements = ["SEL", "SET"]

# Filter by application names (regex match)
# appnames = ["DMSQL"]
# Exclude by application names (OR veto: any match drops the record)
# exclude_appnames = ["monitor", "health"]

# Filter by tags (regex match)
# tags = ["\\[SEL\\]"]
# Exclude by tags (OR veto: any match drops the record)
# exclude_tags = ["\\[SET\\]", "\\[OTH\\]"]
```

注意：
- EN 模板中 "trxids" 是精确匹配（非正则），不受影响，保持原样
- 时间过滤（start_ts/end_ts）不属于 exclude 支持范围，保持原样
  </action>

  <acceptance_criteria>
    - `grep -c "exclude_usernames" src/cli/init.rs` 输出 `2`（ZH 模板 1 处 + EN 模板 1 处）
    - `grep -c "exclude_" src/cli/init.rs` 输出 `14`（7 个字段 × 2 模板 = 14）
    - `grep "substring match" src/cli/init.rs` 无输出（旧注释已全部替换）
    - `cargo build 2>&1 | grep "^error"` 无输出
    - `cargo test -p sqllog2db 2>&1 | grep "test result"` 输出 `test result: ok`
  </acceptance_criteria>

  <verify>
    <automated>grep -c "exclude_" src/cli/init.rs; grep "substring match" src/cli/init.rs; cargo test -p sqllog2db 2>&1 | grep "test result"</automated>
  </verify>

  <done>
    - ZH 模板含 7 个 exclude_* 注释示例，每个 include 字段正下方
    - EN 模板含 7 个 exclude_* 注释示例，"substring match" 全部替换为 "regex match"
    - cargo build 通过
    - cargo test 通过，无回归
    - cargo clippy -- -D warnings 通过
    - cargo fmt --check 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config template → 文件系统 | init.rs 模板内容写入用户指定路径，无外部输入 |
| FilterProcessor → CompiledMetaFilters | has_any_filters() 预计算结果，纯内存操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | `FilterProcessor::has_meta_filters` 预计算 | mitigate | 改为 `has_any_filters()` 确保纯 exclude 配置不被静默跳过（hot path 正确路由） |
| T-08-06 | Information Disclosure | init.rs 注释中的示例 regex 模式 | accept | 示例值为通用占位（"^guest", "^anon"），不含真实系统信息 |
</threat_model>

<verification>
```bash
# 验证 run.rs 修改
grep "has_any_filters" src/cli/run.rs

# 验证 init.rs 模板更新
grep -c "exclude_" src/cli/init.rs
grep "substring match" src/cli/init.rs  # 应无输出

# 全量测试
cargo test -p sqllog2db 2>&1 | tail -5

# Clippy + format
cargo clippy --all-targets -- -D warnings 2>&1 | grep -v "^$" | head -10
cargo fmt --check 2>&1

# 集成验证：含 exclude 配置的 validate 命令
cat > /tmp/test-exclude.toml << 'EOF'
[sqllog]
path = "sqllogs"
[features.filters]
enable = true
exclude_usernames = ["^guest"]
exclude_client_ips = ["^10\\.0"]
[exporter.csv]
file = "/tmp/test-out.csv"
EOF
cargo run --quiet -- validate -c /tmp/test-exclude.toml 2>&1 | head -5

# 集成验证：非法 exclude 正则应被 validate 拦截
cat > /tmp/test-invalid-exclude.toml << 'EOF'
[sqllog]
path = "sqllogs"
[features.filters]
enable = true
exclude_usernames = ["[invalid"]
[exporter.csv]
file = "/tmp/test-out.csv"
EOF
cargo run --quiet -- validate -c /tmp/test-invalid-exclude.toml 2>&1 | grep -i "invalid\|error" | head -3
```
</verification>

<success_criteria>
1. `grep "has_any_filters" src/cli/run.rs` 有输出（确认修改生效）
2. `grep -c "exclude_" src/cli/init.rs` 输出 `14`（7 字段 × 2 模板）
3. `grep "substring match" src/cli/init.rs` 无输出（旧注释清理完毕）
4. `cargo test -p sqllog2db` 输出 `test result: ok`，test 数量 ≥ 651（原有测试无损，新测试在 Plan 01 中）
5. `cargo clippy --all-targets -- -D warnings` 无 error/warning
6. 合法 exclude 配置通过 `cargo run -- validate` 不报错
7. 非法 exclude 正则被 `cargo run -- validate` 拦截并报错
</success_criteria>

<output>
After completion, create `.planning/phases/08-exclude-filter/08-02-SUMMARY.md`
</output>
