---
phase: 04-csv
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/exporter/csv.rs
  - benches/bench_csv.rs
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "开发者可以通过 cargo bench --bench bench_csv -- csv_format_only 直接量化 CSV 格式化层（不含解析）净开销"
    - "write_record_preparsed 在 bench crate 中可见，可被 bench 直接调用"
    - "bench_csv_format_only 的 throughput 数值在 benchmark 输出中可读"
    - "现有 12 个 csv.rs 单元测试全部通过（无功能退化）"
  artifacts:
    - path: "src/exporter/csv.rs"
      provides: "pub(crate) write_record_preparsed 可见性修改"
      contains: "pub(crate) fn write_record_preparsed"
    - path: "benches/bench_csv.rs"
      provides: "bench_csv_format_only group + criterion_group 注册"
      contains: "fn bench_csv_format_only"
  key_links:
    - from: "benches/bench_csv.rs"
      to: "src/exporter/csv.rs::write_record_preparsed"
      via: "bench 直接调用 CsvExporter::write_record_preparsed 或 export_one_preparsed"
      pattern: "write_record_preparsed|export_one_preparsed"
    - from: "criterion_group!(benches, ...)"
      to: "bench_csv_format_only"
      via: "macro 注册"
      pattern: "criterion_group!\\(benches.*bench_csv_format_only"
---

<objective>
Wave 0：建立 CSV 格式化层 micro-benchmark 基础设施。本计划只做"量化前置工作"，不动格式化层逻辑。

Purpose: 在 Wave 1 优化前先用 micro-benchmark 隔离格式化层净开销（不含 parse_meta/parse_performance_metrics），避免盲目优化（D-01）。
Output: `bench_csv_format_only` benchmark group 可运行；`write_record_preparsed` 改为 `pub(crate)`（D-02）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv/04-CONTEXT.md
@.planning/phases/04-csv/04-RESEARCH.md
@.planning/phases/04-csv/04-PATTERNS.md

@src/exporter/csv.rs
@benches/bench_csv.rs

<interfaces>
<!-- 关键签名：Wave 0 需要 bench 调用 write_record_preparsed，目前为私有 fn -->

From src/exporter/csv.rs (现状，需修改可见性):
```rust
#[inline]
fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    writer: &mut BufWriter<File>,
    path: &Path,
    normalize: bool,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    ordered_indices: &[usize],
) -> Result<()>
```

From src/exporter/csv.rs (CsvExporter 已公开方法，bench 可直接用):
```rust
impl Exporter for CsvExporter {
    fn export_one_preparsed(
        &mut self,
        sqllog: &Sqllog<'_>,
        meta: &MetaParts<'_>,
        pm: &PerformanceMetrics<'_>,
        normalized: Option<&str>,
    ) -> Result<()>;
}
```

From dm_database_parser_sqllog (上游 crate，bench 用于预解析):
```rust
impl<'a> Sqllog<'a> {
    pub fn parse_meta(&self) -> MetaParts<'a>;
    pub fn parse_performance_metrics(&self) -> PerformanceMetrics<'a>;
}
pub struct LogParser { ... }
impl LogParser {
    pub fn from_path(path: &str) -> Result<LogParser>;
    pub fn iter(&self) -> impl Iterator<Item = Result<Sqllog<'_>>>;
}
```

已存在 group（参考结构）：`bench_csv_export`（行 57–88）、`bench_csv_real_file`（行 90–123）。
criterion_group 当前注册：`criterion_group!(benches, bench_csv_export, bench_csv_real_file);`（行 125）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 write_record_preparsed 改为 pub(crate)</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（必读完整文件，确认行 78 当前签名 `fn write_record_preparsed` 与同 struct 已存在的 pub(crate) 字段范例 行 31–33）
    - .planning/phases/04-csv/04-PATTERNS.md（章节 "1. src/exporter/csv.rs — write_record_preparsed 可见性修改"）
  </read_first>
  <action>
在 `src/exporter/csv.rs` 中将 `write_record_preparsed` 函数的可见性从私有 `fn` 改为 `pub(crate) fn`（D-02）。

精确修改点（仅一处，行 78）：

修改前：
```rust
#[inline]
fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    ...
```

修改后：
```rust
#[inline]
pub(crate) fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    ...
```

约束：
- 仅修改该函数的可见性修饰符（`fn` → `pub(crate) fn`）
- 函数签名其他部分（参数、返回类型、`#[inline]` 属性）一律保持不变
- 函数体不改动
- 同文件 `write_record`（行 262，私有）保持不变
- 不要把它改为 `pub`（不要暴露为公开 API）；只能 `pub(crate)`
- 修改后运行 `cargo clippy --all-targets -- -D warnings`，必须 0 warnings
- 修改后运行 `cargo fmt`，必须 0 diff
  </action>
  <verify>
    <automated>grep -n "pub(crate) fn write_record_preparsed" src/exporter/csv.rs &amp;&amp; cargo clippy --all-targets -- -D warnings 2&gt;&amp;1 | tail -5 &amp;&amp; cargo test --lib -- exporter::csv 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub(crate) fn write_record_preparsed" src/exporter/csv.rs` 输出 `1`
    - `grep -c "^fn write_record_preparsed" src/exporter/csv.rs`（行首私有 fn）输出 `0`
    - `grep -c "^    fn write_record_preparsed" src/exporter/csv.rs`（缩进私有 fn）输出 `0`
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`，输出无 `warning:` 行
    - `cargo test --lib -- exporter::csv` 输出含 `test result: ok` 且 `passed` ≥ 12
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>write_record_preparsed 对 bench crate 可见，所有 csv.rs 单元测试仍通过</done>
</task>

<task type="auto">
  <name>Task 2: 新增 bench_csv_format_only group</name>
  <files>benches/bench_csv.rs</files>
  <read_first>
    - benches/bench_csv.rs（必读完整文件，确认 `bench_csv_export` 行 57–88、`bench_csv_real_file` 行 90–123 与 `criterion_group!` 行 125 的现有结构）
    - src/exporter/csv.rs（确认 `CsvExporter::new`、`initialize`、`export_one_preparsed`、`finalize` 签名）
    - .planning/phases/04-csv/04-PATTERNS.md（章节 "2. benches/bench_csv.rs — 新增 bench_csv_format_only group"）
    - .planning/phases/04-csv/04-CONTEXT.md（D-03：硬编码典型记录）
  </read_first>
  <action>
在 `benches/bench_csv.rs` 中新增一个 `bench_csv_format_only` 函数，并在 `criterion_group!` 注册中追加它。

具体步骤：

**Step 1：在 `bench_csv_real_file` 函数（行 123 `}` 后）和 `criterion_group!` 行（行 125）之间，插入以下新函数：**

```rust
/// Micro-benchmark：隔离 CSV 格式化层净开销（不含 parse_meta/parse_performance_metrics）。
///
/// 输入采用硬编码典型记录（D-03）：包含 ts, ep, sess, trxid, stmt, appname, ip, sql,
/// EXECTIME, ROWCOUNT, EXEC_ID。10000 条相同记录，与 csv_export/10000 group 对齐，
/// 方便对比格式化层在总开销中的占比。
///
/// 注意：本 group 无 v1.0 baseline。**不要**用 `--baseline v1.0` 对比此 group。
fn bench_csv_format_only(c: &mut Criterion) {
    use dm_database_parser_sqllog::LogParser;
    use dm_database_sqllog2db::exporter::CsvExporter;
    use dm_database_sqllog2db::exporter::Exporter;

    // D-03：硬编码典型记录（中等长度 SQL）
    const LOG_LINE: &str = "2024-01-01 00:00:00.000 (EP[1234] sess:0x0001 user:BENCHUSER trxid:TID001 stmt:0x1 appname:App ip:10.0.0.1) [SEL] SELECT * FROM t WHERE id = 1. EXECTIME: 10(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n";
    const N: usize = 10_000;

    let bench_dir = PathBuf::from("target/bench_csv_format_only");
    fs::create_dir_all(&bench_dir).unwrap();
    let log_path = bench_dir.join("fmt.log");
    let content: String = LOG_LINE.repeat(N);
    fs::write(&log_path, &content).unwrap();

    // 一次性解析全部 N 条记录到 Vec，benchmark 内只跑格式化
    let parser = LogParser::from_path(log_path.to_str().unwrap()).unwrap();
    let records: Vec<_> = parser.iter().filter_map(std::result::Result::ok).collect();
    assert_eq!(records.len(), N, "expected {N} parsed records, got {}", records.len());

    // 预解析 meta + pm（这部分开销不计入 benchmark 测量窗口）
    let parsed: Vec<_> = records
        .iter()
        .map(|r| (r, r.parse_meta(), r.parse_performance_metrics()))
        .collect();

    let out_path = bench_dir.join("out.csv");

    let mut group = c.benchmark_group("csv_format_only");
    group.throughput(Throughput::Elements(N as u64));
    group.bench_function(BenchmarkId::from_parameter(N), |b| {
        b.iter(|| {
            let mut exporter = CsvExporter::new(&out_path);
            exporter.initialize().unwrap();
            for (sqllog, meta, pm) in &parsed {
                exporter.export_one_preparsed(sqllog, meta, pm, None).unwrap();
            }
            exporter.finalize().unwrap();
        });
    });
    group.finish();
}
```

**Step 2：修改 `criterion_group!` 注册（行 125），追加 `bench_csv_format_only`：**

修改前：
```rust
criterion_group!(benches, bench_csv_export, bench_csv_real_file);
```

修改后：
```rust
criterion_group!(benches, bench_csv_export, bench_csv_real_file, bench_csv_format_only);
```

**约束：**
- 调用 `export_one_preparsed`（已是 trait 方法，无需 pub(crate)）。Task 1 的 `pub(crate) write_record_preparsed` 在此 plan 暂不直接调用，留给 Wave 1 任意收紧 hot path 时使用。
- 输出路径放在 `target/bench_csv_format_only/`，避免与 `target/bench_csv` 冲突
- 测试输入硬编码 10000 条相同记录（D-03）
- 必须使用 `LogParser` + `parse_meta()` + `parse_performance_metrics()` 完成预解析，确保 `iter()` 内只测格式化
- 不要给 `csv_format_only` 加 `--baseline v1.0` 文档；它有自己的 baseline 路径（Pitfall 4）
- 使用 `BenchmarkId::from_parameter(N)`，与现有 `bench_csv_export` 的命名风格一致
- 修改后运行 `cargo build --benches`，必须编译通过
- 运行 `cargo bench --bench bench_csv -- csv_format_only --quick` 必须输出含 `csv_format_only/10000` 的 throughput 行
  </action>
  <verify>
    <automated>grep -c "fn bench_csv_format_only" benches/bench_csv.rs &amp;&amp; grep -c "bench_csv_format_only" benches/bench_csv.rs &amp;&amp; cargo build --benches 2&gt;&amp;1 | tail -3 &amp;&amp; cargo bench --bench bench_csv -- csv_format_only --quick 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_csv_format_only" benches/bench_csv.rs` 输出 `1`
    - `grep -c "criterion_group!(benches, bench_csv_export, bench_csv_real_file, bench_csv_format_only)" benches/bench_csv.rs` 输出 `1`
    - `grep -c "csv_format_only" benches/bench_csv.rs` 输出 ≥ `3`（函数定义 + group 名 + criterion_group 注册）
    - `grep -c "Throughput::Elements(N as u64)" benches/bench_csv.rs` 输出 `1`（新 group 含 throughput）
    - `grep -c "BenchmarkId::from_parameter(N)" benches/bench_csv.rs` 输出 `1`
    - `grep -c "const LOG_LINE: &str = \"2024-01-01 00:00:00.000" benches/bench_csv.rs` 输出 `1`（D-03 硬编码记录）
    - `grep -c "const N: usize = 10_000;" benches/bench_csv.rs` 输出 `1`
    - `cargo build --benches` 退出码 `0`
    - `cargo bench --bench bench_csv -- csv_format_only --quick` 退出码 `0` 且输出含 `csv_format_only/10000`
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>bench_csv_format_only group 已注册到 criterion，可直接量化格式化层吞吐</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → bench process | bench 写临时 log 文件并自己读取，无外部不可信输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | `pub(crate) write_record_preparsed` 可见性扩大 | accept | `pub(crate)` 仅暴露给同 crate 内 bench/test，不进入公开 API；不影响外部调用面 |
| T-04-02 | Tampering | bench 写入 `target/bench_csv_format_only/` | accept | 仅在 cargo target 目录写临时文件，CI/dev 隔离环境，无生产数据风险 |
</threat_model>

<verification>
- `cargo test --lib -- exporter::csv` 12 个现有测试全部通过
- `cargo bench --bench bench_csv -- csv_format_only --quick` 可输出 `csv_format_only/10000` 的 elapsed/throughput 数值
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt -- --check` 无 diff
</verification>

<success_criteria>
- write_record_preparsed 是 `pub(crate)`
- bench_csv_format_only group 在 criterion_group!(benches, ...) 中注册
- bench_csv_format_only 输出 throughput 数值（records/sec）
- 全部 csv.rs 单元测试无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/04-csv/04-01-SUMMARY.md`，记录：
1. write_record_preparsed 当前可见性（pub(crate) 已生效）
2. bench_csv_format_only 首次运行的 elapsed/throughput 数值（作为 Wave 1 优化基线）
3. 与 bench_csv_export/10000 的对比观察（格式化层占总开销百分比）
</output>
