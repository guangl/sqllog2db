---
phase: 03-profiling-benchmarking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - benches/bench_csv.rs
autonomous: true
requirements: [PERF-01]
tags: [benchmark, criterion, flamegraph, csv]

must_haves:
  truths:
    - "执行 cargo bench --bench bench_csv 后输出包含 csv_export_real 这个 benchmark group"
    - "Cargo.toml 中存在 [profile.flamegraph] 块，且 strip = \"none\"，debug = true"
    - "bench_csv_real_file 函数在 sqllogs/ 目录缺失时打印 skip 信息并直接 return（不 panic）"
    - "criterion_group! 宏同时注册 bench_csv_export 和 bench_csv_real_file"
  artifacts:
    - path: "Cargo.toml"
      provides: "flamegraph 专用 profile 配置（保留符号）"
      contains: "[profile.flamegraph]"
    - path: "benches/bench_csv.rs"
      provides: "CSV real-file benchmark group（PERF-01 real-file 测量）"
      contains: "fn bench_csv_real_file"
  key_links:
    - from: "Cargo.toml [profile.flamegraph]"
      to: "cargo flamegraph --profile flamegraph"
      via: "cargo profile inheritance"
      pattern: "inherits = \"release\""
    - from: "bench_csv.rs bench_csv_real_file"
      to: "criterion_group! 宏"
      via: "criterion 注册"
      pattern: "criterion_group!\\(benches, bench_csv_export, bench_csv_real_file\\)"
---

<objective>
为 CSV 导出建立 real-file benchmark 路径，并在 Cargo.toml 中新增 flamegraph 专用 profile（保留符号），为后续 flamegraph 采集与 v1.0 baseline 存档奠定基础。

Purpose: 当前 synthetic benchmark 显示 ~4.5M rec/s，但实测 1.1GB 真实日志只有 ~1.55M rec/s。real-file benchmark 是 PERF-01 中"可复现 v1.0 基准"的核心载体；flamegraph profile 是 Phase 3 后续 flamegraph 任务的必要前置（release profile 默认 strip=symbols，会导致火焰图全为 unknown）。

Output:
- Cargo.toml 新增 [profile.flamegraph] 块
- benches/bench_csv.rs 新增 bench_csv_real_file 函数与 criterion_group 注册
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-profiling-benchmarking/03-RESEARCH.md
@.planning/phases/03-profiling-benchmarking/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- handle_run 调用签名（来自 src/cli/run.rs，所有现有 bench 文件遵循）。bench 中必须固定参数 quiet=true 排除进度条 I/O 干扰计时。 -->

```rust
pub fn handle_run(
    cfg: &Config,
    output_path_override: Option<PathBuf>,  // bench 中传 None
    verbose: bool,                           // bench 中传 false
    quiet: bool,                             // bench 中传 true（必须！排除进度条 I/O）
    shutdown: &Arc<AtomicBool>,
    terminal_width: usize,
    dry_run: bool,
    filter_override: Option<String>,
    threads: usize,
) -> Result<(), Error>;
```

<!-- 现有 release profile（不可改动，仅新增 flamegraph profile） -->
```toml
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"  # 原因：flamegraph profile 必须覆盖此项
```

<!-- 真实日志文件状态：sqllogs/ 下已有 269MB × 2 个 .log 文件，总计 ~538MB（不是 1.1GB；BENCHMARKS.md 后续需更新口径） -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 Cargo.toml 新增 [profile.flamegraph] 块</name>
  <files>Cargo.toml</files>
  <read_first>
    - Cargo.toml （查看当前 [profile.release] 块的位置和完整内容，确认在第 88-94 行附近）
    - .planning/phases/03-profiling-benchmarking/03-RESEARCH.md （Pitfall 2 章节，确认 strip="none" + debug=true 的原因）
    - .planning/phases/03-profiling-benchmarking/03-PATTERNS.md （Cargo.toml [profile.release] 块章节，确认追加位置）
  </read_first>
  <action>
    在 Cargo.toml 中 `[profile.release]` 块之后（紧邻 `strip = "symbols"` 行下方一空行后）追加如下确切内容（4 行，不要修改 release profile 的任何字段）：

```toml

[profile.flamegraph]
inherits = "release"
debug = true
strip = "none"
```

关键约束：
- 必须 `inherits = "release"`（保留 LTO、opt-level=3、codegen-units=1 等所有性能设置）
- 必须 `strip = "none"`（覆盖 release 的 `strip = "symbols"`，否则 flamegraph 全是 unknown 符号）
- 必须 `debug = true`（保留 DWARF 调试符号，flamegraph 解析函数名所必需）
- 不要新增其他字段，不要修改 [profile.release]
  </action>
  <verify>
    <automated>cargo build --profile flamegraph --bench bench_csv 2>&1 | tail -5 && grep -c "^\[profile.flamegraph\]" Cargo.toml | grep -q "^1$" && grep -A 3 "^\[profile.flamegraph\]" Cargo.toml | grep -E "(inherits = \"release\"|debug = true|strip = \"none\")" | wc -l | grep -q "^3$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\[profile.flamegraph\]" Cargo.toml` 输出 `1`（且仅 1 个）
    - `grep -A 3 "^\[profile.flamegraph\]" Cargo.toml` 输出包含 `inherits = "release"`
    - `grep -A 3 "^\[profile.flamegraph\]" Cargo.toml` 输出包含 `debug = true`
    - `grep -A 3 "^\[profile.flamegraph\]" Cargo.toml` 输出包含 `strip = "none"`
    - `grep -c "^\[profile.release\]" Cargo.toml` 输出 `1`（release profile 未被破坏）
    - `cargo build --profile flamegraph --bench bench_csv` 退出码为 0
  </acceptance_criteria>
  <done>Cargo.toml 包含完整的 [profile.flamegraph] 块；flamegraph profile 能成功编译 bench_csv（产物在 target/flamegraph/）。</done>
</task>

<task type="auto">
  <name>Task 2: 在 bench_csv.rs 中新增 csv_export_real benchmark group</name>
  <files>benches/bench_csv.rs</files>
  <read_first>
    - benches/bench_csv.rs （完整文件，确认现有 import、make_config、bench_csv_export 与 criterion_group! 宏结构）
    - benches/bench_filters.rs （第 140-170 行，参考 group.sample_size + handle_run 调用的 quiet=true 模式）
    - .planning/phases/03-profiling-benchmarking/03-PATTERNS.md （real-file group 差异点章节）
    - .planning/phases/03-profiling-benchmarking/03-RESEARCH.md （Pitfall 1 章节，理解 real-file vs synthetic 落差原因）
  </read_first>
  <action>
    在 `benches/bench_csv.rs` 中作如下修改：

1. **新增导入（如果尚未导入）：** 在文件顶部 import 块加入 `use std::time::Duration;`（用于 `measurement_time`）。其余 import 已齐全（`PathBuf`、`Arc`、`AtomicBool`、`fs`、criterion 各项）。

2. **新增函数 `bench_csv_real_file`，紧接现有 `bench_csv_export` 函数之后（即 criterion_group! 宏之前）。完整代码如下，逐字写入：**

```rust
fn bench_csv_real_file(c: &mut Criterion) {
    let real_dir = PathBuf::from("sqllogs");
    if !real_dir.exists() {
        eprintln!("sqllogs/ not found, skipping csv_export_real benchmark");
        return;
    }

    let bench_dir = PathBuf::from("target/bench_csv_real");
    fs::create_dir_all(&bench_dir).unwrap();
    let cfg = make_config(&real_dir, &bench_dir);

    let mut group = c.benchmark_group("csv_export_real");
    // 真实文件慢，减少采样次数；measurement_time 给足单次测量窗口
    group.sample_size(10);
    group.measurement_time(Duration::from_secs(60));
    // 记录数未预扫描，省略 Throughput::Elements，仅记录绝对时间
    group.bench_function("real_file", |b| {
        b.iter(|| {
            handle_run(
                &cfg,
                None,
                false,
                true, // quiet=true：排除进度条 I/O
                &Arc::new(AtomicBool::new(false)),
                80,
                false,
                None,
                1,
            )
            .unwrap();
        });
    });
    group.finish();
}
```

3. **更新文件最末尾的 `criterion_group!` 宏**，将原来的：
```rust
criterion_group!(benches, bench_csv_export);
```
替换为：
```rust
criterion_group!(benches, bench_csv_export, bench_csv_real_file);
```

关键约束：
- 复用现有 `make_config(&real_dir, &bench_dir)`，不要重新定义
- `sqllog_dir` 必须传 `PathBuf::from("sqllogs")`（项目根的真实日志目录）
- `bench_dir` 必须用 `target/bench_csv_real` 子目录（避免污染 synthetic bench 的 target/bench_csv）
- handle_run 第 4 参数 `quiet` 必须为 `true`（同其他 bench）
- 不要新建 `[[bench]]` 条目，沿用现有 `bench_csv` 条目即可
- CI skip 模式（`real_dir.exists()` 检查 + return）必不可少，否则 CI 会失败
- 保留现有 `bench_csv_export` 函数和 synthetic 测量逻辑不变
  </action>
  <verify>
    <automated>cargo build --release --bench bench_csv 2>&1 | tail -5 && grep -c "fn bench_csv_real_file" benches/bench_csv.rs | grep -q "^1$" && grep -c "csv_export_real" benches/bench_csv.rs | grep -q "^1$" && grep -c "bench_csv_export, bench_csv_real_file" benches/bench_csv.rs | grep -q "^1$" && cargo bench --bench bench_csv -- --list 2>&1 | grep -q "csv_export_real"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn bench_csv_real_file" benches/bench_csv.rs` 输出 `1`
    - `grep -c "csv_export_real" benches/bench_csv.rs` 输出 `1`（benchmark_group 名称）
    - `grep -c "if !real_dir.exists()" benches/bench_csv.rs` 输出 `1`（CI skip 模式存在）
    - `grep -c "PathBuf::from(\"sqllogs\")" benches/bench_csv.rs` 输出 `1`
    - `grep -c "target/bench_csv_real" benches/bench_csv.rs` 输出 `1`
    - `grep -c "bench_csv_export, bench_csv_real_file" benches/bench_csv.rs` 输出 `1`（criterion_group 已更新）
    - `cargo bench --bench bench_csv -- --list` 输出包含 `csv_export_real/real_file`
    - `cargo build --release --bench bench_csv` 退出码 0
    - `cargo clippy --all-targets -- -D warnings` 退出码 0（项目规范）
    - 现有 `bench_csv_export` 函数仍存在（`grep -c "fn bench_csv_export" benches/bench_csv.rs` 输出 `1`）
  </acceptance_criteria>
  <done>bench_csv.rs 新增 bench_csv_real_file 函数；criterion_group! 已注册两个函数；`cargo bench --bench bench_csv -- --list` 列出 4 个 benchmark（csv_export 的 3 个 + csv_export_real/real_file）；clippy 零警告。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 → benchmark | sqllogs/ 真实日志由本地用户提供，benchmark 仅以只读方式读取 |
| build profile 配置 → 编译产物 | flamegraph profile 仅影响本地开发产物，不发布到 crates.io（exclude 规则已覆盖） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | benches/bench_csv.rs 真实日志读取 | accept | sqllogs/ 已在 .gitignore 中（`/sqllogs`），bench 输出仅写 /dev/null（CSV）和 target/，无外泄路径 |
| T-03-02 | T (Tampering) | Cargo.toml [profile.flamegraph] | accept | 仅本地 dev profile，不影响 release 构建；inherits=release 保证产物语义与 release 一致 |
| T-03-03 | D (Denial of Service) | bench_csv_real_file 长时运行 | mitigate | 设置 `sample_size(10)` 与 `measurement_time(60s)`，并通过 `if !real_dir.exists() return` 在 CI/缺文件环境中安全 skip |
</threat_model>

<verification>
- `cargo build --release --bench bench_csv` 成功
- `cargo build --profile flamegraph --bench bench_csv` 成功（验证 flamegraph profile 配置正确）
- `cargo bench --bench bench_csv -- --list` 输出包含 `csv_export_real/real_file`
- `cargo clippy --all-targets -- -D warnings` 零警告
- `cargo fmt --check` 通过
</verification>

<success_criteria>
- Cargo.toml 包含 [profile.flamegraph] 块（4 字段：inherits/debug/strip 全部正确）
- bench_csv.rs 新增 bench_csv_real_file 函数并在 criterion_group! 中注册
- 在 sqllogs/ 缺失环境下 bench 不 panic（CI skip 模式生效）
- 项目规范全部通过（clippy 零警告 + fmt 无 diff）
</success_criteria>

<output>
After completion, create `.planning/phases/03-profiling-benchmarking/03-01-SUMMARY.md`
</output>
