---
plan_id: "16-01"
phase: 16
title: "TemplateAggregator 扩展 + run.rs observe 调用点更新"
wave: 1
status: pending
files_modified:
  - src/features/template_aggregator.rs
  - src/cli/run.rs
  - src/charts/frequency_bar.rs
---

# Plan 16-01: TemplateAggregator 数据模型扩展

## Goal

在 `TemplateAggregator` 内部新增 `hour_counts` 和 `user_counts` 两个计数器，
更新 `observe()` 签名追加 `user: &str` 参数，新增两个 iter 方法，更新 `merge()`，
并同步更新所有调用点（run.rs、tests）以维持编译通过。

## Tasks

### Task 1: 扩展 TemplateAggregator 结构体和 observe()

文件: `src/features/template_aggregator.rs`

在 struct 定义中新增两个字段：

```rust
use std::collections::BTreeMap;

pub struct TemplateAggregator {
    entries: ahash::AHashMap<String, TemplateEntry>,
    hour_counts: BTreeMap<String, u64>,
    user_counts: ahash::AHashMap<String, u64>,
}
```

更新 `observe()` 签名，追加 `user: &str`：

```rust
pub fn observe(&mut self, key: &str, exectime_us: u64, ts: &str, user: &str) {
    // ... existing entry logic ...

    // hour bucket: 取 ts 前 13 字符，如 "2025-01-15 10"
    if ts.len() >= 13 {
        *self.hour_counts.entry(ts[..13].to_string()).or_insert(0) += 1;
    }

    // user count
    *self.user_counts.entry(user.to_string()).or_insert(0) += 1;
}
```

更新 `Default` 派生 impl，`new()` 直接 `Self::default()`。

### Task 2: 新增 iter_hour_counts() 和 iter_user_counts()

```rust
/// 按 hour bucket key 升序返回 (bucket, count) 迭代器
pub fn iter_hour_counts(&self) -> impl Iterator<Item = (&str, u64)> {
    self.hour_counts.iter().map(|(k, &v)| (k.as_str(), v))
}

/// 按 count 降序返回 (user, count) 迭代器
pub fn iter_user_counts(&self) -> impl Iterator<Item = (&str, u64)> {
    let mut pairs: Vec<(&str, u64)> = self
        .user_counts
        .iter()
        .map(|(k, &v)| (k.as_str(), v))
        .collect();
    pairs.sort_unstable_by(|a, b| b.1.cmp(&a.1).then_with(|| a.0.cmp(b.0)));
    pairs.into_iter()
}
```

注意：`iter_user_counts` 返回 `impl Iterator` 需要将 pairs 收集后 into_iter，
所以返回类型需要是具体的 `std::vec::IntoIter<(&str, u64)>`，
或者使用 `Box<dyn Iterator<Item=(&str, u64)> + '_>`。

实际实现选择收集为 Vec 后返回 `impl Iterator<Item=(&str, u64)> + '_`：

```rust
pub fn iter_user_counts(&self) -> impl Iterator<Item = (&str, u64)> + '_ {
    let mut pairs: Vec<(&str, u64)> = self
        .user_counts
        .iter()
        .map(|(k, &v)| (k.as_str(), v))
        .collect();
    pairs.sort_unstable_by(|a, b| b.1.cmp(&a.1).then_with(|| a.0.cmp(b.0)));
    pairs.into_iter()
}
```

### Task 3: 更新 merge()

在 merge 循环结束后追加：

```rust
// merge hour_counts
for (k, v) in other.hour_counts {
    *self.hour_counts.entry(k).or_insert(0) += v;
}
// merge user_counts
for (k, v) in other.user_counts {
    *self.user_counts.entry(k).or_insert(0) += v;
}
```

### Task 4: 更新 tests 中所有 observe() 调用

`template_aggregator.rs` 内所有测试都要在 `observe()` 调用末尾加上 `""` 作为 user 参数（测试不关心 user，传空串即可）。

例如：
```rust
agg.observe("SELECT * FROM t WHERE id = ?", 500, "2025-01-15 10:00:00", "");
```

同时新增针对 `iter_hour_counts` 和 `iter_user_counts` 的测试：

```rust
#[test]
fn test_iter_hour_counts_basic() {
    let mut agg = TemplateAggregator::new();
    agg.observe("k", 100, "2025-01-15 10:00:00", "alice");
    agg.observe("k", 200, "2025-01-15 10:30:00", "bob");
    agg.observe("k", 300, "2025-01-15 11:00:00", "alice");
    let counts: Vec<_> = agg.iter_hour_counts().collect();
    assert_eq!(counts.len(), 2);
    assert_eq!(counts[0], ("2025-01-15 10", 2));
    assert_eq!(counts[1], ("2025-01-15 11", 1));
}

#[test]
fn test_iter_user_counts_basic() {
    let mut agg = TemplateAggregator::new();
    agg.observe("k", 100, "2025-01-15 10:00:00", "alice");
    agg.observe("k", 200, "2025-01-15 10:30:00", "bob");
    agg.observe("k", 300, "2025-01-15 11:00:00", "alice");
    let counts: Vec<_> = agg.iter_user_counts().collect();
    assert_eq!(counts.len(), 2);
    assert_eq!(counts[0], ("alice", 2));
    assert_eq!(counts[1], ("bob", 1));
}

#[test]
fn test_merge_hour_user_counts() {
    let mut agg1 = TemplateAggregator::new();
    agg1.observe("k", 100, "2025-01-15 10:00:00", "alice");
    let mut agg2 = TemplateAggregator::new();
    agg2.observe("k", 200, "2025-01-15 10:30:00", "alice");
    agg2.observe("k", 300, "2025-01-15 11:00:00", "bob");
    agg1.merge(agg2);
    let hours: Vec<_> = agg1.iter_hour_counts().collect();
    assert_eq!(hours.iter().find(|&&(h,_)| h == "2025-01-15 10").map(|&(_,c)|c), Some(2));
    assert_eq!(hours.iter().find(|&&(h,_)| h == "2025-01-15 11").map(|&(_,c)|c), Some(1));
    let users: Vec<_> = agg1.iter_user_counts().collect();
    assert_eq!(users[0], ("alice", 2));
    assert_eq!(users[1], ("bob", 1));
}
```

### Task 5: 更新 run.rs 调用点

文件: `src/cli/run.rs` 第 255 行

当前:
```rust
agg.observe(&tmpl_key, exectime_us, record.ts.as_ref());
```

更新为:
```rust
agg.observe(&tmpl_key, exectime_us, record.ts.as_ref(), meta.username.as_ref());
```

注意：在 process_log_file 的调用上下文中，`meta` 已经在 `record.parse_meta()` 时解析，
需要确认该行上方的 meta 变量可访问。

### Task 6: 更新 frequency_bar.rs 测试中的 observe() 调用

文件: `src/charts/frequency_bar.rs`

测试 `test_draw_frequency_bar_creates_nonempty_svg` 中：
```rust
agg.observe("SELECT 1", 100, "2025-01-15 10:00:00");
agg.observe("SELECT 2", 200, "2025-01-15 10:00:01");
```
改为：
```rust
agg.observe("SELECT 1", 100, "2025-01-15 10:00:00", "");
agg.observe("SELECT 2", 200, "2025-01-15 10:00:01", "");
```

## Verification

```bash
cargo clippy --all-targets -- -D warnings
cargo test
```

所有测试通过，clippy 无警告。

## Commit Message

```
feat(16-01): extend TemplateAggregator with hour_counts/user_counts and add user param to observe()
```
