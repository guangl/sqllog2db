---
phase: 15-svg
plan: 05
type: execute
wave: 3
depends_on: ["15-03", "15-04"]
files_modified:
  - src/main.rs
  - src/cli/run.rs
  - src/features/mod.rs
  - src/features/template_aggregator.rs
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03]
must_haves:
  truths:
    - "src/main.rs 新增 mod charts; 声明，程序编译成功"
    - "顺序路径（L905 附近）在 exporter_manager.finalize() 之前调用 crate::charts::generate_charts(&agg, charts_cfg)?（D-01 顺序约束）"
    - "并行路径（L804 附近）在 parallel_agg.map(TemplateAggregator::finalize) 之前调用 generate_charts（D-01 + Pitfall 7 防御）"
    - "charts 未配置时两条路径均跳过图表生成（if let Some(charts_cfg) = cfg.features.charts.as_ref()）"
    - "Wave 1 引入的所有 #[allow(dead_code)] / #[allow(unused_imports)] 在 Wave 1 骨架注释指示的位置被移除"
    - "cargo build --release 编译通过，cargo test 全量通过"
  artifacts:
    - path: "src/main.rs"
      provides: "mod charts; 声明"
      contains: "mod charts"
    - path: "src/cli/run.rs"
      provides: "两条路径中 generate_charts 调用点"
      contains: "crate::charts::generate_charts"
    - path: "src/features/mod.rs"
      provides: "#[allow(unused_imports)] 已移除"
    - path: "src/features/template_aggregator.rs"
      provides: "#[allow(dead_code)] 已从 ChartEntry 和 iter_chart_entries 移除"
  key_links:
    - from: "src/cli/run.rs::handle_run（顺序路径）"
      to: "crate::charts::generate_charts"
      via: "if let Some(ref agg) = template_agg { if let Some(charts_cfg) = cfg.features.charts.as_ref() { ... } }"
      pattern: "crate::charts::generate_charts"
    - from: "src/cli/run.rs::handle_run（并行路径）"
      to: "crate::charts::generate_charts"
      via: "if let Some(ref agg) = parallel_agg { if let Some(charts_cfg) = final_cfg.features.charts.as_ref() { ... } }"
      pattern: "crate::charts::generate_charts"
---

<objective>
将 src/charts/ 模块接入主程序和运行路径：在 src/main.rs 声明 mod charts;，在
src/cli/run.rs 的顺序路径和并行路径分别插入 generate_charts 调用点，并移除
Wave 1 留下的所有 #[allow(dead_code)] / #[allow(unused_imports)] 骨架抑制。

Purpose: 完成 Phase 15 最终接入；实现 D-12（单一调用点 generate_charts）和 D-13
（src/main.rs 模块声明）；消除编译器抑制注解，使 clippy 对代码健康状态提供完整保证。
Output: 可工作的端到端 SVG 图表生成路径；全量测试通过；clippy/fmt 干净。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-svg/15-CONTEXT.md
@.planning/phases/15-svg/15-PATTERNS.md
@.planning/phases/15-svg/15-03-SUMMARY.md
@.planning/phases/15-svg/15-04-SUMMARY.md

<interfaces>
<!-- Plan 03/04 已实现的接口，执行者直接使用 -->

From src/charts/mod.rs（Plan 03 实现）:
```rust
pub fn generate_charts(
    agg: &crate::features::TemplateAggregator,
    cfg: &crate::features::ChartsConfig,
) -> crate::error::Result<()>;
```

From src/cli/run.rs（现有代码结构，执行者需精确定位）:

顺序路径（约 L905 附近）的当前代码：
```rust
exporter_manager.finalize()?;
if !quiet {
    exporter_manager.log_stats();
}
let template_stats = template_agg.map(TemplateAggregator::finalize);
if let Some(ref stats) = template_stats {
    info!("Template analysis: {} unique templates", stats.len());
    exporter_manager.write_template_stats(stats, None)?;
}
```

并行路径（约 L804 附近）的当前代码：
```rust
// Phase 14 将消费 finalize() 结果并写出报告；此处先记录聚合摘要。
let template_stats = parallel_agg.map(TemplateAggregator::finalize);
```

Wave 1 骨架注释位置（执行者需读取文件确认行号）：
- src/features/mod.rs：`#[allow(unused_imports)]` 修饰 `pub use template_aggregator::ChartEntry`
- src/features/template_aggregator.rs：`#[allow(dead_code)]` 修饰 ChartEntry struct 和 iter_chart_entries fn

From src/main.rs（现有 mod 块，L6-15）:
```rust
mod cli;
mod color;
mod config;
mod error;
mod exporter;
mod features;
mod lang;
mod logging;
mod parser;
mod resume;
```
按字母序，mod charts; 应插入在 mod cli; 之前。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/main.rs 新增 mod charts; + src/features/*.rs 移除 Wave 1 骨架抑制</name>
  <files>src/main.rs, src/features/mod.rs, src/features/template_aggregator.rs</files>
  <read_first>
    - src/main.rs（完整读取：确认现有 mod 声明块行号；确认 mod cli; 在哪行，以便按字母序在其前插入 mod charts;）
    - src/features/mod.rs（定位 #[allow(unused_imports)] 修饰 pub use template_aggregator::ChartEntry 的行号；VERIFICATION.md 记录此抑制在 L12-13 附近）
    - src/features/template_aggregator.rs（定位 #[allow(dead_code)] 修饰 ChartEntry struct 和 iter_chart_entries fn 的行号；VERIFICATION.md 记录在 L41、L144 附近）
  </read_first>
  <action>
    **修改 1 — src/main.rs：**
    在现有 mod 声明块中按字母序插入 `mod charts;`。charts < cli（字母序），因此在 `mod cli;` 行之前插入。修改后 mod 块开头应为：
    ```rust
    mod charts;
    mod cli;
    mod color;
    // ...（其余不变）
    ```

    **修改 2 — src/features/mod.rs：**
    找到 `#[allow(unused_imports)]` 行（VERIFICATION.md 记录在 mod.rs:12 附近）及其下方的 `pub use template_aggregator::ChartEntry;`。
    仅移除 `#[allow(unused_imports)]` 这一行属性，保留 `pub use template_aggregator::ChartEntry;`（该导出仍需保留，供 charts 模块使用）。
    同时检查是否存在 `#[allow(dead_code)]` 修饰 `ChartsConfig` 或 `pub charts: Option<ChartsConfig>` 字段（VERIFICATION.md 记录在 mod.rs:140、174 附近），若存在则一并移除这些行级 `#[allow(dead_code)]` 注解。

    **修改 3 — src/features/template_aggregator.rs：**
    找到 `#[allow(dead_code)]` 修饰 `pub struct ChartEntry<'a>` 的行（VERIFICATION.md 记录在 :41 附近）并移除该属性行。
    找到 `#[allow(dead_code)]` 修饰 `pub fn iter_chart_entries` 的行（VERIFICATION.md 记录在 :144 附近）并移除该属性行。

    禁止：不要修改任何其他代码（不改变 ChartEntry 结构体定义本身，不改变 iter_chart_entries 签名，不改变 pub use 导出）；不要移除注释说明（若有关于 Phase 15 的上下文注释，保留）。
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | grep -c "warning\[dead_code\]\|warning\[unused_imports\]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mod charts" src/main.rs` 返回 1 行
    - `grep -n "mod charts" src/main.rs | head -1` 显示行号早于 `grep -n "mod cli" src/main.rs`（字母序正确）
    - `grep -n "#\[allow(unused_imports)\]" src/features/mod.rs` 返回 0 行（已全部移除）
    - `grep -n "#\[allow(dead_code)\]" src/features/template_aggregator.rs` 返回 0 行（ChartEntry 和 iter_chart_entries 的抑制已移除）
    - `grep -n "pub use template_aggregator::ChartEntry" src/features/mod.rs` 仍返回 1 行（导出保留）
    - `cargo build --lib 2>&1 | grep -c "warning\[dead_code\]\|warning\[unused_imports\]"` 返回 0（无该类警告）
    - `cargo build --lib` 退出码 0
  </acceptance_criteria>
  <done>mod charts; 已声明；Wave 1 全部抑制注解已移除；cargo build --lib 无 dead_code/unused_imports 警告。</done>
</task>

<task type="auto">
  <name>Task 2: src/cli/run.rs 接入 generate_charts（顺序路径 + 并行路径）</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（读取 L790-920 范围，精确定位以下锚点：
      1. 顺序路径：`exporter_manager.finalize()?;` 行（约 L905）——插入点在此行之前
      2. 并行路径：`let template_stats = parallel_agg.map(TemplateAggregator::finalize);`（约 L805）——插入点在此行之前
      确认变量名：顺序路径用 `template_agg`，并行路径用 `parallel_agg`，配置变量顺序路径用 `cfg`，并行路径用 `final_cfg`）
    - .planning/phases/15-svg/15-PATTERNS.md（§"src/cli/run.rs" — 当前顺序路径代码（L905-915）和并行路径（L804-814）原文；Phase 15 修改后代码片段；`if let Some(ref x) = option` 风格确认）
    - src/charts/mod.rs（确认 generate_charts 签名和完整路径 crate::charts::generate_charts）
  </read_first>
  <action>
    **顺序路径插入（在 `exporter_manager.finalize()?;` 之前）：**

    读取 run.rs 找到 `exporter_manager.finalize()?;` 这一行（约 L905）。
    在该行之前插入以下代码块（与上方代码空一行，与 run.rs 现有 if let 风格一致）：
    ```rust
    if let Some(ref agg) = template_agg {
        if let Some(charts_cfg) = cfg.features.charts.as_ref() {
            crate::charts::generate_charts(agg, charts_cfg)?;
        }
    }
    ```

    **并行路径插入（在 `let template_stats = parallel_agg.map(...)` 之前）：**

    读取 run.rs 找到 `let template_stats = parallel_agg.map(TemplateAggregator::finalize);`（约 L805，PATTERNS.md 给出该行原文）。
    在该行之前插入（注意并行路径的配置变量是 `final_cfg`，不是 `cfg`）：
    ```rust
    if let Some(ref agg) = parallel_agg {
        if let Some(charts_cfg) = final_cfg.features.charts.as_ref() {
            crate::charts::generate_charts(agg, charts_cfg)?;
        }
    }
    ```

    **注意事项：**
    - 两处插入均使用 `if let Some(ref agg) = ...` 模式（借用，不转移所有权），使后续的 `.map(TemplateAggregator::finalize)` 和 `exporter_manager.finalize()` 仍可使用各自变量
    - 顺序路径变量名：`template_agg`（Option<TemplateAggregator>）和 `cfg`（Config ref）
    - 并行路径变量名：`parallel_agg`（Option<TemplateAggregator>）和 `final_cfg`（Config ref 或 Arc<Config>，执行者须读取代码确认确切类型和变量名）
    - 不修改任何现有代码逻辑，只在指定位置插入新的 if let 块

    禁止：不要调整 exporter_manager.finalize() 的位置；不要调整 template_agg.map(finalize) 的位置；不要使用 cfg 替换 final_cfg（并行路径配置变量名与顺序路径不同）。
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -5 && cargo test --lib 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "crate::charts::generate_charts" src/cli/run.rs` 返回 2（顺序路径 + 并行路径各 1 处）
    - `grep -n "crate::charts::generate_charts" src/cli/run.rs` 显示两行行号，较小行号在并行路径区域（约 L804-815），较大行号在顺序路径区域（约 L905-915）
    - `cargo build --release` 退出码 0（release 编译通过）
    - `cargo test --lib` 全量通过（无回归，原有 ≥ 375 个测试继续 pass）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `cargo fmt --check` 退出码 0
    - 手工检查：顺序路径插入点位于 `exporter_manager.finalize()?;` 之前（D-01 约束：图表生成在 finalize 之前，histogram 仍可访问）
    - 手工检查：并行路径插入点位于 `parallel_agg.map(TemplateAggregator::finalize)` 之前（Pitfall 7 防御）
  </acceptance_criteria>
  <done>两条路径均接入 generate_charts；release 编译通过；全量测试无回归；clippy/fmt 干净。Phase 15 实现完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run.rs → charts::generate_charts | 内部函数调用（无外部信任边界，但需确保调用时序：finalize() 之前访问 histogram） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | generate_charts 调用时序（finalize 后 histogram 不可用） | mitigate | 顺序路径插入在 exporter_manager.finalize() 之前；并行路径插入在 parallel_agg.map(finalize) 之前；D-01 锁定调用顺序 |
| T-15-11 | Tampering | 并行路径 parallel_agg 所有权移动 | mitigate | 使用 `if let Some(ref agg) = parallel_agg` 借用模式（不转移所有权），保证后续 .map(finalize) 仍可执行 |
</threat_model>

<verification>
- `grep -c "crate::charts::generate_charts" src/cli/run.rs` = 2
- `grep -n "mod charts" src/main.rs` 返回 1 行（位于 mod cli; 之前）
- `grep -n "#\[allow(dead_code)\]" src/features/template_aggregator.rs` 返回 0 行
- `grep -n "#\[allow(unused_imports)\]" src/features/mod.rs` 返回 0 行
- `cargo build --release` 退出码 0
- `cargo test` 全量通过（覆盖 lib + integration）
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo fmt --check` 退出码 0
</verification>

<success_criteria>
- mod charts; 在 src/main.rs 声明（D-13）
- generate_charts 在顺序路径 exporter_manager.finalize() 之前被调用（D-01、D-12）
- generate_charts 在并行路径 parallel_agg.finalize() 之前被调用（D-01、D-12）
- Wave 1 所有 #[allow(dead_code)] / #[allow(unused_imports)] 已移除
- cargo build --release 和 cargo test 全量通过
- Phase 15 全部五条成功标准（SC-1 ~ SC-5）可验收
</success_criteria>

<output>
Create `.planning/phases/15-svg/15-05-SUMMARY.md` when done
</output>
