---
phase: 09-cli
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - benches/BENCHMARKS.md
autonomous: false
requirements: [PERF-11]

must_haves:
  truths:
    - "hyperfine 对 sqllog2db --version 和 sqllog2db validate -c config.toml 各完成 warmup 3 轮 + 测量"
    - "benches/BENCHMARKS.md 包含 'Phase 9 CLI 冷启动基线' 节"
    - "报告包含优化前/后对比表和 hyperfine 原始输出"
    - "报告包含三个对比维度：before/after、有/无 regex、最终数字"
    - "双重编译消除结论记录（grep 验证 try_from_meta 唯一调用）"
  artifacts:
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 9 CLI 冷启动基线数据"
      contains: "Phase 9 — CLI 冷启动基线"
  key_links:
    - from: "hyperfine 输出"
      to: "benches/BENCHMARKS.md"
      via: "手动填写测量结果"
      pattern: "Phase 9"
---

<objective>
用 hyperfine 量化 Phase 9 代码改造前后的 CLI 冷启动耗时，将原始数据记录到 benches/BENCHMARKS.md。

Purpose: PERF-11 要求 hyperfine 基线测量完成并记录，数据是验收门控（per D-07/D-08/D-09/D-10）。数据本身无硬性阈值要求，记录即通过。

Output: benches/BENCHMARKS.md 新增 "Phase 9 CLI 冷启动基线" 节，含三对比维度数据和 hyperfine 原始输出。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cli/09-CONTEXT.md
@.planning/phases/09-cli/09-PATTERNS.md
@.planning/phases/09-cli/09-03-SUMMARY.md

<interfaces>
<!-- BENCHMARKS.md Phase 4/5 节格式，作为新节的模板 -->

新增节格式（per D-09，per 09-PATTERNS.md）：

```markdown
---

## Phase 9 — CLI 冷启动基线（PERF-11）

**Date:** YYYY-MM-DD
**Goal:** 量化双重 regex 编译消除前后的冷启动耗时；记录 hyperfine 原始输出
**Test environment:** Apple Silicon (Darwin 25.4.0), release build

### 测量命令

```bash
hyperfine --warmup 3 'sqllog2db --version'
hyperfine --warmup 3 'sqllog2db validate -c config.toml'
hyperfine --warmup 3 'sqllog2db validate -c config_no_regex.toml'
```

### 对比维度

| 命令 | 优化前 (mean) | 优化后 (mean) | 差值 |
|------|--------------|--------------|------|
| `sqllog2db --version` | X ms | Y ms | Z ms |
| `validate`（含 regex） | X ms | Y ms | Z ms |
| `validate`（无 regex） | X ms | Y ms | Z ms |

...（hyperfine 原始输出 + 结论）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 构建 release 版本并记录优化后 hyperfine 基线数据</name>
  <read_first>
    - benches/BENCHMARKS.md（完整文件，找到末尾位置，确认追加节的格式）
    - .planning/phases/09-cli/09-CONTEXT.md（D-07 测量方案、D-08 三对比维度、D-09 写入位置、D-10 无阈值）
  </read_first>
  <files>benches/BENCHMARKS.md</files>
  <action>
按以下步骤完成测量和记录：

**步骤 1：构建 release 二进制（优化后版本）**

```bash
cargo build --release
```

确认 target/release/sqllog2db 存在且构建成功。

**步骤 2：准备测量环境**

确认 config.toml 存在（通过 `cargo run -- init -o config.toml --force` 生成）。
准备一个无 regex 配置文件 config_no_regex.toml：直接复制 config.toml 内容，去掉任何 usernames/client_ips 等正则字段（保留最基本的 [sqllog] path 配置即可）。

**步骤 3：运行 hyperfine 测量（优化后）**

```bash
hyperfine --warmup 3 './target/release/sqllog2db --version'
hyperfine --warmup 3 './target/release/sqllog2db validate -c config.toml'
hyperfine --warmup 3 './target/release/sqllog2db validate -c config_no_regex.toml'
```

记录每次输出的 mean 时间（单位 ms）。

**步骤 4：验证双重编译已消除**

```bash
grep -rn "from_meta\b\|from_sql_filters\b" src/
# 预期：0 个匹配（旧函数名已完全删除）

grep -rn "try_from_meta\|try_from_sql_filters" src/
# 预期：只在 filters.rs（定义）、config.rs（validate 调用）、run.rs（try_new 内调用）出现
```

**步骤 5：在 benches/BENCHMARKS.md 末尾追加新节**

在文件末尾追加以下内容（用实际测量数字填充占位符）：

```markdown
---

## Phase 9 — CLI 冷启动基线（PERF-11）

**Date:** 2026-05-11
**Goal:** 量化双重 regex 编译消除前后的冷启动耗时；记录 hyperfine 原始输出
**Test environment:** Apple Silicon (Darwin 25.4.0), release build (`opt-level=3`, LTO=fat, strip=symbols, panic=abort)

### 测量命令

```bash
hyperfine --warmup 3 './target/release/sqllog2db --version'
hyperfine --warmup 3 './target/release/sqllog2db validate -c config.toml'
hyperfine --warmup 3 './target/release/sqllog2db validate -c config_no_regex.toml'
```

### 对比维度（per D-08）

| 命令 | 优化前¹ (mean) | 优化后 (mean) | 差值 |
|------|--------------|--------------|------|
| `sqllog2db --version` | N/A | [实际值] ms | — |
| `validate`（含 regex） | N/A | [实际值] ms | — |
| `validate`（无 regex） | N/A | [实际值] ms | — |

¹ 优化前无历史 hyperfine 数据；本次为首次基线记录。

**有/无 regex 差值：** [validate含regex mean] − [validate无regex mean] ≈ [差值] ms （regex 编译耗时估算）

### Hyperfine 原始输出

<details>
<summary>sqllog2db --version</summary>

```
[粘贴 hyperfine 原始输出]
```

</details>

<details>
<summary>validate -c config.toml（含 regex）</summary>

```
[粘贴 hyperfine 原始输出]
```

</details>

<details>
<summary>validate -c config_no_regex.toml（无 regex）</summary>

```
[粘贴 hyperfine 原始输出]
```

</details>

### 结论

- [x] 双重编译已消除：`grep -rn "from_meta\b" src/` 返回 0 个匹配；每个 regex 在整条代码路径中只调用一次 `Regex::new()`
- [x] hyperfine 数据已记录
- [x] update check 已后台化：`grep -n "thread::spawn" src/cli/update.rs` 确认存在
```

注意：将所有 [实际值]、[粘贴 hyperfine 原始输出] 等占位符替换为实际测量数字和输出。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -c "Phase 9" benches/BENCHMARKS.md</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && grep -n "CLI 冷启动基线" benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Phase 9" benches/BENCHMARKS.md 返回值 >= 1
    - grep -n "CLI 冷启动基线" benches/BENCHMARKS.md 返回至少 1 行
    - grep -n "hyperfine" benches/BENCHMARKS.md 返回至少 3 行（三个测量命令）
    - benches/BENCHMARKS.md 末尾新节包含实际数字（不含未替换的 "[实际值]" 占位符）
    - grep -n "\[实际值\]" benches/BENCHMARKS.md 返回 0 行（所有占位符已填充）
    - grep -rn "from_meta\b" src/ | grep -v "try_from_meta" 返回 0 行（双重编译已消除）
  </acceptance_criteria>
  <done>benches/BENCHMARKS.md 包含 "Phase 9 CLI 冷启动基线" 节，含 hyperfine 实际测量数字和三对比维度，双重编译消除结论已验证。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验收报告人工确认</name>
  <what-built>
    Task 1 完成了 hyperfine 测量并将数据写入 benches/BENCHMARKS.md。
    全部四个 Plan 的改造（filters.rs 重构、update check 后台化、config.rs/run.rs 接入、基线记录）均已完成。
  </what-built>
  <how-to-verify>
    1. 检查 BENCHMARKS.md 末尾新增的 "Phase 9 CLI 冷启动基线" 节是否有实际测量数字
    2. 运行 `cargo test` 确认 651 个测试全部通过
    3. 运行 `cargo clippy --all-targets -- -D warnings` 确认无 error
    4. 运行 `grep -rn "from_meta\b" src/ | grep -v "try_from_meta"` 确认返回 0 行
    5. 运行 `grep -n "thread::spawn" src/cli/update.rs` 确认 update check 已后台化
    6. 阅读 benches/BENCHMARKS.md 中记录的数据，确认内容合理
  </how-to-verify>
  <resume-signal>验证通过后输入 "approved"；如有问题请描述具体问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hyperfine 测量环境 | 本机测量，不涉及外部输入，结果写入文档 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Repudiation | BENCHMARKS.md 记录 | accept | 文档记录测量日期和环境，可重现；无签名机制但 git 历史提供审计链 |
</threat_model>

<verification>
```bash
cd /Users/guang/Projects/sqllog2db
cargo test
cargo clippy --all-targets -- -D warnings
grep -n "Phase 9" benches/BENCHMARKS.md
grep -rn "from_meta\b" src/ | grep -v "try_from_meta"
grep -n "thread::spawn" src/cli/update.rs
```
</verification>

<success_criteria>
- benches/BENCHMARKS.md 含 "Phase 9 — CLI 冷启动基线" 节且无未填充的占位符
- 三个 hyperfine 命令的实际数字已记录
- cargo test：651+ 测试通过
- cargo clippy：0 error
- 双重编译消除确认：from_meta（旧名）在 src/ 中零调用
</success_criteria>

<output>
完成后创建 .planning/phases/09-cli/09-04-SUMMARY.md
</output>
