---
phase: 15-svg
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - Cargo.toml
  - src/charts/mod.rs
  - src/charts/frequency_bar.rs
autonomous: true
requirements: [CHART-01, CHART-02]
must_haves:
  truths:
    - "plotters 0.3.7 被添加到 Cargo.toml 并成功编译（svg_backend + all_series + all_elements）"
    - "src/charts/mod.rs 导出 pub fn generate_charts(agg, cfg)，并在 frequency_bar 开关为 true 时写入 top_n_frequency.svg"
    - "sanitize_filename 将非 ASCII/非字母数字字符替换为 _，截断到 80 字符"
    - "draw_frequency_bar 生成非空 SVG 文件，根节点调用 root.present()（SC-4）"
    - "truncate_label 在超过 40 字符时截断并追加 …（Unicode 安全）"
    - "charts 未启用时（cfg.frequency_bar=false）不写入任何 SVG 文件（SC-5 方向）"
  artifacts:
    - path: "Cargo.toml"
      provides: "plotters 0.3.7 依赖声明"
      contains: "plotters"
    - path: "src/charts/mod.rs"
      provides: "generate_charts 入口 + sanitize_filename + 子模块声明"
      exports: ["generate_charts"]
    - path: "src/charts/frequency_bar.rs"
      provides: "draw_frequency_bar + truncate_label"
      exports: ["draw_frequency_bar"]
  key_links:
    - from: "src/charts/mod.rs::generate_charts"
      to: "src/charts/frequency_bar.rs::draw_frequency_bar"
      via: "frequency_bar::draw_frequency_bar(&entries, cfg.top_n, &path)?"
      pattern: "frequency_bar::draw_frequency_bar"
    - from: "src/charts/mod.rs"
      to: "crate::features::TemplateAggregator"
      via: "agg.iter_chart_entries().collect()"
      pattern: "iter_chart_entries"
    - from: "src/charts/frequency_bar.rs"
      to: "plotters::prelude::SVGBackend"
      via: "SVGBackend::new(output_path, (1200, 600)).into_drawing_area()"
      pattern: "SVGBackend"
---

<objective>
引入 plotters 0.3.7（SVG-only）依赖，新建 src/charts/ 模块，实现图表生成入口
generate_charts 以及 Top N 频率横向条形图 draw_frequency_bar。

Purpose: 实现 CHART-01（SVG 输出目录创建）和 CHART-02（频率条形图）的核心逻辑；
为 Plan 04（耗时直方图）提供 plotters 依赖和模块骨架。
Output: Cargo.toml（plotters 新增）、src/charts/mod.rs、src/charts/frequency_bar.rs。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-svg/15-CONTEXT.md
@.planning/phases/15-svg/15-RESEARCH.md
@.planning/phases/15-svg/15-PATTERNS.md
@.planning/phases/15-svg/15-01-SUMMARY.md
@.planning/phases/15-svg/15-02-SUMMARY.md

<interfaces>
<!-- Wave 1 已实现的接口，执行者直接使用 -->

From src/features/mod.rs（Wave 1 实现）:
```rust
pub use template_aggregator::ChartEntry;  // 已导出（带 #[allow(unused_imports)]）

pub struct ChartsConfig {
    pub output_dir: String,
    pub top_n: usize,       // 默认 10
    pub frequency_bar: bool, // 默认 true
    pub latency_hist: bool,  // 默认 true
}
```

From src/features/template_aggregator.rs（Wave 1 实现）:
```rust
pub struct ChartEntry<'a> {
    pub key: &'a str,
    pub count: u64,
    pub histogram: &'a hdrhistogram::Histogram<u64>,
}

impl TemplateAggregator {
    // 按 count 降序迭代，不消耗 self
    pub fn iter_chart_entries(&self) -> impl Iterator<Item = ChartEntry<'_>>;
}
```

From src/error.rs:
```rust
pub type Result<T> = std::result::Result<T, Error>;

pub enum FileError {
    CreateDirectoryFailed { path: PathBuf, reason: String },
    WriteFailed { path: PathBuf, reason: String },
}
// Error::File(FileError) 可用
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo.toml 新增 plotters 0.3.7 依赖 + 新建 src/charts/mod.rs（generate_charts + sanitize_filename + 单元测试）</name>
  <files>Cargo.toml, src/charts/mod.rs</files>
  <read_first>
    - Cargo.toml（完整读取一次：确认 plotters 未存在，了解现有 default-features = false 依赖写法；hdrhistogram 版本确认为 7.5.4）
    - src/features/mod.rs（确认 ChartsConfig 和 ChartEntry 已导出；了解 pub use template_aggregator::ChartEntry 和 #[allow(unused_imports)] 位置）
    - src/error.rs（完整读取：确认 FileError::CreateDirectoryFailed / WriteFailed 变体签名，了解错误构造方式）
    - .planning/phases/15-svg/15-PATTERNS.md（§"src/charts/mod.rs" 节：错误映射模式、generate_charts 骨架、sanitize_filename 实现）
    - .planning/phases/15-svg/15-RESEARCH.md（§"Package Legitimacy Audit" 确认 plotters [ASSUMED] 状态；§"Standard Stack" Cargo.toml 片段）
  </read_first>
  <behavior>
    - Test test_sanitize_filename_ascii_alphanumeric: "SELECT_count" → "SELECT_count"（无变化）
    - Test test_sanitize_filename_spaces_and_special: "SELECT count(*) FROM t" → "SELECT_count____FROM_t"（空格和括号、星号均替换为 _）
    - Test test_sanitize_filename_truncate_80: 长度 > 80 的字符串截断到恰好 80 个字符
    - Test test_sanitize_filename_non_ascii: 含中文字符的 key 中所有汉字替换为 _
    - Test test_sanitize_filename_hyphen_preserved: "key-name" → "key-name"（连字符保留）
  </behavior>
  <action>
    **步骤 1 — Cargo.toml**：在 `[dependencies]` 块的 `hdrhistogram = "7.5.4"` 行之后新增（按字母序 h → p，plotters 在 p 开头，位于 rayon/regex 之前）：
    ```
    plotters = { version = "0.3.7", default-features = false, features = [
      "svg_backend",
      "all_series",
      "all_elements",
    ] }
    ```
    不修改任何其他依赖；不添加 bitmap_backend/ttf/chrono 等 feature（RESEARCH.md Anti-Patterns）。

    **步骤 2 — 新建 src/charts/mod.rs**：
    1. 声明子模块：`pub mod frequency_bar;` 和 `pub mod latency_hist;`（Plan 04 会创建 latency_hist.rs，此处先声明）
    2. 实现 `pub fn generate_charts(agg: &crate::features::TemplateAggregator, cfg: &crate::features::ChartsConfig) -> crate::error::Result<()>`：
       - 用 `std::path::Path::new(&cfg.output_dir)` 获取输出目录路径
       - 调用 `std::fs::create_dir_all(output_dir).map_err(|e| crate::error::Error::File(crate::error::FileError::CreateDirectoryFailed { path: output_dir.to_path_buf(), reason: e.to_string() }))?`（SC-5：只有在调用 generate_charts 时才创建目录）
       - 用 `let entries: Vec<_> = agg.iter_chart_entries().collect();` 收集条目
       - 若 `cfg.frequency_bar`：构造 `output_dir.join("top_n_frequency.svg")` 路径，调用 `frequency_bar::draw_frequency_bar(&entries, cfg.top_n, &path)?`
       - 若 `cfg.latency_hist`：遍历 `entries.iter().take(cfg.top_n)`，对每个 entry 构造 `format!("latency_histogram_{}.svg", sanitize_filename(entry.key))` 文件名，调用 `latency_hist::draw_latency_hist(entry.key, entry.histogram, &path)?`
       - 函数体 ≤ 40 行；若超出，将 latency_hist 循环提取为 `fn draw_all_latency_hists(...)` 私有函数
    3. 实现私有 `fn sanitize_filename(key: &str) -> String`（PATTERNS.md §sanitize_filename）：将非 `is_ascii_alphanumeric() || c == '_' || c == '-'` 的字符映射为 `_`，结果用 `.chars().take(80).collect()`
    4. 在 `#[cfg(test)]` 模块新增 5 个测试（见 behavior）；测试 sanitize_filename 直接调用私有函数

    禁止：不要为 plotters 错误引入 Box<dyn Error>；统一用 `crate::error::Result`；不要为 error.rs 新增 Chart 变体（用 FileError::WriteFailed 包装 plotters 绘制错误，见 Task 2）。
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | head -20 && cargo test --lib charts::tests::test_sanitize -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "plotters" Cargo.toml` 返回至少 1 行，含 "0.3.7"
    - `grep -n "svg_backend" Cargo.toml` 返回 1 行
    - `grep -n "all_series" Cargo.toml` 返回 1 行
    - `grep -n "all_elements" Cargo.toml` 返回 1 行
    - `grep -n "pub mod frequency_bar" src/charts/mod.rs` 返回 1 行
    - `grep -n "pub mod latency_hist" src/charts/mod.rs` 返回 1 行
    - `grep -n "pub fn generate_charts" src/charts/mod.rs` 返回 1 行
    - `grep -n "fn sanitize_filename" src/charts/mod.rs` 返回 1 行
    - `grep -n "create_dir_all" src/charts/mod.rs` 返回 1 行（非 ensure_parent_dir）
    - `cargo test --lib charts::tests::test_sanitize_filename_ascii_alphanumeric` 通过
    - `cargo test --lib charts::tests::test_sanitize_filename_spaces_and_special` 通过
    - `cargo test --lib charts::tests::test_sanitize_filename_truncate_80` 通过
    - `cargo test --lib charts::tests::test_sanitize_filename_non_ascii` 通过
    - `cargo test --lib charts::tests::test_sanitize_filename_hyphen_preserved` 通过
    - `cargo build --lib` 退出码 0（plotters 可用，charts 模块可编译）
  </acceptance_criteria>
  <done>plotters 依赖已编译；charts/mod.rs 导出 generate_charts；5 个 sanitize 单元测试全部 pass；cargo build --lib 零错误。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 src/charts/frequency_bar.rs（draw_frequency_bar + truncate_label + 单元测试）</name>
  <files>src/charts/frequency_bar.rs</files>
  <read_first>
    - src/charts/mod.rs（刚刚创建：确认 pub mod frequency_bar 已声明，了解 generate_charts 如何调用 draw_frequency_bar）
    - .planning/phases/15-svg/15-RESEARCH.md（§"Pattern 2: 横向频率条形图" — ChartBuilder、into_segmented()、Histogram::horizontal、SegmentValue formatter 完整代码示例；§"Pitfall 5: Y 轴标签 SegmentedCoord"；§"Pitfall 4: SVGBackend 未调用 present()"）
    - .planning/phases/15-svg/15-PATTERNS.md（§"src/charts/frequency_bar.rs" — plotters API 骨架、truncate_label 实现）
    - src/features/template_aggregator.rs（确认 ChartEntry 字段：key: &'a str, count: u64）
  </read_first>
  <behavior>
    - Test test_truncate_label_short: "SELECT 1"（8字符）→ 原样返回，不追加 …
    - Test test_truncate_label_exact_40: 恰好 40 个 ASCII 字符 → 原样返回（不截断）
    - Test test_truncate_label_41_chars: 41 个 ASCII 字符 → 返回前 39 个字符 + "…"（总 Unicode 字符数 = 40）
    - Test test_truncate_label_unicode: 含中文的 key 长度超过 40 字符 → chars().count() 超出时截断（Unicode 安全）
    - Test test_draw_frequency_bar_creates_nonempty_svg: 用 tempfile 目录，传入 2 个 ChartEntry（假造），调用 draw_frequency_bar 后 SVG 文件存在且字节数 > 0
  </behavior>
  <action>
    在 src/charts/frequency_bar.rs 实现：

    **导入区：**
    ```rust
    use plotters::prelude::*;
    use crate::features::ChartEntry;
    use crate::error::{Error, FileError};
    ```

    **常量（在文件顶部）：**
    - `const CHART_W: u32 = 1200;`（D-09）
    - `const CHART_H: u32 = 600;`（D-09）
    - `const STEELBLUE: RGBColor = RGBColor(70, 130, 180);`（Claude 自行决定配色）
    - `const MAX_LABEL_CHARS: usize = 40;`（D-08）

    **draw_frequency_bar 函数签名：**
    `pub fn draw_frequency_bar(entries: &[ChartEntry<'_>], top_n: usize, output_path: &std::path::Path) -> crate::error::Result<()>`

    **函数逻辑（≤ 40 行；若超出按如下方式拆分）：**
    1. 用 `entries.iter().take(top_n).map(|e| (truncate_label(e.key, MAX_LABEL_CHARS), e.count)).collect::<Vec<_>>()` 准备数据
    2. 若 data 为空，直接 return Ok(())（无数据时跳过）
    3. 计算 `max_count` 和 `n = data.len()`
    4. 创建 `SVGBackend::new(output_path, (CHART_W, CHART_H)).into_drawing_area()`；用 `map_err(|e| Error::File(FileError::WriteFailed { path: output_path.to_path_buf(), reason: e.to_string() }))?` 处理 plotters 错误
    5. `root.fill(&WHITE)` + map_err
    6. 调用私有函数 `build_chart(&root, &data, max_count, n)?`（如需拆分）
    7. 最后 `root.present().map_err(...)?`（SC-4：必须显式调用）

    若 draw_frequency_bar 超过 40 行，提取 `fn build_chart(root, data, max_count, n) -> Result<()>` 私有函数，包含 ChartBuilder + configure_mesh + draw_series 逻辑。

    **configure_mesh Y 轴标签格式化（RESEARCH.md Pitfall 5）：**
    先收集 `let labels: Vec<String> = data.iter().map(|(k, _)| k.clone()).collect()`，
    在 `y_label_formatter` closure 中 match SegmentValue：
    - `SegmentValue::CenterOf(i) | SegmentValue::Exact(i)` → `labels.get(*i).cloned().unwrap_or_default()`
    - `SegmentValue::Last` → `String::new()`

    **draw_series（RESEARCH.md Pattern 2）：**
    `Histogram::horizontal(&chart).style(STEELBLUE.filled()).margin(5).data(data.iter().enumerate().map(|(i, (_, count))| (i, *count)))`

    **truncate_label 函数：**
    `fn truncate_label(key: &str, max_chars: usize) -> String`
    使用 `key.chars().collect::<Vec<char>>()` 收集；若 `chars.len() <= max_chars` 返回 `key.to_string()`；否则取 `chars[..max_chars - 1]` 并拼接 `"…"`（Unicode 安全，D-08）

    **单元测试 5 个（见 behavior）：** test_draw_frequency_bar_creates_nonempty_svg 使用 tempfile::TempDir，构造 2 个 ChartEntry（须在测试模块内创建 TemplateAggregator 再借用，或手动构造满足 ChartEntry 结构的数据；ChartEntry 包含 histogram 引用，需创建真实 hdrhistogram::Histogram<u64>）

    禁止：不使用 Box<dyn Error>；不调用 LogRange::new()；root.flush() 不能替代 root.present()（SVGBackend 的 flush 是 present()）。
  </action>
  <verify>
    <automated>cargo test --lib charts::frequency_bar::tests -- --nocapture && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn draw_frequency_bar" src/charts/frequency_bar.rs` 返回 1 行
    - `grep -n "fn truncate_label" src/charts/frequency_bar.rs` 返回 1 行
    - `grep -n "root.present()" src/charts/frequency_bar.rs` 返回至少 1 行（SC-4）
    - `grep -n "Histogram::horizontal" src/charts/frequency_bar.rs` 返回 1 行
    - `grep -n "into_segmented" src/charts/frequency_bar.rs` 返回 1 行
    - `grep -n "SegmentValue" src/charts/frequency_bar.rs` 返回至少 2 行（使用 CenterOf 和 Last 两个 arm）
    - `cargo test --lib charts::frequency_bar::tests::test_truncate_label_short` 通过
    - `cargo test --lib charts::frequency_bar::tests::test_truncate_label_41_chars` 通过
    - `cargo test --lib charts::frequency_bar::tests::test_truncate_label_unicode` 通过
    - `cargo test --lib charts::frequency_bar::tests::test_draw_frequency_bar_creates_nonempty_svg` 通过（SVG 文件非空）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0（无新增警告）
    - 所有函数体 ≤ 40 行（检查：`wc -l src/charts/frequency_bar.rs` 参考值；人工确认无单函数超 40 行）
  </acceptance_criteria>
  <done>draw_frequency_bar 生成非空 SVG 文件；root.present() 显式调用；truncate_label Unicode 安全；5 个测试全部 pass；clippy 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChartsConfig.output_dir → 文件系统 | 用户在 config.toml 指定的输出目录（单机 CLI，用户可信） |
| ChartEntry.key → 文件名 | 模板 key 内容来自 SQL log 文件（外部输入，需 sanitize） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | sanitize_filename（文件名构造） | mitigate | 字符白名单（ASCII 字母数字 + _ + -），截断 80 字符；防止 key 含 `../` 导致路径穿越 |
| T-15-05 | DoS | output_dir 路径过深 | accept | CLI 单机工具，output_dir 由用户在 config.toml 配置，用户可信；create_dir_all 处理深层目录 |
| T-15-06 | Tampering | SVGBackend 文件写入 | mitigate | 使用 FileError::WriteFailed 包装 plotters 错误，统一错误传播；root.present() 确保缓冲区刷新 |
| T-15-SC | Tampering | plotters 0.3.7（[ASSUMED]） | mitigate | slopcheck 不可用；159M 下载 + Criterion.rs 依赖；人工确认 crates.io 来源后视为可信 |
</threat_model>

<verification>
- `grep -c "plotters" Cargo.toml` >= 1
- `cargo build --lib` 退出码 0
- `cargo test --lib charts` 全部通过（sanitize + truncate + svg 写入测试）
- `cargo test --lib` 全部通过（无回归，≥375 个已有测试）
- `cargo clippy --all-targets -- -D warnings` 退出码 0
- `cargo fmt --check` 退出码 0
</verification>

<success_criteria>
- Cargo.toml 含 plotters 0.3.7（svg_backend + all_series + all_elements，无 bitmap_backend）
- src/charts/mod.rs：generate_charts 入口、sanitize_filename、5 个单元测试
- src/charts/frequency_bar.rs：draw_frequency_bar（root.present() 显式调用）、truncate_label（Unicode 安全）、5 个单元测试
- 全部函数体 ≤ 40 行
- cargo build --lib 和 cargo test --lib 均通过
</success_criteria>

<output>
Create `.planning/phases/15-svg/15-03-SUMMARY.md` when done
</output>
