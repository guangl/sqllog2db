---
phase: "02"
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/exporter/mod.rs
  - src/cli/run.rs
autonomous: true
requirements:
  - FIELD-01
must_haves:
  truths:
    - "ExporterManager::from_config() 计算 ordered_indices 并注入到 CSV 和 SQLite exporter"
    - "process_csv_parallel() 函数签名新增 ordered_indices: &[usize] 参数"
    - "并行路径为每个临时 CsvExporter 设置 ordered_indices（Pitfall 1 防护）"
    - "handle_run 计算 ordered_indices 并传入 process_csv_parallel()"
    - "cargo test 全量通过，cargo clippy 无警告"
  artifacts:
    - path: "src/exporter/mod.rs"
      provides: "ordered_indices 注入到两个 exporter"
      contains: "ordered_field_indices"
    - path: "src/cli/run.rs"
      provides: "并行路径 ordered_indices 传递"
      contains: "ordered_indices"
  key_links:
    - from: "ExporterManager::from_config()"
      to: "CsvExporter.ordered_indices / SqliteExporter.ordered_indices"
      via: "config.features.ordered_field_indices()"
      pattern: "ordered_field_indices\\(\\)"
    - from: "handle_run()"
      to: "process_csv_parallel()"
      via: "&ordered_indices 参数"
      pattern: "process_csv_parallel.*ordered_indices"
---

<objective>
将 ordered_indices 接线到 ExporterManager::from_config() 和并行路径 process_csv_parallel()，使整个数据流都能按用户配置的字段顺序输出。

Purpose: 完成 D-01 实现的最后一公里——确保顺序路径和并行路径都正确传递有序索引，防止并行 CSV 路径的列顺序退化（RESEARCH.md Pitfall 1）。
Output: src/exporter/mod.rs 和 src/cli/run.rs 修改完毕，全量测试套件绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-CONTEXT.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-02-SUMMARY.md
@.planning/phases/02-shuchu-ziduan-kongzhi/02-03-SUMMARY.md

<interfaces>
<!-- 从 src/exporter/mod.rs 和 src/cli/run.rs 提取的现有接口，执行时直接使用 -->

ExporterManager::from_config() 当前（约 lines 201-233）：
```rust
pub fn from_config(config: &Config) -> Result<Self> {
    let normalize = config.features.replace_parameters.as_ref().is_none_or(|r| r.enable);
    let field_mask = config.features.field_mask();

    if let Some(cfg) = &config.exporter.csv {
        let mut exporter = CsvExporter::from_config(cfg);
        exporter.normalize = normalize;
        exporter.field_mask = field_mask;
        return Ok(Self { exporter: ExporterKind::Csv(exporter) });
    }

    if let Some(cfg) = &config.exporter.sqlite {
        let mut exporter = SqliteExporter::from_config(cfg);
        exporter.normalize = normalize;
        exporter.field_mask = field_mask;
        return Ok(Self { exporter: ExporterKind::Sqlite(exporter) });
    }

    Err(Error::Config(ConfigError::NoExporters))
}
```

process_csv_parallel() 当前签名（需新增 ordered_indices 参数）：
```rust
fn process_csv_parallel(
    log_files: &[PathBuf],
    cfg: &Config,
    pipeline: &Pipeline,
    // ... （其他参数）
    field_mask: FieldMask,       // ← ordered_indices 新增在此行之后
    sql_record_filter: Option<&CompiledSqlFilters>,
) -> Result<(Vec<(PathBuf, usize)>, usize)>
```

process_csv_parallel() 内部临时 CsvExporter 创建处（约 lines 507-510，需同步设置 ordered_indices）：
```rust
let mut exporter = CsvExporter::new(&temp_path);
exporter.normalize = do_normalize;
exporter.field_mask = field_mask;
// 需在此行之后添加: exporter.ordered_indices = ordered_indices.to_vec();
```

handle_run() 中 field_mask 计算处（约 line 640，需在此后计算 ordered_indices）：
```rust
let field_mask = final_cfg.features.field_mask();
// 需新增: let ordered_indices = final_cfg.features.ordered_field_indices();
```

handle_run() 中调用 process_csv_parallel 处（约 lines 675-688，需传入 &ordered_indices）：
```rust
let (processed_files, parallel_skipped) = process_csv_parallel(
    &log_files,
    final_cfg,
    &pipeline,
    // ...
    field_mask,
    // 需新增: &ordered_indices,
    sql_record_filter,
)?;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 接线 ExporterManager::from_config() 注入 ordered_indices</name>
  <files>src/exporter/mod.rs</files>
  <read_first>
    - src/exporter/mod.rs（重点：from_config() 函数，约 lines 201-233；了解 field_mask 注入模式）
    - src/features/mod.rs（确认 ordered_field_indices() 已存在，了解返回类型 Vec<usize>）
  </read_first>
  <action>
在 ExporterManager::from_config() 函数中，紧跟 `let field_mask = config.features.field_mask();` 之后添加：
```rust
let ordered_indices = config.features.ordered_field_indices();
```

在 CSV exporter 分支中，紧跟 `exporter.field_mask = field_mask;` 之后添加：
```rust
exporter.ordered_indices = ordered_indices.clone();
```

在 SQLite exporter 分支中，紧跟 `exporter.field_mask = field_mask;` 之后添加（最后一个分支无需 clone）：
```rust
exporter.ordered_indices = ordered_indices;
```

注意：若文件中只有一种 exporter 分支，则直接赋值不需要 clone（无需 clone 的地方不要多余的 clone）。
  </action>
  <verify>
    <automated>cargo test -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/exporter/mod.rs 包含 `ordered_field_indices()` 字符串
    - src/exporter/mod.rs 包含 `exporter.ordered_indices =` 字符串（出现 2 次，对应 CSV 和 SQLite 两个分支）
    - cargo build --release 编译成功（无错误）
    - cargo clippy --all-targets -- -D warnings 零警告
  </acceptance_criteria>
  <done>from_config() 已为 CSV 和 SQLite exporter 注入 ordered_indices，编译成功，clippy 零警告。</done>
</task>

<task type="auto">
  <name>Task 2: 接线并行路径 process_csv_parallel() 传递 ordered_indices</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（重点：process_csv_parallel 函数签名约 lines 424-437，内部 CsvExporter 创建约 lines 507-511，handle_run 中 field_mask 计算约 line 640，调用 process_csv_parallel 约 lines 675-688）
  </read_first>
  <action>
1. 在 process_csv_parallel() 函数签名中，在 `field_mask: FieldMask,` 参数之后新增参数：
   ```rust
   ordered_indices: &[usize],
   ```

2. 在 process_csv_parallel() 内部，找到创建临时 CsvExporter 的代码块（约 lines 507-511）：
   ```rust
   let mut exporter = CsvExporter::new(&temp_path);
   exporter.normalize = do_normalize;
   exporter.field_mask = field_mask;
   ```
   在 `exporter.field_mask = field_mask;` 之后添加：
   ```rust
   exporter.ordered_indices = ordered_indices.to_vec();
   ```

3. 在 handle_run() 函数中，找到 `let field_mask = final_cfg.features.field_mask();`（约 line 640），在其之后添加：
   ```rust
   let ordered_indices = final_cfg.features.ordered_field_indices();
   ```

4. 在 handle_run() 中找到调用 process_csv_parallel() 处（约 lines 675-688），在传入 `field_mask,` 之后、`sql_record_filter,` 之前新增：
   ```rust
   &ordered_indices,
   ```

5. 确认 process_csv_parallel() 内部如有多处 `exporter.field_mask = field_mask;` 的赋值（如每个并行任务独立创建），每处都紧跟 `exporter.ordered_indices = ordered_indices.to_vec();`。
  </action>
  <verify>
    <automated>cargo test -q 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/run.rs 中 process_csv_parallel 函数签名包含 `ordered_indices: &[usize]` 字符串
    - src/cli/run.rs 包含 `exporter.ordered_indices = ordered_indices.to_vec()` 字符串
    - src/cli/run.rs 包含 `let ordered_indices = final_cfg.features.ordered_field_indices()` 字符串
    - src/cli/run.rs 中调用 process_csv_parallel 处包含 `&ordered_indices` 参数
    - cargo build --release 编译成功（无错误，无警告）
    - cargo test -q 2>&1 | tail -5 全量测试套件通过（输出 "test result: ok"，无 FAILED）
    - cargo clippy --all-targets -- -D warnings 零警告
    - cargo fmt --check 通过（无格式差异）
  </acceptance_criteria>
  <done>并行路径已正确传递 ordered_indices，全量测试套件绿，clippy 零警告，fmt 整洁。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→ExporterManager | ordered_indices 由 ordered_field_indices() 计算，字段名已在 validate() 阶段验证 |
| process_csv_parallel→CsvExporter | ordered_indices 以 &[usize] 传入，to_vec() 给每个并行任务独立副本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | ExporterManager::from_config 注入 | accept | ordered_indices 值来自静态 FIELD_NAMES 白名单，无外部输入直接注入 |
| T-02-06 | Denial of Service | 并行路径 ordered_indices clone | accept | to_vec() 对 ≤15 个 usize 元素，内存开销可忽略 |
</threat_model>

<verification>
cargo build --release 2>&1 | tail -5
cargo test -q 2>&1 | tail -10
cargo clippy --all-targets -- -D warnings
cargo fmt --check
</verification>

<success_criteria>
- ExporterManager::from_config() 为 CSV 和 SQLite 注入 ordered_indices
- process_csv_parallel() 签名含 ordered_indices: &[usize]，并行任务用 to_vec() 获取独立副本
- handle_run() 计算并传递 ordered_indices
- cargo build --release 无报错
- cargo test 全量套件绿，cargo clippy 零警告，cargo fmt --check 通过
</success_criteria>

<output>
完成后创建 .planning/phases/02-shuchu-ziduan-kongzhi/02-04-SUMMARY.md
</output>
