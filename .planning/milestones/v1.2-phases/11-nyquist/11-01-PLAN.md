---
phase: 11-nyquist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-profiling-benchmarking/03-VALIDATION.md
  - .planning/phases/04-csv/04-VALIDATION.md
  - .planning/phases/05-sqlite/05-VALIDATION.md
autonomous: true
requirements: [DEBT-03]
tags: [nyquist, validation, documentation, retroactive-signoff]

must_haves:
  truths:
    - "Phase 3 VALIDATION.md frontmatter nyquist_compliant 字段值为 true（D-01）"
    - "Phase 3 VALIDATION.md frontmatter status 字段值为 complete（D-01）"
    - "Phase 3 VALIDATION.md Per-Task Verification Map 中所有任务 Status 列从 ⬜ pending 改为 ✅ green（D-01）"
    - "Phase 3 VALIDATION.md Wave 0 Requirements 所有条目均被勾选 [x]（D-01）"
    - "Phase 3 VALIDATION.md Validation Sign-Off 所有条目均被勾选 [x]（D-01）"
    - "Phase 3 VALIDATION.md Approval 行从 'pending' 改为 'signed 2026-05-15'（D-01）"
    - "Phase 4 VALIDATION.md 全量回溯：frontmatter (nyquist_compliant/status/wave_0_complete) + 所有任务 Status + Wave 0 条目 + Sign-Off + Approval 均已更新（D-01，证据来自 04-VERIFICATION.md 与 04-01..04-04-SUMMARY.md / D-02）"
    - "Phase 5 VALIDATION.md 全量回溯：frontmatter + 非 WAL 任务 Status + 非 WAL Wave 0 条目 + Sign-Off + Approval 均已更新（D-01）"
    - "Phase 5 VALIDATION.md 中 WAL 相关项（任务 5-02-01、Wave 0 中的两条 WAL 测试）标记为 N/A 并附用户决策注释（D-03）"
    - "三个 VALIDATION.md 文件中的证据均可追溯至对应 VERIFICATION.md 中的 VERIFIED 行（D-02）"

  artifacts:
    - path: ".planning/phases/03-profiling-benchmarking/03-VALIDATION.md"
      provides: "Phase 3 Nyquist 补签后的完整验证策略文件"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/04-csv/04-VALIDATION.md"
      provides: "Phase 4 Nyquist 补签后的完整验证策略文件"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/05-sqlite/05-VALIDATION.md"
      provides: "Phase 5 Nyquist 补签后的完整验证策略文件（WAL 项 N/A）"
      contains: "nyquist_compliant: true"

  key_links:
    - from: ".planning/phases/03-profiling-benchmarking/03-VALIDATION.md"
      to: ".planning/phases/03-profiling-benchmarking/03-VERIFICATION.md"
      via: "证据引用 — 10/10 truths verified"
      pattern: "Approval: signed"
    - from: ".planning/phases/04-csv/04-VALIDATION.md"
      to: ".planning/phases/04-csv/04-VERIFICATION.md"
      via: "证据引用 — 3/3 truths verified（含 accept-defer override）"
      pattern: "Approval: signed"
    - from: ".planning/phases/05-sqlite/05-VALIDATION.md"
      to: ".planning/phases/05-sqlite/05-VERIFICATION.md"
      via: "证据引用 — 4/4 truths verified（含 WAL 移除说明）"
      pattern: "Approval: signed"
---

<objective>
回溯更新 Phase 3/4/5 的三个 VALIDATION.md 文件，将其从 `nyquist_compliant: false` 状态补签为 `nyquist_compliant: true`，使 Nyquist 审计链对 Phase 3/4/5 段闭合（DEBT-03 的 3/4）。

Purpose: 三个文件都已存在但保留在"执行前 pending"状态，未被回溯。本 plan 全量回溯（D-01），证据来自对应的 VERIFICATION.md（权威）和 SUMMARY.md（细节）（D-02）。Phase 5 因 WAL 已被用户决策移除（PERF-05 canceled），相关条目标记 N/A 并附说明（D-03）。

Output:
- 03-VALIDATION.md（frontmatter + 1 个任务 Status + 1 个 Wave 0 条目 + 6 个 Sign-Off + Approval 全部更新）
- 04-VALIDATION.md（frontmatter + 7 个任务 Status + 2 个 Wave 0 条目 + 6 个 Sign-Off + Approval 全部更新）
- 05-VALIDATION.md（frontmatter + 非 WAL 任务 Status + 非 WAL Wave 0 条目 + WAL 项标记 N/A + Sign-Off + Approval 全部更新）

纯文档编辑，无任何代码变更，无 cargo 命令执行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-nyquist/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 回溯更新 03-VALIDATION.md（全量补签）</name>
  <files>.planning/phases/03-profiling-benchmarking/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-profiling-benchmarking/03-VALIDATION.md（当前需要更新的目标文件，初始状态：nyquist_compliant: false，1 个 pending 任务，未勾选 sign-off）
    - .planning/phases/03-profiling-benchmarking/03-VERIFICATION.md（权威证据来源 D-02：10/10 truths verified，status: passed，PERF-01 SATISFIED）
    - .planning/phases/03-profiling-benchmarking/03-01-SUMMARY.md（任务级实施细节 D-02，requirements-completed: [PERF-01]，commits 350df03 + 2dd5f58）
    - .planning/phases/11-nyquist/11-CONTEXT.md（D-01/D-02 决策定义）
  </read_first>
  <action>
依据 D-01（全量回溯）与 D-02（VERIFICATION.md 为权威证据），对 03-VALIDATION.md 执行以下精确变更（仅修改 4 处）：

1. **frontmatter 字段更新**（文件第 1-8 行）：
   - 第 4 行 `status: draft` → `status: complete`
   - 第 5 行 `nyquist_compliant: false` → `nyquist_compliant: true`
   - 第 6 行 `wave_0_complete: false` → `wave_0_complete: true`（依据 03-VERIFICATION.md truth #1：`[profile.flamegraph]` 块完整，Cargo.toml:95-98 已实施）

2. **Per-Task Verification Map 表格**（文件第 39-43 行）：
   - 任务 `3-01-01` Status 列：`⬜ pending` → `✅ green`（依据 03-VERIFICATION.md PERF-01 SATISFIED + 03-01-SUMMARY.md commits 已落地）

3. **Wave 0 Requirements 清单**（文件第 47-49 行）：
   - 第 49 行 `- [ ] [profile.flamegraph] section in Cargo.toml with debug = true + strip = "none" — required before flamegraph tasks`
   - → `- [x] [profile.flamegraph] section in Cargo.toml with debug = true + strip = "none" — required before flamegraph tasks`
   - （依据 03-VERIFICATION.md truth #1 VERIFIED + Cargo.toml:95-98 实际存在）

4. **Validation Sign-Off 清单**（文件第 66-71 行）：将 6 个 `- [ ]` 全部改为 `- [x]`：
   - `- [x] All tasks have <automated> verify or Wave 0 dependencies`
   - `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`
   - `- [x] Wave 0 covers all MISSING references`
   - `- [x] No watch-mode flags`
   - `- [x] Feedback latency < 120s`
   - `- [x] nyquist_compliant: true set in frontmatter`

5. **Approval 行**（文件第 73 行）：
   - `**Approval:** pending`
   - → `**Approval:** signed 2026-05-15 — 回溯补签于 Phase 11（DEBT-03）；执行验证于 2026-04-27（见 03-VERIFICATION.md，10/10 truths verified）。`

不改动文件结构、不新增章节、不删除现有条目；其他行（Test Infrastructure 表、Sampling Rate、Manual-Only Verifications）保持不变。
  </action>
  <verify>
    <automated>grep -E "^(status|nyquist_compliant|wave_0_complete):" .planning/phases/03-profiling-benchmarking/03-VALIDATION.md | grep -c "true\|complete" | grep -q "^3$" && grep -v "^\*Status:" .planning/phases/03-profiling-benchmarking/03-VALIDATION.md | grep -c "⬜ pending" | grep -q "^0$" && grep -c "\[ \]" .planning/phases/03-profiling-benchmarking/03-VALIDATION.md | grep -q "^0$" && grep -c "signed 2026-05-15" .planning/phases/03-profiling-benchmarking/03-VALIDATION.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - 03-VALIDATION.md frontmatter 包含字符串 `nyquist_compliant: true`
    - 03-VALIDATION.md frontmatter 包含字符串 `status: complete`
    - 03-VALIDATION.md frontmatter 包含字符串 `wave_0_complete: true`
    - 03-VALIDATION.md 文件中不再出现字符串 `⬜ pending`（任务 Status 已全部改为 ✅ green）
    - 03-VALIDATION.md 文件中不再出现未勾选 checkbox `- [ ]`（Wave 0 + Sign-Off 全部勾选）
    - 03-VALIDATION.md 包含字符串 `signed 2026-05-15`
    - 03-VALIDATION.md 包含字符串 `Approval: signed`
  </acceptance_criteria>
  <done>03-VALIDATION.md 全量补签完成；可作为 Phase 11 DEBT-03 Success Criterion #1 的证据。</done>
</task>

<task type="auto">
  <name>Task 2: 回溯更新 04-VALIDATION.md（全量补签）</name>
  <files>.planning/phases/04-csv/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04-csv/04-VALIDATION.md（当前需要更新的目标文件，初始状态：nyquist_compliant: false，7 个 pending 任务，未勾选 sign-off）
    - .planning/phases/04-csv/04-VERIFICATION.md（权威证据来源 D-02：3/3 truths verified，PERF-02 PASSED-override，PERF-03/PERF-08 SATISFIED）
    - .planning/phases/04-csv/04-01-SUMMARY.md（Wave 0 pub(crate) write_record_preparsed + bench_csv_format_only，commits 351a4ab/bfe22f4，requirements-completed: [PERF-03]）
    - .planning/phases/04-csv/04-02-SUMMARY.md（Wave 1 capacity-guarded reserve + 2 boundary tests，commits 54bf6c5/cc1efec，requirements-completed: [PERF-03, PERF-08]）
    - .planning/phases/04-csv/04-03-SUMMARY.md（Wave 2 include_performance_metrics 配置项，requirements-completed: [PERF-02, PERF-08]）
    - .planning/phases/04-csv/04-04-SUMMARY.md（Wave 3 收尾 benchmark + 04-VERIFICATION.md 创建）
    - .planning/phases/11-nyquist/11-CONTEXT.md（D-01/D-02 决策定义）
  </read_first>
  <action>
依据 D-01（全量回溯）与 D-02（VERIFICATION.md 为权威证据），对 04-VALIDATION.md 执行以下精确变更：

1. **frontmatter 字段更新**（文件第 1-8 行）：
   - 第 4 行 `status: draft` → `status: complete`
   - 第 5 行 `nyquist_compliant: false` → `nyquist_compliant: true`
   - 第 6 行 `wave_0_complete: false` → `wave_0_complete: true`（依据 04-01-SUMMARY.md：Wave 0 两项已落地——bench_csv_format_only 已注册 + write_record_preparsed 已改为 pub(crate)）

2. **Per-Task Verification Map 表格**（文件第 39-49 行）：将所有 7 个任务的 Status 列由 `⬜ pending` 改为 `✅ green`：
   - `4-01-01` Status → `✅ green`（PERF-03，bench_csv_format_only，04-VERIFICATION.md PERF-03 SATISFIED）
   - `4-01-02` Status → `✅ green`（PERF-03，csv 单元测试，04-VERIFICATION.md 649 passed）
   - `4-02-01` Status → `✅ green`（PERF-03，capacity-guarded reserve benchmark，04-02-SUMMARY ~20.0M elem/s）
   - `4-02-02` Status → `✅ green`（PERF-08，18 个单元测试通过，04-VERIFICATION.md PERF-08 SATISFIED）
   - `4-03-01` Status → `✅ green`（PERF-05/D-05 → 实际 PERF-02 兜底，include_performance_metrics 实现，04-VERIFICATION.md WIRED）
   - `4-04-01` Status → `✅ green`（PERF-02 PASSED-override，accept-defer 由用户 2026-05-09 签署，04-VERIFICATION.md frontmatter overrides_applied: 1）
   - `4-04-02` Status → `✅ green`（无回归，cargo test 649 passed 0 failed）

3. **Wave 0 Requirements 清单**（文件第 55-58 行）：将 2 个未勾选条目改为已勾选：
   - `- [x] benches/bench_csv.rs 新增 bench_csv_format_only group — 覆盖 PERF-03（格式化路径隔离）`
   - `- [x] src/exporter/csv.rs 中 write_record_preparsed 改为 pub(crate) — 使 benchmark 可直接调用`

4. **Validation Sign-Off 清单**（文件第 71-76 行）：将 6 个 `- [ ]` 全部改为 `- [x]`：
   - `- [x] All tasks have <automated> verify or Wave 0 dependencies`
   - `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`
   - `- [x] Wave 0 covers all MISSING references`
   - `- [x] No watch-mode flags`
   - `- [x] Feedback latency < 30s`
   - `- [x] nyquist_compliant: true set in frontmatter`

5. **Approval 行**（文件第 78 行）：
   - `**Approval:** pending`
   - → `**Approval:** signed 2026-05-15 — 回溯补签于 Phase 11（DEBT-03）；执行验证于 2026-05-09（见 04-VERIFICATION.md，3/3 truths verified，含 PERF-02 accept-defer override）。`

Manual-Only Verifications 表格（第 63-68 行）保持不变（Phase 4 的 flamegraph diff 项属验证策略说明，不影响 sign-off）。
  </action>
  <verify>
    <automated>grep -E "^(status|nyquist_compliant|wave_0_complete):" .planning/phases/04-csv/04-VALIDATION.md | grep -c "true\|complete" | grep -q "^3$" && grep -v "^\*Status:" .planning/phases/04-csv/04-VALIDATION.md | grep -c "⬜ pending" | grep -q "^0$" && grep -c "✅ green" .planning/phases/04-csv/04-VALIDATION.md | awk '$1 >= 7' | grep -q . && grep -c "\[ \]" .planning/phases/04-csv/04-VALIDATION.md | grep -q "^0$" && grep -c "signed 2026-05-15" .planning/phases/04-csv/04-VALIDATION.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - 04-VALIDATION.md frontmatter 包含字符串 `nyquist_compliant: true`
    - 04-VALIDATION.md frontmatter 包含字符串 `status: complete`
    - 04-VALIDATION.md frontmatter 包含字符串 `wave_0_complete: true`
    - 04-VALIDATION.md 文件中不再出现字符串 `⬜ pending`
    - 04-VALIDATION.md 至少出现 7 次字符串 `✅ green`（覆盖 4-01-01 / 4-01-02 / 4-02-01 / 4-02-02 / 4-03-01 / 4-04-01 / 4-04-02 共 7 行 Status；图例行贡献额外 1 次，实际计数 ≥ 7）
    - 04-VALIDATION.md 文件中不再出现未勾选 checkbox `- [ ]`
    - 04-VALIDATION.md 包含字符串 `signed 2026-05-15`
    - 04-VALIDATION.md 包含字符串 `Approval: signed`
  </acceptance_criteria>
  <done>04-VALIDATION.md 全量补签完成；可作为 Phase 11 DEBT-03 Success Criterion #2 的证据。</done>
</task>

<task type="auto">
  <name>Task 3: 回溯更新 05-VALIDATION.md（全量补签 + WAL 项标记 N/A）</name>
  <files>.planning/phases/05-sqlite/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-sqlite/05-VALIDATION.md（当前需要更新的目标文件，初始状态：nyquist_compliant: false，6 个 pending 任务行（含 5-02-01 WAL 测试 + Wave 0 两条 WAL 测试），未勾选 sign-off）
    - .planning/phases/05-sqlite/05-VERIFICATION.md（权威证据来源 D-02：4/4 truths verified，Re-verification: Yes，WAL 已被用户决策移除——PERF-05 canceled，保留 JOURNAL_MODE=OFF SYNCHRONOUS=OFF；PERF-04/PERF-06 SATISFIED）
    - .planning/phases/11-nyquist/11-CONTEXT.md（D-01/D-03 决策定义：WAL 项标记 N/A 注释模板见 §specifics）
  </read_first>
  <action>
依据 D-01（全量回溯）、D-02（VERIFICATION.md 为权威证据）、D-03（WAL 项标记 N/A），对 05-VALIDATION.md 执行以下精确变更：

1. **frontmatter 字段更新**（文件第 1-8 行）：
   - 第 4 行 `status: draft` → `status: complete`
   - 第 5 行 `nyquist_compliant: false` → `nyquist_compliant: true`
   - 第 6 行 `wave_0_complete: false` → `wave_0_complete: true`（依据 05-VERIFICATION.md：batch_commit + page_size 测试 + bench_sqlite_single_row + SqliteExporter.batch_size 字段均已落地；WAL 两条 Wave 0 测试因 PERF-05 移除而标记 N/A，不阻塞 wave_0_complete）

2. **Per-Task Verification Map 表格**（文件第 39-47 行）：
   - 任务 `5-01-01` Status → `✅ green`（PERF-04/PERF-06，config 单元测试，05-VERIFICATION.md batch_size 字段 + validate() 拒绝 0 实施）
   - 任务 `5-01-02` Status → `✅ green`（PERF-04，cargo build --bench bench_sqlite 通过，05-VERIFICATION.md Behavioral Spot-Checks PASS）
   - 任务 `5-02-01` Status → `N/A` 并将 Test Type 列 `integration` 改为 `integration (N/A — WAL removed)`，Automated Command 列保留原文但前置 `N/A —`（依据 D-03：PERF-05 canceled）
   - 任务 `5-02-02` Status → `✅ green`（PERF-04/PERF-06，sqlite 集成测试 14 个通过；PERF-05 已从该任务 Requirement 列移除——将 `PERF-04/PERF-05/PERF-06` 改为 `PERF-04/PERF-06`，依据 05-VERIFICATION.md Re-verification Context）
   - 任务 `5-03-01` Status → `✅ green`（PERF-06，prepare_cached 代码审查 + 注释"PERF-06 满足"已落地）
   - `无回归` 行 Status → `✅ green`（cargo test 651 passed 0 failed）

3. **Wave 0 Requirements 清单**（文件第 53-58 行）：
   - 第 54 行 `- [ ] src/exporter/sqlite.rs — WAL 测试：test_sqlite_wal_mode_enabled（断言 journal_mode 返回 "wal"，PERF-05）`
   - → `- [N/A] src/exporter/sqlite.rs — WAL 测试：test_sqlite_wal_mode_enabled（断言 journal_mode 返回 "wal"，PERF-05）  *N/A — 用户决策移除（PERF-05 canceled）：数据无需崩溃保护，保留 JOURNAL_MODE=OFF SYNCHRONOUS=OFF 高性能模式。见 05-VERIFICATION.md §Re-verification Context。*`
   - 第 55 行 `- [ ] src/exporter/sqlite.rs — page_size 测试：test_sqlite_wal_page_size（断言 WAL 下 page_size=65536 生效）`
   - → `- [N/A] src/exporter/sqlite.rs — page_size 测试：test_sqlite_wal_page_size（断言 WAL 下 page_size=65536 生效）  *N/A — 用户决策移除（PERF-05 canceled）：WAL 模式整体取消，page_size 测试无意义。*`
   - 第 56 行 `- [ ] src/exporter/sqlite.rs — 批量提交测试：test_sqlite_batch_commit（用 batch_size=2 写 5 条记录，验证中间 COMMIT 路径，PERF-04）` → 改为已勾选 `- [x] ...`
   - 第 57 行 `- [ ] benches/bench_sqlite.rs — 新增 sqlite_single_row benchmark group（PERF-04 对照基线）` → `- [x] ...`
   - 第 58 行 `- [ ] src/config.rs — SqliteExporter struct 新增 batch_size: usize 字段（含 serde default = 10000）` → `- [x] ...`

4. **Validation Sign-Off 清单**（文件第 74-79 行）：将 6 个 `- [ ]` 全部改为 `- [x]`（WAL 两条已通过 N/A 处理覆盖，依据 D-03：sign-off 仍全部勾选）：
   - `- [x] All tasks have <automated> verify or Wave 0 dependencies`
   - `- [x] Sampling continuity: no 3 consecutive tasks without automated verify`
   - `- [x] Wave 0 covers all MISSING references`
   - `- [x] No watch-mode flags`
   - `- [x] Feedback latency < 30s`
   - `- [x] nyquist_compliant: true set in frontmatter`

5. **Approval 行**（文件第 81 行）：
   - `**Approval:** pending`
   - → `**Approval:** signed 2026-05-15 — 回溯补签于 Phase 11（DEBT-03）；执行验证于 2026-05-10（见 05-VERIFICATION.md，4/4 truths verified）。WAL 相关项（任务 5-02-01、Wave 0 两条 WAL 测试）依 D-03 标记 N/A：PERF-05 用户决策移除，保留 JOURNAL_MODE=OFF SYNCHRONOUS=OFF 高性能模式。`

Manual-Only Verifications 表格（第 66-70 行）保持不变。
  </action>
  <verify>
    <automated>grep -E "^(status|nyquist_compliant|wave_0_complete):" .planning/phases/05-sqlite/05-VALIDATION.md | grep -c "true\|complete" | grep -q "^3$" && grep -v "^\*Status:" .planning/phases/05-sqlite/05-VALIDATION.md | grep -c "⬜ pending" | grep -q "^0$" && grep -c "N/A" .planning/phases/05-sqlite/05-VALIDATION.md | awk '$1 >= 3' | grep -q . && grep -c "PERF-05 canceled" .planning/phases/05-sqlite/05-VALIDATION.md | awk '$1 >= 2' | grep -q . && grep -c "\[ \]" .planning/phases/05-sqlite/05-VALIDATION.md | grep -q "^0$" && grep -c "signed 2026-05-15" .planning/phases/05-sqlite/05-VALIDATION.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - 05-VALIDATION.md frontmatter 包含字符串 `nyquist_compliant: true`
    - 05-VALIDATION.md frontmatter 包含字符串 `status: complete`
    - 05-VALIDATION.md frontmatter 包含字符串 `wave_0_complete: true`
    - 05-VALIDATION.md 文件中不再出现字符串 `⬜ pending`
    - 05-VALIDATION.md 至少出现 3 次字符串 `N/A`（覆盖任务 5-02-01 Status + Wave 0 两条 WAL 测试的 `[N/A]` 标记）
    - 05-VALIDATION.md 至少出现 2 次字符串 `PERF-05 canceled`（两条 WAL Wave 0 注释）
    - 05-VALIDATION.md 文件中不再出现未勾选 checkbox `- [ ]`（Wave 0 batch + bench + config 三条已勾选；Sign-Off 全部勾选；WAL 两条用 `- [N/A]` 而非 `- [ ]`）
    - 05-VALIDATION.md 包含字符串 `signed 2026-05-15`
    - 05-VALIDATION.md 包含字符串 `Approval: signed`
  </acceptance_criteria>
  <done>05-VALIDATION.md 全量补签完成；WAL 相关项以 N/A 形式记录用户决策；可作为 Phase 11 DEBT-03 Success Criterion #3 的证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无 | 纯文档编辑任务，无新增输入边界、无 IPC、无第三方依赖、无网络调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | (none) | 无 | accept | 本 plan 仅修改 `.planning/phases/*/VALIDATION.md` 三个本地 Markdown 文件；无代码、无 SQL、无网络、无新依赖、无 npm/pip/cargo install；威胁面为空。所有变更可通过 `git diff` 完整审查。 |
</threat_model>

<verification>
所有三个文件均完成全量补签后：

```bash
# 综合校验：三个 VALIDATION.md 均不再有 pending 状态
for f in .planning/phases/03-profiling-benchmarking/03-VALIDATION.md \
         .planning/phases/04-csv/04-VALIDATION.md \
         .planning/phases/05-sqlite/05-VALIDATION.md; do
  echo "=== $f ==="
  grep -E "^(status|nyquist_compliant|wave_0_complete):" "$f"
  grep -c "Approval: signed 2026-05-15" "$f"
done
```

预期：三个文件 frontmatter 中 `nyquist_compliant: true`、`status: complete`、`wave_0_complete: true`；每个文件 `Approval: signed 2026-05-15` 各出现 1 次。

无 cargo 命令需运行（纯文档变更）。
</verification>

<success_criteria>
- 03-VALIDATION.md / 04-VALIDATION.md / 05-VALIDATION.md 三个文件 frontmatter 字段 `nyquist_compliant` 均为 `true`
- 三个文件中 Per-Task Verification Map 中无 `⬜ pending` 残留（Phase 5 的 WAL 测试以 `N/A` 形式记录）
- 三个文件中 Validation Sign-Off 6 条均勾选 `[x]`
- 三个文件 Approval 行均含 `signed 2026-05-15`
- 05-VALIDATION.md 中 WAL 相关项至少 2 处含 `PERF-05 canceled` 注释
- 所有变更可在一次 git commit 中提交（纯文档，无代码改动）
</success_criteria>

<output>
Create `.planning/phases/11-nyquist/11-01-SUMMARY.md` when done，记录三个文件的具体行级变更摘要、补签依据（VERIFICATION.md 证据引用 + D-01/D-02/D-03 决策追溯）、以及 Phase 11 Plan 02 可执行的就绪信号。
</output>
