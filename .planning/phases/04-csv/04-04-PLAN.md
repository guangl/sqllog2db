---
phase: 04-csv
plan: 04
type: execute
wave: 3
depends_on: ["04-csv-03"]
files_modified:
  - benches/BENCHMARKS.md
  - .planning/phases/04-csv/04-VERIFICATION.md
autonomous: false
requirements: [PERF-02]
must_haves:
  truths:
    - "csv_export/10000 group 在 --baseline v1.0 对比中输出 'Performance has improved' 或具体百分比变化"
    - "csv_export_real/real_file 在 --baseline v1.0 对比中给出 median time 变化百分比"
    - "BENCHMARKS.md 含 Phase 4 段落，记录 Wave 0 baseline、Wave 1/2/3 各阶段数值与最终 vs v1.0 对比"
    - "若最终提升 ≥ 10%，phase verify 通过；若不足，checkpoint 记录原因（D-05 是否启用、瓶颈在解析层无法在本 phase 内突破等）"
    - "全部 cargo test 通过，cargo clippy/fmt 无警告/diff"
  artifacts:
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 4 性能对比段落"
      contains: "Phase 4"
    - path: ".planning/phases/04-csv/04-VERIFICATION.md"
      provides: "Phase 4 验收报告（gsd-verify-work 接入点）"
      contains: "PERF-02"
  key_links:
    - from: "criterion baseline benches/baselines/csv_export/10000/v1.0/estimates.json"
      to: "Phase 4 最终代码"
      via: "cargo bench --baseline v1.0"
      pattern: "--baseline v1.0"
---

<objective>
Wave 3：phase 4 收尾——运行最终 benchmark 对比，记录到 BENCHMARKS.md 与 VERIFICATION.md，并通过 human-verify checkpoint 让用户确认结果是否满足 ≥10% 提升目标。

Purpose: 提供 PERF-02 的可验证证据；如未达标，记录原因便于 Phase 6 接力。
Output: BENCHMARKS.md 段落 + VERIFICATION.md 报告 + 人工确认结论。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv/04-CONTEXT.md
@.planning/phases/04-csv/04-RESEARCH.md
@.planning/phases/04-csv/04-01-SUMMARY.md
@.planning/phases/04-csv/04-02-SUMMARY.md
@.planning/phases/04-csv/04-03-SUMMARY.md

@benches/BENCHMARKS.md
@benches/baselines/csv_export/10000/v1.0/estimates.json

<interfaces>
关键命令：
```bash
# 运行 csv_export 全部参数 + csv_export_real 与 v1.0 baseline 对比
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline v1.0

# 运行 csv_format_only（无 v1.0 baseline）
cargo bench --bench bench_csv -- csv_format_only

# 全套测试
cargo test

# clippy + fmt 净化
cargo clippy --all-targets -- -D warnings
cargo fmt -- --check
```

baseline JSON 路径（v1.0 已存在）：
```
benches/baselines/csv_export/1000/v1.0/estimates.json
benches/baselines/csv_export/10000/v1.0/estimates.json
benches/baselines/csv_export/50000/v1.0/estimates.json
benches/baselines/csv_export_real/real_file/v1.0/estimates.json
```

Phase 3 baseline 数值（参考）：csv_export/10000 median = 2.127ms。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行最终 benchmark 对比，将结果写入 BENCHMARKS.md</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（必读完整文件，了解 Phase 3 段落结构与现有数值）
    - .planning/phases/04-csv/04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md（取各 Wave 数值）
    - benches/baselines/csv_export/10000/v1.0/estimates.json（v1.0 baseline 参考）
  </read_first>
  <action>
**Step 1：执行最终 benchmark。**

按顺序运行（默认 include_performance_metrics=true，与 v1.0 行为一致才能与 v1.0 baseline 公平对比）：

```bash
# 1. 默认配置下与 v1.0 对比
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline v1.0

# 2. csv_format_only 与 wave0 baseline 对比（如 Wave 0 已 --save-baseline wave0；否则只跑当前数值）
cargo bench --bench bench_csv -- csv_format_only

# 3. include_performance_metrics=false 路径单次量化（手动构造 toml + 计时脚本）
# 此步可选；若 Plan 03 SUMMARY 已记录可省略
```

将每条命令完整 stdout（含 estimated mean / change% / Performance has improved/regressed 标签）保存到临时文件供下一步引用：
```bash
mkdir -p target/phase4-bench
CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline v1.0 2>&1 | tee target/phase4-bench/baseline_v1.txt
cargo bench --bench bench_csv -- csv_format_only 2>&1 | tee target/phase4-bench/format_only.txt
```

**Step 2：在 benches/BENCHMARKS.md 末尾追加 Phase 4 段落。**

如果 BENCHMARKS.md 已有 `## Phase 4` 段落则更新，否则在文件末尾追加。段落必须包含以下子节，**实际数值由 Step 1 的 stdout 抓取填充，不要写"待填"**：

```markdown
## Phase 4 — CSV 性能优化（v1.1）

**Date:** 2026-04-27
**Goal:** real-file CSV 导出相比 v1.0 baseline median time 降低 ≥10%
**Test environment:** （从 Phase 3 沿用，cpu 型号、内存、Rust 版本）

### 各 Wave 数值

| Group | v1.0 baseline | Wave 0 (Plan 01) | Wave 1 (Plan 02) | Wave 2 (Plan 03 默认) | Wave 2 (Plan 03 include_pm=false) | vs v1.0 |
|-------|--------------|------------------|------------------|----------------------|-----------------------------------|---------|
| csv_export/1000 | <ns from json> | <从 baseline_v1.txt> | <从 baseline_v1.txt> | <最新一次> | （仅默认对比） | <change%> |
| csv_export/10000 | 2.127 ms | <Wave0 数值> | <Wave1 数值> | <Wave2 数值> | <Wave2 数值> | <change%> |
| csv_export/50000 | <ns> | ... | ... | ... | — | <change%> |
| csv_export_real/real_file | <ms> | — | <Wave1 数值> | <Wave2 数值> | <Wave2 数值> | <change%> |
| csv_format_only/10000 | — | <Wave0 数值> | <Wave1 数值> | <Wave2 数值> | <Wave2 数值> | n/a |

### Criterion 输出原文

<details>
<summary>cargo bench --baseline v1.0（默认配置）</summary>

```
<粘贴 target/phase4-bench/baseline_v1.txt 关键段：每个 group 的 estimated/baseline/change 三行>
```
</details>

<details>
<summary>cargo bench csv_format_only</summary>

```
<粘贴 target/phase4-bench/format_only.txt 关键段>
```
</details>

### 解读

- **csv_export/10000 vs v1.0:** <实际百分比> — <improved/regressed/unchanged>
- **csv_export_real/real_file vs v1.0:** <实际百分比> — <improved/regressed/unchanged>
- **格式化层占比:** csv_format_only / csv_export 比值 = <计算值>，说明 <格式化是否瓶颈>
- **D-05 启用情况:** include_performance_metrics=false 单独提升 = <差值百分比>

### 结论

- [ ] PERF-02 (≥10% 提升) 默认配置下达成 / 未达成（**勾选实际情况**）
- [ ] D-05 兜底已启用 / 未启用
- [ ] PERF-08 flamegraph diff 已生成于 docs/flamegraphs/csv_export_real_phase4.json（可选 D-09）
- [ ] 全部 cargo test 通过，clippy/fmt 净化
```

**约束：**
- 表格中每个数值必须来自 Step 1 的实际命令输出，**禁止填占位符或猜测值**
- "vs v1.0" 列必须从 criterion stdout 中的 `change: [-X.XX% +Y.YY%] (p = ...)` 行抓取，并在前缀加 `+`/`-` 表示方向
- 解读段落基于实际数值，描述真实的瓶颈位置
- 结论 checkbox 反映实际状态，不要全部勾选；未达成项保留 `[ ]` 让 checkpoint 任务讨论

完成后将该段落 commit：
```bash
git add benches/BENCHMARKS.md
git commit -m "docs(04): record phase 4 benchmark results"
```
  </action>
  <verify>
    <automated>grep -c "## Phase 4" benches/BENCHMARKS.md ; grep -c "csv_export/10000" benches/BENCHMARKS.md ; grep -c "csv_export_real/real_file" benches/BENCHMARKS.md ; grep -c "csv_format_only" benches/BENCHMARKS.md ; cargo test 2>&amp;1 | tail -3 ; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 4" benches/BENCHMARKS.md` 输出 至少 `1`
    - `grep -c "csv_export/10000" benches/BENCHMARKS.md` 输出 至少 `1`
    - `grep -c "csv_export_real/real_file" benches/BENCHMARKS.md` 输出 至少 `1`
    - `grep -c "csv_format_only" benches/BENCHMARKS.md` 输出 至少 `1`
    - `grep -c "vs v1.0" benches/BENCHMARKS.md` 输出 至少 `1`
    - `grep -E "[+-][0-9]+\\.[0-9]+%" benches/BENCHMARKS.md | wc -l` ≥ `2`（至少包含 csv_export/10000 与 csv_export_real 两条 change% 数值）
    - `target/phase4-bench/baseline_v1.txt` 文件存在且非空
    - `target/phase4-bench/format_only.txt` 文件存在且非空
    - `cargo test` 退出码 `0`
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>BENCHMARKS.md 含 Phase 4 真实数值段落，全部测试通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 04-VERIFICATION.md（gsd-verify-work 接入点）</name>
  <files>.planning/phases/04-csv/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-csv/04-VALIDATION.md（取验收命令清单）
    - benches/BENCHMARKS.md（取 Task 1 写入的 Phase 4 数值）
    - .planning/phases/03-profiling-benchmarking/03-VERIFICATION.md（必读完整文件，复用结构和签名风格作为 analog）
    - .planning/REQUIREMENTS.md（确认 PERF-02/03/08 表述）
  </read_first>
  <action>
基于 03-VERIFICATION.md 的结构，创建 04-VERIFICATION.md，覆盖 PERF-02、PERF-03、PERF-08。

文件内容（**所有数值从 BENCHMARKS.md 抓取实际值，禁止占位符**）：

```markdown
# Phase 4 — CSV 性能优化 Verification

**Phase:** 04-csv
**Date:** 2026-04-27
**Status:** <pending|verified|failed>（任务执行时填写）

## Requirements coverage

| REQ-ID | Description | Evidence | Status |
|--------|-------------|----------|--------|
| PERF-02 | real-file CSV 吞吐相比 v1.0 baseline 提升 ≥10% | benches/BENCHMARKS.md csv_export_real/real_file vs v1.0 = <实际百分比> | <pass/fail> |
| PERF-03 | CSV 格式化路径优化（criterion micro-benchmark 验证） | benches/BENCHMARKS.md csv_format_only/10000 数值 + Wave 0/1 对比 | <pass/fail> |
| PERF-08 | 热循环堆分配显著减少 | reserve 条件化（src/exporter/csv.rs `if line_buf.capacity() < needed`）+ include_performance_metrics=false 路径跳过 parse_performance_metrics 调用层 | <pass/fail> |

## Verification commands run

| Command | Exit code | Notable output |
|---------|-----------|----------------|
| `cargo test` | 0 | <total tests passed> |
| `cargo clippy --all-targets -- -D warnings` | 0 | no warnings |
| `cargo fmt -- --check` | 0 | no diff |
| `CRITERION_HOME=benches/baselines cargo bench --bench bench_csv -- --baseline v1.0` | 0 | <main change% summary> |
| `cargo bench --bench bench_csv -- csv_format_only` | 0 | <throughput> |

## Findings

- 默认配置（include_performance_metrics=true）下与 v1.0 对比：<csv_export/10000: X%; csv_export_real: Y%>
- 关闭性能指标后单独提升：<Z%>
- 格式化层 csv_format_only 占总开销比：<比值>
- D-05 是否启用：<是/否，理由>

## Manual verification

- [ ] flamegraph diff（Phase 3 vs Phase 4）已生成于 docs/flamegraphs/csv_export_real_phase4.json（D-09，可选）
- [ ] 用户确认 PERF-02 ≥10% 目标已达成或接受未达成结论

## Open issues / follow-ups

<列出任何在 Phase 4 内未解决但已 ack 的问题，例如：上游 dm-database-parser-sqllog 内 find_indicators_split 的扫描开销留 Phase 6 评估新 API>
```

**约束：**
- 文件路径精确为 `.planning/phases/04-csv/04-VERIFICATION.md`
- "Status" 列必须为 `pass` 或 `fail`，不要写 `pending` 或 `tbd`
- 如 PERF-02 实测未达 10%，明确写 `fail`，并在 Findings 中说明原因（推荐方向：转入 Phase 6 等上游 API 改进）；checkpoint 任务会要求用户确认
- 如 PERF-02 已达成，写 `pass`，flamegraph 可选

完成后 commit：
```bash
git add .planning/phases/04-csv/04-VERIFICATION.md
git commit -m "docs(04): create verification report"
```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-csv/04-VERIFICATION.md ; grep -c "PERF-02" .planning/phases/04-csv/04-VERIFICATION.md ; grep -c "PERF-03" .planning/phases/04-csv/04-VERIFICATION.md ; grep -c "PERF-08" .planning/phases/04-csv/04-VERIFICATION.md ; grep -c "csv_export_real" .planning/phases/04-csv/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-csv/04-VERIFICATION.md` 文件存在
    - `grep -c "PERF-02" .planning/phases/04-csv/04-VERIFICATION.md` 输出 至少 `1`
    - `grep -c "PERF-03" .planning/phases/04-csv/04-VERIFICATION.md` 输出 至少 `1`
    - `grep -c "PERF-08" .planning/phases/04-csv/04-VERIFICATION.md` 输出 至少 `1`
    - `grep -c "csv_export_real" .planning/phases/04-csv/04-VERIFICATION.md` 输出 至少 `1`
    - `grep -E "(pass|fail)" .planning/phases/04-csv/04-VERIFICATION.md | wc -l` 输出 ≥ `3`（每个 REQ 一个状态）
    - 文件不含字符串 `<实际百分比>` / `<X%>` / `<Y%>` / `pending` / `tbd`（占位符已替换）
  </acceptance_criteria>
  <done>04-VERIFICATION.md 创建完成，覆盖 PERF-02/03/08 三项需求并填入实际数值</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Phase 4 PERF-02 是否达成</name>
  <what-built>
Phase 4 已完成：Wave 0 量化基础（bench_csv_format_only group + write_record_preparsed pub(crate)），Wave 1 reserve 优化，Wave 2 include_performance_metrics 配置项，Wave 3 最终 baseline 对比。BENCHMARKS.md 与 04-VERIFICATION.md 已记录所有实际数值。
  </what-built>
  <how-to-verify>
1. 阅读 `.planning/phases/04-csv/04-VERIFICATION.md`，确认 PERF-02 / PERF-03 / PERF-08 三个 REQ 的 Status 列内容（pass / fail）。

2. 阅读 `benches/BENCHMARKS.md` 末尾的 `## Phase 4 — CSV 性能优化（v1.1）` 段落：
   - csv_export_real/real_file 与 v1.0 的 change% 是否 ≥ -10%（即 median time 减少 ≥10%）
   - csv_export/10000 与 v1.0 的 change% 数值
   - csv_format_only 在 Wave 0/1/2 的变化
   - include_performance_metrics=false 路径相对默认的提升

3. 运行（在工作目录）：
```bash
cargo test 2>&1 | tail -3
cargo clippy --all-targets -- -D warnings 2>&1 | tail -3
cargo fmt -- --check ; echo $?
```
确认全部退出码 0，无警告，无 diff。

4. 决定下一步：
   - **如 PERF-02 已达成（≥10% 提升）：** 输入 `approved`，phase 4 完成并准备 commit ROADMAP/STATE 更新进入 Phase 5。
   - **如 PERF-02 未达成且属于"已穷尽本 phase 内可控范围"（瓶颈在上游解析层）：** 输入 `accept-defer`，记录 Phase 4 已尽力，未达 10% 的部分留 Phase 6 评估上游新 API 接力。
   - **如 PERF-02 未达成且仍有未实施的优化点：** 输入 `replan`，描述具体未实施的优化方向，触发 `/gsd-plan-phase 4 --gaps` 补 plan。
  </how-to-verify>
  <resume-signal>Type `approved`, `accept-defer`, or `replan: <reason>`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark stdout → BENCHMARKS.md | 数值由 cargo bench 输出，非用户可控；写入文档时无篡改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Repudiation | benchmark 数值伪造 | mitigate | criterion stdout 全文保存到 target/phase4-bench/*.txt 与 baselines JSON 同时存档；BENCHMARKS.md 引用 baseline JSON 路径，可由第三方 reproducer 复现 |
| T-04-09 | Information disclosure | flamegraph 含本地路径符号 | accept | flamegraph 仅记录 cargo target 中的二进制符号，无敏感信息 |
</threat_model>

<verification>
- BENCHMARKS.md 末尾 Phase 4 段落含真实数值（来自 target/phase4-bench/*.txt）
- 04-VERIFICATION.md 每个 REQ 有 pass/fail 标记
- cargo test/clippy/fmt 均通过
- human-verify checkpoint 收到 approved / accept-defer / replan 信号
</verification>

<success_criteria>
- benches/BENCHMARKS.md 含 Phase 4 段落与 csv_export_real change% 数值
- .planning/phases/04-csv/04-VERIFICATION.md 创建并覆盖三个 REQ
- 全套测试 + clippy + fmt 通过
- 用户在 checkpoint 给出明确决议
</success_criteria>

<output>
完成后创建 `.planning/phases/04-csv/04-04-SUMMARY.md`，记录：
1. 最终 vs v1.0 数值（含 PERF-02 通过/失败结论）
2. 用户 checkpoint 决议（approved/accept-defer/replan）
3. 若 accept-defer，列出留给 Phase 6 的具体待办项
</output>
