---
phase: 12-sql
plan: 03
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
files_modified:
  - src/cli/run.rs
autonomous: true
requirements:
  - TMPL-01

must_haves:
  truths:
    - "src/cli/run.rs 中 process_log_file 函数签名新增 do_template: bool 参数"
    - "src/cli/run.rs 中 handle_run 函数体内新增 do_template 局部变量，从 final_cfg.features.template_analysis 计算"
    - "process_log_file 内热循环对每条 DML 记录在 do_template == true 时调用 normalize_template，返回的模板 key 暂存为局部 Option<String> 变量"
    - "process_log_file 内热循环对每条 DML 记录在 do_template == false 时不调用 normalize_template（零开销快路径）"
    - "process_csv_parallel 并行 CSV 路径也透传 do_template，每个 rayon task 行为与顺序路径一致"
    - "运行时配置 `[features.template_analysis] enabled = true` 时不导致 cargo test 任一已有用例回归"
    - "cargo clippy --all-targets -- -D warnings 通过"
    - "cargo test 全套通过"
  artifacts:
    - path: "src/cli/run.rs"
      provides: "do_template 计算 + process_log_file 参数 + 热循环条件调用 + 并行路径透传"
      contains: "do_template"
  key_links:
    - from: "src/cli/run.rs handle_run()"
      to: "final_cfg.features.template_analysis"
      via: "Option::is_some_and 守卫"
      pattern: "template_analysis.*is_some_and"
    - from: "src/cli/run.rs process_log_file() 热循环"
      to: "crate::features::normalize_template"
      via: "if do_template { Some(normalize_template(pm.sql.as_ref())) } else { None }"
      pattern: "normalize_template\\(pm\\.sql"
---

<objective>
将 Plan 02 暴露的 `crate::features::normalize_template` 接入 src/cli/run.rs 的热循环：在 handle_run() 入口提前计算 `do_template` bool（参照 do_normalize 模式 D-13），通过 `process_log_file` 新增参数透传至顺序路径与 `process_csv_parallel` 并行路径，并在热循环中仅在 `do_template == true` 时为每条 DML 记录计算模板 key 并暂存为局部 Option<String>（D-14）。

Purpose: 让 Phase 12 完整闭环 — 用户在 config.toml 中设置 `[features.template_analysis] enabled = true` 后运行 sqllog2db，热循环按 D-13 模式调用 normalize_template；禁用时分支不进入、零分配压力。模板 key 暂存供 Phase 13 的 TemplateAggregator::observe() 消费（D-14）。
Output: src/cli/run.rs 三处修改（handle_run 局部变量、process_log_file 签名 + 调用点、process_csv_parallel 透传）+ 1 项集成测试验证启用模板归一化的运行结果与 v1.2 主导出行为完全一致。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sql/12-CONTEXT.md
@.planning/phases/12-sql/12-RESEARCH.md
@.planning/phases/12-sql/12-PATTERNS.md
@.planning/phases/12-sql/12-01-PLAN.md
@.planning/phases/12-sql/12-02-PLAN.md
@src/cli/run.rs
</context>

<interfaces>
<!-- src/cli/run.rs 现有签名与变量（do_normalize 模式，PATTERNS.md §src/cli/run.rs 节） -->

From src/cli/run.rs:
```rust
fn process_log_file(
    file_path: &str,
    file_index: usize,
    total_files: usize,
    exporter_manager: &mut ExporterManager,
    pipeline: &Pipeline,
    pb: &ProgressBar,
    limit: Option<usize>,
    interrupted: &Arc<AtomicBool>,
    do_normalize: bool,
    placeholder_override: Option<bool>,
    params_buffer: &mut ParamBuffer,
    ns_scratch: &mut Vec<u8>,
    reset_pb: bool,
    sql_record_filter: Option<&CompiledSqlFilters>,
) -> Result<usize>

// handle_run() lines 695-708:
let do_normalize = field_mask.includes_normalized_sql()
    && final_cfg.features.replace_parameters.as_ref().is_none_or(|r| r.enable);
```

新增/修改:
- process_log_file 签名追加 `do_template: bool`（位置紧跟 `do_normalize: bool` 之后）
- process_csv_parallel 签名追加 `do_template: bool`（同位置）
- handle_run 中新增 `let do_template = final_cfg.features.template_analysis.as_ref().is_some_and(|t| t.enabled);`
- 热循环内紧邻 `let ns = ...` 之后新增 `let _tmpl_key: Option<String> = if do_template { Some(crate::features::normalize_template(pm.sql.as_ref())) } else { None };` （D-14；变量名以 `_` 前缀避免 Phase 13 接入前的未用 warning）
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 process_log_file / process_csv_parallel 签名与 handle_run 中接入 do_template，并在热循环内条件调用 normalize_template</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（完整文件；重点 lines 114-129 process_log_file 签名、lines 176-219 热循环主体、lines 443-557 process_csv_parallel、lines 695-746 handle_run 中 do_normalize 计算与 process_csv_parallel 调用点、lines 806-825 顺序路径 process_log_file 调用点）
    - .planning/phases/12-sql/12-CONTEXT.md（D-13/D-14）
    - .planning/phases/12-sql/12-PATTERNS.md（§src/cli/run.rs 节）
    - .planning/phases/12-sql/12-RESEARCH.md（§Pattern 4 热循环条件调用）
  </read_first>
  <behavior>
    - process_log_file 函数签名新增形参 `do_template: bool`，位置紧跟现有 `do_normalize: bool` 之后（保持源代码可读性的相邻关系）。
    - process_csv_parallel 函数签名同样新增 `do_template: bool` 形参，位置紧跟 `do_normalize: bool` 之后。该函数内部 `process_log_file` 调用同步传入 `do_template`。
    - handle_run 函数体内：在现有 `let do_normalize = ...`（约 lines 698-703）之后新增 `let do_template = final_cfg.features.template_analysis.as_ref().is_some_and(|t| t.enabled);`。该变量同时传递给顺序路径与并行路径的 process_log_file / process_csv_parallel 调用。
    - 热循环（process_log_file 中约 lines 206-219）：紧接 `let ns = if do_normalize ... else { None };` 块之后，新增 `let _tmpl_key: Option<String> = if do_template { Some(crate::features::normalize_template(pm.sql.as_ref())) } else { None };` 块。变量名前缀 `_` 抑制本 Phase 未消费的 warning；Phase 13 将去掉前缀并接入 TemplateAggregator::observe()（D-14）。
    - **零开销保证**：do_template == false 时 if 分支不进入，normalize_template 调用次数为 0，pm.sql.as_ref() 不被复制。该 if 应在 `if sql_record_filter.is_some_and(...) { } else { ... }` 的 else 分支内、`exporter_manager.export_one_preparsed` 调用之前；位置参考 do_normalize 的 ns 变量。
    - 新增集成测试 `test_handle_run_template_analysis_enabled`（或同等命名）：构造临时目录与一份最小 SQL 日志 + config.toml（含 `[features.template_analysis] enabled = true`），调用 handle_run 应正常完成且生成的 CSV/SQLite 输出与不启用模板归一化时的主导出内容完全一致（模板归一化结果仅暂存局部变量，不影响主导出）。位置：src/cli/run.rs `#[cfg(test)] mod tests` 子模块（若存在）或新增 mod tests。
  </behavior>
  <action>
对 src/cli/run.rs 完成以下四处修改：

1. **process_log_file 签名（lines 114-129）**：在 `do_normalize: bool,` 之后插入 `do_template: bool,` 形参。

2. **process_csv_parallel 签名（lines 443-457）**：在 `do_normalize: bool,` 之后插入 `do_template: bool,` 形参；函数体内对 process_log_file 的调用（约 lines 539-554）同步在 `do_normalize,` 实参之后插入 `do_template,`。

3. **handle_run（lines 698-703 区域）**：在 do_normalize 计算块之后追加 `do_template` 计算块：
   - 表达式：`final_cfg.features.template_analysis.as_ref().is_some_and(|t| t.enabled)`。
   - 调用点同步：
     * process_csv_parallel 调用（约 lines 732-746）在 `do_normalize,` 实参之后插入 `do_template,`。
     * 顺序路径 process_log_file 调用（约 lines 806-825）同样在 `do_normalize,` 实参之后插入 `do_template,`。

4. **process_log_file 热循环（lines 206-219 之后）**：在 `let ns = if do_normalize ... else { None };` 后续逻辑紧邻处新增条件调用块。变量绑定名 `_tmpl_key`（带下划线前缀，Phase 13 去掉）。`crate::features::normalize_template` 路径调用 pm.sql.as_ref() 作为输入。

5. **集成测试**：在 src/cli/run.rs 末尾的 `#[cfg(test)] mod tests` 内（或新增）添加一项测试：
   - 创建 `tempfile::TempDir`，写入一份最小 SQL 日志（参照 src/features/mod.rs 已有 `test_process_with_meta_default_delegates_to_process` 中 lines 364-368 的样例 SQL 日志格式）。
   - 构造 `Config` 实例（不一定走 TOML 解析路径，可直接构造结构）：features.template_analysis = Some(TemplateAnalysisConfig { enabled: true })。
   - 调用 handle_run（或更细粒度的 process_log_file 直接调用，绕过完整 handle_run 的 CLI 包装）；断言执行成功且产生预期记录数。
   - 测试名建议 `test_process_log_file_template_analysis_enabled_does_not_regress`。

不修改 do_normalize 计算逻辑、不改动 process_log_file 内 ns 变量赋值逻辑、不改动 export_one_preparsed 调用签名、不改动 Phase 13/14 才会接入的 TemplateAggregator 相关结构。

所有调用点参数顺序保持代码可读性：`..., do_normalize, do_template, placeholder_override, ...`。

新增函数体（如新测试 fn）须 ≤ 40 行（CLAUDE.md 全局约束）。如测试逻辑超过 40 行，拆分为辅助 fn（如 `fn make_test_sql_log(dir: &Path) -> PathBuf`）。
  </action>
  <verify>
    <automated>cargo build --quiet 2>&amp;1 | tail -20 &amp;&amp; cargo test --package sqllog2db 2>&amp;1 | tail -30 &amp;&amp; cargo clippy --all-targets -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -E 'do_template: bool,' src/cli/run.rs` 输出至少 2 行（process_log_file 签名 + process_csv_parallel 签名）。
    - 源断言：`grep -E 'let do_template' src/cli/run.rs` 输出 1 行（handle_run 中的局部变量）。
    - 源断言：`grep -E 'template_analysis.*is_some_and' src/cli/run.rs` 输出 1 行。
    - 源断言：`grep -E 'crate::features::normalize_template\(pm\.sql' src/cli/run.rs` 输出 1 行（热循环唯一调用点）。
    - 源断言：`grep -c '_tmpl_key' src/cli/run.rs` 至少 1（局部变量名，Phase 13 前以 `_` 前缀避免 warning）。
    - 源断言：`grep -E '^\s*fn test_process_log_file_template_analysis' src/cli/run.rs` 输出 1 行（新增集成测试）。
    - 行为断言：`cargo test --package sqllog2db` 全部通过，且总测试数比当前基线（v1.2 的 729）至少 +1（本 plan 新增 1 项集成测试 + Plan 01/02 新增的 9 项单元测试，phase 累计应 ≥ 730+9 = 739，本 plan 验收时取 ≥ 731 即可，因为本 plan 自身只 +1）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
    - 命令：`cargo fmt --check` 退出码 0。
    - 集成验证：在 cargo build 后通过临时 config.toml（含 `[features.template_analysis] enabled = true`）运行 `./target/debug/sqllog2db run -c <临时 config>`，主导出 CSV 内容与同 config 但 `enabled = false` 时的主导出 CSV 内容完全 bit-for-bit 一致（模板 key 仅暂存为局部变量，不影响 export_one_preparsed 写入路径）。
  </acceptance_criteria>
  <done>process_log_file / process_csv_parallel 签名新增 do_template；handle_run 计算 do_template 并同步透传两条路径；热循环条件调用 normalize_template 并暂存 _tmpl_key（D-14）；新增集成测试通过；cargo test / clippy / fmt 三项全绿；主导出 CSV 内容不受 enabled 切换影响。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handle_run final_cfg → process_log_file 热循环 | 用户 config 决定 do_template，外部不可直接控制函数内部分支 |
| pm.sql.as_ref() → normalize_template | 已通过 dm-database-parser-sqllog 解析的 SQL 字节，UTF-8 已保证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | do_template 条件判断 | mitigate | Option::is_some_and 模式与现有 do_normalize 一致，serde 反序列化已在 Plan 02 验证；config.validate() 保持不变（TemplateAnalysisConfig 无需额外校验，因仅 enabled: bool） |
| T-12-07 | DoS | normalize_template 调用频率 | mitigate | do_template == false 时分支不进入，零分配压力；启用时每条 DML 记录调用 1 次，复杂度 O(len(sql))，与 fingerprint() 同阶 |
| T-12-08 | Information Disclosure | _tmpl_key 局部变量生命周期 | accept | 局部 String 在迭代结束时 Drop，不写入任何 exporter；Phase 13 接入 TemplateAggregator::observe() 时变量消费链显式化 |
| T-12-SC | Tampering | 无 npm/pip/cargo install | accept | 本 plan 不引入任何新依赖 |
</threat_model>

<verification>
- `cargo test --package sqllog2db`：含本 plan 新增的集成测试 + Plan 01/02 的所有新增单元测试全部通过；v1.2 既有 729 项测试零回归。
- `cargo clippy --all-targets -- -D warnings`：退出码 0。
- `cargo fmt --check`：退出码 0。
- 手工 CLI 验证（在 acceptance_criteria 中描述）：启用与禁用 template_analysis 时主导出 CSV bit-for-bit 一致。
</verification>

<success_criteria>
- handle_run 中 do_template 局部变量按 D-13 模式从 features.template_analysis.enabled 计算。
- process_log_file / process_csv_parallel 函数签名新增 do_template 形参，所有调用点同步。
- 热循环对每条 DML 记录在 do_template == true 时调用 normalize_template 并暂存 _tmpl_key（D-14）。
- do_template == false 时分支不进入，零分配压力（与 do_normalize 模式一致）。
- 集成测试验证模板归一化启用不导致主导出回归（行为与 v1.2 一致）。
</success_criteria>

<output>
Create `.planning/phases/12-sql/12-03-SUMMARY.md` when done
</output>
