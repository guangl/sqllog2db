---
phase: 12-sql
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/features/mod.rs
  - src/cli/init.rs
autonomous: true
requirements:
  - TMPL-01

must_haves:
  truths:
    - "src/features/mod.rs 中 pub use sql_fingerprint::normalize_template 导出存在"
    - "src/features/mod.rs 中 TemplateAnalysisConfig 结构体已定义，字段 enabled: bool 默认 false"
    - "FeaturesConfig 新增 template_analysis: Option<TemplateAnalysisConfig> 字段（默认 None）"
    - "TOML `[features.template_analysis]\\nenabled = true` 可被 serde 反序列化为 Config，enabled == true"
    - "TOML 不含 [features.template_analysis] 段时 Config 加载成功且 features.template_analysis 为 None"
    - "cargo run -- init -o /tmp/test_config.toml --force 生成的 TOML 含 `[features.template_analysis]` 段注释说明"
    - "cargo test 中针对 TemplateAnalysisConfig 与 FeaturesConfig 默认值的新增测试全部通过"
  artifacts:
    - path: "src/features/mod.rs"
      provides: "TemplateAnalysisConfig 结构体 + Default impl + pub use normalize_template 导出 + 单元测试"
      contains: "pub struct TemplateAnalysisConfig"
    - path: "src/cli/init.rs"
      provides: "init 命令 TOML 模板中新增 [features.template_analysis] 段"
      contains: "[features.template_analysis]"
  key_links:
    - from: "src/features/mod.rs"
      to: "src/features/sql_fingerprint.rs"
      via: "pub use sql_fingerprint::normalize_template"
      pattern: "pub use sql_fingerprint::normalize_template"
    - from: "FeaturesConfig"
      to: "TemplateAnalysisConfig"
      via: "Option<TemplateAnalysisConfig> 字段"
      pattern: "template_analysis: Option<TemplateAnalysisConfig>"
---

<objective>
在 src/features/mod.rs 新增 TemplateAnalysisConfig 配置结构（D-10/D-11/D-12），接入 FeaturesConfig，并在 src/cli/init.rs 的 TOML 模板（中英文双语版本）中加入 `[features.template_analysis]` 段注释。同步新增 `pub use sql_fingerprint::normalize_template` 导出（D-09），让 Plan 03 的热循环可直接调用。

Purpose: 让用户能在 config.toml 中通过 `[features.template_analysis] enabled = true` 启用模板归一化（D-12），并对外暴露 normalize_template 公共导出路径，使 cli/run.rs 后续在热循环中通过 `crate::features::normalize_template` 调用。
Output: src/features/mod.rs 新增 TemplateAnalysisConfig + Default impl + pub use + 2-3 项单元测试；src/cli/init.rs 中英文模板各追加一段。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sql/12-CONTEXT.md
@.planning/phases/12-sql/12-RESEARCH.md
@.planning/phases/12-sql/12-PATTERNS.md
@.planning/phases/12-sql/12-01-PLAN.md
@src/features/mod.rs
@src/cli/init.rs
</context>

<interfaces>
<!-- 现有 ReplaceParametersConfig 模式（PATTERNS.md §Pattern Assignments / features/mod.rs 节） -->

From src/features/mod.rs:
```rust
#[derive(Debug, Deserialize, Clone)]
pub struct ReplaceParametersConfig {
    #[serde(default = "default_true")]
    pub enable: bool,
    #[serde(default)]
    pub placeholders: Vec<String>,
}

impl Default for ReplaceParametersConfig {
    fn default() -> Self {
        Self { enable: true, placeholders: Vec::new() }
    }
}

pub struct FeaturesConfig {
    pub filters: Option<FiltersFeature>,
    pub replace_parameters: Option<ReplaceParametersConfig>,
    pub fields: Option<Vec<String>>,
    // 新增字段加在此处
}
```

新增导出:
- `pub use sql_fingerprint::normalize_template` （Plan 01 实现的函数）
- `pub struct TemplateAnalysisConfig { pub enabled: bool }`
- `FeaturesConfig::template_analysis: Option<TemplateAnalysisConfig>`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 features/mod.rs 新增 TemplateAnalysisConfig + FeaturesConfig 字段 + pub use 导出</name>
  <files>src/features/mod.rs</files>
  <read_first>
    - src/features/mod.rs（完整文件，重点 lines 4-8 pub use 导出区、lines 73-94 ReplaceParametersConfig、lines 120-126 FeaturesConfig）
    - .planning/phases/12-sql/12-CONTEXT.md（D-09/D-10/D-11/D-12）
    - .planning/phases/12-sql/12-PATTERNS.md（§Pattern Assignments / features/mod.rs 节、§Shared Patterns / serde Default）
  </read_first>
  <behavior>
    - `pub use sql_fingerprint::normalize_template;` 与现有 `pub use sql_fingerprint::fingerprint;`（line 8）并列。
    - `pub struct TemplateAnalysisConfig` 派生 `Debug, Deserialize, Clone`；唯一字段 `pub enabled: bool` 带 `#[serde(default)]`（默认 false，D-10/D-12）。
    - `impl Default for TemplateAnalysisConfig` 返回 `Self { enabled: false }`。
    - `FeaturesConfig` 新增 `pub template_analysis: Option<TemplateAnalysisConfig>` 字段（默认 None，由现有 `#[derive(Default)]` 自动处理）。
    - 新增 3 项单元测试：
      * `test_template_analysis_config_default`：调用 `TemplateAnalysisConfig::default()`，断言 enabled == false。
      * `test_template_analysis_config_deserialize_enabled_true`：用 `toml::from_str::<TemplateAnalysisConfig>("enabled = true")`，断言 enabled == true。
      * 扩展现有 `test_features_config_default` 增加 `assert!(cfg.template_analysis.is_none())`，确保 FeaturesConfig 默认无该字段。
  </behavior>
  <action>
在 src/features/mod.rs 完成三处修改：

1. 在 `pub use sql_fingerprint::fingerprint;`（line 8）之后追加一行 `pub use sql_fingerprint::normalize_template;`（D-09）。

2. 在 `ReplaceParametersConfig` 实现块结束之后、`default_true` 函数定义之前，插入 `TemplateAnalysisConfig` 结构体与其 `Default` impl。字段命名严格采用 `enabled`（D-12，与 `ReplaceParametersConfig::enable` 区分）。`#[serde(default)]` 标注让缺失字段反序列化为 false。

3. 修改 `FeaturesConfig`（line 120-126 区域）的字段列表，在 `pub fields: Option<Vec<String>>,` 之后新增 `pub template_analysis: Option<TemplateAnalysisConfig>,`（D-11）。FeaturesConfig 已 derive Default，新字段自动 None。

4. 在 `#[cfg(test)] mod tests` 中：
   - 扩展现有 `test_features_config_default`（lines 267-272），追加 `assert!(cfg.template_analysis.is_none());`。
   - 新增 `test_template_analysis_config_default`（参照 lines 274-279 `test_replace_parameters_config_default` 模式）。
   - 新增 `test_template_analysis_config_deserialize_enabled_true`：使用 `toml::from_str` 解析 `"enabled = true"` 字符串，断言 enabled == true（参照 lines 337-342 `test_default_true_via_serde` 模式）。

不修改 `default_true` 函数、不修改 `ReplaceParametersConfig`、不修改任何 LogProcessor/Pipeline 相关代码。
  </action>
  <verify>
    <automated>cargo test --package sqllog2db --lib features::tests::test_template_analysis_config_default features::tests::test_template_analysis_config_deserialize_enabled_true features::tests::test_features_config_default 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c '^pub use sql_fingerprint::normalize_template;' src/features/mod.rs` 输出 1。
    - 源断言：`grep -E '^pub struct TemplateAnalysisConfig' src/features/mod.rs` 输出 1 行。
    - 源断言：`grep -E '^\s*pub enabled: bool' src/features/mod.rs` 在 TemplateAnalysisConfig 区域出现且带 `#[serde(default)]` 属性。
    - 源断言：`grep -E 'template_analysis: Option<TemplateAnalysisConfig>' src/features/mod.rs` 出现 1 次（FeaturesConfig 字段）。
    - 源断言：`grep -E '^\s*fn test_template_analysis_config_default' src/features/mod.rs` 输出 1 行。
    - 源断言：`grep -E '^\s*fn test_template_analysis_config_deserialize_enabled_true' src/features/mod.rs` 输出 1 行。
    - 行为断言：`TemplateAnalysisConfig::default().enabled == false`。
    - 行为断言：`toml::from_str::<TemplateAnalysisConfig>("").unwrap().enabled == false`。
    - 行为断言：`toml::from_str::<TemplateAnalysisConfig>("enabled = true").unwrap().enabled == true`。
    - 行为断言：`FeaturesConfig::default().template_analysis.is_none() == true`。
    - 命令：`cargo test features::tests` 全部通过（含原 14 项 + 新增 2-3 项）。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>TemplateAnalysisConfig 已声明，FeaturesConfig 已嵌入 template_analysis 字段，pub use normalize_template 已导出；新增 2-3 项配置测试全绿；现有测试零回归。</done>
</task>

<task type="auto">
  <name>Task 2: 在 init 命令的中英文 TOML 模板中新增 [features.template_analysis] 段</name>
  <files>src/cli/init.rs</files>
  <read_first>
    - src/cli/init.rs（完整文件，重点 lines 79-82 中文 [features.replace_parameters] 段、lines 183-186 英文 [features.replace_parameters] 段）
    - .planning/phases/12-sql/12-CONTEXT.md（D-12 TOML 路径 `[features.template_analysis]`、字段 `enabled`）
    - .planning/phases/12-sql/12-RESEARCH.md（§Code Examples / TemplateAnalysisConfig 的 TOML 示例）
  </read_first>
  <action>
在 src/cli/init.rs 的两个常量字符串中分别新增一段 `[features.template_analysis]` 模板：

1. 中文版本 `CONFIG_TEMPLATE_ZH`（约 lines 79-82）：在 `[features.replace_parameters]` 段之后、`[features.filters]` 段之前插入一段空行 + 段落：
   - 段落标题为 `[features.template_analysis]`。
   - 注释说明：`# SQL 模板归一化（v1.3 新增）`、`# 启用后对 sql_text 执行注释去除、IN 列表折叠、关键字大写、空白折叠四项变换，生成稳定的模板 key`、`# 默认 false（不影响热循环性能）`。
   - 字段：`enabled = false` （默认值与 D-10 一致）。

2. 英文版本 `CONFIG_TEMPLATE_EN`（约 lines 183-186 起的对应位置）：同结构英文翻译。注释三行：`# SQL template normalization (new in v1.3)`、`# Strips comments, folds IN lists, uppercases keywords, collapses whitespace to produce a stable template key`、`# Default false (zero overhead in hot loop when disabled)`；字段 `enabled = false`。

不修改 logging / sqllog / exporter / filters 任何已有段落顺序与文本。仅插入新段。

修改后调用 `cargo run -- init -o /tmp/sqllog2db_init_test.toml --force` 应生成包含 `[features.template_analysis]` 字符串的 TOML 文件，并能被 `cargo run -- validate -c /tmp/sqllog2db_init_test.toml` 成功验证。
  </action>
  <verify>
    <automated>cargo build --release --quiet && ./target/release/sqllog2db init -o /tmp/sqllog2db_init_test_zh.toml --force --lang zh && ./target/release/sqllog2db init -o /tmp/sqllog2db_init_test_en.toml --force --lang en && grep -c '\[features.template_analysis\]' /tmp/sqllog2db_init_test_zh.toml | grep -qx 1 && grep -c '\[features.template_analysis\]' /tmp/sqllog2db_init_test_en.toml | grep -qx 1 && grep -E '^enabled = false' /tmp/sqllog2db_init_test_zh.toml | head -1 && ./target/release/sqllog2db validate -c /tmp/sqllog2db_init_test_zh.toml</automated>
  </verify>
  <acceptance_criteria>
    - 源断言：`grep -c '\[features.template_analysis\]' src/cli/init.rs` 输出 2（中英文各 1 段）。
    - 源断言：在 `CONFIG_TEMPLATE_ZH` 常量范围内，`[features.template_analysis]` 出现在 `[features.replace_parameters]` 之后、`[features.filters]` 之前（按行号顺序）。
    - 源断言：在 `CONFIG_TEMPLATE_EN` 常量范围内，同样的相对顺序成立。
    - CLI 输出断言：`./target/release/sqllog2db init -o <tmp> --force --lang zh` 生成的 TOML 文件含字符串 `[features.template_analysis]` 和 `enabled = false`。
    - CLI 输出断言：`./target/release/sqllog2db init -o <tmp> --force --lang en` 同上。
    - CLI 行为断言：`./target/release/sqllog2db validate -c <生成的 zh 模板>` 退出码 0。
    - 命令：`cargo clippy --all-targets -- -D warnings` 退出码 0。
  </acceptance_criteria>
  <done>init 命令中英文 TOML 模板均含 `[features.template_analysis] enabled = false` 段；生成的 TOML 可通过 validate 命令验证；clippy 零 warning。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML 配置文件 → Config 反序列化 | 用户提供的 config.toml 可能含未预期字段类型 |
| init 命令模板 → 用户文件系统 | init 写入 `-o` 路径文件，可能覆盖现有文件（已有 --force 控制） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | TemplateAnalysisConfig serde 反序列化 | mitigate | `#[derive(Deserialize)]` 自动拒绝未声明字段（serde 默认 deny 未知字段策略由 Config 顶层控制；TemplateAnalysisConfig 仅 1 个字段，攻击面极小） |
| T-12-05 | DoS | init 命令模板大小 | accept | 新增 ~4 行注释 + 1 行字段，无可观感影响 |
| T-12-SC | Tampering | 无 npm/pip/cargo install | accept | 本 plan 不引入任何新依赖 |
</threat_model>

<verification>
- `cargo test features::tests` 通过（含新增 2-3 项 TemplateAnalysisConfig 测试）。
- `cargo build --release` 通过，`./target/release/sqllog2db init --lang zh` 和 `--lang en` 均生成含 `[features.template_analysis]` 的 TOML，且生成的 TOML 可通过 `validate` 命令。
- `cargo clippy --all-targets -- -D warnings` 零 warning。
- 现有 `test_features_config_default` 等已有测试零回归。
</verification>

<success_criteria>
- TemplateAnalysisConfig 已嵌入 FeaturesConfig（D-10/D-11/D-12）。
- pub use sql_fingerprint::normalize_template 已暴露公共导出（D-09）。
- init 命令中英文模板新增 `[features.template_analysis]` 段（D-12 注释 + 字段）。
- 反序列化与默认值测试覆盖：缺省段 → None / 缺省 enabled → false / 显式 enabled = true → true。
</success_criteria>

<output>
Create `.planning/phases/12-sql/12-02-SUMMARY.md` when done
</output>
