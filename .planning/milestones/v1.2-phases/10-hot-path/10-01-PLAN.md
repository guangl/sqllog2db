---
phase: 10-hot-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benches/bench_filters.rs
  - benches/BENCHMARKS.md
autonomous: true
requirements:
  - PERF-10
must_haves:
  truths:
    - "用户运行 `cargo bench --bench bench_filters` 后能看到 exclude_passthrough 和 exclude_active 两个新场景的 throughput 数据"
    - "用户运行 `cargo build --profile flamegraph && samply record ./target/flamegraph/sqllog2db run -c config.toml` 能产出可在浏览器中查看的 profile"
    - "BENCHMARKS.md 中存在 `## Phase 10` 节，记录 samply Top N 函数（含 self time 占比）和 exclude_* 两个新场景的 throughput"
    - "BENCHMARKS.md Phase 10 节末尾给出明确的门控判定结论：是否命中 D-G1（>5% src/ 函数 + 明确优化路径）"
  artifacts:
    - path: "benches/bench_filters.rs"
      provides: "新增 cfg_exclude_passthrough / cfg_exclude_active 配置函数，scenarios 列表扩展为 7 项"
      contains: "fn cfg_exclude_passthrough"
    - path: "benches/bench_filters.rs"
      provides: "exclude_active 配置函数"
      contains: "fn cfg_exclude_active"
    - path: "benches/BENCHMARKS.md"
      provides: "Phase 10 节，包含 samply 结论 + exclude bench 数据表 + 门控判定"
      contains: "## Phase 10"
  key_links:
    - from: "benches/bench_filters.rs scenarios 列表"
      to: "新 cfg_exclude_passthrough / cfg_exclude_active 配置"
      via: "scenarios &[(&str, Config)] 数组中追加两个 tuple"
      pattern: "(\"exclude_passthrough\""
    - from: "BENCHMARKS.md Phase 10 节"
      to: "10-02 或 10-03 计划的执行决策"
      via: "样品判定结论（命中/未命中 D-G1）作为下游分支选择依据"
      pattern: "命中 D-G1|未命中 D-G1"
---

<objective>
Branch A（始终执行）：补全 exclude_passthrough / exclude_active 两个 criterion bench 场景，使用 samply 对发布版二进制进行真实日志 profiling，记录 Top N 函数 self time 占比，并在 `benches/BENCHMARKS.md` 中创建 Phase 10 节记录 samply 结论与 criterion exclude 场景数据，最后给出明确的 D-G1 门控判定结论（命中/未命中），供下游计划（10-02 或 10-03）二选一执行。

Purpose: D-O1 数据驱动 — 必须先收集真实 profiling 数据，才能决定是否实施优化。本计划是 Phase 10 的唯一前置数据采集步骤。
Output: bench_filters.rs 新增两个 exclude 场景 + BENCHMARKS.md Phase 10 节（含 samply Top N + criterion 数据 + 门控判定）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hot-path/10-CONTEXT.md
@.planning/phases/10-hot-path/10-PATTERNS.md

<interfaces>
<!-- 关键签名与配置项，executor 无需再次探索代码库 -->

来自 benches/bench_filters.rs（既有 helper，不要重复定义）：
- fn synthetic_log(record_count: usize) -> String  （生成 10_000 条 username=BENCH 的日志）
- fn base_toml(sqllog_dir: &Path, bench_dir: &Path) -> String  （生成包含 [sqllog]/[error]/[logging]/[exporter.csv] 的基础 TOML）
- const RECORDS: usize = 10_000
- handle_run(cfg, None, false, true, &Arc::new(AtomicBool::new(false)), 80, false, None, 1, compiled_filters)
- scenarios: &[(&str, Config)]  在 bench_filters() 函数内

来自 src/features/filters.rs（用于了解 exclude 字段 serde 名称）：
- MetaFilters 字段：exclude_usernames: Option<Vec<String>>（snake_case 与 TOML key 同名）

来自 Cargo.toml [profile.flamegraph]（用于 samply 构建）：
- inherits = "release"
- debug = true
- strip = "none"
- 构建命令：cargo build --profile flamegraph
- 二进制路径：target/flamegraph/sqllog2db

samply 命令规范（D-P1 / D-P2）：
- 安装位置：~/.cargo/bin/samply
- 启动：samply record ./target/flamegraph/sqllog2db run -c config.toml
- 真实日志位置：./sqllogs/（项目根目录现有真实达梦日志）
- 浏览器查看 Top N self time，截图 + 文字描述写入 BENCHMARKS.md（不保存二进制 profile）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 bench_filters.rs 补充 exclude_passthrough / exclude_active 两个场景</name>
  <files>benches/bench_filters.rs</files>
  <read_first>
    - benches/bench_filters.rs（完整读取，理解 base_toml / synthetic_log / handle_run 调用模式与 scenarios 数组扩展位置）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-B1/D-B2/D-B3 场景定义细节）
    - .planning/phases/10-hot-path/10-PATTERNS.md（Pattern Assignments §`benches/bench_filters.rs` 节给出确切的 cfg_ 函数模板与 scenarios 扩展模式）
    - src/features/filters.rs lines 70-90（确认 MetaFilters 中 exclude_usernames 字段的 serde 名称与 TOML key 一致）
  </read_first>
  <action>
    在 benches/bench_filters.rs 中沿用现有 `cfg_pipeline_passthrough` 函数风格新增两个配置函数：

    1. `fn cfg_exclude_passthrough(sqllog_dir: &Path, bench_dir: &Path) -> Config`：在 base_toml 之后追加 `[features.filters]` 节，`enable = true`，`exclude_usernames = ["BENCH_EXCLUDE"]`。由于 synthetic_log 中所有记录的 username 固定为 `BENCH`，该配置对应零命中场景（测量纯排除过滤开销）。
    2. `fn cfg_exclude_active(sqllog_dir: &Path, bench_dir: &Path) -> Config`：同结构，`exclude_usernames = ["BENCH"]`，对应 100% 命中（OR-veto 极端压力场景，所有记录被排除）。

    在 `bench_filters` 函数内 scenarios 数组末尾追加两个 tuple：
    - `("exclude_passthrough", cfg_exclude_passthrough(&sqllog_dir, &bench_dir))`
    - `("exclude_active", cfg_exclude_active(&sqllog_dir, &bench_dir))`

    runner 闭包（lines 152-173）不改动，复用现有 `iter_with_setup` + `cfg.validate_and_compile().unwrap()` + `handle_run(..., compiled_filters)` 模式。同时更新文件头注释表格，加入两个新场景行说明。函数采用 `toml::from_str(&toml).unwrap()`，不使用 `unwrap_or`，与 D-B3 一致。

    严格遵循项目约定：不引入新 imports，函数 <40 行，描述性变量名。
  </action>
  <verify>
    <automated>cargo bench --bench bench_filters --no-run 2>&amp;1 | grep -E "Compiling|Finished" &amp;&amp; grep -c "exclude_passthrough\|exclude_active" benches/bench_filters.rs | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion：`grep -c "fn cfg_exclude_passthrough" benches/bench_filters.rs` 返回 1
    - Source assertion：`grep -c "fn cfg_exclude_active" benches/bench_filters.rs` 返回 1
    - Source assertion：`grep -c '"exclude_passthrough"' benches/bench_filters.rs` 返回 1（scenarios 数组中的注册）
    - Source assertion：`grep -c '"exclude_active"' benches/bench_filters.rs` 返回 1
    - Source assertion：`grep -c 'exclude_usernames = \["BENCH_EXCLUDE"\]' benches/bench_filters.rs` 返回 1
    - Source assertion：`grep -c 'exclude_usernames = \["BENCH"\]' benches/bench_filters.rs` 返回 1
    - Behavior：`cargo bench --bench bench_filters --no-run` 成功编译，exit 0
    - Lint：`cargo clippy --benches --all-targets -- -D warnings` 通过，exit 0
    - Format：`cargo fmt --check` 通过，exit 0
  </acceptance_criteria>
  <done>bench_filters.rs 含两个新场景，cargo bench --no-run 编译通过，clippy + fmt 净化</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 运行 criterion exclude 场景采集吞吐数据 + samply profiling 采集 Top N 函数</name>
  <files>benches/BENCHMARKS.md（仅原始数据，结论分析在 Task 3 完成）</files>
  <read_first>
    - benches/BENCHMARKS.md（完整读取，理解 Phase 5 与 Phase 9 节的格式骨架，特别是 `<details><summary>` 折叠 criterion 原始输出的写法）
    - Cargo.toml lines 88-99（确认 [profile.flamegraph] 配置：inherits=release, debug=true, strip=none，构建产物在 target/flamegraph/）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-P1/D-P2/D-P3 工具链与采集要求）
    - config.toml（确认 samply record 时 -c 指向的实际配置文件存在且能跑通）
  </read_first>
  <action>
    分两步采集数据，所有产物记录到 BENCHMARKS.md 文件末尾（暂以临时小节形式追加，Task 3 重组为正式 Phase 10 节）：

    第 1 步 - criterion exclude 场景：
    - 运行 `cargo bench --bench bench_filters -- exclude_passthrough exclude_active`，记录这两个场景的 median time 与 Throughput（criterion 输出会标注 `time:` 与 `thrpt:`）。
    - 同时运行一次完整 `cargo bench --bench bench_filters` 作为整体 baseline 备份（criterion 自动保存到 target/criterion/，不需额外操作）。
    - 将两个新场景的 criterion stdout 完整原文（含 `time:` 行、`thrpt:` 行）追加到 BENCHMARKS.md 文件末尾，用 `<details><summary>Phase 10 criterion (exclude_passthrough)</summary>` 与 `<details><summary>Phase 10 criterion (exclude_active)</summary>` 两个折叠块包裹。

    第 2 步 - samply profiling：
    - 用 `cargo build --profile flamegraph` 构建（产物：target/flamegraph/sqllog2db）。
    - 运行 `~/.cargo/bin/samply record ./target/flamegraph/sqllog2db run -c config.toml`，samply 会自动打开浏览器展示 profile。
    - 从 samply 浏览器 UI 中读取按 self time 倒序的 Top 10 函数列表（函数名 + self time 百分比），逐项以纯文本形式追加到 BENCHMARKS.md 末尾。每行格式：`N. <function_name> — X.X% self time（src/<file>:<line> 或 third_party::<crate>）`。
    - 针对每个 src/ 下且 self time >5% 的函数，标注"候选热点（待 D-G1 判定）"；针对每个第三方库内部函数（regex/alloc/memchr/dm_database_parser_sqllog 等），标注"第三方库内部（D-G2 排除）"。

    重要约束：
    - 若 sqllogs/ 目录或 config.toml 不可用导致 samply record 无法跑完整流程，在 BENCHMARKS.md 中明确记录"samply 数据采集环境受限：<原因>"并改用 `cargo bench --bench bench_filters` 的 criterion profile 数据（target/criterion/）作为退路，但必须在 Task 3 的结论中显式说明此情况。
    - 不保存二进制 profile 文件到 git（D-P3），只保留文字 Top N。
  </action>
  <verify>
    <automated>grep -c "Phase 10 criterion (exclude_passthrough)" benches/BENCHMARKS.md &amp;&amp; grep -c "Phase 10 criterion (exclude_active)" benches/BENCHMARKS.md &amp;&amp; ls target/flamegraph/sqllog2db</automated>
  </verify>
  <acceptance_criteria>
    - Behavior：`cargo bench --bench bench_filters -- exclude_passthrough exclude_active 2>&1 | grep -E "time:|thrpt:"` 输出至少 4 行（两个场景各两行）
    - File exists：`target/flamegraph/sqllog2db` 二进制存在（`ls target/flamegraph/sqllog2db` exit 0）
    - Source assertion：`grep -c "Phase 10 criterion (exclude_passthrough)" benches/BENCHMARKS.md` 返回 ≥ 1
    - Source assertion：`grep -c "Phase 10 criterion (exclude_active)" benches/BENCHMARKS.md` 返回 ≥ 1
    - Source assertion：BENCHMARKS.md 末尾包含至少 10 行格式为 `N. <function> — X.X%` 的 samply Top N 列表（或显式记录"samply 数据采集环境受限"原因 + 退路说明）
    - Source assertion：每个 self time >5% 的 src/ 函数标注 `候选热点（待 D-G1 判定）`，每个第三方库函数标注 `第三方库内部（D-G2 排除）`
  </acceptance_criteria>
  <done>criterion 两个新场景原始输出已折叠记录到 BENCHMARKS.md；samply Top N 函数列表已记录（含每个函数的 src/ 或第三方库归属标注）；target/flamegraph/sqllog2db 已构建</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在 BENCHMARKS.md 创建正式的 Phase 10 节并给出 D-G1 门控判定结论</name>
  <files>benches/BENCHMARKS.md</files>
  <read_first>
    - benches/BENCHMARKS.md（完整读取，特别是 Phase 9 节 lines 315-388 与 Phase 5 节作为节结构类比；Task 2 追加的原始数据小节）
    - .planning/phases/10-hot-path/10-CONTEXT.md（D-G1/D-G2/D-G3 门控判定标准）
    - .planning/phases/10-hot-path/10-PATTERNS.md（§`benches/BENCHMARKS.md` 节给出 Phase 10 节头与两个分支的 markdown 模板）
  </read_first>
  <action>
    将 Task 2 追加的临时数据小节重组为正式的 `## Phase 10 — 热路径优化（samply + criterion）` 节，放在 Phase 9 节之后（约 BENCHMARKS.md 末尾）。节结构（沿用 Phase 9 模板）：

    1. 节头：`## Phase 10 — 热路径优化（samply + criterion）` + Date / Goal / Test environment 三行元数据（参考 Phase 9 lines 316-320 写法）。
    2. `### samply Profiling 结论` 子节：列出 Task 2 采集的 Top 10 函数，每行带 self time 百分比 + 来源标注（src/ 或第三方）。
    3. `### Filter Benchmark（Phase 10 新增场景）` 子节：写出 exclude_passthrough / exclude_active 的 Median time / Throughput 数值表（两列 + Notes 列），数值来自 Task 2 的 criterion 输出。同时附 `<details><summary>` 折叠原始 criterion stdout（直接复用 Task 2 追加的折叠块）。
    4. `### D-G1 门控判定` 子节：基于 D-G1（>5% self time + src/ 业务逻辑 + 明确优化路径，三条全满足才命中）+ D-G2（第三方库内部不算）做明确二选一判定，必须以下二选一格式之一收尾（注意：结论文本中的句号一律使用英文 `.`，不要使用中文 `。`，以确保下游 grep 模式 `\.` 能精确命中），供 10-02 / 10-03 计划的执行者据此选择：

       命中 D-G1（10-02 执行）：
       `**结论：命中 D-G1.** 热点函数：\`src/<path>::<function>\`（X.X% self time）。优化路径：<一句话>。下游计划：10-02。`

       未命中 D-G1（10-03 执行）：
       `**结论：未命中 D-G1.** Top self time 函数均属于第三方库内部（D-G2 排除）或缺乏明确优化路径，已达当前瓶颈。下游计划：10-03。`

    5. `### 结论` 子节：checkbox 列表（D-B1 两场景已补全 / samply 已采集 / D-G1 判定已执行 / 下游计划已选择 / cargo test 全量通过 / clippy + fmt 净化），全部勾选 `[x]`。

    清理：删除 Task 2 临时追加的非折叠纯文本 Top N 列表与说明（合并进 §samply Profiling 结论）；保留两个 criterion `<details>` 折叠块到 §Filter Benchmark 之内。

    确保整个 Phase 10 节末尾的判定结论文本能被 grep 检索：明确包含 `**结论：命中 D-G1.**` 或 `**结论：未命中 D-G1.**` 字面值（关键约束 — 使用英文句号 `.`，不要写中文句号 `。`），下游计划据此自动决策。
  </action>
  <verify>
    <automated>grep -c "^## Phase 10 — 热路径优化" benches/BENCHMARKS.md &amp;&amp; grep -cE "\*\*结论：命中 D-G1\.\*\*|\*\*结论：未命中 D-G1\.\*\*" benches/BENCHMARKS.md &amp;&amp; grep -c "### D-G1 门控判定" benches/BENCHMARKS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion：`grep -c "^## Phase 10 — 热路径优化（samply + criterion）" benches/BENCHMARKS.md` 返回 1
    - Source assertion：`grep -c "### samply Profiling 结论" benches/BENCHMARKS.md` 返回 1
    - Source assertion：`grep -c "### Filter Benchmark（Phase 10 新增场景）" benches/BENCHMARKS.md` 返回 1
    - Source assertion：`grep -c "### D-G1 门控判定" benches/BENCHMARKS.md` 返回 1
    - Source assertion：`grep -cE "\*\*结论：命中 D-G1\.\*\*|\*\*结论：未命中 D-G1\.\*\*" benches/BENCHMARKS.md` 返回恰好 1（二选一互斥；注意：结论文本使用英文句号 `.`，下游 10-02/10-03 grep 模式与此完全一致）
    - Source assertion：`grep -cE "下游计划：10-0[23]" benches/BENCHMARKS.md` 返回恰好 1
    - Source assertion：Phase 10 §Filter Benchmark 表格存在 exclude_passthrough 与 exclude_active 两行（`grep -cE "^\| .exclude_(passthrough|active)" benches/BENCHMARKS.md` 返回 2）
    - Behavior：`cargo test` 全量通过，exit 0（无回归）
    - Lint：`cargo clippy --all-targets -- -D warnings` 通过，exit 0
    - Format：`cargo fmt --check` 通过，exit 0
  </acceptance_criteria>
  <done>BENCHMARKS.md 含完整的 Phase 10 节；D-G1 判定结论明确（命中 → 10-02，未命中 → 10-03，使用英文句号 `.`）；cargo test/clippy/fmt 全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| samply → 用户文件系统 | samply record 子进程读取真实日志文件并采样；本地工具，无外部网络 |
| criterion → target/criterion/ | 写入本地基准产物，不持久化至 git |
| BENCHMARKS.md（人工编辑） | 工程师手动转录 samply Top N，输入由人控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Info Disclosure) | samply record 二进制 profile | accept | D-P3 明确不保存二进制 profile 文件，仅截图 + 文字描述；不入库，不发布 |
| T-10-02 | T (Tampering) | BENCHMARKS.md 人工转录 Top N | mitigate | acceptance criteria 用 grep 强制结论关键字（"命中 D-G1"/"未命中 D-G1"）必须存在，避免转录遗漏或自由发挥；下游计划据此自动决策 |
| T-10-03 | D (DoS) | cargo bench 长耗时 | accept | criterion 已配置合理 sample size；single-threaded synthetic 10k 记录数据集 < 1s/iter，不会卡死 CI |
| T-10-04 | I | samply record 读取 sqllogs/ 真实日志（可能含真实 SQL） | accept | 本地开发流程，profile 数据不入库；BENCHMARKS.md 仅记录函数名 + 百分比，无 SQL 内容 |
</threat_model>

<verification>
- `cargo bench --bench bench_filters --no-run` 编译通过
- `cargo bench --bench bench_filters -- exclude_passthrough exclude_active` 输出含 `time:` 与 `thrpt:` 行
- `target/flamegraph/sqllog2db` 构建产物存在
- `cargo test` 全量通过（≥ 651 测试，无回归）
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt --check` 通过
- BENCHMARKS.md 包含完整 Phase 10 节 + 明确的 D-G1 判定结论（命中或未命中二选一，结论文本使用英文句号 `.`）
</verification>

<success_criteria>
- D-B1 两个 exclude bench 场景已补全（exclude_passthrough + exclude_active）
- samply Top N 函数已采集并以文字形式记录在 BENCHMARKS.md Phase 10 节
- D-G1 门控判定已执行：BENCHMARKS.md 中存在唯一一条 `**结论：命中 D-G1.**` 或 `**结论：未命中 D-G1.**` 文本（英文句号）
- 判定结论指明下游计划编号（10-02 或 10-03），供执行者选择
- 全量 cargo test 通过，无性能回归（criterion 既有 5 个场景 baseline 无显著恶化）
</success_criteria>

<output>
After completion, create `.planning/phases/10-hot-path/10-01-SUMMARY.md` 包含：samply Top 10 函数列表、criterion exclude 场景数值、D-G1 判定结论、下一步计划编号建议。
</output>
</content>
</invoke>