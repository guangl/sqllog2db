---
phase: 09-cli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/update.rs
autonomous: true
requirements: [PERF-11]

must_haves:
  truths:
    - "check_for_updates_at_startup 内部使用 std::thread::spawn 包裹所有网络逻辑"
    - "函数签名不变（仍为 pub fn check_for_updates_at_startup()，无返回值）"
    - "不存储 JoinHandle，fire-and-forget 语义"
    - "warn!() 调用保留在 spawn 闭包内部"
    - "cargo test 通过，cargo clippy 通过"
  artifacts:
    - path: "src/cli/update.rs"
      provides: "后台化的 check_for_updates_at_startup"
      contains: "std::thread::spawn"
  key_links:
    - from: "check_for_updates_at_startup"
      to: "std::thread::spawn 闭包"
      via: "移动所有网络逻辑入闭包"
      pattern: "thread::spawn"
---

<objective>
将 check_for_updates_at_startup 中的网络 I/O（GitHub API 调用）移入后台线程，使主流程不阻塞于网络等待。

Purpose: 当前函数同步调用 GitHub API，若网络慢会直接阻塞 CLI 启动的主流程。后台化后主流程立即继续，update 警告在后台完成后写入 stderr。

Output: 改造后的 update.rs，check_for_updates_at_startup 内部用 thread::spawn fire-and-forget。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cli/09-CONTEXT.md
@.planning/phases/09-cli/09-PATTERNS.md

<interfaces>
<!-- 当前实现（update.rs L66-91），executor 对照修改 -->

当前（同步）：
```rust
pub fn check_for_updates_at_startup() {
    let current_version = cargo_crate_version!();
    let status = self_update::backends::github::Update::configure()
        .repo_owner("guangl")
        .repo_name("sqllog2db")
        .bin_name("sqllog2db")
        .current_version(current_version)
        .build();
    if let Ok(status) = status {
        if let Ok(release) = status.get_latest_release() {
            if self_update::version::bump_is_greater(current_version, &release.version)
                .unwrap_or(false)
            {
                warn!(
                    "A new version is available: {} (current: {})",
                    release.version, current_version
                );
                warn!("Run 'sqllog2db self-update' to update.");
            }
        }
    }
}
```

目标（fire-and-forget，per D-05/D-06）：
```rust
pub fn check_for_updates_at_startup() {
    std::thread::spawn(|| {
        let current_version = cargo_crate_version!();
        let status = self_update::backends::github::Update::configure()
            .repo_owner("guangl")
            .repo_name("sqllog2db")
            .bin_name("sqllog2db")
            .current_version(current_version)
            .build();
        if let Ok(status) = status {
            if let Ok(release) = status.get_latest_release() {
                if self_update::version::bump_is_greater(current_version, &release.version)
                    .unwrap_or(false)
                {
                    warn!(
                        "A new version is available: {} (current: {})",
                        release.version, current_version
                    );
                    warn!("Run 'sqllog2db self-update' to update.");
                }
            }
        }
    });
    // 不保留 JoinHandle，fire-and-forget
}
```

src/main.rs 调用位置（L139-146）签名不变，无需修改：
```rust
cli::update::check_for_updates_at_startup();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 将 check_for_updates_at_startup 网络逻辑移入 thread::spawn</name>
  <read_first>
    - src/cli/update.rs（完整文件，确认当前实现细节）
    - src/main.rs（确认调用位置和签名，确认不需要改动）
  </read_first>
  <files>src/cli/update.rs</files>
  <action>
修改 src/cli/update.rs 中的 check_for_updates_at_startup 函数（L66-91）：

将函数体内的所有逻辑（从 `let current_version = ...` 到最后的 `}` 的闭合括号）整体移入 `std::thread::spawn(|| { ... });` 闭包中。

改后的函数结构：
```rust
pub fn check_for_updates_at_startup() {
    std::thread::spawn(|| {
        let current_version = cargo_crate_version!();
        let status = self_update::backends::github::Update::configure()
            .repo_owner("guangl")
            .repo_name("sqllog2db")
            .bin_name("sqllog2db")
            .current_version(current_version)
            .build();
        if let Ok(status) = status {
            if let Ok(release) = status.get_latest_release() {
                if self_update::version::bump_is_greater(current_version, &release.version)
                    .unwrap_or(false)
                {
                    warn!(
                        "A new version is available: {} (current: {})",
                        release.version, current_version
                    );
                    warn!("Run 'sqllog2db self-update' to update.");
                }
            }
        }
    });
    // 不保留 JoinHandle，fire-and-forget（per D-05）
}
```

注意事项：
1. thread::spawn 返回的 JoinHandle 直接丢弃（不赋值给变量），实现 fire-and-forget
2. cargo_crate_version!() 宏在闭包内部调用仍有效（编译期展开）
3. warn!() 调用位置不变，仍在闭包内，通过 env_logger 输出到 stderr（per D-06）
4. 函数签名 `pub fn check_for_updates_at_startup()` 完全不变
5. 文件顶部的 `use log::{info, warn};` 保持不变（handle_update 仍需要 info!）
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo build --release 2>&1 | tail -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "thread::spawn" src/cli/update.rs 返回 1 行（在 check_for_updates_at_startup 内）
    - grep -n "fn check_for_updates_at_startup" src/cli/update.rs 返回 1 行，签名仍为 "pub fn check_for_updates_at_startup()"
    - grep -n "JoinHandle\|\.join()" src/cli/update.rs 返回 0 行
    - cargo build --release 2>&1 中不含 "error["
    - cargo clippy --all-targets -- -D warnings 2>&1 中不含 "error["
    - cargo test 2>&1 | grep "^test result" 显示 "0 failed"
  </acceptance_criteria>
  <done>check_for_updates_at_startup 内部所有网络逻辑移入 std::thread::spawn 闭包，JoinHandle 丢弃，主流程立即返回，函数签名不变，编译和测试通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background thread → stderr | warn!() 输出与主流程输出可能交错，属于已接受的行为（per D-06） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Denial of Service | thread::spawn 网络调用 | accept | 后台线程无法阻塞主流程；网络超时由 self_update 库内部处理；错误被 if let Ok 静默忽略 |
| T-09-04 | Information Disclosure | warn!() 输出 | accept | 仅输出版本号信息，无敏感数据 |
</threat_model>

<verification>
```bash
cd /Users/guang/Projects/sqllog2db
cargo build --release
cargo clippy --all-targets -- -D warnings
cargo test
grep -n "thread::spawn\|JoinHandle" src/cli/update.rs
```
</verification>

<success_criteria>
- check_for_updates_at_startup 函数体包含 std::thread::spawn
- 无 JoinHandle 存储，无 .join() 调用
- 函数签名与改前完全相同
- cargo build --release 成功
- cargo clippy --all-targets -- -D warnings 通过
- cargo test 全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/09-cli/09-02-SUMMARY.md
</output>
