---
phase: 09-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/filters.rs
autonomous: true
requirements: [PERF-11]

must_haves:
  truths:
    - "compile_patterns 接受 field: &str 参数，返回 crate::error::Result<Option<Vec<Regex>>>"
    - "CompiledMetaFilters::try_from_meta 替代 from_meta，全部用 ? 传播错误，无 .expect()"
    - "CompiledSqlFilters::try_from_sql_filters 替代 from_sql_filters，全部用 ? 传播错误"
    - "FiltersFeature::validate_regexes、validate_include_regexes、validate_exclude_regexes 三个方法完全删除"
    - "cargo test 651 个测试全部通过（测试调用 from_meta 改为 try_from_meta().unwrap()）"
    - "cargo clippy --all-targets -- -D warnings 通过，无新 warning"
  artifacts:
    - path: "src/features/filters.rs"
      provides: "compile_patterns 新签名 + try_from_meta + try_from_sql_filters"
      contains: "fn compile_patterns(\\s*field: &str"
  key_links:
    - from: "compile_patterns"
      to: "crate::error::ConfigError::InvalidValue"
      via: "map_err 包装 Regex::new 错误"
      pattern: "ConfigError::InvalidValue"
    - from: "try_from_meta"
      to: "compile_patterns"
      via: "? 传播"
      pattern: "try_from_meta"
---

<objective>
重构 src/features/filters.rs 消除双重 regex 编译的根本原因：让 compile_patterns 本身承担验证职责（返回 ConfigError），从而使单独的 validate_regexes 系列方法变得多余并可删除。

Purpose: 当前 validate 路径和 compile 路径各自调用一次 Regex::new()，每条命令执行时 14 个 regex 字段编译两遍。改造后 compile 即验证，每个字段只编译一次。

Output: 改造后的 filters.rs，包含新签名的 compile_patterns、try_from_meta、try_from_sql_filters，以及删除的 validate_regexes 系列。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-cli/09-CONTEXT.md
@.planning/phases/09-cli/09-PATTERNS.md

<interfaces>
<!-- 当前关键签名，executor 直接对照修改，无需再读文件 -->

当前 compile_patterns（L322-336）：
```rust
fn compile_patterns(
    patterns: Option<&[String]>,
) -> std::result::Result<Option<Vec<Regex>>, String>
```

目标签名（per D-01）：
```rust
fn compile_patterns(
    field: &str,
    patterns: Option<&[String]>,
) -> crate::error::Result<Option<Vec<Regex>>>
```

错误构造模式（从 validate_pattern_list L348-362 直接复制）：
```rust
Regex::new(p).map_err(|e| {
    crate::error::Error::Config(crate::error::ConfigError::InvalidValue {
        field: field.to_string(),
        value: p.clone(),
        reason: format!("invalid regex: {e}"),
    })
})
```

当前 from_meta（L384-416）所有字段格式：
```rust
usernames: compile_patterns(meta.usernames.as_deref()).expect("regex validated"),
```

目标 try_from_meta（per D-02）所有字段格式：
```rust
usernames: compile_patterns("features.filters.usernames", meta.usernames.as_deref())?,
```

当前 from_sql_filters（L549-562）：
```rust
pub fn from_sql_filters(sf: &SqlFilters) -> Self {
    Self {
        include_patterns: compile_patterns(sf.include_patterns.as_deref()).expect("regex validated"),
        exclude_patterns: compile_patterns(sf.exclude_patterns.as_deref()).expect("regex validated"),
    }
}
```

目标 try_from_sql_filters：
```rust
pub fn try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self> {
    Ok(Self {
        include_patterns: compile_patterns(
            "features.filters.record_sql.include_patterns",
            sf.include_patterns.as_deref())?,
        exclude_patterns: compile_patterns(
            "features.filters.record_sql.exclude_patterns",
            sf.exclude_patterns.as_deref())?,
    })
}
```

field 参数字符串（全部 14 个字段）：
- "features.filters.usernames"
- "features.filters.client_ips"
- "features.filters.sess_ids"
- "features.filters.thrd_ids"
- "features.filters.statements"
- "features.filters.appnames"
- "features.filters.tags"
- "features.filters.exclude_usernames"
- "features.filters.exclude_client_ips"
- "features.filters.exclude_sess_ids"
- "features.filters.exclude_thrd_ids"
- "features.filters.exclude_statements"
- "features.filters.exclude_appnames"
- "features.filters.exclude_tags"
- "features.filters.record_sql.include_patterns"
- "features.filters.record_sql.exclude_patterns"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 重构 compile_patterns 签名并重命名 from_meta/from_sql_filters</name>
  <read_first>
    - src/features/filters.rs（完整文件，一次性读取所有需要改动的位置）
  </read_first>
  <files>src/features/filters.rs</files>
  <action>
对 src/features/filters.rs 做以下四处改动（按顺序，每处独立修改不影响其他）：

**改动 1：compile_patterns 新签名（L322-336）**

将函数签名从：
```rust
fn compile_patterns(
    patterns: Option<&[String]>,
) -> std::result::Result<Option<Vec<Regex>>, String>
```
改为：
```rust
fn compile_patterns(
    field: &str,
    patterns: Option<&[String]>,
) -> crate::error::Result<Option<Vec<Regex>>>
```

函数体中的 map_err 从 `|_| p.clone()` 改为：
```rust
|e| {
    crate::error::Error::Config(crate::error::ConfigError::InvalidValue {
        field: field.to_string(),
        value: p.clone(),
        reason: format!("invalid regex: {e}"),
    })
}
```
collect 的类型标注从 `std::result::Result<Vec<_>, _>` 改为 `crate::error::Result<Vec<_>>`。

**改动 2：CompiledMetaFilters::from_meta → try_from_meta（L384-416）**

将方法改名为 `try_from_meta`，返回类型改为 `crate::error::Result<Self>`，方法体改为：
```rust
pub fn try_from_meta(meta: &MetaFilters) -> crate::error::Result<Self> {
    Ok(Self {
        usernames: compile_patterns("features.filters.usernames", meta.usernames.as_deref())?,
        client_ips: compile_patterns("features.filters.client_ips", meta.client_ips.as_deref())?,
        sess_ids: compile_patterns("features.filters.sess_ids", meta.sess_ids.as_deref())?,
        thrd_ids: compile_patterns("features.filters.thrd_ids", meta.thrd_ids.as_deref())?,
        statements: compile_patterns("features.filters.statements", meta.statements.as_deref())?,
        appnames: compile_patterns("features.filters.appnames", meta.appnames.as_deref())?,
        tags: compile_patterns("features.filters.tags", meta.tags.as_deref())?,
        trxids: meta.trxids.clone(),
        exclude_usernames: compile_patterns("features.filters.exclude_usernames", meta.exclude_usernames.as_deref())?,
        exclude_client_ips: compile_patterns("features.filters.exclude_client_ips", meta.exclude_client_ips.as_deref())?,
        exclude_sess_ids: compile_patterns("features.filters.exclude_sess_ids", meta.exclude_sess_ids.as_deref())?,
        exclude_thrd_ids: compile_patterns("features.filters.exclude_thrd_ids", meta.exclude_thrd_ids.as_deref())?,
        exclude_statements: compile_patterns("features.filters.exclude_statements", meta.exclude_statements.as_deref())?,
        exclude_appnames: compile_patterns("features.filters.exclude_appnames", meta.exclude_appnames.as_deref())?,
        exclude_tags: compile_patterns("features.filters.exclude_tags", meta.exclude_tags.as_deref())?,
    })
}
```
删除原 `#[must_use]` 属性（函数现在返回 Result，不再适用 must_use）。删除 doc comment 中 "须在 Config::validate() 之后调用" 和 "# Panics" 段落，改为简洁说明："从 MetaFilters 编译所有正则，遇到非法 pattern 返回 ConfigError::InvalidValue。"

**改动 3：CompiledSqlFilters::from_sql_filters → try_from_sql_filters（L549-562）**

将方法改名为 `try_from_sql_filters`，返回类型改为 `crate::error::Result<Self>`，方法体改为：
```rust
pub fn try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self> {
    Ok(Self {
        include_patterns: compile_patterns(
            "features.filters.record_sql.include_patterns",
            sf.include_patterns.as_deref())?,
        exclude_patterns: compile_patterns(
            "features.filters.record_sql.exclude_patterns",
            sf.exclude_patterns.as_deref())?,
    })
}
```
删除原 `#[must_use]` 属性和 "# Panics" doc 段落。

**改动 4：删除 FiltersFeature::validate_regexes 系列（L111-174）**

完整删除以下三个方法（含 doc comment 和方法体）：
- `pub fn validate_regexes(&self) -> crate::error::Result<()>`（L113-125）
- `fn validate_include_regexes(&self) -> crate::error::Result<()>`（L127-142）
- `fn validate_exclude_regexes(&self) -> crate::error::Result<()>`（L144-174）

同时删除 `validate_pattern_list` 函数（L347-362），因为 compile_patterns 已经包含了相同的错误构造逻辑，该函数不再需要。

**改动 5：更新测试中所有 from_meta/from_sql_filters 调用**

在 #[cfg(test)] 模块中，将所有 `CompiledMetaFilters::from_meta(&meta)` 改为 `CompiledMetaFilters::try_from_meta(&meta).unwrap()`，将所有 `CompiledSqlFilters::from_sql_filters(&sf)` 改为 `CompiledSqlFilters::try_from_sql_filters(&sf).unwrap()`。

需要更新的调用位置（完整列表）：
- make_compiled_meta 辅助函数（L1012）
- test_compiled_meta_single_field_or（L1041）
- test_compiled_meta_tags_none_rejected（L1053）
- test_compiled_meta_trxids_and（L1072）
- test_t1_compiled_from_meta_exclude_usernames（L1102）
- test_t1_compiled_from_meta_exclude_none（L1108）
- test_t1_has_any_filters_include_only（L1118）
- test_t1_has_any_filters_exclude_only（L1128）
- make_compiled_with_exclude 辅助函数（L1143）
- test_exclude_tags_drops_matching（L1227）和系列（L1237、L1247）
- test_exclude_with_include_veto_wins（L1192）
- test_exclude_with_include_both_pass（L1204）
- test_exclude_with_include_include_fails（L1216）
- test_has_any_filters_exclude_only（L1258）

test_exclude_invalid_regex_validate_fails（L1278）需要特殊处理：将 `feature.validate_regexes().is_err()` 改为：
```rust
let result = crate::features::filters::CompiledMetaFilters::try_from_meta(&feature.meta);
assert!(result.is_err());
```
同时删除对 `FiltersFeature` 的构造（因为不再需要 feature 变量来调用 validate_regexes），简化为直接构造 MetaFilters 并调用 try_from_meta。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test 2>&1 | tail -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "fn compile_patterns" src/features/filters.rs 输出含 "field: &str"
    - grep -n "fn try_from_meta" src/features/filters.rs 返回至少 1 行
    - grep -n "fn from_meta" src/features/filters.rs 返回 0 行（旧名已删除）
    - grep -n "fn try_from_sql_filters" src/features/filters.rs 返回至少 1 行
    - grep -n "fn from_sql_filters" src/features/filters.rs 返回 0 行（旧名已删除）
    - grep -n "fn validate_regexes" src/features/filters.rs 返回 0 行
    - grep -n "fn validate_pattern_list" src/features/filters.rs 返回 0 行
    - grep -n '\.expect("regex validated")' src/features/filters.rs 返回 0 行
    - cargo test 2>&1 | grep -E "^test result" 显示 "651 passed" 或更多，"0 failed"
    - cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "error\[" 为 0
  </acceptance_criteria>
  <done>filters.rs 中 compile_patterns 接受 field 参数返回 crate::Result，try_from_meta 和 try_from_sql_filters 均用 ? 传播错误，validate_regexes 系列和 validate_pattern_list 已完全删除，所有测试通过，clippy 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → validate() | 用户提供的 TOML 中的 regex 字符串，在 validate 阶段捕获非法 pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | compile_patterns Regex::new | accept | regex crate 已有 size limit，catastrophic backtracking 风险由调用方配置可控；非网络暴露 |
| T-09-02 | Information Disclosure | ConfigError::InvalidValue.reason | accept | reason 字段含原始 regex 字符串，用于诊断，不包含敏感数据 |
</threat_model>

<verification>
```bash
cd /Users/guang/Projects/sqllog2db
cargo test
cargo clippy --all-targets -- -D warnings
grep -n "fn compile_patterns" src/features/filters.rs
grep -n "validate_regexes\|from_meta\b\|from_sql_filters\b" src/features/filters.rs
```
</verification>

<success_criteria>
- compile_patterns 签名含 field: &str 参数，返回 crate::error::Result
- try_from_meta 和 try_from_sql_filters 存在，from_meta 和 from_sql_filters 不存在
- validate_regexes、validate_include_regexes、validate_exclude_regexes、validate_pattern_list 均不存在
- 无任何 .expect("regex validated") 调用
- cargo test：651+ 测试通过，0 失败
- cargo clippy --all-targets -- -D warnings：0 error
</success_criteria>

<output>
完成后创建 .planning/phases/09-cli/09-01-SUMMARY.md
</output>
