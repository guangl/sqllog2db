---
phase: 09-cli
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/config.rs
  - src/cli/run.rs
autonomous: true
requirements: [PERF-11]

must_haves:
  truths:
    - "Config::validate() 调用 CompiledMetaFilters::try_from_meta 和 CompiledSqlFilters::try_from_sql_filters 替代 validate_regexes"
    - "FilterProcessor::new 改名为 try_new，返回 crate::error::Result<Self>"
    - "build_pipeline 返回 crate::error::Result<Pipeline>，调用 try_new 传播错误"
    - "cargo test 651 个测试全部通过"
    - "cargo clippy --all-targets -- -D warnings 通过"
  artifacts:
    - path: "src/config.rs"
      provides: "Config::validate 调用 try_from_meta/try_from_sql_filters"
      contains: "try_from_meta"
    - path: "src/cli/run.rs"
      provides: "FilterProcessor::try_new + build_pipeline 返回 Result"
      contains: "fn try_new"
  key_links:
    - from: "Config::validate"
      to: "CompiledMetaFilters::try_from_meta"
      via: "? 传播"
      pattern: "try_from_meta"
    - from: "build_pipeline"
      to: "FilterProcessor::try_new"
      via: "? 传播"
      pattern: "try_new"
    - from: "handle_run"
      to: "build_pipeline"
      via: "build_pipeline(cfg)? 调用"
      pattern: "build_pipeline.*\\?"
---

<objective>
将 Plan 01 中新建的 try_from_meta / try_from_sql_filters 接入调用链：在 config.rs 的 validate() 中替换 validate_regexes 调用，在 cli/run.rs 中将 FilterProcessor::new 改为 try_new 并让 build_pipeline 返回 Result。

Purpose: Plan 01 完成了函数签名重构，本 Plan 完成调用链接入——让 validate 路径和 run 路径各自只调用一次 try_from_meta，消除双重编译。

Output: 更新后的 config.rs 和 cli/run.rs，调用链完整连通，每条代码路径每个 regex 只编译一次（per D-04）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-cli/09-CONTEXT.md
@.planning/phases/09-cli/09-PATTERNS.md
@.planning/phases/09-cli/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01 产出的新接口 -->

src/features/filters.rs（Plan 01 改造后）：
```rust
// CompiledMetaFilters
pub fn try_from_meta(meta: &MetaFilters) -> crate::error::Result<Self>

// CompiledSqlFilters
pub fn try_from_sql_filters(sf: &SqlFilters) -> crate::error::Result<Self>
```

src/config.rs 当前 validate()（L54-78）需要修改：
```rust
pub fn validate(&self) -> Result<()> {
    self.logging.validate()?;
    self.exporter.validate()?;
    self.sqllog.validate()?;
    if let Some(filters) = &self.features.filters {
        if filters.enable {
            filters.validate_regexes()?;  // ← 删除此行
        }
    }
    // ...
    Ok(())
}
```

src/cli/run.rs 当前 build_pipeline（L21-31）和 FilterProcessor::new（L44-56）：
```rust
fn build_pipeline(cfg: &Config) -> Pipeline {
    let mut pipeline = Pipeline::new();
    if let Some(f) = &cfg.features.filters {
        if f.has_filters() {
            pipeline.add(Box::new(FilterProcessor::new(f)));
        }
    }
    pipeline
}

impl FilterProcessor {
    fn new(filter: &crate::features::FiltersFeature) -> Self {
        let compiled_meta = CompiledMetaFilters::from_meta(&filter.meta);
        let has_meta_filters = compiled_meta.has_any_filters();
        Self {
            compiled_meta,
            start_ts: filter.meta.start_ts.clone(),
            end_ts: filter.meta.end_ts.clone(),
            has_meta_filters,
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 config.rs validate() 调用链</name>
  <read_first>
    - src/config.rs（完整文件，重点看 L1-10 use 声明和 L54-78 validate 方法）
    - src/features/mod.rs（确认 CompiledMetaFilters、CompiledSqlFilters 的导出路径）
  </read_first>
  <files>src/config.rs</files>
  <action>
修改 src/config.rs 的两处：

**修改 1：顶部 use 声明**

当前 L4：`pub use crate::features::FeaturesConfig;`

在此行后面新增：
```rust
use crate::features::{CompiledMetaFilters, CompiledSqlFilters};
```

如果 CompiledMetaFilters/CompiledSqlFilters 不在 crate::features 的公开导出中，则在 validate() 函数内部使用全路径 `crate::features::filters::CompiledMetaFilters::try_from_meta(...)` 即可，不需要 use 声明。

**修改 2：validate() 方法中的 if let Some(filters) 块**

将：
```rust
if let Some(filters) = &self.features.filters {
    if filters.enable {
        filters.validate_regexes()?;
    }
}
```

改为：
```rust
if let Some(filters) = &self.features.filters {
    if filters.enable {
        // 用 try_from_meta/try_from_sql_filters 验证正则合法性
        // compile 本身即验证，结果丢弃（per D-03/D-04）
        CompiledMetaFilters::try_from_meta(&filters.meta)?;
        CompiledSqlFilters::try_from_sql_filters(&filters.record_sql)?;
    }
}
```

注意：Config::validate() 的签名 `pub fn validate(&self) -> Result<()>` 保持不变，`?` 可直接使用。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo build 2>&1 | grep -E "^error" | head -10</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "try_from_meta" src/config.rs 返回至少 1 行
    - grep -n "validate_regexes" src/config.rs 返回 0 行
    - grep -n "try_from_sql_filters" src/config.rs 返回至少 1 行
    - cargo build 2>&1 中不含 "error["（编译成功）
    - cargo clippy --all-targets -- -D warnings 2>&1 中不含 "error["
  </acceptance_criteria>
  <done>Config::validate() 不再调用 validate_regexes，改为调用 try_from_meta 和 try_from_sql_filters 并用 ? 传播错误，编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 cli/run.rs 的 FilterProcessor::try_new 和 build_pipeline</name>
  <read_first>
    - src/cli/run.rs（完整文件，重点看 L1-30 build_pipeline 和 L44-56 FilterProcessor::new，以及 handle_run 函数签名以确认 ? 可用）
  </read_first>
  <files>src/cli/run.rs</files>
  <action>
修改 src/cli/run.rs 的两处：

**修改 1：FilterProcessor::new → try_new**

将（L44-56）：
```rust
impl FilterProcessor {
    fn new(filter: &crate::features::FiltersFeature) -> Self {
        let compiled_meta = CompiledMetaFilters::from_meta(&filter.meta);
        let has_meta_filters = compiled_meta.has_any_filters();
        Self {
            compiled_meta,
            start_ts: filter.meta.start_ts.clone(),
            end_ts: filter.meta.end_ts.clone(),
            has_meta_filters,
        }
    }
}
```

改为：
```rust
impl FilterProcessor {
    fn try_new(filter: &crate::features::FiltersFeature) -> crate::error::Result<Self> {
        let compiled_meta = CompiledMetaFilters::try_from_meta(&filter.meta)?;
        let has_meta_filters = compiled_meta.has_any_filters();
        Ok(Self {
            compiled_meta,
            start_ts: filter.meta.start_ts.clone(),
            end_ts: filter.meta.end_ts.clone(),
            has_meta_filters,
        })
    }
}
```

**修改 2：build_pipeline 返回 Result**

将（L21-31）：
```rust
fn build_pipeline(cfg: &Config) -> Pipeline {
    let mut pipeline = Pipeline::new();
    if let Some(f) = &cfg.features.filters {
        if f.has_filters() {
            pipeline.add(Box::new(FilterProcessor::new(f)));
        }
    }
    pipeline
}
```

改为：
```rust
fn build_pipeline(cfg: &Config) -> crate::error::Result<Pipeline> {
    let mut pipeline = Pipeline::new();
    if let Some(f) = &cfg.features.filters {
        if f.has_filters() {
            pipeline.add(Box::new(FilterProcessor::try_new(f)?));
        }
    }
    Ok(pipeline)
}
```

**修改 3：handle_run 中调用 build_pipeline 的地方**

在 handle_run 函数体内找到 `build_pipeline(cfg)` 调用，改为 `build_pipeline(cfg)?`。

handle_run 已经是 `-> Result<()>`，所以 `?` 可以直接用。
  </action>
  <verify>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo test 2>&1 | tail -5</automated>
    <automated>cd /Users/guang/Projects/sqllog2db && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "fn try_new" src/cli/run.rs 返回 1 行，含 "-> crate::error::Result<Self>"
    - grep -n "fn new" src/cli/run.rs 对应 FilterProcessor 的 impl 块不再有 "fn new" 方法（旧名已删除）
    - grep -n "fn build_pipeline" src/cli/run.rs 返回 1 行，含 "crate::error::Result<Pipeline>"
    - grep -n "from_meta\b" src/cli/run.rs 返回 0 行（已改为 try_from_meta）
    - cargo test 2>&1 | grep "^test result" 显示 "651 passed" 或更多，"0 failed"
    - cargo clippy --all-targets -- -D warnings 2>&1 | grep -c "error\[" 为 0
  </acceptance_criteria>
  <done>FilterProcessor::try_new 返回 Result，build_pipeline 返回 Result，handle_run 用 ? 处理，cargo test 全量通过，clippy 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → validate() → try_from_meta | regex 字符串从 TOML 文件流入，在 validate 阶段统一捕获 |
| handle_run → build_pipeline → try_new | 运行路径中 regex 编译错误通过 ? 传播到顶层，以 ConfigError 形式呈现给用户 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | build_pipeline 错误路径 | accept | 错误通过 ? 传播至 handle_run，以非零退出码终止，用户可见错误信息 |
</threat_model>

<verification>
```bash
cd /Users/guang/Projects/sqllog2db
cargo test
cargo clippy --all-targets -- -D warnings
grep -n "validate_regexes\|from_meta\b" src/config.rs src/cli/run.rs
grep -n "try_from_meta\|try_new\|try_from_sql_filters" src/config.rs src/cli/run.rs
```
</verification>

<success_criteria>
- config.rs validate() 调用 try_from_meta 和 try_from_sql_filters，不调用 validate_regexes
- cli/run.rs 中 FilterProcessor::try_new 返回 Result，build_pipeline 返回 Result
- 无任何 from_meta 或 from_sql_filters 调用（旧名不存在）
- cargo test：651+ 测试通过，0 失败
- cargo clippy --all-targets -- -D warnings：0 error
</success_criteria>

<output>
完成后创建 .planning/phases/09-cli/09-03-SUMMARY.md
</output>
