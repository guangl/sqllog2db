---
phase: 14-exporter
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/exporter/csv.rs
autonomous: true
requirements:
  - TMPL-04
tags:
  - exporter
  - csv
  - rust
must_haves:
  truths:
    - "CsvExporter::write_template_stats() 写入 <basename>_templates.csv 伴随文件到主 CSV 同目录"
    - "伴随文件路径推导：final_path=Some(p) 时用 p，None 时用 self.path（D-09）"
    - "伴随文件始终覆盖写入，不跟随主 CSV append 标志（D-10）"
    - "伴随文件首行为表头 `template_key,count,avg_us,min_us,max_us,p50_us,p95_us,p99_us,first_seen,last_seen`"
    - "template_key 字段经 CSV 转义（用既有 write_csv_escaped），数值字段用 itoa 零分配写出（与主 CSV 一致）"
    - "BufWriter 在返回前调用 flush()，确保文件内容完整不截断"
    - "实现按职责拆分为 build_companion_path / write_companion_rows / write_template_stats 三个函数，每个函数体均 ≤40 行（遵守 CLAUDE.md 40 行限制）"
  artifacts:
    - path: "src/exporter/csv.rs"
      provides: "CsvExporter::write_template_stats impl + build_companion_path helper + write_companion_rows helper + 2 integration tests"
      contains: "fn write_template_stats"
    - path: "src/exporter/csv.rs"
      provides: "build_companion_path 路径推导辅助（D-09）"
      contains: "fn build_companion_path"
    - path: "src/exporter/csv.rs"
      provides: "write_companion_rows 文件写入辅助（headers + rows + flush）"
      contains: "fn write_companion_rows"
  key_links:
    - from: "write_template_stats"
      to: "build_companion_path"
      via: "路径推导委托"
      pattern: "build_companion_path\\("
    - from: "write_template_stats"
      to: "write_companion_rows"
      via: "I/O 委托"
      pattern: "write_companion_rows\\("
    - from: "write_companion_rows"
      to: "write_csv_escaped"
      via: "template_key 字段转义"
      pattern: "write_csv_escaped\\(.*s\\.template_key"
    - from: "write_companion_rows"
      to: "writer.flush()"
      via: "返回前显式 flush"
      pattern: "writer\\.flush\\(\\)"
---

<objective>
为 `CsvExporter` 实现 `Exporter` trait 新增方法 `write_template_stats()`：在 `finalize()` 后被 ExporterManager 调用，根据 D-09 路径推导规则生成 `<basename>_templates.csv` 伴随文件，写入与 `sql_templates` 表列名一致的表头与数据行（数值用 itoa 零分配，template_key 用既有 `write_csv_escaped` 转义），写完显式 `flush()`。为遵守 CLAUDE.md "Keep functions under 40 lines" 约束，按职责将实现拆分为 `build_companion_path` (~8 行) + `write_companion_rows` (~25 行) + 主函数 `write_template_stats` (~10 行) 三层。同时新增 2 项集成测试（基本伴随文件写入 + final_path 覆盖路径推导）。

Purpose: 满足 ROADMAP 成功标准 SC-2（"CSV 导出模式下，run 结束后在 CSV 文件同目录生成 `<basename>_templates.csv` 伴随文件，列结构与 SQLite 表一致"），TMPL-04-B/H 测试映射。
Output: `src/exporter/csv.rs` 新增 1 个 impl Exporter 方法 + 2 个模块级辅助函数 + 2 个测试函数；`cargo test csv_templates` 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-exporter/14-CONTEXT.md
@.planning/phases/14-exporter/14-RESEARCH.md
@.planning/phases/14-exporter/14-PATTERNS.md
@.planning/phases/14-exporter/14-VALIDATION.md
@.planning/phases/14-exporter/14-01-SUMMARY.md
@src/exporter/csv.rs
@src/features/template_aggregator.rs
@src/exporter/mod.rs

<interfaces>
<!-- 关键已有代码，executor 必须按此使用，不要再去 codebase 探索 -->

From src/exporter/csv.rs（现有代码）:
- 顶部 `use super::{ensure_parent_dir, f32_ms_to_i64, strip_ip_prefix};` 已存在
- 顶部 `use std::fs::{File, OpenOptions};` 与 `use std::io::{BufWriter, Write};` 已存在
- 顶部 `use std::path::{Path, PathBuf};` 已存在
- `pub struct CsvExporter { pub path: PathBuf, writer: Option<BufWriter<File>>, ... }`，`path` 是 PathBuf
- `pub fn new(path: impl AsRef<Path>) -> Self`（约 L51）已存在
- `fn write_csv_escaped(buf: &mut Vec<u8>, bytes: &[u8])`（约 L13-21）：模块私有，同文件可见，已用 memchr 优化
- 现有 `fn initialize(...)` 示范 `ensure_parent_dir(&self.path)` + `File::create`/`OpenOptions` + 错误用 `Error::Export(ExportError::WriteFailed { path: ..., reason: ... })` 包装

From src/error/:
- `ExportError::WriteFailed { path: PathBuf, reason: String }`

From src/features/template_aggregator.rs:
- `pub struct TemplateStats { pub template_key: String, pub count: u64, pub avg_us: u64, pub min_us: u64, pub max_us: u64, pub p50_us: u64, pub p95_us: u64, pub p99_us: u64, pub first_seen: String, pub last_seen: String }`

From Plan 01:
- Exporter trait 已有默认 `fn write_template_stats(...)` no-op 实现
- `ExporterKind::Csv(e) => e.write_template_stats(stats, final_path)` 透传已存在
- `crate::features::TemplateStats` 已 re-export，可直接使用
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 CsvExporter::write_template_stats() + 两个辅助函数（build_companion_path、write_companion_rows），并新增 2 项集成测试</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（完整文件：write_csv_escaped、CsvExporter struct 字段、new/from_config/initialize/finalize 现有实现、测试模块结构）
    - src/exporter/mod.rs（ensure_parent_dir 签名）
    - src/features/template_aggregator.rs（TemplateStats 10 字段顺序）
    - .planning/phases/14-exporter/14-CONTEXT.md（D-09、D-10 决策）
    - .planning/phases/14-exporter/14-RESEARCH.md（Pattern 3 csv write_template_stats 完整模板 + Pitfall 5 路径 edge case）
    - .planning/phases/14-exporter/14-PATTERNS.md（src/exporter/csv.rs 一节，含 CSV 转义 + ensure_parent_dir 调用 + itoa 写整数 + flush 模式）
    - .planning/phases/14-exporter/14-01-SUMMARY.md（确认 trait 默认签名一致）
    - /Users/guang/Projects/sqllog2db/CLAUDE.md（"Keep functions under 40 lines — split if longer" 约束）
  </read_first>
  <behavior>
    - Test 1（test_csv_write_template_stats，TMPL-04-B）：构造 `CsvExporter::new(&temp_dir/"output.csv")`，调用 initialize → finalize → write_template_stats 传入 2 个 TemplateStats，验证 `temp_dir/output_templates.csv` 存在；读取文件内容，断言：(a) 首行精确等于 `template_key,count,avg_us,min_us,max_us,p50_us,p95_us,p99_us,first_seen,last_seen\n`；(b) 数据行数 = 2；(c) 第一行 template_key 字段含有逗号则被双引号包裹（即测试至少 1 个含逗号或引号的 template_key，验证 CSV 转义）；(d) 数值字段无引号包裹，可直接 `parse::<u64>()`
    - Test 2（test_parallel_csv_companion_file，TMPL-04-H）：构造 `CsvExporter::new(&temp_dir/"output.csv")`（self.path）；调用 write_template_stats 时传入 `Some(temp_dir/"actual_output.csv")` 作为 `final_path`；验证伴随文件被生成到 `temp_dir/actual_output_templates.csv`（用 final_path 推导）而非 `temp_dir/output_templates.csv`
  </behavior>
  <action>
    **CLAUDE.md 约束**：项目根 `CLAUDE.md` 明确要求 "Keep functions under 40 lines — split if longer"。原 inline 实现的 `write_template_stats()` 包含路径推导 + ensure_parent_dir + File::create + BufWriter + 表头写入 + 数据行循环（含 itoa + write_csv_escaped）+ flush + 错误包装会超出 60 行。本任务必须按职责将实现拆分为三层（路径推导 / 文件写入 / 顶层入口），最终三个函数均小于 40 行。

    在 `src/exporter/csv.rs` 中按以下结构实现：

    **拆分 1：模块级辅助函数 `build_companion_path` (~8 行)**

    在 `src/exporter/csv.rs` 的模块级（与 `write_csv_escaped` 同位置，文件顶部、impl 块之外），新增私有函数：

    `fn build_companion_path(base_path: &Path) -> PathBuf`

    职责（目标 ≤10 行）：
    1. `let stem = base_path.file_stem().unwrap_or_default();`
    2. `base_path.with_file_name(format!("{}_templates.csv", stem.to_string_lossy()))`
    3. 返回上式结果（PathBuf）

    无错误返回（路径推导纯计算，不涉及 I/O）。函数体目标 ≤8 行。

    **拆分 2：模块级辅助函数 `write_companion_rows` (~25 行)**

    在 `build_companion_path` 之后新增私有函数：

    `fn write_companion_rows(path: &Path, stats: &[crate::features::TemplateStats]) -> Result<()>`

    职责（目标 ≤30 行）：
    1. **目录确保 + 覆盖写入**：
       - `ensure_parent_dir(path).map_err(|e| Error::Export(ExportError::WriteFailed { path: path.to_path_buf(), reason: format!("create dir failed: {e}") }))?;`
       - `let file = std::fs::File::create(path).map_err(|e| Error::Export(ExportError::WriteFailed { path: path.to_path_buf(), reason: format!("create companion failed: {e}") }))?;`（D-10 始终覆盖，不用 OpenOptions append）
       - `let mut writer = std::io::BufWriter::new(file);`
    2. **表头**：`writer.write_all(b"template_key,count,avg_us,min_us,max_us,p50_us,p95_us,p99_us,first_seen,last_seen\n").map_err(|e| Error::Export(ExportError::WriteFailed { path: path.to_path_buf(), reason: format!("write header failed: {e}") }))?;`
    3. **数据行循环**（用 line_buf + clear 复用 + itoa + write_csv_escaped）：
       - `let mut itoa_buf = itoa::Buffer::new();`
       - `let mut line_buf: Vec<u8> = Vec::with_capacity(512);`
       - `for s in stats { line_buf.clear(); line_buf.push(b'"'); write_csv_escaped(&mut line_buf, s.template_key.as_bytes()); line_buf.push(b'"'); line_buf.push(b','); line_buf.extend_from_slice(itoa_buf.format(s.count).as_bytes()); line_buf.push(b','); line_buf.extend_from_slice(itoa_buf.format(s.avg_us).as_bytes()); line_buf.push(b','); ... (其余数值字段类似，重复 7 次：count/avg_us/min_us/max_us/p50_us/p95_us/p99_us) ...; line_buf.extend_from_slice(s.first_seen.as_bytes()); line_buf.push(b','); line_buf.extend_from_slice(s.last_seen.as_bytes()); line_buf.push(b'\n'); writer.write_all(&line_buf).map_err(|e| Error::Export(ExportError::WriteFailed { path: path.to_path_buf(), reason: format!("write row failed: {e}") }))?; }`
    4. **flush + 返回**：`writer.flush().map_err(|e| Error::Export(ExportError::WriteFailed { path: path.to_path_buf(), reason: format!("flush failed: {e}") }))?; Ok(())`

    若 INSERT-列代码块导致函数过长，可将"行 buffer 构造"内联在 for 循环体内（保持 single closure，目标整体 ≤30 行）。如仍超 40 行需进一步拆分（例如新增 `format_companion_row(&mut Vec<u8>, &mut itoa::Buffer, &TemplateStats)`）。

    **拆分 3：主方法 `write_template_stats` (~10 行)**

    在 `impl Exporter for CsvExporter { ... }` 块中，紧随 `fn finalize(...)` 之后添加 `fn write_template_stats(...)` 方法。签名必须与 Plan 01 的 trait 默认实现一致：`fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], final_path: Option<&std::path::Path>) -> Result<()>`。

    职责（目标 ≤10 行）：
    1. **D-09 路径推导**：`let base_path: &Path = final_path.unwrap_or(self.path.as_path()); let companion = build_companion_path(base_path);`
    2. **委托写入**：`write_companion_rows(&companion, stats)?;`
    3. **日志**：`info!("Template companion CSV written: {}", companion.display());`
    4. **返回**：`Ok(())`

    主函数体目标 ≤10 行（含括号）。

    **测试（参照已有 CSV 测试结构，如 `test_csv_basic_export`，使用 `tempfile::TempDir`）**：

    在同文件 `#[cfg(test)] mod tests` 块内新增 2 项测试：
    - test_csv_write_template_stats：构造 stats 时第一个 template_key 含逗号或双引号（如 `r#"SELECT * FROM t WHERE name = "John", age = ?"#`），验证转义被正确写出；用 `std::fs::read_to_string(companion)` 读回再断言行内容
    - test_parallel_csv_companion_file：self.path = `output.csv`，final_path = `Some(actual_output.csv)`，断言伴随文件位于 `actual_output_templates.csv`，并通过 `Path::exists()` 验证 `output_templates.csv` 不存在

    禁止：
    - 修改 `finalize()` 或其他现有方法签名
    - 用 OpenOptions append 模式（破坏 D-10）
    - 在 template_key 之外的字段使用 CSV 转义（仅 template_key 含特殊字符风险；时间戳为达梦日志固定格式无逗号；数值字段无特殊字符）
    - 重新实现 write_csv_escaped 或 ensure_parent_dir（直接调用既有函数）
    - 用 println!/eprintln! 替代 info!
    - 将三个职责（路径推导 / 文件写入 / 顶层入口）合并到单一函数（违反 CLAUDE.md 40 行限制）
    - 将辅助函数 `build_companion_path` / `write_companion_rows` 放在 `impl Exporter for CsvExporter` 块内（应在模块级，方便单元测试 + 与 `write_csv_escaped` 同模式）
  </action>
  <verify>
    <automated>cargo test --lib exporter::csv 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "fn write_template_stats" src/exporter/csv.rs` 命中 1 处（impl Exporter 块内）
    - `grep -nE "fn build_companion_path" src/exporter/csv.rs` 命中 1 处（模块级）
    - `grep -nE "fn write_companion_rows" src/exporter/csv.rs` 命中 1 处（模块级）
    - `grep -E "build_companion_path\\(" src/exporter/csv.rs` 至少命中 2 处（定义 + 在 write_template_stats 内调用）
    - `grep -E "write_companion_rows\\(" src/exporter/csv.rs` 至少命中 2 处（定义 + 在 write_template_stats 内调用）
    - `grep -E "_templates\\.csv" src/exporter/csv.rs` 至少命中 1 处（path 构造，应在 build_companion_path 内）
    - `grep -E "template_key,count,avg_us,min_us,max_us,p50_us,p95_us,p99_us,first_seen,last_seen" src/exporter/csv.rs` 命中 1 处（表头字面量，应在 write_companion_rows 内）
    - `grep -E "write_csv_escaped\\(.*s\\.template_key" src/exporter/csv.rs` 命中 1 处（在 write_companion_rows 内）
    - `grep -E "writer\\.flush\\(\\)" src/exporter/csv.rs` 在新方法范围内至少 1 处（在 write_companion_rows 内）
    - `grep -E "ensure_parent_dir\\(" src/exporter/csv.rs` 在新增代码中至少 1 处（在 write_companion_rows 内）
    - `grep -E "File::create\\(" src/exporter/csv.rs` 在新增代码中至少 1 处（覆盖写入，非 OpenOptions append）
    - **函数长度 ≤40 行（CLAUDE.md）**：`awk '/^fn build_companion_path\(/,/^\}$/' src/exporter/csv.rs | wc -l` 输出值 ≤15
    - **函数长度 ≤40 行（CLAUDE.md）**：`awk '/^fn write_companion_rows\(/,/^\}$/' src/exporter/csv.rs | wc -l` 输出值 ≤40
    - **函数长度 ≤40 行（CLAUDE.md）**：`awk '/fn write_template_stats\(/,/^    \}$/' src/exporter/csv.rs | wc -l` 输出值 ≤15
    - `grep -cE "^\\s*fn test_csv_write_template_stats\\b|^\\s*fn test_parallel_csv_companion_file\\b" src/exporter/csv.rs` 返回 2
    - `cargo test test_csv_write_template_stats` 退出码 0
    - `cargo test test_parallel_csv_companion_file` 退出码 0
    - `cargo test --lib exporter::csv` 全部通过（无现有测试回归）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
  </acceptance_criteria>
  <done>
    CsvExporter::write_template_stats 完整实现：实现按职责拆分为 build_companion_path（≤8 行，路径推导）+ write_companion_rows（≤30 行，I/O 与序列化）+ 主函数 write_template_stats（≤10 行，委托 + 日志）三层，所有函数体均符合 CLAUDE.md "Keep functions under 40 lines" 约束；路径推导按 D-09 处理 final_path 优先，覆盖写入 D-10，使用既有 write_csv_escaped + itoa 零分配数值写出，flush 显式调用确保完整；2 项测试覆盖基础写入 + final_path 路径推导；clippy 全绿；run.rs 接入（Plan 04）可基于此方法工作。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TemplateStats → CSV 文件系统 | `template_key` 来自 `normalize_template()`（内部生成），但仍走 CSV 转义防止字段嵌入逗号/引号导致解析错位 |
| 伴随文件路径 | `final_path` 来自配置（顺序路径为 None，并行路径为 csv_cfg.file）；`self.path` 与 `final_path` 均已通过 Config 校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering / Injection | template_key 写入 CSV | mitigate | 用 `write_csv_escaped`（memchr-based 双引号转义）+ 双引号包裹字段，杜绝 CSV 字段越界 |
| T-14-07 | Information Disclosure | 伴随文件路径推导 | accept | 路径来源于配置或并行路径 csv_cfg.file，与主 CSV 同目录；不引入新外部输入 |
| T-14-08 | Tampering | 覆盖写入（File::create） | accept | 模板统计是全量结果（D-10）；首次运行无目标文件，重跑覆盖与主 CSV overwrite 语义一致 |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装新包 |
</threat_model>

<verification>
- `cargo test --lib exporter::csv` 全绿
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt` 后无 diff
- 2 项新增测试函数（test_csv_write_template_stats、test_parallel_csv_companion_file）全部 PASS
- 现有 CsvExporter 测试无回归
- 三个新函数（build_companion_path、write_companion_rows、write_template_stats）函数体均 ≤40 行
</verification>

<success_criteria>
- CsvExporter::write_template_stats() 实现存在且通过 trait 透传被调用
- build_companion_path() 模块级辅助函数存在，封装 D-09 路径推导
- write_companion_rows() 模块级辅助函数存在，封装文件创建 + headers + rows + flush
- 三函数均符合 CLAUDE.md 40 行限制
- 伴随文件 path 按 final_path 优先、self.path 退路推导
- 文件首行表头精确匹配 sql_templates 列结构
- template_key 经 CSV 转义安全写入
- flush() 显式调用，文件无截断
- 全部测试与 clippy 通过
</success_criteria>

<output>
Create `.planning/phases/14-exporter/14-03-SUMMARY.md` when done with:
- Files modified（src/exporter/csv.rs）
- New method/function signatures（build_companion_path / write_companion_rows / write_template_stats）
- Each function's actual line count (must be ≤40)
- Test names + actual test outputs（pass count）
- 伴随文件命名示例（output.csv → output_templates.csv）
- Affects: [CsvExporter behavior]
- Provides: [_templates.csv companion file writer]
- Patterns: [既有 write_csv_escaped + itoa + ensure_parent_dir + File::create 覆盖 + 三层职责拆分]
</output>
