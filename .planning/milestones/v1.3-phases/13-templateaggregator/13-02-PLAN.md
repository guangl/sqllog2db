---
phase: 13-templateaggregator
plan: 02
type: execute
wave: 2
depends_on:
  - 13-templateaggregator-01
files_modified:
  - src/cli/run.rs   # process_log_file 签名替换 + 热循环侧路径接入 + process_csv_parallel map-reduce + handle_run 创建/finalize
autonomous: true
requirements:
  - TMPL-02

tags:
  - rust
  - hot-loop
  - rayon
  - side-channel

must_haves:
  truths:
    - "src/cli/run.rs 中 process_log_file 函数签名第 9 个参数从 `_do_template: bool` 替换为 `aggregator: Option<&mut crate::features::TemplateAggregator>`（删除 _do_template 行，新增 aggregator 行；保持参数总数与上下游一致）"
    - "src/cli/run.rs process_log_file 热循环内（原 lines 222-228 占位注释块位置）含 `if let Some(agg) = aggregator.as_deref_mut() { if record.tag.is_some() { let tmpl_key = crate::features::normalize_template(pm.sql.as_ref()); let exectime_us = (pm.exectime * 1000.0) as u64; if exectime_us > 0 { agg.observe(&tmpl_key, exectime_us, record.ts.as_ref()); } } }` 的等价实现"
    - "src/cli/run.rs 热循环内删除原 lines 222-228 的占位注释块（`// D-14: Phase 13 will wire this into TemplateAggregator::observe().` 与其下五行注释代码）"
    - "src/cli/run.rs process_csv_parallel 内每个任务闭包创建 `let mut task_agg: Option<crate::features::TemplateAggregator> = if do_template { Some(crate::features::TemplateAggregator::new()) } else { None };`，并把 `task_agg.as_mut()` 作为第 9 个参数传给 process_log_file"
    - "src/cli/run.rs process_csv_parallel 任务返回类型 TaskResult 从 `Option<(PathBuf, PathBuf, usize)>` 扩展为 `Option<(PathBuf, PathBuf, usize, Option<crate::features::TemplateAggregator>)>`；任务体最后返回 `Ok(Some((file.clone(), temp_path, count, task_agg)))`"
    - "src/cli/run.rs process_csv_parallel 函数签名新增返回值携带 `Option<crate::features::TemplateAggregator>`：返回类型从 `Result<(Vec<(PathBuf, usize)>, usize)>` 改为 `Result<(Vec<(PathBuf, usize)>, usize, Option<crate::features::TemplateAggregator>)>`；主线程在 parts_info 收集后通过 `parts_info.iter_mut().filter_map(|(_, _, _, agg)| agg.take()).reduce(|mut a, b| { a.merge(b); a })` 合并得到 merged_agg"
    - "src/cli/run.rs handle_run 内 `do_template` 标志（lines 714-718 现有）之后创建 `let mut aggregator: Option<crate::features::TemplateAggregator> = if do_template { Some(crate::features::TemplateAggregator::new()) } else { None };`"
    - "src/cli/run.rs handle_run 顺序路径 process_log_file 调用处（line 822 附近）第 9 个参数为 `aggregator.as_mut()`（替换原 `do_template` 实参）；并行路径接收 process_csv_parallel 第三返回值并赋值给 aggregator"
    - "src/cli/run.rs handle_run 在 `exporter_manager.finalize()?;`（line 853）之后、`pb.finish_and_clear();` 之前调用 `let template_stats = aggregator.map(|agg| agg.finalize());` 并 `if let Some(ref stats) = template_stats { info!(\"Template analysis: {} unique templates\", stats.len()); }`（并行路径同样在 pb.finish_and_clear() 之前完成 finalize 调用）"
    - "禁用统计 `template_analysis.enabled = false` 时，do_template == false → aggregator 为 None → process_log_file 收到 None → 热循环内 `if let Some(agg) = aggregator.as_deref_mut()` 分支不进入；行为与 Phase 12 完全一致（v1.2 零回归）"
    - "并行 CSV 路径与顺序路径在 enabled=true 的同输入日志下，finalize() 输出的 TemplateStats（按 count desc 排序）首三个元素的 template_key 与 count 完全相等（merge 等价性集成测试）"
    - "cargo test 中新增至少 2 项集成测试：test_aggregator_disabled_none_path（enabled=false 时 handle_run 不创建 aggregator）、test_parallel_merge_consistent（多文件并行 vs 单文件顺序结果一致）"
    - "cargo clippy --all-targets -- -D warnings 通过零警告"
    - "cargo test 全部既有测试 + 新增集成测试通过"
    - "cargo build --release 成功；生成的二进制接受 `template_analysis.enabled = true` 配置文件并正常 run（CLI 烟测，无 panic）"
  artifacts:
    - path: "src/cli/run.rs"
      provides: "process_log_file 侧路径接入 + process_csv_parallel map-reduce + handle_run 创建/finalize 三层完整接入"
      contains: "aggregator: Option<&mut crate::features::TemplateAggregator>"
  key_links:
    - from: "process_log_file 热循环"
      to: "TemplateAggregator::observe"
      via: "aggregator.as_deref_mut().map(|agg| agg.observe(&tmpl_key, exectime_us, record.ts.as_ref()))"
      pattern: "agg\\.observe\\("
    - from: "process_log_file 热循环"
      to: "normalize_template"
      via: "crate::features::normalize_template(pm.sql.as_ref())"
      pattern: "crate::features::normalize_template"
    - from: "process_csv_parallel 任务闭包"
      to: "TemplateAggregator::new"
      via: "每任务独立实例（D-12 map-reduce 无锁）"
      pattern: "TemplateAggregator::new\\(\\)"
    - from: "process_csv_parallel 主线程"
      to: "TemplateAggregator::merge"
      via: "results.reduce(|mut a, b| { a.merge(b); a })"
      pattern: "\\.merge\\("
    - from: "handle_run finalize"
      to: "TemplateAggregator::finalize"
      via: "aggregator.map(|agg| agg.finalize())"
      pattern: "aggregator\\.map\\(.*finalize"
---

<objective>
把 Wave 1 落地的 `TemplateAggregator` 通过侧路径接入 `src/cli/run.rs` 三个关键函数：
1. `process_log_file` 签名变更：`_do_template: bool` → `aggregator: Option<&mut TemplateAggregator>`；热循环内删除 D-14 占位注释，新增 `observe()` 调用
2. `process_csv_parallel` 改造为 map-reduce：每个 rayon 任务持有独立 `TemplateAggregator`，主线程通过 `merge()` 合并，函数返回值扩展第三项 `Option<TemplateAggregator>`
3. `handle_run` 创建/传递/finalize：在 `do_template` 计算后创建 aggregator，顺序与并行路径分别传递与收回，`exporter_manager.finalize()?;` 之后调用 `aggregator.map(|agg| agg.finalize())`

Purpose: 实现 TMPL-02 全部 5 条 ROADMAP Success Criteria——零开销快路径保护（D-07）、hdrhistogram 接入（D-10）、TemplateStats 完整字段（D-02）、并行路径 merge 等价性（D-12）、clippy + 测试全绿。
Output: src/cli/run.rs ~50 行净增改（process_log_file 热循环新增 ~10 行 + process_csv_parallel 返回类型/任务闭包/reduce 共 ~20 行 + handle_run 创建/finalize ~10 行）+ 2 项集成测试。
</objective>

<execution_context>
@/Users/guang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-templateaggregator/13-CONTEXT.md
@.planning/phases/13-templateaggregator/13-RESEARCH.md
@.planning/phases/13-templateaggregator/13-PATTERNS.md
@.planning/phases/13-templateaggregator/13-VALIDATION.md
@.planning/phases/13-templateaggregator/13-01-PLAN.md
@src/cli/run.rs
@src/features/template_aggregator.rs
@src/features/mod.rs
</context>

<interfaces>
<!-- 来自 Plan 01 的可用类型与方法 -->

From src/features/template_aggregator.rs:
- `pub struct TemplateAggregator`（Plan 01 Task 1 创建）
- `pub fn new() -> Self`
- `pub fn observe(&mut self, key: &str, exectime_us: u64, ts: &str)`
- `pub fn merge(&mut self, other: TemplateAggregator)`
- `pub fn finalize(self) -> Vec<TemplateStats>`

From src/features/mod.rs:
- `pub use template_aggregator::TemplateAggregator;`（Plan 01 已暴露）
- `pub use sql_fingerprint::normalize_template;`（Phase 12 已暴露）

From src/cli/run.rs 当前签名（要修改的目标位置，行号参考已读文件状态）:
- `process_log_file(file_path, file_index, total_files, exporter_manager, pipeline, pb, limit, interrupted, do_normalize, _do_template, placeholder_override, params_buffer, ns_scratch, reset_pb, sql_record_filter) -> Result<usize>` ← 第 10 个参数 `_do_template: bool` 替换为 `aggregator: Option<&mut crate::features::TemplateAggregator>`
- `process_csv_parallel(...) -> Result<(Vec<(PathBuf, usize)>, usize)>` ← 返回类型扩展为 `Result<(Vec<(PathBuf, usize)>, usize, Option<crate::features::TemplateAggregator>)>`
- `handle_run(cfg, limit, dry_run, quiet, interrupted, progress_interval, resume, state_file_override, jobs, compiled_filters) -> Result<()>` ← 签名不变，仅函数体修改

From dm_database_parser_sqllog (existing):
- `Sqllog.tag: Option<...>`、`Sqllog.ts: CompactString`（`.as_ref()` 取 &str）、`PerformanceMetrics { sql: CompactString, exectime: f32, rowcount, exec_id }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: process_log_file 签名替换 + 热循环 observe() 接入</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（重点 lines 110-130 函数签名 + lines 200-230 热循环 normalize 块 + lines 222-228 D-14 占位注释）
    - src/features/template_aggregator.rs（Plan 01 产物，确认 observe 签名 `(&mut self, key: &str, exectime_us: u64, ts: &str)`）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（"修改点 1" 与 "修改点 2"）
    - .planning/phases/13-templateaggregator/13-RESEARCH.md（Pattern 5 + Code Examples 热循环调用点 + Open Question Q2 关于 exectime=0 的 guard）
  </read_first>
  <behavior>
    - 修改后 `cargo check` 通过：所有上游调用处（handle_run 顺序路径 + process_csv_parallel 任务闭包）必须在 Task 2/3 之前给出**与新签名匹配的实参**——所以 Task 1 同步更新两处调用点为 `None` 临时实参，让编译通过；Task 2/3 再替换为真实的 `Option<&mut TemplateAggregator>`
    - `template_analysis.enabled = false` 时（do_template=false），handle_run 给两处调用点传 None，热循环 `aggregator.as_deref_mut()` 取出 None → 内层逻辑跳过，与 v1.2 零回归
    - PARAMS 记录（record.tag.is_none()）即使 aggregator=Some 也不触发 observe（避免统计参数行）
    - exectime_us == 0 时（include_performance_metrics=false 合成 pm 场景）跳过 observe，避免触发 hdrhistogram 最小界 1µs
  </behavior>
  <action>
    src/cli/run.rs 修改 1：function `process_log_file` 签名（当前 lines 114-130）：
    - 删除原第 10 个参数 `_do_template: bool,`
    - 在其原位置插入 `aggregator: Option<&mut crate::features::TemplateAggregator>,`
    - 保持其他参数顺序与上下游一致（do_normalize 仍在它之前，placeholder_override 仍在它之后）

    src/cli/run.rs 修改 2：function `process_log_file` 热循环内（原 lines 222-228 占位）：
    - 删除整段 6 行注释块（从 `// D-14: Phase 13 will wire this into TemplateAggregator::observe().` 到 `// };`）
    - 替换为侧路径调用块（紧接 `compute_normalized` 块之后、`if let Some(remaining) = limit` 之前），结构为：
      （a）`if let Some(agg) = aggregator.as_deref_mut() {`
      （b）  `if record.tag.is_some() {`（仅统计 DML，跳过 PARAMS）
      （c）    let `tmpl_key = crate::features::normalize_template(pm.sql.as_ref());`
      （d）    let `exectime_us = (pm.exectime * 1000.0) as u64;`（D-01 µs 转换）
      （e）    `if exectime_us > 0 { agg.observe(&tmpl_key, exectime_us, record.ts.as_ref()); }`（exectime=0 guard，RESEARCH Q2）
      （f）  `}`
      （g）`}`

    src/cli/run.rs 修改 3：临时维持编译通过——更新两处调用点以避免编译错误：
    - `process_csv_parallel` 内 line ~548-564 调用 `process_log_file(..., do_template, ...)`：将第 10 个实参从 `do_template` 改为 `None`（Task 2 会在任务闭包内引入 `task_agg.as_mut()` 替代）
    - `handle_run` 内 line ~822-838 顺序路径调用：将第 10 个实参从 `do_template` 改为 `None`（Task 3 会替换为 `aggregator.as_mut()`）

    禁止：在本任务删除上层的 `do_template` 局部变量（Task 2/3 仍要读它来决定是否创建 aggregator）。禁止改动 process_log_file 其他参数顺序。禁止把 sql_record_filter 检查与 aggregator 调用合并（aggregator 必须在 sql_record_filter 通过分支内执行，自然得到"仅统计实际导出记录"的语义——这是当前位置选择的关键）。
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -10 && cargo test process_log_file 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo build` 退出码 0
    - 命令 `grep -c '_do_template: bool' src/cli/run.rs` 等于 0（原占位参数已删除）
    - 命令 `grep -c 'aggregator: Option<&mut crate::features::TemplateAggregator>' src/cli/run.rs` 等于 1（在 process_log_file 签名中出现一次）
    - 命令 `grep -c 'D-14: Phase 13 will wire this into TemplateAggregator::observe' src/cli/run.rs` 等于 0（占位注释已删除）
    - 命令 `grep -c 'agg.observe(&tmpl_key' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'crate::features::normalize_template(pm.sql.as_ref())' src/cli/run.rs` 等于 1
    - 命令 `grep -c '(pm.exectime \* 1000.0) as u64' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'aggregator.as_deref_mut()' src/cli/run.rs` 等于 1（仅 process_log_file 内使用）
    - cargo build 输出无 warning（`grep -E 'warning:' build.log` 输出 0 行——本地手动校验）
    - process_log_file 函数体行数仍 ≤ 200 行（已含新增 ~10 行，整体函数仍超过 40 行限制但 Phase 12 已通过 too_many_lines = "allow"，确认 lints 配置允许）
  </acceptance_criteria>
  <done>process_log_file 签名 + 热循环 observe 接入完成；两处调用点临时传 None 让 cargo build 通过；Task 2/3 将逐一恢复真实 aggregator 传递。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: process_csv_parallel 改造为 map-reduce（每任务独立 aggregator + 主线程 merge）</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（重点 lines 446-620 process_csv_parallel 完整函数体）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（"修改点 3"）
    - .planning/phases/13-templateaggregator/13-RESEARCH.md（Pattern 6 + Common Pitfalls 2 关于 `&mut TemplateAggregator` 非 Send 必须每任务闭包内创建）
  </read_first>
  <behavior>
    - do_template=true 时：每个 rayon 任务闭包内创建 `Some(TemplateAggregator::new())`，调用 process_log_file 传 `task_agg.as_mut()`，任务返回 task_agg 给主线程；主线程把所有非空 task_agg 通过 `reduce(|mut a, b| { a.merge(b); a })` 合并为单个 merged_agg
    - do_template=false 时：每任务 task_agg 为 None，process_log_file 收到 None，merged_agg 也为 None
    - 函数返回值新增第三项 `Option<TemplateAggregator>` — handle_run 据此决定后续 finalize 行为
    - 出错路径（first_err 非空、跳过文件）不影响 merged_agg 收集——已收集到的 task_agg 仍可参与 merge；与 v1.2 错误清理逻辑一致
  </behavior>
  <action>
    src/cli/run.rs 修改 process_csv_parallel：

    1. 函数返回类型（lines 451-466 签名末尾）：
    - 从 `-> Result<(Vec<(PathBuf, usize)>, usize)>` 改为 `-> Result<(Vec<(PathBuf, usize)>, usize, Option<crate::features::TemplateAggregator>)>`

    2. TaskResult 类型别名（line 511）：
    - 从 `type TaskResult = Option<(PathBuf, PathBuf, usize)>;`
    - 改为 `type TaskResult = Option<(PathBuf, PathBuf, usize, Option<crate::features::TemplateAggregator>)>;`

    3. 任务闭包内（line 511 之后的 par_iter().map 块，在调用 process_log_file 之前）：
    - 新增 `let mut task_agg: Option<crate::features::TemplateAggregator> = if do_template { Some(crate::features::TemplateAggregator::new()) } else { None };`

    4. 任务闭包内调用 process_log_file（line ~548-564）：
    - 把 Task 1 临时填入的 `None` 第 10 个实参改为 `task_agg.as_mut()`

    5. 任务闭包尾部返回（line ~567）：
    - 从 `Ok(Some((file.clone(), temp_path, count)))`
    - 改为 `Ok(Some((file.clone(), temp_path, count, task_agg)))`

    6. parts_info 类型与收集（line ~573-584）：
    - 把 `let mut parts_info: Vec<(PathBuf, PathBuf, usize)> = ...` 改为 `let mut parts_info: Vec<(PathBuf, PathBuf, usize, Option<crate::features::TemplateAggregator>)> = ...`

    7. 错误清理循环（line ~585-590）：
    - 把 `for (_, temp, _) in &parts_info { ... }` 改为 `for (_, temp, _, _) in &parts_info { ... }` 以匹配新元组形状

    8. parts_for_concat 构造（line ~593-597）：
    - 把 `.map(|(_, temp, count)| (temp.clone(), *count))` 改为 `.map(|(_, temp, count, _)| (temp.clone(), *count))`

    9. **新增** merged_agg 合并（在 parts_for_concat 构造之后、concat_csv_parts 调用之前）：
    - `let merged_agg: Option<crate::features::TemplateAggregator> = parts_info.iter_mut().filter_map(|(_, _, _, agg)| agg.take()).reduce(|mut a, b| { a.merge(b); a });`

    10. 函数返回（line ~613-619）：
    - 把 `Ok((parts_info.into_iter().map(|(orig, _, count)| (orig, count)).collect(), skipped))` 改为
    - `Ok((parts_info.into_iter().map(|(orig, _, count, _)| (orig, count)).collect(), skipped, merged_agg))`

    禁止：把 `do_template` 参数从签名删除（Task 3 仍要读它来决定 handle_run 端是否消费 merged_agg）。禁止把 task_agg 改为 `Arc<Mutex<...>>`（D-12 明确每任务独立实例 + 主线程 merge）。
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -10 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo build` 退出码 0
    - 命令 `cargo clippy --all-targets -- -D warnings` 退出码 0
    - 命令 `grep -c 'fn process_csv_parallel' src/cli/run.rs` 等于 1
    - 命令 `grep -cE 'Option<crate::features::TemplateAggregator>' src/cli/run.rs` ≥ 3（函数返回类型 + TaskResult + handle_run 中将出现）
    - 命令 `grep -c 'task_agg.as_mut()' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'crate::features::TemplateAggregator::new()' src/cli/run.rs` ≥ 1（任务闭包内）
    - 命令 `grep -c 'parts_info.iter_mut()' src/cli/run.rs` 等于 1
    - 命令 `grep -cE '\.reduce\(\|mut a, b\| \{ a\.merge\(b\); a \}\)' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'Arc<Mutex' src/cli/run.rs` 不增加（无引入 Mutex 包裹 aggregator——sed 前后对比由 reviewer 把关）
  </acceptance_criteria>
  <done>process_csv_parallel 改造完成，每任务独立 aggregator，主线程 reduce 合并，返回值扩展第三项；cargo clippy 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: handle_run 创建/传递/finalize aggregator + 集成测试</name>
  <files>src/cli/run.rs</files>
  <read_first>
    - src/cli/run.rs（重点 lines 640-886 handle_run 完整函数体，特别注意 lines 714-718 do_template 计算、line 747 process_csv_parallel 调用、line 822 顺序 process_log_file 调用、line 853 exporter_manager.finalize、line 859 pb.finish_and_clear）
    - src/features/template_aggregator.rs（Plan 01 产物：finalize 返回 Vec<TemplateStats>）
    - .planning/phases/13-templateaggregator/13-PATTERNS.md（"修改点 4"）
    - .planning/phases/13-templateaggregator/13-RESEARCH.md（finalize 生命周期参照 exporter_manager.finalize() 位置）
  </read_first>
  <behavior>
    - test_aggregator_disabled_none_path：构造 cfg 不含 `template_analysis.enabled`（或 enabled=false），调用 handle_run 处理含 2 条 SQL 的日志文件，不 panic，导出文件正常生成；通过 grep 校验 logging 输出**不**包含 "Template analysis:" 字样
    - test_parallel_merge_consistent：构造 4 个日志文件每个含若干 DML（共约 20 条），enabled=true；run 1：jobs=1（顺序路径）+ run 2：jobs=4（并行路径）；分别记录 info!("Template analysis: N unique templates") 中的 N 值；断言两者相等
    - 顺序路径：aggregator 由 handle_run 创建，直接传 `aggregator.as_mut()` 给 process_log_file；exporter_manager.finalize 之后调用 aggregator.map(|agg| agg.finalize())
    - 并行路径：handle_run 接收 process_csv_parallel 第三返回值并赋给 aggregator；后续 finalize 同顺序路径
  </behavior>
  <action>
    src/cli/run.rs 修改 handle_run：

    1. 在 lines 714-718 `let do_template = ...` 之后新增：
    - `let mut aggregator: Option<crate::features::TemplateAggregator> = if do_template { Some(crate::features::TemplateAggregator::new()) } else { None };`

    2. 并行路径 process_csv_parallel 调用（line ~747）：
    - 把返回值解构从 `let (processed_files, parallel_skipped) = process_csv_parallel(...)?;` 改为 `let (processed_files, parallel_skipped, parallel_agg) = process_csv_parallel(...)?;`
    - 紧接其后追加 `aggregator = parallel_agg;`（覆盖 step 1 在并行路径下创建的初始 Some，因为并行路径需要把 task_agg 的 merge 结果作为最终 aggregator——并行路径下 step 1 创建的实例从未被任何任务使用，正确做法是丢弃由 process_csv_parallel 返回的 merged_agg 接管）

    3. 顺序路径 process_log_file 调用（line ~822-838）：
    - 把 Task 1 临时填入的 `None` 第 10 个实参改为 `aggregator.as_mut()`

    4. 在 `exporter_manager.finalize()?;`（line 853）之后、`if !quiet { exporter_manager.log_stats(); }` 之后、`pb.finish_and_clear();` 之前，新增统一 finalize 块（顺序与并行路径**共享**此位置）：
    - 注意 line 853 在 `} else {`（顺序路径）分支末尾——需要把 finalize 移到外层（pb.finish_and_clear 之前），让两条路径都执行
    - 正确放置：把以下两行新增到 line 859 `pb.finish_and_clear();` 之前：
    - `let template_stats = aggregator.map(|agg| agg.finalize());`
    - `if let Some(ref stats) = template_stats { info!("Template analysis: {} unique templates", stats.len()); }`

    5. 新增 `#[test]` 集成测试（src/cli/run.rs 底部 `#[cfg(test)] mod tests` 内）：
    - test_aggregator_disabled_none_path：构造 toml 不含 `[features.template_analysis]`，写一条简单日志文件，调用 handle_run，断言 cmd 返回 Ok 且不 panic（运行时 do_template=false）
    - test_parallel_merge_consistent：构造 4 个日志文件每个含至少 5 条 DML SQL（确保 normalize 后产生少量 unique templates，比如 3 个），分别用 jobs=1 与 jobs=4 调用 handle_run；本测试**不要求逐字段比对 TemplateStats**（不暴露 stats 输出口），改为通过 `env_logger` capture 比对 info! 行中的 "Template analysis: N unique templates" 的 N 值。若 env_logger capture 在测试环境不可用，可通过临时把 aggregator finalize 结果暴露给 test-only 内部 helper 方法（在 #[cfg(test)] feature gate 下加 fn for_test_take_aggregator(...) -> Option<Vec<TemplateStats>>）。具体方案由执行者按测试可观察性最简实现挑选——优先 env_logger capture，回退方案为 test-only helper

    禁止：在测试中假设 normalize_template 的具体输出形态（已被 Phase 12 测试覆盖）。禁止把 aggregator 设为全局静态（破坏并行安全）。
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -15 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 命令 `cargo test` 退出码 0
    - 命令 `cargo clippy --all-targets -- -D warnings` 退出码 0
    - 命令 `cargo test test_aggregator_disabled_none_path 2>&1 | grep -E 'test test_aggregator_disabled_none_path .* ok'` 有输出
    - 命令 `cargo test test_parallel_merge_consistent 2>&1 | grep -E 'test test_parallel_merge_consistent .* ok'` 有输出
    - 命令 `grep -c 'aggregator: Option<crate::features::TemplateAggregator>' src/cli/run.rs` ≥ 1（handle_run 内 let 声明）
    - 命令 `grep -c 'aggregator.as_mut()' src/cli/run.rs` 等于 1（顺序 process_log_file 调用）
    - 命令 `grep -c 'aggregator = parallel_agg' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'aggregator.map(|agg| agg.finalize())' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'Template analysis: {} unique templates' src/cli/run.rs` 等于 1
    - 命令 `grep -c 'do_template' src/cli/run.rs` ≥ 4（至少 do_template let、并行调用 do_template 实参、顺序调用 do_template 实参、aggregator 创建条件——4 处保留，对应原 Phase 12 既有用法不退化）
    - 顺序路径与并行路径 finalize 调用只出现在 `pb.finish_and_clear()` 之前的一处（grep -c 'aggregator.map(|agg| agg.finalize())' = 1 已覆盖）
    - cargo build --release 退出码 0
  </acceptance_criteria>
  <done>handle_run 完整接入：do_template=false 走 Phase 12 零开销路径；do_template=true 顺序/并行均正常 finalize，info! 输出 unique template 数量；新增 2 项集成测试通过；全项目 cargo test + cargo clippy 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 日志记录 → process_log_file → TemplateAggregator::observe | 外部数据（pm.exectime f32 + record.ts CompactString + pm.sql CompactString）进入累积器；无新 IO 边界 |
| rayon 工作线程 ↔ 主线程 | task_agg 通过 Move 跨线程返回；merge 在主线程完成；无共享可变状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Integer Overflow | `(pm.exectime * 1000.0) as u64` 转换 | mitigate | exectime 来源于 `parse_performance_metrics`（f32 ≥ 0），最大约 1e9 ms（11.5 天），乘 1000 后 ≤ 1e12 远小于 u64::MAX (~1.8e19)；溢出路径不存在；超过 histogram bounds 60_000_000 的值由 `let _ = entry.histogram.record(...)` 静默截断（Plan 01 已 mitigate） |
| T-13-02-02 | Denial of Service | 单条异常 SQL 触发 observe panic | mitigate | observe() 内 record() 用 `let _ = ...` 静默忽略错误（Plan 01 既定行为）；exectime_us == 0 在调用点 guard 跳过；normalize_template 由 Phase 12 测试覆盖不会 panic |
| T-13-02-03 | Resource Exhaustion (memory) | aggregator entries 数量爆炸 | accept | normalize_template 已合并 IN 列表与空白，模板基数受 SQL 多样性限制；每模板 ~24 KB；5M 记录场景下即便 10000 unique templates 也仅 ~240 MB——属可接受范围。未来若用户上报 OOM，可在 Phase 14/15 加 `top_n` 截断（Deferred Ideas 已记录） |
| T-13-02-04 | Information Disclosure | Template stats 通过 info! 日志输出 unique 数量 | accept | info! 仅打印 unique template 数量，不含 template_key 或 SQL 内容；与现有 ExporterManager log_stats 行为同级别 |
| T-13-02-05 | Race Condition | 并行路径 task_agg 共享 | mitigate | D-12 决策：每任务独立实例，task_agg 用 Move 返回主线程；编译期由 Send bound 保证（&mut TemplateAggregator 不 Send，故必须在闭包内创建——RESEARCH Common Pitfalls 2） |
| T-13-02-06 | Tampering | 顺序路径 enabled=true 与并行路径合并结果差异 | mitigate | test_parallel_merge_consistent 集成测试：固定输入下 jobs=1 与 jobs=4 的 finalize unique template 数量必须相等（hdrhistogram add() 无精度损失，merge 字典序 first/last_seen 与单线程顺序等价） |
</threat_model>

<verification>
- 全部 acceptance_criteria 中的 grep / cargo 命令通过
- cargo build --release 通过；cargo clippy --all-targets -- -D warnings 零警告
- cargo test 全部既有测试 + 新增 test_aggregator_disabled_none_path + test_parallel_merge_consistent 通过
- 手动烟测：`cargo run --release -- run -c config.toml`（启用 `[features.template_analysis] enabled = true`）成功完成且 stderr 含 "Template analysis: N unique templates" 行
- 手动烟测：`cargo run --release -- run -c config.toml`（禁用或不配置 template_analysis）成功完成且 stderr **不** 含 "Template analysis:" 行
</verification>

<success_criteria>
1. ROADMAP Phase 13 Success Criteria 1：TemplateAggregator 不实现 LogProcessor（D-09，由 Plan 01 mod tests 验证），通过 `Option<&mut TemplateAggregator>` 侧路径接入 process_log_file，禁用时行为与 v1.2 完全一致（test_aggregator_disabled_none_path 覆盖）
2. ROADMAP Phase 13 Success Criteria 2：每模板 Histogram<u64>（~24 KB），5M 记录规模下内存占用与记录数无线性增长（Plan 01 已 mitigate by hdrhistogram bounds）
3. ROADMAP Phase 13 Success Criteria 3：finalize() 输出 TemplateStats 含 10 字段（D-02 由 Plan 01 验证）
4. ROADMAP Phase 13 Success Criteria 4：并行 CSV 路径 map-reduce merge，结果与单线程一致（test_parallel_merge_consistent 覆盖）
5. ROADMAP Phase 13 Success Criteria 5：cargo clippy --all-targets -- -D warnings 通过，cargo test 新增 observe/finalize/merge 单元测试全部通过（Plan 01 6 项 + Plan 02 2 项）
</success_criteria>

<output>
Create `.planning/phases/13-templateaggregator/13-02-SUMMARY.md` when done.
Key fields:
- affects: ["src/cli/run.rs"]
- provides: ["process_log_file 侧路径接入", "process_csv_parallel map-reduce", "handle_run 创建/finalize 三层接入"]
- patterns: ["Option<&mut T> 侧路径热循环守卫", "rayon 任务闭包内独立实例 + 主线程 reduce", "exectime guard 防 hdrhistogram 最小界超界"]
- decisions: ["aggregator 仅统计 record.tag.is_some() 的 DML（PARAMS 跳过）", "exectime_us > 0 guard（include_performance_metrics=false 场景）", "finalize 调用位置在 exporter_manager.finalize 之后、pb.finish_and_clear 之前（两路径共用）"]
- tests_added: 2
</output>
</content>
</invoke>