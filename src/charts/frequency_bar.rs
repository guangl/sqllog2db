/// 频率柱状图占位（将在本文件完整实现）
pub fn draw_frequency_bar(
    _entries: &[crate::features::ChartEntry<'_>],
    _top_n: usize,
    _output_path: &std::path::Path,
) -> crate::error::Result<()> {
    Ok(())
}
