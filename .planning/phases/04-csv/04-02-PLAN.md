---
phase: 04-csv
plan: 02
type: execute
wave: 1
depends_on: ["04-csv-01"]
files_modified:
  - src/exporter/csv.rs
autonomous: true
requirements: [PERF-03, PERF-08]
must_haves:
  truths:
    - "write_record_preparsed 在 Wave 0 micro-benchmark 上吞吐相比 Wave 0 baseline 不下降，目标提升（任何可量化的提升均接受，不强求 10%）"
    - "csv.rs 内消除至少一处可识别的多余 reserve 调用或冗余字节拷贝"
    - "全部 12 个 csv.rs 单元测试通过；CSV 输出与 Wave 0 字节级一致"
    - "clippy 无 warning，fmt 无 diff"
  artifacts:
    - path: "src/exporter/csv.rs"
      provides: "格式化热路径精准优化"
      contains: "pub(crate) fn write_record_preparsed"
  key_links:
    - from: "write_record_preparsed"
      to: "line_buf.reserve / extend_from_slice"
      via: "格式化层字节写入"
      pattern: "line_buf\\.(reserve|extend_from_slice|push)"
---

<objective>
Wave 1：基于 Wave 0 micro-benchmark 数据，对 `write_record_preparsed` 格式化层做精准优化。

Purpose: 减少热循环堆分配/冗余分支，提升 csv_format_only 吞吐，为 PERF-08 / PERF-03 提供可观测改进。
Output: csv.rs 中至少一处可识别的格式化层优化（reserve 策略或字节拷贝路径），并通过 csv_format_only benchmark 验证不退化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-csv/04-CONTEXT.md
@.planning/phases/04-csv/04-RESEARCH.md
@.planning/phases/04-csv/04-PATTERNS.md
@.planning/phases/04-csv/04-01-SUMMARY.md

@src/exporter/csv.rs

<interfaces>
<!-- write_record_preparsed 当前结构（行 78–258，Wave 0 已改 pub(crate)） -->

```rust
#[inline]
pub(crate) fn write_record_preparsed(
    itoa_buf: &mut itoa::Buffer,
    line_buf: &mut Vec<u8>,
    sqllog: &Sqllog<'_>,
    meta: &MetaParts<'_>,
    pm: &PerformanceMetrics<'_>,
    writer: &mut BufWriter<File>,
    path: &Path,
    normalize: bool,
    normalized_sql: Option<&str>,
    field_mask: crate::features::FieldMask,
    ordered_indices: &[usize],
) -> Result<()> {
    line_buf.clear();
    let sql_len = pm.sql.len();
    let ns_len = if normalize {
        normalized_sql.map_or(0, str::len)
    } else {
        0
    };
    line_buf.reserve(120 + sql_len + ns_len + 8);
    // ... 字段写入
}
```

写入字段顺序（全量路径，行 102–144）：
ts, ep, sess_id, thrd_id, username, trxid, statement, appname, client_ip, tag, sql(quoted), exectime, rowcount, exec_id, normalized_sql(quoted)

write_csv_escaped 函数（行 13–21，已用 memchr SIMD，不要改）。

ExportError::WriteFailed 错误构造路径（行 252–257，仅在 write_all 失败时进入；不在热路径）。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 优化 write_record_preparsed 的 reserve 与冗余字节拷贝</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（必读完整文件，重点 行 78–258 `write_record_preparsed` 函数体；行 13–21 `write_csv_escaped`）
    - .planning/phases/04-csv/04-RESEARCH.md（章节 "Hot Path Deep Analysis" 与 "Anti-Patterns to Avoid"）
    - .planning/phases/04-csv/04-01-SUMMARY.md（Wave 0 baseline 数值，决定优化方向）
  </read_first>
  <action>
基于 Wave 0 实测数据，对 `write_record_preparsed` 做以下精准优化（**至少完成第 1 项**；第 2、3 项视 Wave 0 数据决定是否实施）：

**优化 1（必做）：消除 reserve 在 line_buf 已有足够容量时的冗余调用**

将现有：
```rust
line_buf.clear();
let sql_len = pm.sql.len();
let ns_len = if normalize {
    normalized_sql.map_or(0, str::len)
} else {
    0
};
line_buf.reserve(120 + sql_len + ns_len + 8);
```

改为只在 capacity 不足时才 reserve：
```rust
line_buf.clear();
let sql_len = pm.sql.len();
let ns_len = if normalize {
    normalized_sql.map_or(0, str::len)
} else {
    0
};
let needed = 128 + sql_len + ns_len; // 120 + 8 = 128，常量化
if line_buf.capacity() < needed {
    line_buf.reserve(needed - line_buf.len());
}
```

理由（A2 假设验证）：`Vec::reserve` 在 capacity 已足够时也会做单次 capacity 检查；显式检查后跳过 reserve 调用可让编译器内联 cap 比较。**实际测得 Wave 0 baseline 后，若 capacity 已稳定（首条记录后 capacity ≥ 2KB 已能容纳大多数行），此优化的可观测收益主要来自 branch elision 而非内存分配。**

**优化 2（可选，视 Wave 0 数据）：合并 `,,` 字面量 push**

行 134–136 当前：
```rust
} else {
    line_buf.extend_from_slice(b",,");
}
```

这部分已是最优。**不要改它**，但确认 has_metrics=false 路径下没有遗漏的字段分隔符。

**优化 3（可选）：合并 strip_ip_prefix 调用**

行 118 `strip_ip_prefix(meta.client_ip.as_ref())` 每条记录调用一次。检查 `strip_ip_prefix` 实现位置（`src/exporter/mod.rs` 同文件 import 来自 `super`）：如果它只是字符串切片操作（无分配），保留；若有分配则记录到 SUMMARY，但 **本任务不修改它**（超出 csv.rs 文件范围）。

**约束：**
- 修改后 CSV 字节级输出必须与修改前完全一致（test_csv_basic_export、test_csv_export_method 等 12 个测试断言不变）
- 不要引入新依赖
- 不要修改 `write_csv_escaped`（已是 memchr SIMD 最优）
- 不要改 `BufWriter` 容量（D-04，保持 16MB）
- 不要把 `write_record_preparsed` 的 `#[inline]` 删掉
- 不要把 itoa::Buffer 替换为别的库
- 修改完成后必须运行：
  - `cargo test --lib -- exporter::csv`（12 个测试通过）
  - `cargo test`（629+ 全套测试通过）
  - `cargo bench --bench bench_csv -- csv_format_only --quick`（与 Wave 0 baseline 对比）
  - `cargo clippy --all-targets -- -D warnings`（0 warning）
  - `cargo fmt -- --check`（0 diff）
- 在 SUMMARY 中记录 Wave 0 vs Wave 1 csv_format_only 数值对比；**若提升 < 1%（噪声范围内），说明 reserve 不是瓶颈，记录此结论但保留代码改动（更清晰的意图）**
  </action>
  <verify>
    <automated>grep -c "if line_buf.capacity() &lt; needed" src/exporter/csv.rs &amp;&amp; cargo test --lib -- exporter::csv 2&gt;&amp;1 | tail -5 &amp;&amp; cargo bench --bench bench_csv -- csv_format_only --quick 2&gt;&amp;1 | tail -10 &amp;&amp; cargo clippy --all-targets -- -D warnings 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if line_buf.capacity() < needed" src/exporter/csv.rs` 输出 `1`（条件 reserve）
    - `grep -c "let needed = 128 + sql_len + ns_len;" src/exporter/csv.rs` 输出 `1`
    - `grep -c "line_buf.reserve(120 + sql_len + ns_len + 8);" src/exporter/csv.rs` 输出 `0`（旧无条件 reserve 已移除）
    - `grep -c "pub(crate) fn write_record_preparsed" src/exporter/csv.rs` 输出 `1`（Wave 0 修改保持）
    - `grep -c "#\[inline\]" src/exporter/csv.rs` ≥ `3`（write_csv_escaped, write_record_preparsed, write_record 三处 inline 保留）
    - `cargo test --lib -- exporter::csv` 输出含 `test result: ok` 且 passed ≥ 12
    - `cargo test` 退出码 `0`，输出含 `test result: ok` 总通过数 ≥ 629
    - `cargo bench --bench bench_csv -- csv_format_only --quick` 退出码 `0`，输出含 `csv_format_only/10000`
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`，无 warning
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>write_record_preparsed 已用条件 reserve 替代无条件 reserve，全部测试通过且 csv_format_only 不退化</done>
</task>

<task type="auto">
  <name>Task 2: 输出格式回归测试 + Wave 0/1 数据对比记录</name>
  <files>src/exporter/csv.rs</files>
  <read_first>
    - src/exporter/csv.rs（确认已有 `tests` mod，行 461 起）
    - .planning/phases/04-csv/04-01-SUMMARY.md（取 Wave 0 csv_format_only baseline 时间）
  </read_first>
  <action>
在 `src/exporter/csv.rs` 的 `#[cfg(test)] mod tests` 中新增一个回归测试，断言 Task 1 优化后 CSV 输出在多种 SQL 长度下字节级保持稳定（防止 reserve 边界条件破坏输出）。

在现有最后一个测试 `test_csv_field_order_normalized_sql_skipped_when_normalize_false` 函数 `}` 之后、模块 `}` 之前，追加：

```rust
    #[test]
    fn test_csv_reserve_boundary_short_sql() {
        // 回归：极短 SQL（10 字节级）触发 reserve 路径，输出格式必须完整
        let dir = tempfile::TempDir::new().unwrap();
        let log = dir.path().join("short.log");
        std::fs::write(
            &log,
            "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:A ip:10.0.0.1) [SEL] SELECT 1. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n",
        ).unwrap();

        let out = dir.path().join("out.csv");
        let mut exporter = CsvExporter::new(&out);
        exporter.initialize().unwrap();

        let parser = LogParser::from_path(log.to_str().unwrap()).unwrap();
        for record in parser.iter().flatten() {
            exporter.export(&record).unwrap();
        }
        exporter.finalize().unwrap();

        let content = std::fs::read_to_string(&out).unwrap();
        // header + 1 data row
        assert_eq!(content.lines().count(), 2);
        let data = content.lines().nth(1).unwrap();
        assert!(data.contains("\"SELECT 1\""), "data row: {data}");
    }

    #[test]
    fn test_csv_reserve_boundary_long_sql() {
        // 回归：长 SQL（>2KB）触发 reserve 扩容路径，line_buf 容量正确扩展
        let dir = tempfile::TempDir::new().unwrap();
        let log = dir.path().join("long.log");
        let big_sql = "x".repeat(4096);
        let line = format!(
            "2025-01-15 10:30:28.001 (EP[0] sess:0x0001 user:U trxid:1 stmt:0x1 appname:A ip:10.0.0.1) [SEL] SELECT '{big_sql}'. EXECTIME: 1(ms) ROWCOUNT: 1(rows) EXEC_ID: 1.\n"
        );
        std::fs::write(&log, &line).unwrap();

        let out = dir.path().join("out.csv");
        let mut exporter = CsvExporter::new(&out);
        exporter.initialize().unwrap();

        let parser = LogParser::from_path(log.to_str().unwrap()).unwrap();
        for record in parser.iter().flatten() {
            exporter.export(&record).unwrap();
        }
        exporter.finalize().unwrap();

        let content = std::fs::read_to_string(&out).unwrap();
        assert_eq!(content.lines().count(), 2);
        // 长 SQL 在数据行内完整存在
        assert!(content.contains(&big_sql), "long SQL missing from output");
    }
```

**约束：**
- 在 `mod tests {` 块内追加，不要新建文件
- 使用现有 import（`super::*`、`LogParser`），不要新增 import
- 测试函数名前缀必须是 `test_csv_reserve_boundary_`
- 运行后两个测试必须通过；现有 12 个测试也不能退化（即至少 14 个 csv 测试通过）

完成后将 Wave 0 baseline、Wave 1 优化后 csv_format_only 数值与解读写入即将创建的 `04-02-SUMMARY.md`。
  </action>
  <verify>
    <automated>grep -c "fn test_csv_reserve_boundary_short_sql" src/exporter/csv.rs &amp;&amp; grep -c "fn test_csv_reserve_boundary_long_sql" src/exporter/csv.rs &amp;&amp; cargo test --lib -- exporter::csv 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn test_csv_reserve_boundary_short_sql" src/exporter/csv.rs` 输出 `1`
    - `grep -c "fn test_csv_reserve_boundary_long_sql" src/exporter/csv.rs` 输出 `1`
    - `grep -c "let big_sql = \"x\".repeat(4096);" src/exporter/csv.rs` 输出 `1`
    - `cargo test --lib -- exporter::csv` 输出含 `test result: ok` 且 passed ≥ 14
    - `cargo clippy --all-targets -- -D warnings` 退出码 `0`
    - `cargo fmt -- --check` 退出码 `0`
  </acceptance_criteria>
  <done>新增两个 boundary 测试通过，确保 reserve 优化未破坏字节级输出</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log file → parser → exporter | 解析后的 SQL 内容写入 line_buf；csv_escape 已防注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | line_buf reserve 边界条件 | mitigate | 新增 short/long SQL boundary 测试，断言扩容路径不破坏输出 |
| T-04-04 | Denial of service | 极长 SQL 输入触发反复 realloc | accept | line_buf clear() 保留 capacity，单次扩容到 needed；4KB SQL 测试覆盖 |
</threat_model>

<verification>
- `cargo test`（含 629+ 套件 + 新增 2 个 boundary 测试）退出码 0
- `cargo bench --bench bench_csv -- csv_format_only --quick` 数值不低于 Wave 0 baseline（容差 5%，吸收测量噪声）
- `cargo clippy --all-targets -- -D warnings` 无警告
- `cargo fmt -- --check` 无 diff
</verification>

<success_criteria>
- write_record_preparsed 使用 `if line_buf.capacity() < needed { reserve(...) }` 替代无条件 reserve
- 新增 `test_csv_reserve_boundary_short_sql` 和 `test_csv_reserve_boundary_long_sql` 测试通过
- csv_format_only benchmark 相比 Wave 0 不退化（容差 5%）
- 全部 629+ 测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04-csv/04-02-SUMMARY.md`，记录：
1. csv_format_only Wave 0 baseline vs Wave 1 数值对比（median time + throughput）
2. 优化措施清单（reserve 条件化）
3. 测试新增/通过情况（≥ 14 csv 测试通过）
4. 若 Wave 1 提升 < 1%（噪声内），明确指出"格式化层非瓶颈，应转向 lazy parse_pm 兜底（Plan 03）"
</output>
