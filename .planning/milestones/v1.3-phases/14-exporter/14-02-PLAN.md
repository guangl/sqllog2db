---
phase: 14-exporter
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/exporter/sqlite.rs
autonomous: true
requirements:
  - TMPL-04
tags:
  - exporter
  - sqlite
  - rust
must_haves:
  truths:
    - "SqliteExporter::write_template_stats() 在 SqliteExporter::finalize() 之后调用时，sql_templates 表被创建并包含全部 10 列（template_key, count, avg_us, min_us, max_us, p50_us, p95_us, p99_us, first_seen, last_seen）"
    - "overwrite=true 时 sql_templates 表被 DROP IF EXISTS 后重建，旧行不残留（D-07, D-08）"
    - "overwrite=false（append=true 隐含）时表用 CREATE TABLE IF NOT EXISTS 创建，旧行保留，新行 INSERT 累加（D-07）"
    - "写入失败（SQL/DDL 异常）转换为 Error::Export(ExportError::DatabaseFailed)，向上返回 Err（D-04）"
    - "SqliteExporter::finalize() 不被修改（保持只执行 COMMIT、不 take self.conn 的现有行为，D-06）"
    - "数据库连接（self.conn）在 write_template_stats 完成后仍持有，直到 exporter drop 时自动关闭"
    - "DDL 建表逻辑被拆分到独立辅助函数 create_or_replace_template_table()，主函数 write_template_stats 行数不超过 30 行（遵守 CLAUDE.md 40 行限制）"
  artifacts:
    - path: "src/exporter/sqlite.rs"
      provides: "SqliteExporter::write_template_stats impl + create_or_replace_template_table helper + 3 integration tests"
      contains: "fn write_template_stats"
    - path: "src/exporter/sqlite.rs"
      provides: "sql_templates DDL 字面量，10 列定义"
      contains: "CREATE TABLE IF NOT EXISTS sql_templates"
    - path: "src/exporter/sqlite.rs"
      provides: "create_or_replace_template_table 辅助函数（DROP IF EXISTS + CREATE TABLE 拆分）"
      contains: "fn create_or_replace_template_table"
  key_links:
    - from: "write_template_stats"
      to: "create_or_replace_template_table"
      via: "DDL 处理委托"
      pattern: "self\\.create_or_replace_template_table\\("
    - from: "create_or_replace_template_table"
      to: "self.conn"
      via: "as_ref + execute"
      pattern: "self\\.conn\\.as_ref\\(\\)"
    - from: "write_template_stats"
      to: "rusqlite::params!"
      via: "参数化 INSERT 绑定"
      pattern: "rusqlite::params!"
---

<objective>
为 `SqliteExporter` 实现 `Exporter` trait 新增方法 `write_template_stats()`：在 `finalize()` 后被 ExporterManager 调用，复用现有 `self.conn` 连接（D-06），按 overwrite/append 语义 DROP/CREATE `sql_templates` 表，并在单事务内批量 INSERT 所有 `TemplateStats` 行。为遵守 CLAUDE.md "Keep functions under 40 lines" 约束，将 DDL 建表逻辑拆分到独立辅助函数 `create_or_replace_template_table()`。同时为该方法新增 3 项集成测试（基本写入、overwrite 覆盖、append 累加）。

Purpose: 满足 ROADMAP 成功标准 SC-1（"SQLite 导出模式下，run 结束后数据库文件中存在 sql_templates 表，包含每个模板的全 10 列"），TMPL-04-A/E/F 测试映射。
Output: `src/exporter/sqlite.rs` 新增 1 个 impl Exporter 方法 + 1 个 inherent 辅助函数 + 3 个测试函数；执行 `cargo test sqlite` 全绿。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-exporter/14-CONTEXT.md
@.planning/phases/14-exporter/14-RESEARCH.md
@.planning/phases/14-exporter/14-PATTERNS.md
@.planning/phases/14-exporter/14-VALIDATION.md
@.planning/phases/14-exporter/14-01-SUMMARY.md
@src/exporter/sqlite.rs
@src/features/template_aggregator.rs
@src/exporter/mod.rs

<interfaces>
<!-- 关键已有代码，executor 必须按此使用，不要再去 codebase 探索 -->

From src/exporter/sqlite.rs（现有代码）:
- `pub struct SqliteExporter { conn: Option<rusqlite::Connection>, database_url: String, table_name: String, overwrite: bool, append: bool, batch_size: usize, stats: ExportStats, normalize: bool, field_mask: FieldMask, ordered_indices: Vec<usize>, row_count: usize, ... }`
- `impl SqliteExporter { fn db_err(reason: impl Into<String>) -> Error { Error::Export(ExportError::DatabaseFailed { reason: reason.into() }) } }`
- `fn finalize(&mut self) -> Result<()>`（约 L397-407）当前体：`if let Some(conn) = &self.conn { conn.execute_batch("COMMIT;")... } info!(...) Ok(())` — 不修改此方法
- 现有 `fn prepare_target_table(&self) -> Result<()>` 演示 DROP IF EXISTS 模式（约 L239-254）

From src/features/template_aggregator.rs:
- `pub struct TemplateStats { pub template_key: String, pub count: u64, pub avg_us: u64, pub min_us: u64, pub max_us: u64, pub p50_us: u64, pub p95_us: u64, pub p99_us: u64, pub first_seen: String, pub last_seen: String }`

From Plan 01:
- Exporter trait 已有默认 `fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], final_path: Option<&std::path::Path>) -> Result<()>` no-op 实现
- `ExporterKind::Sqlite(e) => e.write_template_stats(stats, final_path)` 透传已存在
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 SqliteExporter::write_template_stats() + 辅助函数 create_or_replace_template_table()，并新增 3 项集成测试</name>
  <files>src/exporter/sqlite.rs</files>
  <read_first>
    - src/exporter/sqlite.rs（必须读取完整文件，至少包括 SqliteExporter struct 字段、`db_err` 辅助、`prepare_target_table` 与 `finalize` 现有实现、`test_sqlite_basic_export` 测试结构）
    - src/features/template_aggregator.rs（TemplateStats 字段顺序与类型，10 字段全部 u64/String）
    - .planning/phases/14-exporter/14-CONTEXT.md（D-04、D-06、D-07、D-08 决策约束）
    - .planning/phases/14-exporter/14-RESEARCH.md（Pattern 2 sqlite write_template_stats 完整模板 + Pitfall 1 借用规则 + Pitfall 4 EXCLUSIVE lock 测试规避）
    - .planning/phases/14-exporter/14-PATTERNS.md（src/exporter/sqlite.rs 一节，含 db_err 模式 + 测试作用域模式）
    - .planning/phases/14-exporter/14-01-SUMMARY.md（确认 trait 默认签名以保持一致）
    - /Users/guang/Projects/sqllog2db/CLAUDE.md（"Keep functions under 40 lines — split if longer" 约束）
  </read_first>
  <behavior>
    - Test 1（test_sqlite_write_template_stats，TMPL-04-A）：构造 SqliteExporter（overwrite=true），调用 initialize → finalize → write_template_stats 传入 2 个 TemplateStats（如 `{template_key:"SELECT 1", count:5, avg_us:100, min_us:50, max_us:200, p50_us:90, p95_us:180, p99_us:195, first_seen:"2025-01-15 10:00:00", last_seen:"2025-01-15 10:05:00"}` + 另一个），drop exporter 后重新打开数据库，验证 `sql_templates` 表存在，`SELECT COUNT(*) FROM sql_templates` 返回 2，`SELECT template_key, count, avg_us, p99_us, first_seen FROM sql_templates ORDER BY template_key` 行内容与输入逐字段相等
    - Test 2（test_sqlite_templates_overwrite，TMPL-04-E）：先用 overwrite=true 写入 1 行（template_key="OLD"），drop exporter；再用新 overwrite=true exporter 调用 write_template_stats 写入 1 行（template_key="NEW"），drop；重新打开验证只有 1 行且 `template_key="NEW"`（旧行已被 DROP）
    - Test 3（test_sqlite_templates_append，TMPL-04-F）：先用 overwrite=true 写入 1 行（template_key="A"），drop exporter；再用 overwrite=false、append=true 的新 exporter 调用 write_template_stats 写入 1 行（template_key="B"），drop；重新打开验证有 2 行（A 与 B 并存），ORDER BY template_key 顺序正确
  </behavior>
  <action>
    **CLAUDE.md 约束**：项目根 `CLAUDE.md` 明确要求 "Keep functions under 40 lines — split if longer"。原 inline 实现的 `write_template_stats()` 包含 DROP + CREATE DDL + BEGIN + INSERT 循环 + COMMIT + 错误包装会超出 60 行。本任务必须将 DDL 部分拆分为独立辅助函数，最终两个函数均小于 40 行。

    在 `src/exporter/sqlite.rs` 中按以下结构实现：

    **拆分 1：新增 inherent 辅助函数 `create_or_replace_template_table` (~20 行)**

    在 `impl SqliteExporter { ... }` inherent 块内（与 `db_err` 等私有辅助同位置；非 `impl Exporter for SqliteExporter` 块内），新增私有方法：

    `fn create_or_replace_template_table(&self) -> Result<()>`

    职责：
    1. 读取 `let overwrite = self.overwrite;` 局部变量（Pitfall 1 借用规则）
    2. 借用 `let conn = self.conn.as_ref().ok_or_else(|| Self::db_err("create_or_replace_template_table: not initialized"))?;`
    3. 若 `overwrite` 为真，执行 `conn.execute("DROP TABLE IF EXISTS sql_templates", [])`，错误用 `Self::db_err(format!("drop sql_templates failed: {e}"))` 包装并 `?` 上传
    4. 始终执行 `conn.execute("CREATE TABLE IF NOT EXISTS sql_templates (template_key TEXT NOT NULL PRIMARY KEY, count INTEGER NOT NULL, avg_us INTEGER NOT NULL, min_us INTEGER NOT NULL, max_us INTEGER NOT NULL, p50_us INTEGER NOT NULL, p95_us INTEGER NOT NULL, p99_us INTEGER NOT NULL, first_seen TEXT NOT NULL, last_seen TEXT NOT NULL)", [])`，错误用 `Self::db_err(format!("create sql_templates failed: {e}"))` 包装并 `?` 上传
    5. 返回 `Ok(())`

    DDL 是固定字面量，10 列顺序与 TemplateStats 字段顺序对应。整个函数体目标 ≤20 行（含错误包装 closure）。

    **拆分 2：主方法 `write_template_stats` (~25 行)**

    在 `impl Exporter for SqliteExporter { ... }` 块中，紧随 `fn finalize(...)` 之后添加 `fn write_template_stats(...)` 方法。签名必须与 Plan 01 的 trait 默认实现一致：`fn write_template_stats(&mut self, stats: &[crate::features::TemplateStats], _final_path: Option<&std::path::Path>) -> Result<()>`（`final_path` 在 sqlite 不使用，前置下划线）。

    职责（每步一行或短块，目标整体 ≤30 行）：
    1. **委托 DDL**：`self.create_or_replace_template_table()?;`（DROP 与 CREATE 在辅助函数内完成）
    2. **借用 conn**：`let conn = self.conn.as_ref().ok_or_else(|| Self::db_err("write_template_stats: not initialized"))?;`
    3. **批量 INSERT 事务**：
       - `conn.execute_batch("BEGIN;").map_err(|e| Self::db_err(format!("begin failed: {e}")))?;`
       - 循环 `for s in stats { conn.execute("INSERT INTO sql_templates VALUES (?,?,?,?,?,?,?,?,?,?)", rusqlite::params![s.template_key, s.count, s.avg_us, s.min_us, s.max_us, s.p50_us, s.p95_us, s.p99_us, s.first_seen, s.last_seen]).map_err(|e| Self::db_err(format!("insert sql_templates failed: {e}")))?; }`
       - `conn.execute_batch("COMMIT;").map_err(|e| Self::db_err(format!("commit sql_templates failed: {e}")))?;`
    4. **日志**：`info!("sql_templates: {} rows written to {}", stats.len(), self.database_url);`
    5. **返回**：`Ok(())`

    任何中间步骤 Err 直接 `?` 向上传播（D-04）。

    **不修改 `fn finalize()`**：确认 finalize 仍只执行 COMMIT，不 take/drop self.conn（D-06 前提）。

    **测试（参照 `test_sqlite_basic_export` 现有模式 L434-463）**：

    在同文件的 `#[cfg(test)] mod tests` 块内新增 3 项测试，遵循 Pitfall 4 EXCLUSIVE lock 测试规避：
    - 用 `tempfile::TempDir` 创建临时目录、命名 `out.db`
    - 在内层 `{ ... }` 作用域内创建 `SqliteExporter::new(db_path.to_string_lossy().into(), "sqllog_records".into(), true /* overwrite */, false /* append */)`，调用 `initialize().unwrap()` → `finalize().unwrap()` → `write_template_stats(&stats, None).unwrap()`，作用域结束时 exporter drop、EXCLUSIVE lock 释放
    - 作用域外 `rusqlite::Connection::open(&db_path).unwrap()` 验证结果（`query_row` 拿 count + 各字段值）
    - 测试中构造的 `TemplateStats` 必须 `use crate::features::TemplateStats;`（Plan 01 已 re-export）

    禁止：
    - 修改 `SqliteExporter::finalize()`（破坏 D-06）
    - 把 DROP/CREATE 提前到 `initialize()`（违反 D-08）
    - 用动态拼接 SQL 替代 `rusqlite::params!`（破坏参数化绑定的注入防护）
    - 改变 ExportError 类型或 Self::db_err 包装语义
    - 将 DDL 直接 inline 在 `write_template_stats` 内（违反 CLAUDE.md 40 行限制）
    - 把 `create_or_replace_template_table` 放在 `impl Exporter for ...` 块内（应在 inherent `impl SqliteExporter` 块中，作为私有辅助）
  </action>
  <verify>
    <automated>cargo test --lib exporter::sqlite 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "fn write_template_stats" src/exporter/sqlite.rs` 命中 1 处（impl Exporter 块内）
    - `grep -nE "fn create_or_replace_template_table" src/exporter/sqlite.rs` 命中 1 处（inherent impl 块内，私有辅助）
    - `grep -E "self\\.create_or_replace_template_table\\(\\)" src/exporter/sqlite.rs` 命中 1 处（write_template_stats 内部委托）
    - `grep -E "CREATE TABLE IF NOT EXISTS sql_templates" src/exporter/sqlite.rs` 命中 1 处（在 create_or_replace_template_table 内）
    - `grep -E "DROP TABLE IF EXISTS sql_templates" src/exporter/sqlite.rs` 命中 1 处（在 create_or_replace_template_table 内）
    - `grep -E "INSERT INTO sql_templates VALUES \\(\\?,\\?,\\?,\\?,\\?,\\?,\\?,\\?,\\?,\\?\\)" src/exporter/sqlite.rs` 命中 1 处
    - `grep -E "rusqlite::params!\\[\\s*s\\.template_key" src/exporter/sqlite.rs` 命中 1 处
    - **函数长度 ≤40 行（CLAUDE.md）**：`awk '/fn write_template_stats\(/,/^    \}$/' src/exporter/sqlite.rs | wc -l` 输出值 ≤40（主函数体）
    - **函数长度 ≤40 行（CLAUDE.md）**：`awk '/fn create_or_replace_template_table\(/,/^    \}$/' src/exporter/sqlite.rs | wc -l` 输出值 ≤40（辅助函数体）
    - `grep -cE "^\\s*fn test_sqlite_write_template_stats\\b|^\\s*fn test_sqlite_templates_overwrite\\b|^\\s*fn test_sqlite_templates_append\\b" src/exporter/sqlite.rs` 返回 3
    - `cargo test test_sqlite_write_template_stats` 退出码 0
    - `cargo test test_sqlite_templates_overwrite` 退出码 0
    - `cargo test test_sqlite_templates_append` 退出码 0
    - `cargo test --lib exporter::sqlite` 全部通过（含现有 finalize/initialize 测试不退化）
    - `cargo clippy --all-targets -- -D warnings` 退出码 0
    - `grep -E "self\\.conn\\.take\\(\\)" src/exporter/sqlite.rs` 在 `finalize` 函数体范围内不应出现新增（保持 D-06 现状）
  </acceptance_criteria>
  <done>
    SqliteExporter::write_template_stats 完整实现并验证：DDL 拆分到 create_or_replace_template_table 辅助函数（≤20 行），主函数 write_template_stats（≤30 行）通过委托调用辅助函数 + 单事务批量 INSERT 完成实现；基本写入 / overwrite 覆盖 / append 累加三种情形均通过测试；两个函数均符合 CLAUDE.md "Keep functions under 40 lines" 约束；clippy 全绿；finalize 行为不变；EXCLUSIVE lock 通过测试作用域规避正确处理；run.rs 的接入（Plan 04）可基于此方法工作。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TemplateStats → SQLite | `template_key` 是 String，来自 `normalize_template()`（内部生成，非用户直输入），但仍走 SQL 参数化绑定避免任何注入风险 |
| 文件系统 | 数据库文件路径来自配置 `database_url`，由 Config 校验 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering / Injection | INSERT INTO sql_templates | mitigate | 所有值字段用 `rusqlite::params![]` 绑定，DDL 用固定字面量；不使用 format! 拼接值 |
| T-14-04 | Denial of Service | 批量 INSERT 长事务 | accept | 单 exporter 模板数 N 远小于主记录数（M），N << M，单事务 INSERT 完全可承受；与现有主表 batch_commit 模式相比开销可忽略 |
| T-14-05 | Tampering | sql_templates 表名固定 | accept | 表名为 ASCII 字面量 `sql_templates`，不接受任何用户输入，无需白名单（与 table_name 不同） |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | 本 plan 不安装新包 |
</threat_model>

<verification>
- `cargo test --lib exporter::sqlite` 全绿
- `cargo clippy --all-targets -- -D warnings` 通过
- `cargo fmt` 后无 diff
- 3 项新增测试函数（test_sqlite_write_template_stats、test_sqlite_templates_overwrite、test_sqlite_templates_append）全部 PASS
- 现有 SqliteExporter 测试无回归
- 两个新函数（write_template_stats、create_or_replace_template_table）函数体均 ≤40 行
</verification>

<success_criteria>
- SqliteExporter::write_template_stats() 实现存在且通过 trait 透传被调用
- create_or_replace_template_table() 辅助函数存在，封装 DROP/CREATE DDL
- 两函数均符合 CLAUDE.md 40 行限制
- sql_templates 表 10 列完整且类型正确
- overwrite=true 时表被重建，append=true 时表行被累加
- 写入失败 Err 上传（不降级 warn）
- finalize() 行为零变化
- 全部测试与 clippy 通过
</success_criteria>

<output>
Create `.planning/phases/14-exporter/14-02-SUMMARY.md` when done with:
- Files modified（src/exporter/sqlite.rs）
- New method signature（write_template_stats + create_or_replace_template_table）
- Each function's actual line count (must be ≤40)
- Test names + actual test outputs（pass count）
- DDL 字面量 actual text
- Affects: [SqliteExporter behavior]
- Provides: [sql_templates table writer]
- Patterns: [db_err 错误包装 + 单事务批量 INSERT + 测试作用域 lock 规避 + DDL 函数拆分]
</output>
